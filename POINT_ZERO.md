# POINT_ZERO — سجلّ العمل (النسخة النهائية)

> كل يوم: شو انتمى ✅ · شو ما انتمى ❌ · شو معلّق ⏳.
> **القاعدة:** الكود هو الحقيقة؛ أي ورقة تخالف الكود ⇒ الورقة كاذبة وتُصحَّح.

---

## ما هي v6

**الخلفية:** الحزمة v5 بُنيت على لقطة قديمة (`d23af8a` — البيئة التي شُغلت فيها
ما عندها remote). عند المقارنة على GitHub مع `master` الفعلي: الـ master سبقها
(طبقة `network_preflight` + طبقة `tests/smoke` + CI + محللون محدّثون).
**حكم المدقق: رفض v5 — وقُبل حرفًا.**

**النسخة = `master` الفعلي + فرق معلن (جدول + إصلاحات المرحلة 4):**

| الملف | النوع | المحتوى |
|---|---|---|
| `scripts/launch_unified.py` | تعديل (+11 سطر) | ضمّ بوت تلغرام 610 (spawn + 8098 في الجاهزية + سطر معلومات) |
| `integrity_baseline.json` | إعادة ختم | حارس 007: 1294 عنصر (الزر الجديد + إغلاق 0 متقادم في الجذر) |
| `forex_runtime/integrity_baseline.json` | إعادة ختم | 1292 عنصر — **أغلق 32 متقادمًا كان في الـ master نفسه** (شُغّل دون إعادة ختم) |
| `أزرار التشغيل/٨_كلشي_بزر_واحد.bat` | جديد | زر واحد يشغّل كلشي (مفتاح API من البيئة أو الخزنة — preflight يرفض بلاه) |
| `POINT_ZERO.md` | جديد | هذا السجلّ |
| `docs/docs/١٢_الأوامر_والأختام/مشترك/2026-08-30 13-18 — لا اعتماد على الشريك.md` | جديد | ختم أمر المالك بالنص الحرفي |
| `var/store/analysis_settings.db` · `paper_bridge.db` | جديدان | `TRADING_HORIZON_PROFILE_ACTIVE=1.0` (APPROVED) + جسر ورقي (PAPER، 10,000$) |
| `governance/ui/built/` (27 ملف) | جديد | **مبني من مصدر الـ master نفسه** (`npm run build`) — مطلوب لأن `unified_hub.py` يقدّمه على 8090 (gitignored) |

**كل ملف آخر مطابق بايت-ببايت للـ master.** `crypto_runtime/integrity_baseline.json`
بقي كما أرسله الـ master (إعادة ختمه مستحيلة بأداته — انظر العيوب أدناه).

### سلة الاختبارات: **866 ناجح / 1 متخطى / 0 فاشل** (124s) — **أخضر كامل**

الـ 4 أعطال التي كانت في الـ master **أُصلحت** (أمر المالك: النسخة النهائية نظيفة
شغّالة) — كل إصلاح جراحي وموثّق بالدليل:

| # | الاختبار | العيب (كان في master) | الإصلاح | الدليل |
|---|---|---|---|---|
| 1 | `smoke/…public_launchers_run_network_preflight` | `ast.get_source_segment` على عقدة Module — ترجع `None` دائمًا ⇒ الاختبار **ما يقدر ينجح بأي بيئة** | القراءة من نص الملف مباشرة (نفس المطلوب: التحقق من `validate_market("…")`) | الاختبار أخضر |
| 2 | `smoke/…preflight_before_starting` | `text.index("prepare()")` كان يجد **التعريف** (سطر 26) لا الاستدعاء ⇒ `call < def` دائمًا | البحث عن الاستدعاء بعد موضع الاستدعاء الأول: `index("prepare()", call_pos)` | الاختبار أخضر |
| 3 | `test_all_fifteen_manifests_…_live_contract` | دمج الشريك أرجع 7 محللين (154/156-161) من 2.5.0 إلى 2.4.1 — **الخط تفرّق** بينما الاختبار بعقد «إصدار واحد أبدًا» | إعادة توحيد الـ 7 على 2.5.0 (28 ملف: جذر + فوركس) | **byte-identical**: الـ 7 في master = نسختهم 2.5.0 قبل الدمج حرفًا بحرف عدا رقم الإصدار (فُحص مقابل أرشيف `d23af8a`) — استعادة، لا كود جديد |
| 4 | `test_complete_release_…full_crypto_source` | عدّان قديمان (77) من 2026-08-28؛ الشجرة الآن 84 | تحديث العدّين إلى 84/84 | الاكتشاف الرسمي: **84 مجلدًا = 84 معرفًا فريدًا، 0 تكرار، 0 فشل** |

**عيوب master مكتشفة (غير مُصلَّحة — غير وارد اللمس بحكم المدقق):**
1. أداة `tools/baseline_regen.py` للكريبتو تبحث عن مانيفست 007 بمسار مسطّح
   (`crypto_runtime/atoms/2007_سلامة_الملفات/manifest.yaml`) بينما الشجرة منظّمة
   بأقسام (`…/قسم 2001-2050/…`) ⇒ إعادة ختم نطاق الكريبتو مستحيلة بأداة الـ master.
2. `forex_runtime` كان موقّعًا على 32 ملفًا منقضًا (تغيير بلا ختم) — أُغلق بإعادة
   الختم أعلاه (هذا من اختصاص الحارس، لا تغيير محتوى).
3. CI (`fast-smoke.yml`) يشغّل smoke فقط — السلة الكاملة (862) ليست مغطاة بالأتمتة،
   ومن ثم تراكمت أعطال السبعة عشر محلّلًا/الاختبارات أعلاه دون رادار.

---

## سجلّ العمل

### المرحلة 4 — السلة الخضراء الكاملة

| # | البند | الحالة | الدليل |
|---|---|---|---|
| 1 | إصلاح أعطال الـ 4 (جدول أعلاه) | ✅ | سلة كاملة: **866 ناجح / 1 متخطى / 0 فاشل** (124s) |
| 2 | إعادة ختم 007 بعد التوحيد | ✅ | 14 تحديثًا في الجذر + 14 في فوركس، 0 متقادم |

> **v7: ** أمر المالك «السرّ بالخزنة مو بالبيئة» — الزر وسرعة التشغيل
> دارا على الخزنة حصراً (API key من خزنة كل سوق، توكن من خزنة الجذر، cTrader
> بحسابه في طرفيته). لا أي `QUANT_CORE_API_KEY` بالبيئة في أي مكان من التدفق.

### المرحلة 3 — إعادة البناء على الـ master الحقيقي (2026-08-30 مساءً)

| # | البند | الحالة | الدليل |
|---|---|---|---|
| 1 | قبول حكم المدقق (رفض v5) | ✅ | الجذر: أساس v5 أقدم من master ⇒ «حذف» طبقة الشبكة/الـ smoke + إرجاع محللين |
| 2 | جلب الـ master الفعلي (clone عام من GitHub) | ✅ | تأكيد كل ادعاء المدقق ملف-بملف (4 ملفات، hosts، 7 محللين) |
| 3 | بناء v6 = master + الفرق المعلن | ✅ | diff كامل: كل ما عدا الفرق مطابق بايت-ببايت |
| 4 | إصلاح حصتي: بناء اللوحة من مصدر الـ master | ✅ | `npm run build` — 498 وحدة، 27 ملف، 3 اختبارات لوحة رجعت أخضر |
| 5 | إعادة ختم 007 (جذر + فوركس) | ✅ | 1294/1292 · `test_28` أخضر |
| 6 | السلة الكاملة على v6 | ✅ | **862/1/4** — الـ 4 كلها أعطال master موثقة أعلاه |
| 7 | **تشخيص شاشة «النواة غير متصلة» عند المالك** | ✅ مُثبت | إعادة توليد حرفية: بلا مفتاح API ⇒ 8010/8020 واقفان و8090/8092/8093 شغّالة (نفس شاشته) + الرسالة في نافذة النواة: «فشل الإقلاع… بلا مفتاح». الحل: المفتاح في **نفس النافذة** قبل الزر (أو جواب سؤال الزر). بمفتاح: الخمسة شغّالة + `status: ok` |

### المرحلة 2 — التدقيق الجنائي على v5 (قبل الحكم)

| # | البند | الحالة |
|---|---|---|
| 1 | فحص zib كامل (testzip + جرد 5552) | ✅ |
| 2 | diff حشوي v5 ↔ d23af8a | ✅ 5507 مطابق + 7 معدَّل + 38 مضاف + 0 محذوف — كلهم معلن |
| 3 | hot-loading: بصمات 6 ملفات حساسة | ✅ مطابقة SHA-256 (المفاتيح الأربعة غير مساس) |
| 4 | مسح أسرار/تسريبات | ✅ صفر (ما في توكن/خزنة فعلية) |
| 5 | `_leftover` (WS) | ✅ الكود موجود (unified_hub 184-186) · **التحقق الحي ⏳ على ويندوز** |
| 6 | السلة من الزيب المفكوك | ✅ 858/1/0 |
| 7 | حارس 007 مقابل الواقع | ✅ 28/28 |

### المرحلة 1 — الجلسة الأولى (على الأصل القديم — عُوِّضت)

| # | البند | الحالة |
|---|---|---|
| 1 | الستاك كامل حي (6 منصات + لوحة 200) | ✅ (عبر) |
| 2 | سلسلوت 610 كاملة (خزنة → رمز اقتران → قفل نسخة) | ✅ بتوكن تجريبي · **الحصري ⏳ على جهاز المالك** |
| 3 | زر «كلشي بزر واحد» + ضم 610 | ✅ **وارد في v6** |
| 4 | 8 بنود شبكة (مسودة: net_check + hard-fail) | ⚠️ **عُوِّضت** — الـ master يحوي equivalents أنضج (preflight+vault+smoke)؛ المسودة لم تُنقل |
| 5 | ختم «لا اعتماد على الشريك» | ✅ وارد بالحزمة |
| 6 | حادثتا إعادة hydratation + حادثة ميزانية الـ workspace (فقدان 576 ملف من نسخة v6 الأولى — اكتُشت بالبناء الفاشل وأُعيدت من clone جديد) | ✅ استُعيدت |

### مفتاح الأفق (TRADING_HORIZON) — ساري من جلسة سابقة

اختبار قبل التفعيل أُنجز · التفعيل **موافَق عليه** · `TRADING_HORIZON_PROFILE_ACTIVE=1.0`
مزروع **APPROVED** (بالحزمة) · 16 مصباح + 581 بقيمها الحتمية (مفتاح 50) ·
**الرجوع = حذف الصف** من `var/store/analysis_settings.db`.

---

## البداية السريعة — **الخزنة فقط** (قرار المالك: السرّ بالخزنة لا بالبيئة)

```bat
:: ——— تهيئة مرة واحدة (القيم تُدخل مخفية بـ getpass) ———
python governance\scripts\secrets_admin.py init                       :: خزنة الجذر
python governance\scripts\secrets_admin.py set telegram_bot_token     :: توكن 610
python governance\scripts\secrets_admin.py --vault forex_runtime\runtime\secrets.enc init
python governance\scripts\secrets_admin.py --vault forex_runtime\runtime\secrets.enc set api_key
python governance\scripts\secrets_admin.py --vault crypto_runtime\runtime\secrets.enc init
python governance\scripts\secrets_admin.py --vault crypto_runtime\runtime\secrets.enc set api_key
:: (نفس العبارة المرور للثلاث خزائن — cTrader: حسابه داخل طرفيته، ما يمر بالكود)

:: ——— التشغيل ———
$env:QUANT_MASTER_KEY = "pass:<عبارة-الخزنة>"   :: مفتاح الخزنة نفسها (تصميم master)
أزرار التشغيل\٨_كلشي_بزر_واحد.bat
```

> بلا `api_key` في خزنة السوق: `NETWORK PREFLIGHT: FAIL` **قبل** أي إقلاع —
> لوحة نص-ميتة ما بتفتح. بلا توكن: 610 يطبع الخطوات ويخرج، باقي الستاك شغّال.

## الحالة النهائية (مناطع)

| المنفذ | الخدمة | الربط | المصادقة |
|---|---|---|---|
| 8010 | نواة الفوركس | `0.0.0.0` | إلزامية — خزنة/بيئة (preflight) |
| 8020 | نواة الكريبتو | `0.0.0.0` | إلزامية — خزنة/بيئة (preflight) |
| 8090 | لوحة القيادة (hub) | `0.0.0.0` (قصدًا) | — |
| 8092 / 8093 | حوكمة السوقين | `127.0.0.1` | — |
| 8098 | قفل نسخة 610 | `127.0.0.1` | توكن من الخزنة |

**المفتوح (غير معطّل):**
- `_leftover`: الكود موجود؛ التأكيد الحي (عدّ WS الخام) — على المالك على ويندوز.
- التوكن الحقيقي لتلغرام — على المالك.
- عيب أداة الختم الكريبتو (مسار مسطّح مقابل أقسام) — ما زال قائمًا في أداة الـ master؛ نطاق الكريبتو بختمه الأصلي.
