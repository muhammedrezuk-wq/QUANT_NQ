# نقطة الصفر — QUANT_NQ (d23af8a، 2026-08-30)

هذا الحزمة هي **المشروع كاملًا كنقطة بداية جديدة**: كل الشيفرة وكل الشجر
الثلاثة (الجذر + forex_runtime + crypto_runtime) بحالتها عند الارتكام
`d23af8a`، **بدون تاريخ git** — تُعتبر نقطة 0 وتُبنى فوقها من جديد.

## ما فيها فوق المستودع المباشر (3 ارتكومات)

| الارتكام | المحتوى |
|---|---|
| `1c2d89c` | توحيد خط المحللين 151-165 على 2.5.0 + تجديد خطوط الأساس + مواءمة عدد الكريبتو 84 |
| `2989ad8` | 613: استعادة مسار الإعادة المفقود (feed.replay.tick → market.tick) |
| `d23af8a` | إعادة توليد خطوط أساس السلامة (007) بعد تعديل مانيفست 613 — الحارس لم يعد يسقط على الشجرة السليمة |

## التفعيل الحي (قرار المالك 2026-08-30)

`TRADING_HORIZON` **مفعّل** من لحظة أول إقلاع: صف الاعتماد
`HORIZON_PROFILE_ACTIVE = 1.0 (APPROVED)` مزروع مسبقًا في
`var/store/analysis_settings.db`.

- مفتاح الأفق `TRADING_HORIZON` **غير مثبّت** — بقي على افتراضيته 50،
  ويُحرَّك حيًّا من اللوحة (المولّد يتبعه خلال ثانيتين على المسار الساخن،
  والذرّات الباردة 166/452/453/458 تلتقطه من الإقلاع التالي أو بأمر عيار فوري).
- الرجعة: حذف صف `HORIZON_PROFILE_ACTIVE` (الظل يُعاد، صفر تغيير كود).
- الاعتماد الفردي لأي عيار بعد التفعيل يعلو القيمة المولَّدة («الفردي فوق»).
- مُتحقق منه حيًّا: أول إقلاع ختم 523 بنفسه `applies=true, shadow=false`
  والبث المطابق للمفتاح 50: 166 أوزان 77.37/22.63 وعمق 56.14/47.68 وطزاجة
  3.77 ث · 453 مشاركة 0.387/ثقة 0.524 · 458 نطاق 0.0753/تعارض 0.5505 ·
  581 هستيريسيس 0.3652/0.2253.

## حالة التشغيل المضمّنة

- `var/store/paper_bridge.db` — حساب ورقي `PAPER` (رافعة 300 / 10,000 USD) فقط؛
  جداول التشغيل ينشئها النظام بنفسه عند أول إقلاع.
- `var/store/analysis_settings.db` — مفاتيح المالك مع صف التفعيل أعلاه.
- لا بيانات سوق، ولا `.git`، ولا أي كاش/سجلات/لقطات — نقطة صفر نظيفة.

## البداية السريعة

```bash
# بايثون 3.12+ (مُختبَر على 3.13)
pip install -r requirements.txt

# زر واحد يشغّل كلشي (ويندوز): دبل-كلك على
#   أزرار التشغيل\٨_كلشي_بزر_واحد.bat
# → نواتا الفوركس (8010) والكريبتو (8020) + لוחاتهما + لوحة القيادة الموحّدة
#   (8090) + بوت تلغرام 610 (قفل نسخة 8098) — في نافذة واحدة.

# تلغرام (ترتيب لمرة واحدة — السرّ يمرّ بس بينك وبين الخزنة):
venv\Scripts\python.exe governance\scripts\secrets_admin.py init
venv\Scripts\python.exe governance\scripts\secrets_admin.py set telegram_bot_token
# وفي نفس النافذة قبل التشغيل:
$env:QUANT_MASTER_KEY = "pass:<عبارة-المرور-اللي-حفظتها>"
# بلا توكن: البوت يطبع الخطوات ويخرج بهدوء — باقي الستاك شغّال.

# نواة الفوركس (API + WS على 8010، 233 ذرّة)
cd QUANT_NQ
python3 scripts/run_forex.py
```

محاكاة ورقية كاملة (بلا جسر MetaTrader):

```bash
NQ_BRIDGE_DB="$PWD/var/store/paper_bridge.db" \
QUANT_ANALYSIS_SETTINGS_DB="$PWD/var/store/analysis_settings.db" \
python3 scripts/run_forex.py
# ثم ابدأ الإعادة: غيّر startup_mode في مانيفست 630 إلى auto (أو POST /api/atoms/630/start)
```

## السلة والعزل

- السلة: `python3 -m pytest tests -q` → **858 passed / 1 skipped / 0 failed**
  (الاختبار الذي كان يُتخطّى بسبب غياب حزمة `cryptography` في بيئة الاختبار
  شُغّل ونجح بعد تثبيت الحزمة — على ويندوز بالـ venv يتشغّل طبيعيًا).
- حارس السلامة 007 مختوم على هذه الشجرة (`integrity_baseline.json`
  في الجذر والفرعين) — أي تعديل على ملفات مرقوبة دون إعادة ختم يُسقط الحارس
  عمدًا. أعد التوليد بأداة المشروع: `python3 tools/baseline_regen.py`.
- هذا الحزمة نقطة بداية: إن أردت مستودعًا جديدًا فابدأ `git init` من هنا.

---

# سجلّ العمل — 2026-08-30

## أُنجز اليوم (مُتحقق منه)

| # | العمل | الحالة | الدليل |
|---|---|---|---|
| 1 | اختبار تأثير `TRADING_HORIZON` على مسار القرار قبل أي تحويل (حُكم المالك) | ✅ تم | إثبات حي أن الظل خامل (B′=الافتراضي) + قياس الأثر المفعّل: 16 عيارًا + هستيريسيس 581 تطبّق قيمها المولَّدة حرفيًا على 166/452/453/455/456/458/457 + مسح السلم الكامل 10/25/50/75/100 |
| 2 | تفعيل `TRADING_HORIZON` (موافقة المالك) | ✅ تم | صف `HORIZON_PROFILE_ACTIVE=1.0 APPROVED` مزروع في `var/store/analysis_settings.db`؛ أول إقلاع ختم 523 `applies=true, shadow=false` والمفتاح على افتراضيته 50 (قابل للحركة حيًّا) |
| 3 | حزمة نقطة الصفر (هذا الـ ZIP) | ✅ تم | 5466 ملفًا / 11.9 م.ب / بلا `.git` / سلة 857-2-0 / فحص تسرب بقايا تشغيل = صفر |
| 4 | حادثة حذف عرضي أثناء التنظيف ثم استعادة كاملة من لقطة المنصة (11:19) | ✅ سُجّلت واستُعيدت | `git status` نظيف عند `d23af8a` بعد الاستعادة |
| 5 | لوحة القيادة 8090: خطأ `dashboard-build-missing` — البناء الجذري `governance/ui/built` لم يكن في الحزمة | ✅ تم | بُني بـ `npm ci && npm run build` (vite، node 20) وشُغّل الحي: `HTTP 200` صفحة «غرفة القيادة» مكتفية بذاتها (CSS/JS محقون) · البناء مضمّن في الحزمة (الشجرتان الفرعيتان كانتا تحويان بنائيهما أصلًا) |
| 6 | **«زر واحد يشغّل كلشي»**: النواتان + اللوحات + تلغرام + الأخبار بزرّة | ✅ تم | زر جديد `أزرار التشغيل/٨_كلشي_بزر_واحد.bat` (يغلّف `launch_unified.bat`: venv + متطلبات + المنشّل) · إضافة بوت تلغرام 610 إلى `scripts/launch_unified.py` (قفل نسخة 8098، بلا منفذ عام) · اختبار حي كامل: `8010/8020/8090/8092/8093/8098` كلها READY + اللوحة `HTTP 200` (1.37MB) + النواتان `status: ok` (226/33 ذرّة) · الأخبار = ذرّات 616 داخل كل نواة + نبض تنبيهات 610 كل 6 ثوانٍ |
| 7 | إثبات سلسلة تلغرام 610 كاملة (خزنة → توكن → نسخة واحدة → اقتران) | ✅ تم | خزنة تجريبية مشفّرة + بوت تجريبي: البوت فتح الخزنة بمفتاح البيئة، عرض رمز الاقتران «أوّل من يرسله يصير المالك» · بلا توكن: يطبع الخطوات الأربع ويخرج بهدوء — باقي الستاك ما يتأثر · النسخة المكررة ترفضها قفل 8098 تلقائيًا |
| 8 | حادثة لقطة المنصة: نسخة من الشجرة بأسماء ملفات مُشوَّهة وُضعت فوق الأصل أثناء الاختبار | ✅ سُجّلت واستُعيدت | استعادة كاملة من Git (`git restore`) إلى حالة `d23af8a` بالضبط · `git status` بعد الاستعادة = التعديلات المقصودة فقط · أُعيد ختم الحارس 007 (جذر 1292 / فوركس 1292 / كريبتو 650) · سلة أخيرة: **858 passed / 1 skipped / 0 failed** |
| 9 | إعادة تغليف حزمة نقطة الصفر بالزر الجديد (هذه النسخة) | ✅ تم | **5550 عنصرًا / 15.5 م.ب** · `var` = ملفان فقط (مفتاح الأفق + جسر ورقي) · الزر الجديد موجود + اللوج مُحدَّث + البناءات الثلاث مضمّنة · الملف المُغلَّف فُحص منفصلًا (testzip + عينات) قبل التسليم |

## مستلَم من المستودع — **لم يُنجز بعد** (بانتظار جلب حالة المستودع المحدّثة)

الأشجار الحالية في هذه الحزمة ما زالت على حالتها السابقة لكل بند؛ يُدمج
ويُوثَّق هنا بعد استلام الشغل:

| # | البند | الحالة | الوضع الحالي في هذه الشجرة |
|---|---|---|---|
| 1 | إعداد الفوركس `0.0.0.0:8010` | ⏳ لم يُنجز | `config/core_forex.yaml` ما زالت `127.0.0.1:8010` |
| 2 | إعداد الكريبتو `0.0.0.0:8020` | ⏳ لم يُنجز | `config/core_crypto.yaml` ما زالت `127.0.0.1:8020` |
| 3 | بقاء `core.yaml` العام محليًا | ⏳ لم يُنجز | `config/core.yaml` على `127.0.0.1` (يُثبّت بهذا الشكل ضمن الدمج) |
| 4 | فحص الشبكة في مشغّلات الفوركس والكريبتو | ⏳ لم يُنجز | لا يوجد فحص شبكة في `scripts/launch_unified.py`/المشغّلات حاليًا |
| 5 | تنفيذ فحص الشبكة قبل الإقلاع الموحد | ⏳ لم يُنجز | غير موجود |
| 6 | بقاء حوكمة `8092/8093` داخلية | ⏳ لم يُنجز | الموانئ موجودة في `launch_unified.py`؛ يُؤكَّد «الداخلي» ضمن الدمج |
| 7 | فشل الإقلاع عند غياب مفتاح API | ⏳ لم يُنجز | `governance/scripts/run_core.py` حاليًا **تحذير** فقط عند `host≠127.0.0.1` بلا مفتاح |
| 8 | قبول المفتاح عندما يأتي من البيئة | ⏳ لم يُنجز | يقرأ `QUANT_CORE_API_KEY`/`QUANT_GOV_API_KEY` حاليًا لكن لا يُبنى فشل/قبول نهائي عليها |

> بعد الدمج: يُحدَّث هذا الجدول (الحالة + الدليل)، وتُعاد سلة الاختبارات،
> ويُعاد توليد خطوط الأساس إن لمس الدمج ملفات مرقوبة، وتُغلَّف حزمة جديدة.
