# تشغيل QUANT_NQ على Windows

## تشغيل اللوحات والفحص المحلي

1. افتح مجلد المشروع.
2. افتح مجلد `أزرار التشغيل`.
3. شغّل:
   `ابدأ من هنا — محلي.bat`
4. الزر ينشئ `venv` إذا لم تكن موجودة، ثم يثبت كل مكتبات `requirements.txt`، وبعدها يشغّل الوضع المحلي.

الوضع المحلي:

- يشغّل اللوحات والنواة للفحص.
- لا يحتاج توكنًا للتوثيق المحلي.
- لا يرسل صفقة حقيقية.
- لا يفتح حساب وسيط.

## إذا أردت فحصًا واحدًا

- `٤_المدقق_الشامل.bat` — فحص عقود الذرات.
- `٥_فحص_المشروع.bat` — فحص المشروع.
- `٦_فحص_الأحداث.bat` — فحص وصلات الأحداث.
- `٧_الاختبارات_الكاملة.bat` — تشغيل الاختبارات.

## التداول الحقيقي

لا يبدأ التداول الحقيقي بزر محلي تلقائيًا. يلزم أولًا حساب وسيط موثّق، بيانات سوق حية، مفتاح معتمد، وفتح بوابة التنفيذ بقرار صريح. هذه الحزمة مضبوطة للفحص الآمن، لذلك لا تستخدم التشغيل المحلي لإرسال أوامر.
