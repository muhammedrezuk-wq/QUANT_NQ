# ابدأ من هنا — QUANT_NQ

> **هذه أوّل ورقة تُقرأ بعد سحب المستودع.** لا تبدأ عملًا قبلها.
> **آخر تدقيق مطابق للكود: 2026-09-01 — الالتزام `270f96bf0eae`.**
>
> **بعد إصلاحات ختم `NQ`:** سلة المستودع خضراء، وكريبتو `237 passed`، وخطوط
> السلامة الثلاثة مطابقة. بُنيت الواجهة ونجح فحص المشروع. بقي التشغيل الحي
> على Windows والخزنة والشيكسَم النهائي؛ فلا تُحوّل نجاح الاختبارات أو
> `verify_unified.py` إلى حكم تشغيل مالي حي.

---

## ١) القواعد قبل أي شيء

| القاعدة | ماذا تعني عمليًّا |
|---|---|
| **الكود هو الحقيقة** | أي ورقة تخالف الكود ⇒ **الورقة كاذبة** وتُصحَّح. لا رقم بلا قياس. |
| **لا تخمين** | رقم لم يُقَس يُكتب «لم يُقَس». إصلاح لم يُختبر يُكتب «لم يُختبر». |
| **العربيّة فقط** | المالك **لا يقرأ الإنكليزيّة**. كل نصّ يظهر له عربيّ ١٠٠٪، والأرقام لاتينيّة (`60.0` لا `٦٠٫٠`). وأي مصدر أجنبيّ (عناوين أخبار مثلًا) **يُترجَم قبل العرض**. |
| **ختم `NQ`** | لا تعديل كود بلا كلمة `NQ` صريحة من المالك. «موافق» و«تمام» **ليست ختمًا**. |
| **لا حذف** | أرشفة فقط. الحذف بيد المالك وحده. |
| **لا حركة بلا تسجيل** | كل إصلاح/فحص/قرار يُسجَّل فور تنفيذه في `docs/docs/١١_المناوبات/`. |
| **ويندوز فقط** | باورشيل. لا أوامر لينكس. |

---

## ٢) ما هو هذا النظام

نواة **لا تعرف شيئًا** (`core/`، `23` ملفًا مختومة SHA-256، الإصدار
`1.31.0`) تُشغّل **ذرّات** مستقلة. لكل سوق عملية Core وناقل أحداث مستقلان؛
لا ذرّة تنادي ذرّة أخرى مباشرة.

| | |
|---|---|
| ذرّات الفوركس | `atoms/` — `233` مانيفستًا |
| ذرّات الكريبتو | `atoms_crypto/` — `80` مانيفستًا |
| الحوكمة واللوحة | `governance/` (فحوص + مصدر واجهة React) |
| التوثيق | **`docs/docs/` فقط** |

مانيفست كل ذرّة كريبتو وحقل `startup_mode` فيه هو سلطة الإقلاع الوحيدة:
`33` تلقائيًا و`47` يدويًا. الذرّات `2702` و`2707` و`2720` تبقى يدوية.
وجذر بيانات كريبتو التشغيلي الوحيد هو `crypto_runtime/var`؛ لا نسخة مزدوجة
ولا مزامنة مع `var/crypto` في الجذر.

**شراكة بمناوبتين:** `NQ` (محمد رزوق) للفوركس · `ASMAR` للكريبتو.

---

## ٣) خريطة المنافذ

```
8010  نواة الفوركس        8020  نواة الكريبتو
8090  لوحة الفوركس        8091  لوحة الكريبتو
8092  حوكمة الفوركس       8093  حوكمة الكريبتو   (داخليّان)
8098  منصّة تلغرام ٦١٠ (قفل نسخة واحدة)
```

⚠️ **لوحة كل سوق على منفذها.** الكوكي مرقَّم بالمنفذ (`QUANT_MARKET_8090`)
لأنّ المتصفّح **لا يفصل الكوكي بالمنفذ** — بلا الترقيم يقلب سوقٌ لوحةَ الآخر.

---

## ٤) التشغيل من نسخة نظيفة — على جهاز جديد

**عقد التشغيل الرسمي مستقل لكل سوق:**

```
1. أزرار التشغيل\تهيئة المشروع الموحد.bat          ← مرّة واحدة على كل جهاز
2. أزرار التشغيل\تشغيل الفوركس الموحد.bat          ← عند تشغيل الفوركس
   أو
   أزرار التشغيل\تشغيل الكريبتو الموحد.bat          ← عند تشغيل الكريبتو
```

الأول يجهّز مفسّر المشروع داخل `vendor/python/runtime/`، وينصّب الحزم، ويبني
شجرة `var/`، ويصل روابط التشغيل، ثم **يتحقق** من وجود اللوحتين. هو لا ينفذ
`npm run build` ولا ينشئ بناء الواجهة بنفسه. كل زر سوق يشغّل Core والناقل
والحوكمة واللوحة لذلك السوق فقط. `غرفة القيادة.bat` و`START_LOCAL.bat`
و`launch_unified.py` أدوات داخلية/قديمة وليست عقد البدء الرسمي.

> **حالة مساحة العمل المقاسة:** بُني `governance/ui/built/index.html` ونجح
> `check_project.py`. البناء مخرَج محلي غير متعقّب؛ لذلك checkout نظيف يبنيه
> أولًا بـ`cd governance\\ui && npm ci && npm run build`، وCI ينفذ ذلك تلقائيًا.
> حزمة ZIP النهائية يجب أن تحمله. المفسّر المضمن لا يُشحن في Git؛ يبدأ سكربت
> التهيئة ببايثون الجهاز ثم ينزّل النسخة المثبتة.

> **٢٠٢٦-٠٩-٠١ — لماذا كُتب هذا:** النسخة المتنقّلة فتحت على جهاز صاحبها ولم
> تفتح على جهاز آخر. السبب مقيس: النسخة تحمل الشيفرة واللوحتين المبنيّتين
> و`requirements.txt`، ولا تحمل `venv/` (مئات الميغابايت بمسارات مطلقة لجهاز
> صاحبها، ولا تصلح على غيره). و`scripts\py.bat` عند غياب البيئة يرجع إلى
> `py -3` — بايثون النظام العاري — فيسقط أوّل `import fastapi` ولا تقلع خدمة
> ولا تفتح لوحة. ولم يكن في المشروع **مسار تنصيب أوّل مرّة إطلاقًا**:
> `prepare_unified.py` يصل روابط الذرّات فقط ولا ينصّب حزمة واحدة — وكان
> يعمل عند صاحبه لأن بيئته موجودة سلفًا.

**ملاحظات تبقى صحيحة:**

* اللوحة تُخدَم **مبنيّة** من `governance/ui/built/`. بُنيت في مساحة العمل
  ونجح فحص المشروع، لكنها غير متعقبة في Git؛ من يستنسخ يلزمه:
  `cd governance\ui && npm ci && npm run build` قبل التهيئة. CI يفعل ذلك
  تلقائيًا، وحزمة ZIP النهائية يجب أن تحمل البناء.
* وفق تقرير Windows السابق، فُحصت ستة أزرار على `cmd`. في التدقيق الحالي
  قُرئت نقاط الدخول من المصدر فقط؛ لم يُعَد اختبارها حيًا على Windows.
* بعد فكّ ضغط: أزل روابط Junction إن منعت الإقلاع
  (`forex_runtime/` و`crypto_runtime/` مرايا للجذر لا نسخ).

---

## ٥) ما ليس في المستودع (ولماذا)

| المستثنى | السبب |
|---|---|
| `runtime/secrets.enc` · `runtime/device.key` | **الخزنة المشفّرة ومفتاحها — لا تُرفع أبدًا.** |
| `news_bot/.env` · `news_bot/data/` · `news_bot/logs/` | توكن البوت بنصّ صريح، ومعرّفات محادثات. |
| `venv/` · `**/node_modules/` | مكتبات تُولَّد بأمر واحد. |
| `var/` وكل `*.db` | بيانات تشغيل حيّة — أحدها **١٢٩ ميغا** وغيت هب يرفض فوق ١٠٠. |
| `governance/ui/built/` | مخرَج بناء غير متعقّب؛ يُبنى محليًا/في CI ويُضمَّن في ZIP النهائية. |

**الأسرار التي يحتاجها النظام** (تُضاف من اللوحة › تبويب «الأمان»، أو
`governance/scripts/secrets_admin.py set <الاسم>`):

```
telegram_bot_token     منصّة ٦١٠
ctrader_fix_password   كلمة سرّ حساب cTrader الذي يذكره مانيفست الذرّة 622
api_key                مفتاح الكشف العلني (غير مطلوب محلّيًّا)
```

---

## ٦) مصادر تعيش خارج هذا المجلّد

* **`news_bot/`** نسخة مصدر من مشروع الأخبار المستقلّ. **الحيّ يعمل من
  `C:\Users\NQ\QUANT_NQ_NEWS`** ببيئته و`.env` الخاصّين. يكتب جدولَي `news`
  و`calendar` في جسر ميتاتريدر، ومنه تقرأ الذرّة 616 ثمّ 108 فيمتلئ تبويب
  «الأخبار». **تبويب أخبار فارغ ⇒ افحص البوت أوّلًا، لا الذرّات.**
* **`nq_brain.db`** في مجلّد MetaQuotes المشترك — **يكتبه إكسبرت MT5**، لا
  النظام. إن مات الإكسبرت جمدت جداوله ولو كان النظام كلّه سليمًا.

التفصيل الكامل في
`docs/docs/١٦_الخرائط_والمراجع/مشتركة/٠٠ المشاريع الشقيقة — ما هو خارج هذا المجلّد.md`.

---

## ٧) تقرير الجهاز بتاريخ 2026-08-31، ثم حكم checkout الحالي

الفقرة التالية **تقرير تشغيل تاريخي على جهاز المالك** وليست برهانًا حيًا
للـcheckout الحالي: ذُكر أن النواة والسوقين واللوحتين ومنصة تلغرام `610`
وتبويب الأخبار وإنذار سقوط النواة وحارس مصدر التغذية كانت تعمل.

بعد جولة إصلاح مختومة بـ`NQ` في `2026-09-01`: Core بقيت مختومة، وفوركس
`233/233`، وسلة المستودع `875 passed, 2 skipped`، وكريبتو `237 passed`،
وخطوط السلامة الثلاثة مطابقة. بُنيت واجهة governance ونجح فحص المشروع؛ بقيت
الخزنة غير مهيأة، ولم تُقَس الخدمات المالية الحية في بيئة التدقيق.

**النواة `1.31.0` — توحيد خطّي عمل متوازيين.** يوم ٢٠٢٦-٠٨-٣١ اشتغل خطّان
على الجذر نفسه: خطٌّ محليّ، وخطٌّ على المستودع (فرعا `fix/global-scheduler*`
المدموجان في master). كلٌّ منهما أصلح ما لم يصلحه الآخر، فوُحّدا:

* **من خطّ المستودع:** الناقل لا يملك ساعة إطلاقًا (كل ختم من `clock.now()`،
  و`set_time_offset` أُلغيت، والمُقلِع ما عاد يشترك على `time.utc.synced`)؛
  بِركتا خيوط محدودتان مع بِركة **محجوزة** للوقت والحالة والأوامر؛ النشر من
  حلقة أخرى يُعاد توجيهه لحلقة النواة؛ الإقلاع دخل سياسة دورة الحياة
  الموحّدة؛ والذرّة 717 صارت `2.1.0` تقرأ `clock`.
* **من الخطّ المحليّ:** الذرّة 619 `3.1.0` (مع إعلان البيات عند **تبدّله**
  لا كل دورة)؛ والذرّة 582 `1.5.0` (عدّاد مقارنات حقيقيّ — الصحّة لا تُعلَن
  سليمة لمجرّد وصول واقعة، وإلّا أعلن كاشف التلاعب السلامة وهو أعمى)؛
  وحارس التغذية 613 `2.6.0`؛ وتصليحات اللوحة؛ ومسار `GET /api/bus-stats`.
* **أُصلح قبل الاعتماد (مقيسًا لا رأيًا):** `_coalesce_key` كانت انتقلت
  داخل الصنف فانكسر استيرادها في `transport/ownership.py` وسقطت طبقة ناقل
  الأحداث كلّها عند التجميع؛ و`_worker_entry` كانت تنادي `asyncio.run` **لكل
  تسليم**، أي حلقة أحداث جديدة لكل حدث — والمقيس على هذه الشجرة: ٢٦ ذرّة
  تستعمل `create_task`، و٥ `Lock`، و٣ `Event`، و٨ `get_running_loop`، وكلّها
  مربوطة بحلقة النواة. المعالج غير المتزامن بقي على حلقة النواة.
* **أُضيف:** ميزانية زمن لكل تسليم — التجاوز يُعدّ ويُعلَن في
  `stats()["overrun"]`، ومعه `oldest_pending_age_s`، لأن طول الطابور وحده
  لا يميّز واقعة عمرها ١ ملّي من أخرى عمرها ٣٠ ثانية.
* **وأُصلح سكربت الختم نفسه:** ختمٌ ثانٍ بلا تغيير كود كان يمسح حلقة
  السلسلة (`previous_root_digest` تصير مساويةً للجذر). السلسلة هي ما يُعرَف
  به أن ختمًا واردًا من الخارج امتدادٌ لنواتنا لا فرعٌ عنها.

**مفتوح — لا تفترض نتيجته:**

1. **اقتران تلغرام** لم يتمّ (يُرسَل رمز الاقتران من موبايل المالك).
2. **الذرّة 622 (تغذية cTrader):** `FIX_LOGON_REFUSED: RET_INVALID_DATA`.
   الهويّة في المانيفست **مطابقة لإعدادات الوسيط حرفيًّا**؛ المتغيّر الوحيد
   قيمة `ctrader_fix_password`.
3. **الذرّة 582 لا تقارن بعد** (`compared=0`) — لأن سعر المرجع من سي‑تريدر
   لا يصل (البند ٢). العدّاد صادق الآن: لا يعلن سلامة بلا مقارنة.
4. **قائمة الأصول (468):** `BTCUSD` وحده مسموح — أي رمز آخر يُحجب عند أوّل
   بوّابة. **تفعيل الرمز قرار المالك من اللوحة.**
5. **حاجز التداول القائم:** `VOLUME_BELOW_BROKER_MIN` — الرصيد التجريبي
   أصغر من الحدّ الأدنى للوت على الرموز المفعّلة. **حاجز حقيقيّ لا يُتجاوَز**؛
   الحلّ رمزٌ بحدّ أدنى أصغر أو رصيد أكبر، وهو قرار المالك.
6. **أُغلق عطل حارس سلامة كريبتو بختم `NQ`:** صحح مسار مانيفست 2007، وصار
   `--check` يرفض stale/removed/added وتغير النطاق. الخطوط الحالية: root
   `1300`، Forex `1300`، Crypto `642`، وكلها مطابقة.

**ثلاثة دروس لا تُكرَّر:**

* **حارس بلا مفتاح = لا حارس.** إنذار سقوط النواة كان مكتوبًا وسليمًا،
  ومعطَّلًا بمؤقّت يُصفَّر قبل أن يصل. ومكتبة الترجمة كان غيابها يُبتلع
  بصمت. **كل `except` صامت مشتبه به.**
* **الفرض يغلب الافتراض.** مُشغّل الحوكمة كان يفرض مسار قاعدة أخبار غير
  موجودة، فيُبطل افتراضًا صحيحًا موثَّقًا في الخادم — وأعاد عطلًا سبق
  إصلاحه. لا تفرض مسارًا في مُشغِّل إن كان الخادم يحمل افتراضًا موثَّقًا.
* **عدّادٌ يعدّ الوصول لا العمل يكذب.** 582 كانت تقول
  `divergence_updates=50496 unaligned=0` وهي لم تقارن ولا مرّة.
  **`unaligned=0` تعني «لم نقِس» لا «لا تلاعب».**

---

## ٨) ترتيب القراءة لأي مناوبة جديدة

```
1. هذه الورقة
2. docs/docs/٠٠ اقرأني — البروتوكول الإلزامي.md
3. docs/docs/١٣_المفتوح_والمعلّق/     ← القائمة الحيّة
4. docs/docs/١١_المناوبات/            ← آخر ملفّ: ماذا فعل من سبقك
5. docs/docs/١٤_الأعطال_المعروفة/     ← قبل أي تشخيص
```

> **`docs/docs/` هي شجرة التوثيق الوحيدة.** كانت هناك نسخة ثانية
> (`docs/اكمل من هنا/`) حذفها المالك بيده يوم ٢٠٢٦-٠٨-٣١ لأنها تكرار.
> ملفّاتها محفوظة في تاريخ المستودع ولا تضيع — تُستعاد بـ
> `git show 5a08104:"docs/اكمل من هنا/<المسار>"`. **لا تُنشئ نسخة ثانية.**
