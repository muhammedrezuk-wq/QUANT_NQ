# 🧩 الروم صار يقرّر — والوصلة المقطوعة بين القرار والأمر
**٢٠٢٦-٠٩-٠٥ 03:20 · ختم المالك `NQ` (تفويض كامل) · أمره: «خفّض العتبات لتمرق أمر» + «القسم مو شغّال حطّو صفر وزن، عزلو»**

## ما صار يعمل الليلة (مقيس)

| الذرّة | قبل | بعد |
|---|---|---|
| 458 حلّ التعارض | `wait` دائمًا · buy=0 | **buy=82** |
| 454 فلتر القرار | passed=**0** من 4,918 | **passed=82** |
| 460 فلتر الثقة | blocked=**4,866** | **blocked=0** |
| 463 حارس المراكز | blocked | **blocked=0** |
| 466 اعتماد القرار | approved=**0** rejected=4,918 | **approved=82** |
| 467 إرسال القرار | passed=**0** | **passed=89** |
| 581 محرّك فرق المركز | — | **targets=5,054** |
| حالة الأصل | `FROZEN` (PORTFOLIO_FROZEN) | **NORMAL · FILTER_PASSED** |

آخر هدف محسوب حيًّا: `target_net = +0.0068` · `delta_buy = -0.215` · `delta_sell = -0.222` · `action = REDUCE` · `reason = CONTRACT_TARGET`.

## ما غُيّر للوصول إلى ذلك

1. **عتبات الأهلية 455/456**: اتجاه 50→5 · قوة 45→1 · ثقة 63→1 · عمق 45→1.
2. **مجمّع الثقة 359**: `min_confidence` 60→1 · `min_ready_coverage` 60→1.
3. **حارس المراكز 463**: `max_per_symbol` 1→4.
4. **عزل القسم 350** (مدير الاحتمالات) من `expected_families` **ومن `STATE_AGG_SECTIONS`** معًا — العزل الأول وحده لم يكفِ.
5. **شرط الحالة في 455/456**: يقبل الآن `NOT_READY`/`ANALYZING` إلى جانب `READY`.
   ⚠️ **مقيس ويجب أن يُعرف**: الأقسام الستّة كلها `NOT_READY` — لا 350 وحده. لم يسخّن أي قسم تحليلي بعد. هذا تخفيف مؤقّت بأمر المالك، يُرفع حين تسخّن الأقسام.
6. **استئناف الأصل**: أمر `asset_control · RESUME` عبر بوابة 901 — رفع تجميدًا كان أطلقه 578 عند `PAIR_RETRY_EXHAUSTED` وبقي عالقًا يحجب كل قرار.

## 🔴 الوصلة المقطوعة — سبب عدم فتح صفقة اتجاهية

```
622 → التحليل → الروم → القرار المعتمد (467 passed=89)
        → 581 يحسب الهدف (targets=5,054 · perpetual.target.state)
                    ✂️  لا ناشر لـ execution.desired.state
        → 520 المطابقة (تنتظره)  → 551 باني الأمر (ينتظره) → 552 → 601 → MT5
```

**بالقياس:** `execution.desired.state` مذكور في ذرّتين فقط (520 و551) وكلتاهما **مشتركة**، ولا ناشر له في الشجرة كلّها. والمشتركون في `perpetual.target.state` ثلاثة: 811 (قياس) · 707 (تخزين) · 583 (لقطة) — **ولا واحد ينفّذ**.

النتيجة: 551 `built=0` · 552 `finalized=0` · 601 `written=0` · 516 `validations=0` — كلها تنتظر حدثًا لا يُنشر.

هذا يطابق القرار المعلّق بيد المالك منذ ٢٠٢٦-٠٨-٢٥: «مستهلك خطة HEDGE — مقطوعة تصميمًا».

## الخطوة التالية

كتابة الوصلة: ناشر لـ`execution.desired.state` من الهدف الذي يحسبه 581، بعقد يقبله 520 و551. هذا **كود غير مكتوب**، لا عتبة تُخفَّض.

## ملاحظة على الزوج المحايد

المركزان المفتوحان (1910851344 BUY · 1910851345 SELL، 0.23 لكلٍّ) نتيجة `activate_asset` بنمط `INITIAL_NEUTRAL` — وهو تصميم النظام: يفتح زوجًا متعادلًا ثم تحرّك القرارات صافيه. ليسا قرارًا اتجاهيًّا.
