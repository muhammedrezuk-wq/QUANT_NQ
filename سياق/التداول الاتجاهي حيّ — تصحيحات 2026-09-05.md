# ✅ التداول الاتجاهي حيّ — سجلّ التصحيحات
**٢٠٢٦-٠٩-٠٥ 13:05 · ختم `NQ` · حساب الديمو 52992818 · BTCUSD · التغذية من cTrader**

## الحالة الآن

أربعة مراكز **شراء فقط** (لا ساق بيع واحدة — تداول اتجاهي لا تحوّط):

| التذكرة | الدخول | الوقف | المسافة |
|---|---|---|---|
| 1910962432 | 79,677.15 | 78,083.61 | 2.00% |
| 1910962676 | 79,674.82 | 78,081.32 | 2.00% |
| 1910963102 | 79,659.68 | 78,066.49 | 2.00% |
| 1910963476 | 79,652.37 | 79,206.34 | 0.56% |

كلها بأوامر `dir-dec:…` — أي بهوية قرار الروم، عبر السلسلة الكاملة.

## التصحيحات الخمسة (كلها دائمة في الكود)

1. **الهيدج بدل التداول** — كان المترجم ينشر الجهتين معًا، وسقف الخطوة يقصّهما إلى نفس الرقم فيمحو الاتجاه (الهدف المقيس: شراء 2.06 مقابل بيع 0.23 → خرجتا 0.05 و0.05). الآن ينشر **الساق الاتجاهية وحدها** حسب إشارة `target_net`. (`shared/position_delta_recompute.py`)

2. **قفل المطابقة من جذره** — أي مركز فعلي لا يظهر في `desired` كان `EXTRA_AT_BROKER` → `ATTENTION` → رفض كل أمر جديد. الآن مركز يحمل ماجيك النظام (20260801) أو تعليقه (`NQ`) يُصنَّف **`ADOPTED_OWN`** ويمرّ؛ والمركز الغريب فعلًا يبقى إنذارًا. (`520/reconcile_support.py` · دالة `own_position`)

3. **وقف عند نصف السعر** — قِيس وقف شراء 39,677 على دخول 79,677 (مسافة 50%، أي مركز بلا حماية). ثلاثة حواجز أُضيفت:
   - `513`: أي وقف هيكلي أبعد من `MAX_STRUCT_STOP_FRAC=0.05` يُرفض ويُستعمل الافتراضي.
   - المترجم: `frac = min(frac, MAX_STOP_FRAC=0.02)`.
   - **`601` (البوابة الأخيرة قبل الجسر):** أي `stop_loss` يبعد أكثر من `MAX_SL_FRAC=0.05` عن السعر المرجعي أو سعر الدخول يُرفض بـ`STOP_LOSS_TOO_FAR` بدل أن يُكتب — يحمي من أي مصدر مهما كان.

4. **ذرّة 580 ساقطة عند كل قرار** — `import gate_runner` داخل المعالج كان يرمي `ModuleNotFoundError` لأن مجلد الذرّة لا يبقى في `sys.path` وقت الاستدعاء المتأخّر. **مقيس: 12,316 خطأ**، آخرها 12:48:36، و**صفر** بعد الإصلاح (`_sibling()` تحمّل من مسار الملف).

5. **المراكز المفتوحة بوقف خاطئ** صُحّحت إلى 2% عبر `MODIFY_SL` (أوامر 17,18,36,37,38 — كلها `DONE/OK`).

## ما يجب أن يُعرف قبل أي شوط قادم

- **الحساب مجمّد الآن** لأن المراكز بلغت `max_per_symbol=4` في 463 — سلوك حماية، لا عطل. لن يفتح جديدًا حتى تُغلق مراكز.
- **التخفيفات المؤقّتة ما زالت قائمة** (بأمر المالك «خفّض العتبات لتمرق أمر»): عتبات 455/456 عند 1-5 · 359 عند 1 · قبول `NOT_READY` في بوابة الأهلية · عزل العائلة 350. **الأقسام التحليلية الستّة كلها `NOT_READY`** — لم يسخّن أيٌّ منها بعد. رفع هذه التخفيفات قبل تسخين الأقسام يوقف التداول من جديد.
- **576 و578 يعملان** الآن (لم يعودا موقوفين) — إصلاح المطابقة أغنى عن إيقافهما.
- مصدر الوقف البعيد الأصلي (أمر إدارة أعطى 66,337 ثم 39,677) لم يُلاحَق إلى ذرّته بعد؛ حارس 601 يمنع أثره، لكن السبب يستحق فحصًا.

## أدوات القياس في مجلد الجلسة المؤقّت

`launch.py` (يقتل السابق ويشغّل) · `revive.py` (622 ثم 618 — إلزامي بعد كل إقلاع) · `final_check.py` · `watch_chain.py` · `identity_thread.py` (يتتبّع هوية القرار حتى التذكرة) · `fix_open_stops.py` · `flatten.py`.
