# 🏁 أول صفقة عبر السلسلة الكاملة — ٢٠٢٦-٠٩-٠٥ (01:12)

**الختم:** `NQ` من المالك (تفويض كامل ٢٠٢٦-٠٩-٠٥) — «اعمل اللي بدك، ولا تحكي معي غير لما تفتح صفقة».

## النتيجة المقيسة

حساب **52992818** · `ICMarketsSC-Demo` · Raw Trading Ltd · رصيد 10,494.87$

| التذكرة | الرمز | الجهة | الحجم | الدخول |
|---|---|---|---|---|
| **1910851344** | BTCUSD | BUY | 0.23 | 79,674.96 |
| **1910851345** | BTCUSD | SELL | 0.23 | 79,669.96 |

أمرا الجسر `id=1` و`id=2`: `status=DONE · result=OK`.

**المسار المقطوع كاملًا:** تِكّة من **622** (cTrader FIX، لا MT5) → 613 → 101 → التحليل والاستراتيجيات → **452 قيّم** → الروم → **901** بوابة الأوامر → **576** المحرّك الدائم → **578** الزوج → 551/552 → **601** كاتب جسر الدماغ → `nq_brain.db` → الإكسبرت → **MT5**.

## الحيطان الخمسة التي كانت تمنع الصفقة (بالأرقام)

1. **سباق الخزنة يقتل التغذية نهائيًّا** — 622 تحاول الاتصال قبل أن تُفتح `secrets.enc`، تفشل، والكود يوقفها عمدًا «حتى يعيد إنسان تشغيلها». النتيجة: `FIX_NEVER_SEEN` و**89,848 فشل** يوم ٠٩-٠٤. **الحل المؤقّت: stop/start للذرّة 622 بعد الإقلاع** (يعطي فورًا آلاف التِكّات). ⚠️ **الإصلاح الدائم لم يُنفَّذ بعد** — يجب تأخير أول اتصال حتى جهوز الخزنة، أو إعادة المحاولة بعدها.
2. **أداة تفعيل الأصل تقرأ جدولًا مهجورًا** — `start_asset.py` كانت تقرأ `account` (0 صف) بدل `account_v2` (1 صف) فتردّ «لا توجد هوية حساب» وتوقف كل شيء. **أُصلحت** (تقرأ الحيّ أولًا والقديم احتياطًا).
3. **`vpu=None` يجمّد 576 صامتًا** — المحرّك يملك السعر ويفتقد قيمة الوحدة، فيبقى `pending` بلا رسالة. السبب: حدث `market.symbol_specs` من 618 يصل **قبل** أن يعرف 576 هوية الوسيط، فيُركن جانبًا ولا يُعاد. **الحل المؤقّت: stop/start للذرّة 618 بعد استقرار الهوية** → تصل المواصفات (BTCUSD: tick_value=0.01 tick_size=0.01 → vpu=1.0) ويُبنى الزوج فورًا. ⚠️ **الإصلاح الدائم لم يُنفَّذ** — `_pending_specs` يجب أن يُعاد تشغيلها عند وصول هوية الحساب.
4. **`SPREAD_TOO_WIDE` كاذبة** — 552 يعامل «سبريد غير معروف» معاملة «سبريد واسع» فيرفض. السبريد الحقيقي كان **500 نقطة** مقابل حدّ **2000**. الرسالة تكذب على المالك. ⚠️ **لم يُصلح** — يجب التمييز بين UNKNOWN وWIDE.
5. **حالة `EXHAUSTED` دائمة تمنع أي محاولة جديدة** — تبقى في `pair_memory_578.db` **وفي لقطة `snapshots/578.json`**، فتُستعاد عند كل إقلاع. أُرشف الملفان (نقل لا حذف) فعاد 578 `READY`.

## تعديلات دائمة على الكود

- `start_asset.py` (النسختان): تقرأ `account_v2`.
- تسجيل تشخيصي أُضيف في: 611/618/619 (يطبع مسار قاعدة الجسر عند الفشل) · 622 (دورة الضخّ) · 576 (سبب البقاء pending) · 578 (سبب فشل كل ساق) · 552 (ما يراه لحظة رفض السبريد).
  ⚠️ تتبّع 622 (`622 pump#`) صاخب — يُزال بعد الاستقرار.

## طريقة التشغيل التي أوصلت للصفقة

```
python scripts\run_forex.py        (مع QUANT_GOV_API_KEY)
ثم stop/start للذرّة 622          ← تعيد التغذية
ثم stop/start للذرّة 618          ← تعيد نشر المواصفات
ثم governance\scripts\start_asset.py BTCUSD 100
```

## الناقص للجلسة القادمة

- إصلاح دائم للحيطان 1 و3 و4 (أعلاه) — بلا stop/start يدوي.
- تقليص رموز 622 إلى BTCUSD وحده (المالك: «كل الأزواج ما بقى بدي ياها»). حاليًّا المانيفست فيه سبعة.
- إنذار على اللوحة عند فشل ذرّة جسر متكرّر — 89,848 سطرًا ذهبت لملف لم يقرأه أحد يومًا كاملًا.
- إزالة التتبّع الصاخب من 622.
