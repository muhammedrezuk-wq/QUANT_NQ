# 📌 حالة التسليم — ٢٠٢٦-٠٩-٠٥ 14:45
**الحساب 52992818 (`ICMarketsSC-Demo`) · BTCUSD · التغذية من cTrader**

## المراكز المفتوحة — آمنة

أربعة مراكز **شراء** 0.05 لكلٍّ، وقفها **2%** وهدفها محدَّد:
1910962432 · 1910962676 · 1910963102 · 1910963476.
الرصيد 10,491 · الملكية 10,483.

## ما أُنجز اليوم (كله دائم في الكود)

| # | التصحيح | الدليل المقيس |
|---|---|---|
| ١ | **الوصلة المفقودة بُنيت** — `request_orders()` تترجم هدف 581 إلى `execution.order.requested` بهوية القرار (`parent_decision_id` + `gate_request_id`) | خمس صفقات نُفِّذت بمعرّف `dir-dec:…` |
| ٢ | **ساق اتجاهية وحدها** بدل زوج متعادل | الطلبات صارت `side=buy` فقط |
| ٣ | **حجم معقول**: سقف الخطوة 0.05→0.5 · سقف المراكز 4→20 · لا إضافة أصغر من 20% من الهدف | الطلبات صارت 0.5 بدل أربعين خطوة 0.05 متطابقة |
| ٤ | **حارس الوقف عند 601** + سدّ ثغرته (التذكرة نصًّا مقابل عمود رقمي) + رفض عند غياب المرجع | أوقاف 39,677 (50%) و54,666 (31%) صُحّحت كلها إلى 2% |
| ٥ | **513**: أي وقف هيكلي أبعد من 5% يُرفض | — |
| ٦ | **قفل المطابقة** — مركز بماجيك النظام يُتبنّى (`ADOPTED_OWN`) | 552 صار `rejected=0` بعدها |
| ٧ | **580 كانت تسقط عند كل قرار** (`gate_runner` لا يُستورد متأخرًا) | **12,316 خطأ → صفر** |
| ٨ | **578**: الأزواج المنتهية (EXHAUSTED) لا تُستعاد عند الإقلاع فلا تعيد إرسال `pause` | — |
| ٩ | **519**: FROZEN لم تعد حالة نهائية أبدية — تعود NORMAL بزوال سببها | — |
| ١٠ | **قاطع الأمان صُفِّر** بأمر المالك عبر بوابة 901 | `901: reset=1` |

## 🔴 أين وقفت بالضبط

النظام **مجمّد على `PORTFOLIO_FROZEN`** رغم أن:
- دفتر المخاطر 518 **نظيف**: `budget=5000` وصفر استهلاك مسجّل.
- قاطع الأمان **مُصفَّر** (`halt=0 reset=1`).
- الأصل **مفعّل** والساعة **متزامنة** (الذرّة 3: `quality=SYNCED`).
- 581 يصدر طلبات شراء 0.5 كل دقيقة، والقرار `FILTER_PASSED · net=+4.57`.

**الخطوة التالية المحدَّدة:** حقن تسجيل في `519._portfolio_row` يطبع `(scope, u, warning, breached, current)` عند كل تحديث دفتر — يكشف أيّ الحقول يُبقي الحالة FROZEN بينما الدفتر نظيف. بدون هذا الرقم أي محاولة أخرى تخمين.

**حاجز محتمل بعده:** 552 يرفض بـ`CLOCK_NOT_SYNCED` رغم أن الذرّة 3 تقول `SYNCED` — يُرجَّح أن كلًّا منهما يحمّل نسخة منفصلة من وحدة `clock` (نفس نمط عطب 580). التسجيل مُضاف في 552 ويطبع `clock.__file__` و`id(clock)` عند أول خمس حالات رفض — يحسمها فور وصول طلب إلى 552.

## تكوين التشغيل الحالي

- **576 و578 `startup_mode: manual`** — لا يعملان تلقائيًّا (لا تحوّط ابتدائي). أعِدهما `auto` متى أراد المالك التحوّط.
- التخفيفات المؤقّتة قائمة: عتبات 455/456 عند 1-5 · 359 عند 1 · قبول `NOT_READY` · عزل العائلة 350.
- الأقسام التحليلية الستّة كلها `NOT_READY` — لم يسخّن أيٌّ منها بعد.

## أدوات الجلسة (في مجلد scratchpad المؤقّت)

`launch.py` · `revive.py` (622 ثم 618، إلزامي بعد كل إقلاع) · `activate.py` · `gov_command.py` (أوامر البوابة) · `final_check.py` · `wait_directional.py` · `fix_open_stops.py` · `identity_thread.py` · `status_now.py`.
