# مناوبة NQ — 2026-08-28 · فوركس

**النهاية المسجَّلة:** 21:15
**ملاحظة:** هذه أوّل مناوبة موثَّقة بهذا النظام. ما قبلها موثّق في
`docs/٦_التدقيق/` (الأوراق ٣٧–٤٥) و`docs/٥_السياق/`.

---

## سجلّ الحركة

| الساعة | ماذا | النتيجة (مقاسة) |
|---|---|---|
| ~14:00 | تشغيل المشروع بعد فكّ ضغط جديد | ❌ فشل — روابط Junction معطوبة (26) |
| ~14:30 | إزالة الروابط + `prepare_unified` | ✅ 26 رابطًا · 5 منافذ خضراء |
| ~15:00 | تشغيل تلغرام + بوت الأخبار | ✅ 8098 حيّ · البوت يعمل |
| ~16:40 | نشر على غيت هب | ✅ `ba44940..7ee5c23` نظيف |
| ~17:00 | تدقيق شامل حيّ للنظام | 226 ذرّة: **212 خضراء · 14 كهرمانيّة · 0 حمراء** |
| ~18:30 | **المالك اعتمد المُعامِلات الستّة** | ✅ `provisional=false` · النماذج 2→3 |
| ~19:00 | تشخيص «لماذا لا تُفتح صفقة» | مساران مسدودان بسببين مختلفين |
| ~20:15 | **كشف جذر مشترك: ساعة خادم الوسيط** | 3 ساعات بالضبط — مؤكَّد بالكود |
| ~20:37 | إصلاح زرّ غرفة القيادة (ختم `NQ`) | ✅ `%~dp0\..` |
| ~21:09 | تنظيم شامل لـ`docs` | 301 ورقة · 6 أوراق مالك أُنقذت |
| 21:15 | **بناء نظام التوثيق المشترك (ختم `NQ`)** | 24 مجلّدًا + بروتوكول + قوالب |

---

## الاكتشافات (الأثمن للشريك)

### ١) جداول v1 مهجورة منذ 11 يومًا — **خطر قراءة كاذبة**
الـEA (نسخة 3.10) انتقل كليًّا لجداول `_v2`. **أي قراءة من `account` أو
`prices` أو `trade_events` (v1) تعطي صورة قديمة كاذبة.** وقعتُ في هذا الفخّ
فعلًا وظننت الـEA متوقّفًا وهو يعمل. **اقرأ `_v2` دائمًا.**

### ٢) جذر واحد يفسّر أعطالًا كثيرة تبدو منفصلة
`REFERENCE_NOT_USABLE` · `CLOCK_NOT_SYNCED` · `ACCOUNT_STATE_STALE` ·
`ROWS_IN_THE_FUTURE` ⇒ **كلّها من فرق ساعة خادم الوسيط (3 ساعات).**

### ٣) الخطأ الذي وقعتُ فيه — مسجَّل كي لا يتكرّر
شخّصت «ساعة الويندوز» ثمّ «خادم متلاعب» — **وكلاهما خطأ**، نفاه المالك بقياس
`w32tm` (103 ملّي ثانية). **الدرس: لا تشخّص من الاستنتاج — قِس المصدر مباشرة.**
الحقيقة ظهرت فقط عند قراءة `mt5/QUANT_NQ.mq5:775` مباشرة.

---

## الإغلاق

**ما أُنجز:** النظام يقلع كاملًا · النشر تلقائيّ يُصلح نفسه · تدقيق شامل
موثّق (9 أوراق) · اعتماد المُعامِلات · نظام التوثيق المشترك.

**ما بقي مفتوحًا:** ← `٦_المفتوح_والمعلّق/`

**⚠️ تحذير للشريك:**
1. **لا تعدّل كودًا** — المشروع مجمّد `FRZ-20260827-F39E291C` بلا ختم `NQ`.
2. **بعد أي فكّ ضغط جديد** النظام لن يقلع حتى تُزال روابط Junction المعطوبة
   (الوصفة في `٣_مشترك/البنية_التحتية/`).
3. يوجد **`QUANT_NQ_RC7`** (21 ميغا) داخل جذر المشروع — لم يُلمس، ينتظر قرارًا.
