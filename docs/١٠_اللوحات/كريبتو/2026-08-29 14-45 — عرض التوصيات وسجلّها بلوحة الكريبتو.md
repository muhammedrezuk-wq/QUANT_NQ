# عرض التوصيات وسجلّها بلوحة الكريبتو

**الوقت:** 2026-08-29 14:45 · **المناوب:** NQ · **الختم:** `NQ`

---

## العَرَض

> «بس التوصيات ما عم تظهر على لوحة، ما شفت شي — ولا سجلّ توصيات.»

**والذرّات كانت تُصدرها فعلًا:** `#2277 published=2` ثمّ `emitted=5`.

## السبب — «النمط النائم»: حدث يُنشر بلا مستهلك

```
#2277  →  crypto.decision.signal_card.state   ← يُنشر ✅
الواجهة (ui/src كاملة)  →  صفر ذكر لـsignal_card   🔴
```
**التوصية تصدر وتضيع.** `Judgement.tsx` (تبويب «الحكم والقرار») كان يعرض
**صحّة القضاة فقط** — لا محتوى البطاقة.

*(نسخة أحمد `CryptoDashboard.tsx` = 40,501 بايت وتعرضها؛ نسختنا 14,889 بايت
ولا تعرضها — بنيتنا فرّقت اللوحة إلى تبويبات (`Universe` · `Senses` ·
`Judgement` · `Mexc`) ولم تُنقل لوحة البطاقة معها.)*

## ما نُفِّذ — في `Judgement.tsx` فقط

**١) لوحة البطاقة الحيّة** — تُصيَّر بحقولها كما تصل من الحدث، بلا حساب:
```
الرمز · الاتجاه (شراء/بيع بلون) · الصنف · الدرجة · الحلقة
المرساة · الدخول · سلّم الدخول · الوقف (+%) · الهدف١ · الهدف٢ · الراكض
الإلغاء · وقف زمنيّ · أقصى مخاطرة · المتنافسة · خبر طازج
```

**٢) سجلّ دائم** من `/gov/decisions?limit=60` (مرشَّح على `stage=APPROVED`)
كل 20 ثانية، **وأي بطاقة حيّة تتصدّره فورًا.**

> السبب: البطاقة تُنشر لحظيًّا — فمن يفتح اللوحة بعد صدورها **لا يرى شيئًا**.
> السجلّ الدائم يبقيها بعد إعادة الفتح.

## التحقّق — مقاس بعد البناء

```
التوصيات المستلمة: 27

13:44:01  PEPE_USDT  بيع  B   وقف 3.620e-6
13:40:10  XRP_USDT   بيع  B   وقف 1.42090872
13:38:15  DOGE_USDT  بيع  A   وقف 0.08735259
13:21:39  LTC_USDT   بيع  A   وقف 49.703724
```
**السلسلة الحيّة وقتها:** `#2274 emitted=5` · `#2276 published=4`.

## ملفّات لُمست

```
governance/ui/src/sections/Judgement.tsx     (لوحة + سجلّ)
governance/ui/built/                          أُعيد بناؤه
```
**صفر لمسة على ذرّات أحمد** — `atoms_crypto` تبقى مرآة ملفّه (84/84 مطابقة).
التغيير في الواجهة فقط، وهي أصلًا متفرّعة عن نسخته ببنية تبويبات مختلفة.

## ما بقي مفتوحًا

- **حقل «الدخول» يظهر `—` في صفوف السجلّ الدائم** — `/gov/decisions` لا يخزّن
  `entry_price` (يخزّن `stop_loss` و`take_profit` فقط). البطاقة الحيّة تعرضه
  كاملًا. **غياب مُعلَن لا مخترَع.**
- `#2275` و`#2277` يظهران `CANDIDATES_STALE` / `SIZED_ENTRY_STALE` بين
  التوصيات — **طبيعيّ**: التوصية حدث نادر، لا تدفّق مستمرّ.
