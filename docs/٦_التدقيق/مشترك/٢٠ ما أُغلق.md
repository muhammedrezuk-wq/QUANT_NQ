# ٩ · ✅ ما أُغلق

| البند | ما أغلقه |
|---|---|
| سلسلة القرار مقطوعة عند `613` | `_IDENTITY_KEYS` صارت تحمل `sequence` و`tick_id` — السلسلة تصل البوّابة |
| قسم التحليل بلا بطاقة | `150` ينشر دورتين · `166` v2.6.0 يدمج ويُخرج بطاقة · `451` يقبلها كقسم `150` |
| المسار البطيء مُسقَط | `slow` و`fused` لم يعودا صفرًا — المسار البطيء يعمل |
| فلتر السبريد كحاجز | `552` v5.2.0 · حاجز fail-closed |
| دورة حياة الزوج غير معلنة | `578` ينشر `perpetual.pair.state` |
| فكّ التحوّط غير موجود | `unhedge.py` موجود في `578` |
| مسافة التقلّب `ATR` | منفَّذة في `153` |
| لا انتقال من نشِط إلى منتهٍ | `576` فيه ثلاث إشارات إنهاء |
| **طريق تنفيذ خارج بوّابة القرار** | **`576` v3.3.1 و`578` v5.1.1 — الوحدة ١ (٢٠٢٦-٠٨-٢٣):** السلطة المعلَنة صارت **سلطة محقَّقة**. `parent_decision_id` لا يُقبل إلا إذا نشر `467` له `decision.gate.passed` فعلًا. وأوامر المالك من `901` تمرّ كما هي. **وحارس `check_no_side_path_contract` يفحصها: ١١/١١ نجاحًا · صفر فشل.** |
| حالة الحساب بائتة | `619` ينشر بانتظام — لم تعد بائتة |
| رسالة صحّة `451` تكذب باسمها | `health_check` صار يعرض العدّاد الصحيح |
| `517` لا يميّز صفرًا حقيقيًّا من حقل فارغ | الكود يميّز صراحةً |
| `622` لا توقظ نفسها من صمت الوسيط | فيها إعادة اتّصال ونبضة وفحص صمت |
| الناقل ينتظر مستمعيه بالتسلسل | صار توازيًا + مهلة لكل معالج + عزل |

---

---

**`الدستور/` للقوانين والقيود · `الهندسة/` للمعمار والرؤية**
