# ٦ · التكيّف — كان مطفأً بلا رجعة، وصار له طريق إرجاع

```
860  ADAPTATION_OFF:HOT_PATH_LATENCY_REGRESSION:150
```

**الشرط الذي أطفأه:**

```python
DEFAULT_LATENCY_BUDGET_MS = 50.0
LATENCY_TRIP_MULTIPLIER   = 3.0
if p99 > budget * 3: trip(...)
```

**والمشكلة كانت أنّ الطريق ذهابٌ بلا إياب:** حدث الإرجاع `adaptation.kill_switch.command`
**لم يكن له ناشر في المشروع كلّه** — فمتى أُطفئ التكيّف، لا يُعاد إلا بإعادة تشغيل.

**(تحديث ٢٥-٠٨: أُغلق — `901` بوّابة الأوامر `v2.6.0` صارت تنشر الحدث (`manifest.yaml:12` ·
`atom.py:62`) بفعل `adaptation_switch` بهويّة المالك، ON/OFF فقط بلا توسيع.
⇒ الإطفاء صار قابلًا للإرجاع من بوّابة أوامر المالك بلا إعادة تشغيل.)**

---

---

**`الدستور/` للقوانين والقيود · `الهندسة/` للمعمار والرؤية**
