# قرار عقد مخالفات الذرات ونتيجة X Build 2

**التاريخ:** 2026-08-27  
**الحالة:** تصنيف وقرار مطلوب؛ لا إصلاح Atom ولا فتح HotReload.  
**المصدر:** `validate_atoms.py --json` والاختبار الكامل ببيئة dependencies مكتملة.

---

## 1. الوضع المؤكد

```text
Build Contract       = RESOLVED
Build ID             = QUANT_NQ_COMPLETE_FULL
Registry              = PASS
Core Boundary         = PASS
Core Boot             = PASS
Execution Safety      = PASS
Documentation         = WARNING فقط
X Build 2             = PASS
```

الـ44 مخالفة ليست مشكلة إقلاع أو Registry أو Core.

---

## 2. جدول الـ44

| الفئة | العدد | الحالة الحالية | القرار المطلوب |
|---|---:|---|---|
| لغة الكود | 26 | Validator rule mismatch / Legacy candidate | تحديد نطاق قاعدة اللغة |
| أرقام سحرية | 14 | Contract review required | تحديد مالك كل قيمة |
| حجم الذرة | 2 | مخالفة جودة حقيقية | إصلاح لاحق أو استثناء مالك موثق |
| اختبارات ناقصة | 2 | فجوة اختبار حقيقية | إضافة اختبار حقيقي أو قرار نطاق |

```text
26 + 14 + 2 + 2 = 44
```

---

## 3. لغة الكود — 26

الذرات:

```text
150, 151, 152, 153, 154, 155,
162, 163, 164,
201, 208,
262, 263, 264,
516, 520, 523, 575, 578, 581,
601, 707, 717, 832, 840, 870
```

### الدليل

`validate_atoms.py:294-295` يفحص وجود العربية في كامل نص `atom.py`:

```python
if re.search(r"[\\u0600-\\u06ff]", source):
    fail("لغة الكود", ...)
```

بينما `validate_atoms.py:451-460` يحتوي علمًا منفصلًا:

```text
--strict-language
```

وتقول تعليقاته إن المنع الصارم مخصص للذرات الجديدة/الفحص الصارم، لا رجعيًا على الذرات القائمة.

الفحص الحالي يلتقط:

- docstrings عربية.
- تعليقات إصدارات وحواجز.
- رسائل عربية داخل المخرجات.

### التصنيف

```text
VALIDATOR_RULE_MISMATCH / LEGACY POLICY CANDIDATE
```

لا يمكن اعتبار هذه الـ26 مخالفات Runtime مثبتة قبل تحديد المقصود:

```text
A — منع العربي من كل atom.py، بما فيه التعليقات والـdocstrings
B — منع النص التنفيذي فقط، مع strict-language للجديد
```

لا يتم تخفيف الفحص أو تعديل الذرات قبل القرار.

---

## 4. الأرقام السحرية — 14

| الذرات | القيم | نوع القيمة المحتمل | الحالة |
|---|---|---|---|
| 151–155 | 3، 5، 6، 8 | أرضيات/نوافذ `_speed_window` | تحتاج عقد إعداد أو ثابت معلن |
| 162–164 | 5 | أرضية نافذة السرعة | تحتاج تصنيف |
| 201 | 5 | أرضية نافذة البنية | تحتاج تصنيف |
| 262 | 10000.0 | حد كمية/ميكرو | قيمة سياسة محتملة |
| 264 | 1e-12، 1e-09، 10000.0 | epsilon تقني + حد كمية | خليط يحتاج فصلًا مفاهيميًا |
| 618 | 0.005 | حد أدنى لـpolling sleep | حد تشغيلي محتمل |
| 832 | 0.6، 0.9 | معادلة ثقة/تشخيص | قيمة سياسة محتملة |
| 870 | 5 | حد أدنى لنافذة تقييم | قيمة أمان/تشغيل محتملة |

### تفصيل القرار

#### قيم مرشحة لعقد إعداد/سياسة

```text
151–155, 162–164, 201
```

تُمرّر هذه القيم كـfloors إلى `_speed_window`، ولا تظهر كحقول مطابقة في manifests الحالية. لا يجوز نقلها إلى `config` دون تحديد إن كانت جزءًا من قانون التحليل أم مجرد حد أمان.

#### قيم تقنية محتملة

```text
264: 1e-12 و1e-09
618: 0.005
```

قد تكون حدود دقة/استقرار وليست إعدادات مالك. نقلها إلى manifest قد يفتح تغييرًا غير مقصود في السلوك.

#### قيم سياسة واضحة محتملة

```text
262: 10000.0
264: 10000.0
832: 0.6 و0.9
870: 5
```

تحتاج كل قيمة إلى مالك وتعريف ونطاق اختبار، لا مجرد اسم ثابت.

### التصنيف

```text
CONTRACT REVIEW REQUIRED
لا تعديل Atom ولا validator قبل تحديد مالك القيمة.
```

---

## 5. حجم الذرة — 2

```text
578 = 379 سطرًا فعليًا > 350
601 = 389 سطرًا فعليًا > 350
```

الاستثناءات الموثقة الحالية:

```text
166, 454, 622
```

ولا تشمل 578 أو601.

### التصنيف

```text
REAL QUALITY CONTRACT VIOLATION
```

هذه ليست مشكلة Core. لا تُحل بتغيير الحد أو بإضافة استثناء تلقائي. القرار اللاحق يجب أن يكون أحد الخيارين:

```text
تفكيك الكود مع الحفاظ على السلوك
أو
استثناء مالك موثق ومحدد النطاق
```

---

## 6. الاختبارات الناقصة — 2

```text
832 = لا ملف اختبار
870 = لا ملف اختبار
```

### التصنيف

```text
REAL TEST CONTRACT GAP
```

لا يُنشأ اختبار شكلي فقط لتصفير العداد. يجب لاحقًا اختبار منطق التشخيص والمعايرة فعليًا أو اعتماد قرار صريح بأنهما خارج عقد الاختبار الحالي.

---

## 7. التحذيرات الـ21

هذه لا تدخل ضمن الـ44:

```text
3 أحجام مستثناة بأمر المالك: 166, 454, 622
17 حلقات خلفية معلنة
1 إسقاط صامت محتمل في 870
```

حاليًا هي تحذيرات، وليست حواجز Release.

---

## 8. X Build 2

كانت هناك 3 failures في محاكاة X Build 2 بسبب عقد الاختبار مع EventBus الجديد:

- كان الاختبار يعتمد على scheduler tick واحد.
- EventBus الحالي يستخدم mailboxes و`drain()`.
- مقارنة القرارات كانت تتأثر بـstate coalescing عند السرعة الصفرية.

تم تعديل الاختبار فقط:

```text
await bus.drain()
pace_seconds = 0.01
```

النتيجة:

```text
tests/test_x_build2_simulation.py
8 passed
```

لم يتغير أي Atom implementation.

---

## 9. Full Test

النتيجة الحالية:

```text
854 passed
1 failed
1 skipped
```

الفشل الوحيد الظاهر في الجولة الأخيرة:

```text
test_old_shutdown_failure_does_not_mark_upgrade_failed
```

وعند عزل هذا الاختبار، تظهر حالتا HotReload إضافيتان متعلقتان بحذف/تغيير الملف. لذلك التصنيف التشغيلي هو:

```text
HotReload/Upgrade = 3 failures غير محسومة
```

وهذا هو Gate 15 نفسه، ولا يتم فتحه الآن.

بعد استبعاد اختبارات HotReload:

```text
854 passed
1 skipped
```

---

## 10. قرار الجولة

```text
26 لغة       = قرار Validator مطلوب
14 أرقام     = تحديد مالك كل قيمة مطلوب
2 حجم        = جودة Atom حقيقية
2 اختبارات   = فجوة اختبار حقيقية
X Build 2    = PASS
HotReload    = STOP
```

ما لم يُفعل:

```text
لا تعديل على Atom
لا تغيير startup_mode
لا تخفيف validator
لا استثناءات جديدة
لا فتح HotReload
لا تغيير Core أو Registry
```

**لا يبدأ أي إصلاح للـ44 قبل إصدار عقد مالك واضح لكل فئة.**
