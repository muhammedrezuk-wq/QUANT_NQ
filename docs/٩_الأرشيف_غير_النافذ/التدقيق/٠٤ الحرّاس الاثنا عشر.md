# الحرّاس الاثنا عشر — ما يمنع الخطأ فعلًا

| الحارس | الموضع | ما يمنعه |
|---|---|---|
| عمر الأمر ثانيتان | `QUANT_NQ.mq5` `InpMaxCmdAgeSec=2` | أمرٌ كُتب والذراع ميتة **لا يُنفَّذ** بسعر ميت |
| وقف إجباريّ | `InpRequireStop=true` | `OPEN` بلا ستوب **يُرفض عند الإكسبرت** |
| مطالبة ذرّيّة | `WHERE status='PENDING'` | ذراعان لا ينفّذان الأمر مرّتين |
| ملكيّة الحساب | `COMMAND_OWNERSHIP_MISMATCH` | أمرٌ لحساب آخر يُختم فاشلًا |
| هويّة البناء | `PROJECT_BUILD_MISMATCH` | نسخة مشروع قديمة لا تُنفَّذ |
| رقم سحريّ | `601` والإكسبرت = `20260801` | أمر أجنبيّ لا يُكتب أصلًا |
| قائمة بيضاء fail-closed | `552` `if not whitelist_seen: return False` | **بلا قائمة = منع تام** |
| منع التكرار | `601` `BEGIN IMMEDIATE` + `DUPLICATE_REQUEST_ID` | نفس الطلب لا يُكتب مرّتين |
| حارس الإعادة | `core/event_bus.py` `_COMMAND_MARKERS` | أمر قديم **لا يُعاد بثّه** |
| الأسرار بخزنة | `622` `get_secret_provider()` | لا كلمة مرور بالمانيفست · وبلا خزنة يفشل مغلقًا |
| عقد التحوّط متطابق الطرفين | `552` ↔ الإكسبرت | الاستثناء نفسه في الجهتين — لا تناقض |
| **سلطة محقَّقة لا معلَنة** | `576` v3.3.1 · `578` v5.1.1 | **`parent_decision_id` لا يُقبل إلا إذا رأت الذرّةُ `467` تنشر `decision.gate.passed` له** — فسلطة مزعومة لا تفتح مركزًا |

**البنية محكمة. الخطر في الأرقام والإعدادات لا في المعمار.**

**والحارس الثاني عشر أُضيف ٢٠٢٦-٠٨-٢٣ بأمر المالك المباشر — «الوحدة ١».**

---

**`الدستور/` للقوانين والقيود · `الهندسة/` للمعمار والرؤية**
