# لا سقف حجم عند البوّابة

**بحث شامل عن سقف الحجم في المشروع كلّه أظهر موضعين فقط:**

```
513   broker_max = min(max_lot, spec.volume_max)
576   lot = min(max_lot, max(0.0, stepped))
```

**أمّا البوّابتان الأخيرتان فتفحصان الإشارة فقط:**

```python
552   if volume is None or volume <= 0.0: return "BAD_VOLUME"
601   valid_volume = float(payload.get("volume")) > 0
```

**الأرقام المعلَنة:**

```
513   max_lot 20.0  ·  risk_per_trade_pct 0.5  ·  default_stop_pct 0.5
576   max_lot 20.0  ·  fallback_stop_distance_frac 0.005
585   margin_buffer_pct 0.1        ← عشرة بالمئة هامش أمان
552   max_spread_points 2000.0
الإكسبرت  InpDeviationPts 500      ← على BTCUSD تعني خمسة دولارات انزلاقًا
```

**⇒ الوسيط هو آخر مانع للحجم، لا النظام.**

---

---

**`الدستور/` للقوانين والقيود · `الهندسة/` للمعمار والرؤية**
