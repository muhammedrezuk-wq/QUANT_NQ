# ١٠ · ⏳ ما ينتظر قرار المالك

## أُضيف بقياس ٢٠٢٦-٠٨-٢٥

| القرار | ما يقوله القياس |
|---|---|
| ~~**عتبة `413` مقابل سقف `context_factor`**~~ | **حُسمت (تحديث ٢٥-٠٨): `413 v2.2.0` بختم nq — «البوّابة المستحيلة» أُغلقت والعتبة صارت قابلة للبلوغ. وأوّل قرار وقع اليوم فعلًا.** |
| **`402` جودة الإبطال جاهزة ١١٫٦٪** | وهي التي تسقّف الضارب (`0.2348`). **السبب لم يُقَس — يُقاس قبل أيّ حكم** |
| **`200`/`250`/`300` بلا اتّجاه ولا قوّة ولا نسبة** | **كود لا عيار.** الوحدات تُنتج ٣٬٨٤٤ نتيجة كل ٥٠ ثانية والقسم يستعمل منها عدّة رؤوس فقط |
| **خطّ أساس جديد للذرّة `7`** | فاصل المسار أُصلح والحارس صار صادقًا — ويبقى أحمر حتى يُولَّد خطّ أساس بعد الكبسات |
| **فريم الخمس ثوانٍ** | أُسقط مؤقّتًا؛ فريم الثانية ضارّ بيقين والخمس ثوانٍ داخل ضجيج السوق ولم يُفصل. **يحتاج نافذة سوق هادئة** |
| ~~**الأوامر الثلاثة المبنيّة**~~ | **تجاوزتها الأحداث (تحديث ٢٥-٠٨ ليلًا): الجسر بلغته الأوامر — `commands=5` وأوّل صفقة بتاريخ النظام نُفّذت.** والسؤال التاريخيّ (أين توقّفت الثلاثة القديمة) محلّه ورقة ٢٢ |
| **الوقف على الزوج المحايد** | الثلاثة بُنيت بلا وقف — وهو **مقصود بالعقد**. *(تحديث ٢٥-٠٨: وقع فعلًا — الزوج الحيّ 0.23+0.23 مفتوح عند الوسيط بـ`stop_loss=0.0` على الساقين)* |
| **`707` قفل القاعدة** | عاد بعد أن زال ٠٨-٢٢. **السبب لم يُقَس** |
| **ذاكرة الجهاز ٨٦٫٣٪** | ١٣٫٧ من ١٥٫٩ غيغابايت — أعلى ما يجب مراقبته |

---

## القرارات الأقدم — ما بقي منها قائم (والمحسوم مُعلَّم بسطره)

| القرار | الخيارات |
|---|---|
| ~~**إعلان الثقة غير المعرَّفة**~~ | **نُفّذ (تحديث ٢٥-٠٨): `413 v2.1.0` — الثقة غير المعرَّفة تُعلَن `None` مع `confidence_defined` (atom.py:199-200)** |
| **وزن المسار البطيء** | يبقى `45` · أم يُخفَّض · أم تتحوّل المحلّلات إلى مراكم تِكّيّة |
| ~~**إيقاع دورة القسم**~~ | **نُفّذ ٠٨-٢٣ (تحديث ٢٥-٠٨): الدورة تُفتح من أوّل تسليم وحدة بهويّتها (`200 atom.py:95-101`) — لا دورة لكل تِكّة ولا انتظار شمعة** |
| ~~**حدود `516`**~~ | **حُسمت (تحديث ٢٥-٠٨): `v5.1.0` بختم nq — 5% خسارة يوميّة و10 خسائر متتالية كعيارات لوحة. بقي `max_reserved_risk_pct=100` رقم اختبار** |
| **تمييز الحساب** | حاجز يمنع الحساب الحقيقيّ حتى إذن صريح |
| **الطوارئ** | *(حُسم جزئيًّا ٢٥-٠٨: `575 v2.2.0` — الإيقاف لا يمنع الحماية/الخروج.)* **المتبقّي للمالك: هل تُصدر الطوارئ نفسُها إغلاقًا فعّالًا** |
| **نمط التداول لكل أصل** | تحوّط دائم أم ستوب وهدف |
| **أداة الاختبار** | النظام يُختبر على أداة تعمل ٢٤/٧ وحدها |
| **عتبة بدء التحوّط** | مستقلّة أم تتبع النطاقات |
| **قانون تقليص الإجماليّ** | متى وكم يُنزَل مجموع الرجلين |
| **قاعدة الوقف المتحرّك** | قاع/قمّة · تقلّب · نسبة · مركّبة |
| **حالة «ضُرب الوقف وأنت متحوّط»** | الحالة نفسها غير مولَّدة في المشروع |
| **مواصفات الرمز** | جدول `symbol_specs_v2` يحمل رمزًا واحدًا |
| **الشارت** | أدوات الرسم غير متاحة في المكتبة الحرّة — مكتبة بديلة أم تنازل |

---

---

**`الدستور/` للقوانين والقيود · `الهندسة/` للمعمار والرؤية**
