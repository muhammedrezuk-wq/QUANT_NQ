# لا يوجد في المشروع كلّه تمييز بين حساب تجريبيّ وحساب حقيقيّ

```
البحث عن: is_demo · DEMO · trade_mode · LIVE_ACCOUNT   →   صفر نتيجة
الحقل موجود ويُقرأ: 619 يقرأ account_server ويمرّره باسم "server"
                     ولا سطر واحد يفحص قيمته
```

**الحساب المتّصل حاليًّا — من قاعدة الجسر:**

```
account_server   ICMarketsSC-Demo        ← تجريبيّ
balance          616.11 USD              leverage   5000
connected 1  ·  trade_allowed 1  ·  expert_allowed 1
expert_version 3.10  ·  project_build_id QUANT_NQ_FULL_212
```

*(تحديث ٢٥-٠٨ ليلًا: الرصيد والإكسبرت من قاعدة الجسر الحيّة — كانا 630.08 و3.00 عند القياس الأوّل.)*

**وإعداد `601`:** `account_id: "CURRENT_ACCOUNT"` و`blocked_account_ids: []`.

⇒ **الذرّة تتبنّى أيّ حساب يعلنه الجسر.** فلحظة تسجيل الدخول بحساب حقيقيّ في
ميتاتريدر، **يتداوله النظام بلا تعديل سطر ولا تحذير.**

**والطريق لم يعد مفتوحًا فقط — بل مسلوكًا (تحديث ٢٥-٠٨ ليلًا):** الأوامر صارت واقعًا —
`commands=5` في قاعدة الجسر وزوج 0.23+0.23 BTCUSD مفتوح في `positions_v2` بتذاكر حقيقيّة.
**النظام يتداول فعلًا على الحساب الذي يعلنه الجسر، وما زال بلا أيّ فحص تجريبيّ/حقيقيّ**
(البحث عن `is_demo · trade_mode · LIVE_ACCOUNT` أُعيد الليلة: صفر نتيجة).
⇒ **الخطر انتقل من كامن إلى حيّ.**

---

---

**`الدستور/` للقوانين والقيود · `الهندسة/` للمعمار والرؤية**
