# سياق QUANT_NQ والقرارات المعتمدة

**آخر تحديث:** 2026-08-27

هذا الملف يلخص القرارات النافذة حتى لا يرجع أي نموذج إلى أوراق قديمة.

---

## 1. Build

```text
QUANT_NQ_COMPLETE_FULL = الرسمي
QUANT_NQ_FULL_212      = Legacy
```

القرار محفوظ في:

```text
config/build_contract.json
```

ولا يُستخدم عدد `309` كهوية Build.

---

## 2. Documentation

```text
Documentation = مستقلة
Documentation = INCOMPLETE / Warning
Documentation لا تحجب Release أو Core Boot
```

الشروحات الناقصة لا تُخترع ولا تُكتب آليًا لتصفير رقم.

---

## 3. Language Contract

```text
executable-only
```

- أسماء الدوال والمتغيرات والأحداث Runtime إنجليزية.
- التعليقات والـdocstrings والرسائل البشرية العربية مسموحة.
- `strict-language` للذرات الجديدة أو الفحص الصارم.

---

## 4. Numeric Ownership

القيم التي كانت تظهر كأرقام سحرية لها ملكية في:

```text
config/atom_quality_contract.json
```

التصنيف:

```text
Technical
Analysis Contract
Policy
```

لا تنقل القيمة إلى config ولا تغيرها إلا بقرار عقدي واختبار سلوك.

---

## 5. حد حجم الذرة

```text
ATOM_MAX_LINES = 400
```

هذا قرار جودة، لا تغيير تنفيذ. لذلك:

```text
578 = 379 → PASS
601 = 389 → PASS
```

---

## 6. 519

تم اعتماد الحل B:

```text
asset.portfolio.owner_intent.state
```

- الحدث القديم محفوظ.
- الحدث الجديد replayable.
- الحالة الفاسدة fail-closed.
- الاختبار يطابق الحالة قبل وبعد reload.

نسخة 519 الحالية:

```text
3.3.0
```

---

## 7. Core

```text
Core = 1.27.0
CORE.lock = valid
```

Core Boot مستقل عن الذرات وعن Execution Safety.

---

## 8. نتائج الاختبار

```text
Full pytest = 858 passed / 1 skipped / 0 failed
Validator = 0 errors / 21 warnings
Build Registry = PASS
Release verification = PASS
Governance project = PASS
HotReload state contract = PASS
```

التحذيرات الـ21 معلنة ولا تُخفى:

```text
17 حلقات خلفية
3 أحجام مستثناة بأمر المالك
1 إسقاط صامت محتمل
```

---

## 9. الأمن

```text
No secrets in package
Vault = NOT_CONFIGURED
```

لا يتم ربط حساب حقيقي من الحزمة قبل تهيئة خزنة الأسرار واختبار الجسر الفعلي.

---

## 10. ممنوعات ثابتة

```text
لا إعادة ترقيم
لا حذف Atom
لا تغيير startup_mode لتمرير فحص
لا waiver مخفي
لا استيراد مباشر بين Domains
لا أمر تداول من اللوحة مباشرة
لا قرار من Governance بدل Atom
لا استخدام الأرشيف كمصدر نافذ
```
