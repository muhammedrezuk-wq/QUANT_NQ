# ختم تغليف مصدر — QUANT_NQ.zip

**التاريخ:** 2026-09-02  
**النصّ الحرفيّ للأمر:** «غلف زيب نظيف واختم لا نشغل على ويندوز»

---

## الحكم

```
تغليف المصدر = نعم
تشغيل ويندوز بهذا الختم = لا  (لم يُشغَّل هنا، بأمر المالك)
اعتماد تداول حي = ليس موضوع هذا الختم
```

هذا ختم **حزمة مصدر نظيفة**. ليس إثبات وسيط، وليس تشغيلًا على ويندوز.

---

## الحزمة

| بند | القيمة |
|---|---|
| الملف | `QUANT_NQ.zip` بجانب مجلد المشروع |
| البصمة | `QUANT_NQ.zip.sha256` بجانبه (خارج الزيب عمدًا) |
| الشجرة داخل الزيب | مجلد جذر `QUANT_NQ/` |
| `SHA256SUMS.txt` | داخل الحزمة — بصمة الملفات لا بصمة الزيب |

---

## ما دخل / ما خرج

**داخل:** الكود، الذرّات، النواة، اللوحة المبنية (`governance/ui/built` · `index-DwVvlIy4.js`)، الأزرار، الإكسبرت، تغذية سي تريدر، الوثائق، بيانات المعايرة التاريخية، الاختبارات.

**خارج عمدًا (نظافة لا نقص هندسة):**

- أسرار (`runtime/secrets.enc` · `device.key`) — تُنشأ من لوحة الحوكمة عند المالك
- `var/` وجسور `*.db` لهذ الصندوق
- مجلد `C:` العرضي (اسم مسار ويندوز كملف لينكس)
- `__pycache__` · `.pyc` · `.env`

---

## الممنوع

- لا `git push` من هنا (المستودع بيد المالك).
- لا قراءة هذا الختم كتشغيل ويندوز أو كاعتماد ملء وسيط.
