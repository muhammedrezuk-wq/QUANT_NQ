# تغليف مصدر — بعد شارت التنفيذ وذرّة ٧

**التاريخ:** 2026-09-03 01:40
**النصّ الحرفيّ:** «سجل شغلك بي ورق مشروع وحزم ولا تنسى7»

---

## الحكم

```
تغليف المصدر = نعم
تشغيل ويندوز بهذا الأمر = لا
اعتماد تداول حي = لا  (ليس ختم NQ حي)
git push = لا
إعادة ختم النواة = لا  (verify نجح)
```

---

## الحزمة

| بند | القيمة |
|---|---|
| الملف المقدَّم | `QUANT_NQ.zip` بجانب مجلد المشروع |
| `.sha256` بجانبه | موجود للمطابقة · **ليس** الملف المقدَّم |
| الشجرة داخل الزيب | مجلد جذر `QUANT_NQ/` |
| `SHA256SUMS.txt` | داخل الحزمة — بصمة الملفات |

**داخل:** المصدر بعد شارت MT5 + أوراق اليوم + `integrity_baseline.json` المجدَّد + `governance/ui/built` **القديم** كما كان.

**خارج عمدًا:** أسرار · `var/` · `*.db` · `__pycache__` · `.pyc` · `.env` · `node_modules` · `venv` · `vendor/python/runtime` · `vendor/wheels`

---

## الممنوع

- لا قراءة هذا التغليف كإثبات أن المالك **يرى** الشارت الجديد: الـ`built` قديم وEA يحتاج MetaEditor.
- لا `git push`.
