# 2026-09-07 07-10 — الجسر حيّ والصفقات تُفتح · نقض حاجز «601 ميتة»

**مقيس مباشرةً من `nq_brain.db` (قراءة فقط) ومن الكود — لا من ورقة ولا تخمين.**

> **سبب القياس:** حكم المالك ٠٩-٠٧ — **«معقول ما ينحلّ فتح صفقة… إنت ما لقيت
> حل، عم تخمّن»**. كان محقًّا: الحاجز المسجَّل عندي كان **باطلًا**، وهذه الورقة
> تنقضه بالرقم.

---

## ١ · ما كان مسجَّلًا — وسقط كلّه

| الادّعاء المسجَّل | **المقيس ٠٩-٠٧ 07:00** |
|---|---|
| «601 ميتة» | **حيّة** — كتبت **225 أمرًا** في الجسر |
| «611/618/619 تقرأ `crypto_runtime\var\bridge.db`» | **لا** — كلّها `db_path` **مطلق** إلى `nq_brain.db` الصحيحة |
| «لا أمر يصل ميتاتريدر» | **204 أمرًا DONE** · منها **74 فتحًا ناجحًا** |
| «مصدر الطغيان لم يُوجد» | **لا طغيان أصلًا** — `NQ_BRIDGE_DB` **غير معرَّف** لا في المستخدم ولا في النظام |

**سبب الوهم القديم:** قراءة `config_schema` (تعريف نوع الحقل) على أنّها قيمة
الإعداد. الحقل `db_path:` تحت `config_schema` **بلا قيمة بالتصميم** — القيمة
الحقيقية تحت `config:` وهي صحيحة في 601 و611 و618 و619 و609.

---

## ٢ · الحقيقة المقيسة — الجسر

```text
nq_brain.db   C:\Users\NQ\AppData\Roaming\MetaQuotes\Terminal\Common\Files\
              20.13 MB · آخر كتابة 2026-09-07 06:01 · 18 جدولًا
commands           225      trade_events_v2   174
candles_history   9557      ticks_v2           66
calendar           277      news              161
symbol_specs_v2      8      account_v2          1
positions / positions_v2   0   ← لا مركز مفتوح الآن
```

## ٣ · مصير الأوامر — 225 أمرًا

| الحالة | العدد |
|---|---|
| **DONE** | **204** |
| FAILED | 19 |
| CANCELLED | 1 (`EMERGENCY_HALT`) |
| EXPIRED | 1 |

| النوع | DONE | FAILED |
|---|---|---|
| **OPEN** | **74** | 10 |
| MODIFY_SL | 110 | 6 |
| CLOSE_PARTIAL | 12 | 1 |
| CLOSE | 8 | 2 |

**آخر دورة كاملة (تذكرة `1911388023` · BTCUSD @ 79811.99):**
`OPENED 1.5` ← `PARTIAL 0.75` ← `CLOSED` — السلسلة تعمل من طرفها إلى طرفها.

---

## ٤ · الفشل العشرة في الفتح — كلّها قديمة ومفسَّرة

| المدى | الرمز | المعنى | العدد |
|---|---|---|---|
| ٠٩-٠٥ 17:48–17:59 | `RETCODE_10016` | **وقف غير صالح** (Invalid stops) | 6 |
| ٠٩-٠٦ 02:27–02:38 | `RETCODE_10012` | **انتهت المهلة** قبل التنفيذ | 4 |

**كلاهما عُولج بعدها:** الوقف بسلسلة ٠٩-٠٦ (أرضية مطلقة ← الوقف المُرسَل ←
حارس المئة)، والمهلة بكبسة ٠٩-٠٦ 02:57 **«المهلة ليست رفضًا»**.

**آخر فشل فتح على الإطلاق: ٠٩-٠٦ 02:38.** بعده **صفر فشل فتح**.

---

## ٥ · الخلاصة — أين المشكلة فعلًا

**فتح الصفقة ليس معطوبًا.** آخر فتح ناجح: **٠٩-٠٦ 16:53**.

المقيس أن الأوامر **لا تُرفض** — بل **لا تصدر أصلًا** بعد ذلك الوقت.
الجسر سليم · الذرّات تقرأ الملف الصحيح · ميتاتريدر ينفّذ ويردّ `OK`.

**ما يلي استنتاج لا قياس (النظام موقوف فلا يمكن تأكيده الآن):**
بين ٠٩-٠٦ 22:40 و٠٩-٠٧ 03:10 شُدّدت العيارات — وقف ≥ 100 · هدف ≥ 300 ·
نسبة 2.0 · اتجاه 20 · ثقة 45. **لم تُختبر حيًّا بعد.** الفرضية الأولى للفحص:
**العتبات الجديدة تخنق ولادة الفكرة قبل أن تصل بوّابة الأمر** — لا الجسر.

### كيف تُثبَت أو تُنقض بعد أوّل إقلاع
1. أقلع من `غرفة القيادة.bat` ودع دورة سوق تمرّ.
2. راقب عدّادات مسار الإعداد (كُبست ٠٩-٠٦ 22:31 «عدّادات بلا كتم»).
3. إن لم يصل أمر `OPEN` جديد إلى `commands` **مع وجود أفكار مرفوضة** ⇒
   العتبات هي المانع، ويُعاير المقياس بأمرك.
4. إن وصل ونجح ⇒ لا مانع أصلًا، والحاجز كان وهم ورقة.
