# خلاصة الجولة (٢٠٢٦‑09‑03 مساءً) — أرقام مقيسة

| # | الفحص | النتيجة |
|---|---|---|
| ١ | `tools/baseline_regen.py` + `--check` | متقادم ٠ · مزال ٠ · مضاف ٠ — (1313 / 1313 / 655) |
| ٢ | `pytest tests/test_device_contract.py -q` | **28 passed** |
| ٣ | `pytest tests/backtest tests/core tests/smoke -q` | **384 passed** |
| ٤ | `pytest tests/test_*.py` (٤٥ ملفًّا، ٣ دفعات) | **759 passed · 1 skipped** |
| ٥ | `governance/scripts/validate_atoms.py` | **مخالفات ٠ · تحذيرات ٢٠** · rc=0 |
| ٦ | `check_path_authority.py` ×٣ (جذر + forex_runtime + crypto_runtime) | **🟢 rc=٠** |
| ٧ | `check_crypto_isolation.py` | **الاختلافات ٠ · 🟢 العزل سليم** |
| ٨ | `scripts/check_project.py` | **rc=٠** — المتوقع ٢٢٦ · بدأ ٢٢٦ · فشلت ∅ |
| ٩ | إقلاع فعلي (Linux، `QUANT_LOCAL_MODE=1`) | فوركس ٨٠١٠: ١٨١ ذرّة نجاح=True · كريبتو ٨٠٢٠: ٢٢ + ٥٧ manual · لوحتان ٨٠٩٠/٨٠٩٣ |
| ١٠ | `SHA256SUMS.txt` | **لم يُمسّ** — `f0fe09aa6d84904af200…` · `git` عند `9e4f254` |

## ما تغيّر في الكود (جذر `backtest/` + ذرّة ٦١٩ ومراياها)
- `backtest/runner.py`: ١٢ محمولاً ناقصاً أضيف (`560,563,586,601,619,450,460-464,466`…) ⇒ `loaded=136`؛
  جسر اختبار معزول لصفّ `account_v2`؛ `ReplayOfficialClock` (مرجع زمني واحد)؛ بوّابة ٤٦٨ على رمز البيانات.
- `atoms/قسم 601-650/619_حالة_الحساب/atom.py`: غلاف `read_now()` (نفس `_read_once`، بلا تغيير عقد) — ونُسِخ على `forex_runtime`.

## القرار
**جاهز للتشغيل الورقيّ/الحيّ: لا.**
السبب بالضبط: السلسلة مُوصَلة ومقيسة حتى `risk.account.state=READY` و`decision.approved.state=768/768`،
لكن الطريق إلى أمر مُنفَّذ مقفول **بغياب اعتماد المعايرة** — ٣٦ صفاً كلّها `UNAPPROVED`
و`analysis_settings` فارغ (٠ صف) ⇒ لا قسم `READY` ⇒ `weight_effect=0` ⇒ عتبة ٤٥٥ تفشل ⇒ ٤٦٧ WAIT.
وهذا سلوك الحارس المقصود، لا عطل؛ فتحه قرار اعتماد من المالك (`confirm=true`).

## عُطلان مكتشفان ولم يُصلَحا (قرار نشر، لا تصحيح تقني)
1. عقد الوصلة الموحّدة منهار: `prepare_unified.py --verify-only` → **rc=2**، ١٨ `real-dir-drift` + ٨ `real-dir-identical`.
2. ثلاث نسخ متباعدة من ذرّات الكريبتو: `atoms_crypto/` فيها 2270‑2277 بلا 2160؛
   `crypto_runtime/atoms_crypto/` (المحمولة فعلياً) فيها 2160 بلا 2270‑2277؛
   `crypto_runtime/atoms/` ٦١ ملفاً بنفس الأسماء ومحتوىً مختلف. الحارسان لا يريان هذا.
3. على Linux بلا MT5/جسر: `611/618/619 read failed: unable to open database file` (مانيفست 601/619
   يحمل `db_path` ويندوزياً مطلقاً) ⇒ حكم المسار الويندوزي **قياسيّ بلا اتصال**.

## للعلم (لم يكن عطلك)
اختفت حزم من البيئة فأفسدت الفحوص: ثُبّت `pytest-asyncio==1.4.0` + ٩ من `requirements.lock.txt`
⇒ عاد `test_device_contract` من ٢٨/٢٨ فشلاً إلى **28 passed**.

---

## جولة ١٨ — «اعتماد المعاير»: المسار كان الجاني، لا الزرّ

| المقياس | القيمة |
|---|---|
| مواضع patch (٩٠١ + ٢٩٠١، كل الأشجار) | 5 |
| بصمة ملفّ الفوركس بعد الترقيع | `de4575a78ba4` (جذر ≡ `forex_runtime`) |
| بصمة ملفّ الكريبتو | `207c96ed96eb` (الشجر الثلاث متطابقة) |
| عمر سطر `PENDING` قبل الإصلاح | 259 ث (المهلة 120 ث) بلا لمس |
| بعد الإصلاح: زمن تنفيذ الأمر | ≤ 3 ثوانٍ → `DONE` |
| حالة السجلّ بعد الأمر (نسخة `/tmp`) | `APPROVED · OWNER · v1` |
| حالة سجلّ المستودع الآن (مُرجَّعة) | 36 صفًّا `UNAPPROVED/UNSET` · تدقيق 0 · `analysis_settings` 0 |
| اختبارات ٩٠١/٢٩٠١ (٥ مواضع) | 15 + 57 = **72 passed** |
| `check_path_authority.py` | 🟢 rc=0 (نقاط اللقاء · الحفظ≠الاعتماد · عقد الوصلات) |
| `check_parameters_endpoint.py` | OK — `declared=6 dials=30` |
| `check_crypto_isolation.py` | 🟢 الاختلافات 0 (بعد إعادة النواتين) |
| `baseline_regen --check` (٣ جذور) | متقادم 0 · مزال 0 · مضاف 0 (بصمات: 1313/1313/655) |
| `test_device_contract.py` | 28 passed |
| `validate_atoms.py` / `check_project.py` | 0 مخالفات / ✅ ناجح |
| حزمة أمان الاختبار | نسخة `/tmp` معزولة؛ `SHA256SUMS.txt` لم يُمَسّ؛ لا commit/push/tag |

## جولة ١٩ — شقّ المسار في المخازن (٧٠١–٧٠٩ · ٥٦٣)

| المقياس | القيمة |
|---|---|
| ملفات atom.py حاملة المُعلِّب | 37 (بما فيها ٩٠١/٢٩٠١) |
| مفاتيح var/ النسبية المُعلَّبة / الإجمالي | 50 / 50 · متبقٍّ 0 |
| `core/` | لم يُمَسّ — الختم سليم `1b0d0f6b91505984…` (V1.31.0) |
| إقلاع حيّ: fd خارج الـruntime | 0 |
| validate_atoms / check_project | مخالفات 0 · ✅ ناجح |
| device_contract · 701-750+563 · backtest | 28 · 88 · 244 passed |
| baseline_regen --check (٣ جذور) | 0 / 0 / 0 (1313·1313·655) |
| حارس النواة | `check_path_authority` 🟢 rc=0 |

## جولة ٢٠ — «لا تُصدِّق رقعةً جماعية قبل أن تُقْلِع بها»: الترميم كاملًا

| المقياس | قبل الترميم | بعد الترميم |
|---|---|---|
| إقلاع النواة (فوركس) | بدأت **224** · فشلت **[518, 512]** | **226 · فشلت=[] · استُبعدت=7 (يدويّ، م-٢١)** |
| ملفات `atom.py` حاملة المُعلِّب | 37 (منسوخ من جولة ١٩) | **111** (٢٦ جذر فوركس · ٢٦ مرآته · ٢١ `atoms_crypto` · ١٧ `crypto_runtime/atoms` · ٢١ `crypto_runtime/atoms_crypto`)؛ ٩٠١/٢٩٠١ خارج الحساب بمُحوِّلها الخاصّ |
| ندّاءات `_rebased_config` الذاتية (`cfg=…(cfg.config)`) | 23 بلا ربط + 2 ذاتية | **0** |
| حاملة المُحوِّل بلا استيراد `Path` | 13 | **0** (111/111) |
| مفاتيح `var/` النسبية: مُعلَّبة / ناقصة / أخطاء ترجمة | — | **110 / 5 (مقصودة) / 0** |
| `check_project` · `check_boot` · `freeze_core verify` | — | ✅ · ✅ · ✅ `1b0d0f6b91505984…` |
| `validate_atoms` | — | مخالفات 0 · تحذيرات 20 |
| `baseline_regen --check` (٣ جذور) | — | 0 / 0 / 0 (1313·1313·655) |
| device_contract · 701-750+563 · backtest | 28 · 88 · 244 | **28 · 116 · 244 passed** |
| `check_crypto_isolation` | 🛑 (نواتان موقوفتان) | **🟢 الاختلافات = 0** عند سؤال النواتين بـ`X-API-Key`؛ الأصل بلا مصادقة يظل 🛑 لأن `get()` لا يرفق المفتاح (DEC-038) |
| `/proc/<pid>/fd` لنواتين حيّتين | — | فوركس 6 ملفات حالة كلها في `forex_runtime/var/` · الكريبتو 0 · **خارج الشجرة 0** |
| `var/` في جذر المستودع | 38 ملفًّا (مخلفات قياس) | **0** — حُذفت بأمر المالك (خيار أ)، ومعها شجرة `C:/…` |
| انكشاف جديد | — | `check_boot.py` يشغّل `run_core.py` بـ`cwd=ROOT` (السطر ٩٨) فينبت **26 ملفًّا** في `var/` الجذر **من HEAD نفسه** (منها `var/store\trades.db`)؛ رقعة سطر واحد تقيس 226/0 — لم تُطبَّق، معروضة على المالك |

## جولة ٢١ — «أ» ثُمّ «ب»: مرسى الإقلاع مُثبَّت، وعيارات المالك نُفِّذت بالمسار القانوني

| المقياس | القيمة |
|---|---|
| `check_boot.py` (الجذر + المرايا الثلاث، متطابقة الحبر) | `cwd` = `<runtime>` + عقد البيئة (`QUANT_CORE_STATE_ROOT/RUNTIME_ROOT/CORE_CONFIG`) · **226 ذرّة · فشل 0** |
| نَباتُ `var/` في جذر المستودع أثناء الفحص | **26 ملفًّا قبل ← 0 بعد** (منها `var/store\trades.db` بظهر مائل حرفيٍّ) |
| انكشاف طبقة النواة | `journal.path`/`snapshot_root` كانا يُلْصقان بمرسىٍ هو نفسه `…/var` ⇒ `forex_runtime/var/var/forex/journal.jsonl` (3460 سطرًا) — رُقعة `_state_join()` في `governance/scripts/run_core.py` ×3 (النواة المختومة لم تُمَسّ) |
| بعد الرُقعة | `forex_runtime/var/forex/journal.jsonl` (231 سطرًا) · `crypto_runtime/var/journal.jsonl` · `var/var` **لا وجود** |
| أوامر المعايرة | **9** كتبت ونُفِّذت عبر `/gov/command` → بوّابة ٩٠١: حصّة `commands.db` = `PENDING → DONE` الكلّ |
| عيارات طُبِّقت (v1) | `DECISION_MIN_STRENGTH 45→32` · `DECISION_ELIGIBILITY_MIN_CONFIDENCE 63→56` · `ANALYSIS_FAST/SLOW_REQUIRED_DEPTH 60→32` · `DECISION_LIVE_STALE_AFTER_S 5.0→4.2` · `DECISION_MIN_SCORE 0→52` · `DECISION_BUY/SELL_MIN_DIRECTION 50→52` |
| ما لم يُكتب (بمعلَن، لا بتخمين) | جدول أوزان المحلّلين الخمسة عشر يحتاج `account_id`+`symbol` (معدومان في كل ملفّات `config/`) · 0.25% مخاطرة لا خريطة لها إلى سلَّم `RISK_DIAL` (0–100) · `operator=dashboard` هو المسجَّل (افتراضي المُسجِّل، لا اسم مشغِّل مُختلَق) |
| حالة السلسلة الآن | 452 و458: `degraded · NO_INPUT_YET` · `market_data` = **0 سطرًا** — الحاجزُ الحالي مدخلٌ حيّ لا مسار ولا عيار |
| الفحوص على الحالة النهائية | `check_project` ✅ · `baseline_regen --check` 0/0/0 (1313·1313·655) · `validate_atoms` 0 مخالفات/20 تحذيرًا · device_contract **28** · 701-750+563 **116** · backtest **244** · `check_path_authority` 🟢 · `freeze_core verify` 🟢 · العزل (بنسخة مؤمَّنة) **الاختلافات = 0** · `var/` الجذر **0** · `SHA256SUMS.txt` لم يُمَسّ (`git status` خالٍ) |

## جولة ٢٢ — محاولة «ب»: الحلقة مُثبَتة حتى وصلة السوق، والسببُ سياسةٌ لا عطل

| المقياس | النتيجة |
|---|---|
| مدخل صناعي | `ticks_v2`+`symbol_specs_v2`+`account_v2` في `/tmp/feed.db`، نواة بـ`NQ_BRIDGE_DB`، زرع ~٤/ث |
| ما جرى | ٦١٨ `published=586 symbols=1` ← ٦١٣ `forwarding 1 symbols via 2 routes`؛ ١٠١ `healthy symbols=3`؛ ١١٣ `side_processed=3 dropped=0`؛ ١١١ `synced=1 divergence=0` |
| حيث توقّفت | ١١٢ `NO_TICKS_YET` ومنه ١٠٢/١٠٣/١٥٠/١٦٦/٤٠١/٤٠٢/٤٥٢–٤٦٧ — **لسببٍ سياسي**: `613/atom.py:196` لا يمرّر للمسار التحليلي إلّا `analyst_sources=["CTRADER"]` |
| قنوات محكومة (لا خلل) | `POST /api/events`: `_CONTROL_EVENTS["forex"]=frozenset()` (fail-closed) · `market.broker_tick`: لا مشتركَ له (قناة عرض) · ٦٢٢ FIX: يحتاج `cryptography` (ثُبِّت 50.0.1) + جلسة إلى `live-uk-eqx-01…:5211` وسرًّا من الـvault — لا منفذ ولا اعتماديّة في البيئة |
| الأمانة | تعديل `analyst_sources` المؤقّت **رُجِّع** حرفيًّا؛ `--check` 0/0/0 · `check_project` ✅ · `var/` الجذر 0 |
| المتبقّي للإثبات الكامل | خيار ١٩-أ (مُجيب FIX صناعي + تعديل موقوت لمانيفست ٦٢٢) أو ١٩-ب (إقرار MT5 مصدرَ تحليل بقرار مالك) — تفصيلهما في §ح/١٩/٤ |

## قرار الإغلاق (جولة ٢٢ · اختيار المالك «ج»)

| البند | الحالة النهائية |
|---|---|
| الحكم المُعتمَد | **الهندسة سليمة من شقّ المسار إلى بوّابة الأوامر؛ التداول الفعلي يحتاج مصدر سوق حقيقي عند المالك** (MT5/cTrader) — لا تعديل سياسة (`analyst_sources`) ولا بنية إضافية (مُجيب FIX صناعي) |
| ما تُرِك كما هو بقصد | `613/atom.py:196` حارسَ مصدر التحليل (`analyst_sources=["CTRADER"]`) · `_CONTROL_EVENTS["forex"]=frozenset()` (إحكامُ قناة الأحداث) · قناة `market.broker_tick` للعرض فقط |
| ما لم يُكتَب | أوزان المحلّلين الخمسة عشر وجدول المعايرة لكل محلّل (يلزم `account_id`+`symbol`) · `RISK_DIAL` بنسبة 0.25% (لا خريطة إلى سلَّم 0–100) · لا `approved_by` مختلَق — المسجَّل `dashboard` (افتراضي المُسجِّل) |
| ما ثبَت وبقِيَ | 226 ذرّة · فشل 0 · `var/` جذر المستودع 0 · journal/snapshots في `<runtime>/var/forex/` · 8 عيارات إلى v1 عبر ٩٠١ (9 أوامر DONE) · الفحوص: `check_project` ✅ · `baseline_regen --check` 0/0/0 · `validate_atoms` 0/20 · device_contract 28 · 701-750+563 116 · backtest 244 · `freeze_core verify` 🟢 · `check_path_authority` 🟢 · العزل (مؤمَّن) الاختلافات 0 |
| ملاحظة بيئة مسجَّعة | أُفرغت `tmp/` في نهاية الجلسة فذهب معها مفتاح الـAPI الاختباري وسجلّات الإقلاع وقاعدة الجسر الصناعية؛ النواتان توقّفتا معه. لا أثر على المستودع: كل شيء مقاسٌ ومُثبَت أعلاه كُتب في التقريرات، والحزمة النهائية مبنية بعده |
| التسليم | `QUANT_NQ_bundle.zip` · 16,112,284 ب · SHA-256 `722f0ce1a7f38954da4d125b6184098d` · 5301 ملفًّا (المستودع + التقريرات + `tools/جولة_الترميم/`) — لا commit ولا push ولا إعادة ختم، و`SHA256SUMS.txt` لم يُمَسّ |

## جولة ٢٣ — تشغيلُ المالك على ويندوز: ما انكشف وما صُحِّح

| # | السؤال | القياس (لا الرواية) | ما فُعِل |
|---|---|---|---|
| ٢٣-١ | «ما في لوحة عمّ تشتغل» | `run_forex.py` يقلع النواة فقط؛ اللوحة = `unified_hub.py` على 8090 فوق حوكمة 8092، و`governance/ui/built` موجود (٢٨ ملفًّا × ٣) | لا تعديل — عُرِض أمر التشغيل الصحيح `python scripts\launch_market.py --markets forex`؛ والمنفذ 8010 مشغول بعملية سابقة (المُقلِع يتجاوزه، والنواة هي التي اصطدمت) |
| ٢٣-٢ | `bind 8010` | `launch_market.py:28-30` و`launch_unified.py:60-62` يمرران `dict(os.environ)`، وكلاهما `setdefault` | مقياسٌ مُثبَت: أي `QUANT_CORE_STATE_ROOT/QUANT_RUNTIME_ROOT` عتيق في بيئة المالك **يورَّث ولا يُداس** — أُضيف سكربت قياس يقرأها |
| ٢٣-٣ | `no such table: ticks_v2 / account_v2` | 618/619 يقرآن `C:\Users\NQ\…\Common\Files\nq_brain.db`؛ الجداول يبنيها EA فقط (`QUANT_NQ.mq5` L370/L389) | لا جداول يدوية ولا `account_id` مختلَق — العطب في الجذر (٢٣-٤) |
| ٢٣-٤ | الإكسبرت «مكسور بالترجمة» | الصياغة سليمة (١٧٣/١٧٣ أقواس · ٥٥ دالة)؛ **UTF‑8 بلا BOM + ٨٢٤٦ حرفًا غير‑ASCII + ١١٦ نصًّا عربيًّا + ٢٥٦٣ `?` (تلف سابق)**؛ النسخ الثلاث بصمة واحدة `5edb4df2ff7f7550` | ولِّد `mt5/QUANT_NQ.utf16.mq5` (رموز حرفية، UTF‑16LE+BOM+CRLF) و`mt5/QUANT_NQ.ascii.mq5` (ASCII صرف، بنية مطابقة) — الأصل لم يُمَسّ؛ الترجمة لا تُقاس بلا MetaEditor |
| ٢٣-٥ | «`var/` رجع بالجذر» | الفاعلون: `pair_store.py:20` ثابت نسبي · `517/atom.py:101` قراءة خام · `shared/runtime_paths.py:249` فرع مُتعاطٍ يعيد الجذر · `server.py:851` إحالة `rel` إلى `ROOT.parent` | **صُحِّحت الأربعة** (مرسى `runtime_var` · قراءة من `cfg` مُعلَّب · `runtime_root()/var` · مرسى الحوكمة)؛ المانيفست **لم يُجعَل** يعترض عليه العقد (`config_schema`): `additionalProperties: false` — فقُسنا وحفظنا العقد |
| ٢٣-٦ | تباعد كريبتو ١٣ | الحاملة هي الرسمية (`run_crypto.py:27`): `atoms_crypto` 80 ↔ `crypto_runtime/atoms_crypto` 77 · 2270‑2277 عند المصدر · 2160+2261‑2264 عند الحاملة · ١٠ محتلفات **كلها مطابقة لHEAD** · الحاملة **أحدث** في 2615/2621/2170 | لا مزج ولا نسخ — لا يُحسَم إلا بحكم ذرّة‑بذرّة (قائمة جاهزة في ح/٢٠/٥) |
| ٢٣-٧ | كود زائد استشهدتَ عليه | `if not isinstance(config, dict)` مكرر **فعلاً** — من أداتي في جولة الرقاعة · والفرع المتعاطي في `shared/runtime_paths.py` **ملف كتبتُه أنا** (غير متتبَّع `??`) فادِّعائي أنه «قبل HEAD» كان خطأً | حُذف التكرار، وأُبطل الفرع، وصحَّحتُ الإقرار في ح/٢٠/٣ |
| ٢٣-٨ | الحصّة | — | **لم تُلمَس**: لا commit ولا push ولا إعادة ختم لـ`SHA256SUMS.txt` ولا لمس لـ`core/` المختوم |

**فحوص ما بعد الرقاعة (كلها مُعاد تشغيلها):** `check_project` ✅ · `check_boot` **226 · فشل=[]** · `validate_atoms` **لا مخالفات** · `baseline_regen --check` **0/0/0** (1316·1313·655) · `check_path_authority` 🟢 · `freeze_core verify` **Core V1.31.0 · 23 ملفًّا · `1b0d0f6b91505984…`** · pytest (28 + 701‑750 + 563 + 551‑600 + 578 + 244 backtest) = **472 نجحت** · `var/` الجذر **0** من الجذر ومن الـmirror · 578 مُوحَّد v5.4.1 كودًا ومَانيفستًا.

## جولة ٢٤ — قياسُ المالك يردّ، والأنماط الثلاثة تُغلَق بحارسٍ يراها

| # | ما ورد في `تقارير_المالك/تشخيص_var_ويندوز_جولة23.md` | القياسُ عندي (قبل/بعد) | الحال |
|---|---|---|---|
| ٢٤-١ | «٧ ملفات تحسب `cfg=_rebased_config(...)` ولا تستعمله» | ٥ في حالتي (٧٠٢/٢٧٠١… + ٥١٧ التي أصلحتها للحظة قبله) | **مُغلَق**: القراءة من `cfg`، والحارس يرفض العود |
| ٢٤-٢ | «٤ ذرّات بلا معلِّب أصلًا · مساراتها في قوائم» (٧١٦·٧١٧·٢٧١٦·٢٧١٧ = ١٤ مسارًا) | ✓ `helper=False` و`stores: [{db_path: 'var\\store\\…'}]` | **مُغلَق من الجذر**: `manifest_config_rebase` صار عائديًّا (× ٣ نسخ، بصمة واحدة) + حارسٌ مُعلَّب في العشر ملفات |
| ٢٤-٣ | «ثوابت وحدة خارج الإعداد» (٥٧٨ · ٦٠١) | ✓ ٦٠١: `_CURSOR_DB` نسبيّ والمفتاح `cursor_db` **غائب** عن المانيفست | **مُغلَق**: مرسًى عبر `anchored_state_path` (سطر + `BARE_*`) في ٦٠١·٦٢٥·٨٣١·٨٧٠·٢٨٣١·٥٧٨ = ١٢ ملفًا، **بلا نسخٍ لمنطق المسارات** |
| ٢٤-٤ | «الحزمة نظيفة، والملفات نبتت بعد الفكّ على ويندوز» | ✓ ٢٢ ملف `var/` في الزيب = ٠؛ و`var/` الجذر عندي ٠ | التناقضُ مع حكم جولة ٢٢ كان **قِصورَ سياقٍ لا كذبَ قياس**: ح/٢٠/٣ سمّى الفاعلين، وح/٢١ أغلقهم |
| ٢٤-٥ | «الحرّاس الأربعة كلّهم أخضر ولا واحد منهم يرى هذا» | ✓ مصادَقة مؤلمة: `check_project`/`validate_atoms`/`baseline --check`/`py_compile` مرّت كلها والأنماط الثلاثة حيّة | **جديد**: `governance/checks/check_config_rebase.py` — ٦ أنماط × ٤٦٦ ذرّة، مُختبَرة عكسيًّا (تسقط عند إعادة الزرع، تنجح عند الرجوع) |
| ٢٤-٦ | «كود زائد: حارسٌ مكرر وفرعٌ متعاطٍ» | الحارس المكرّر من أداتي (✓ أُقرّ)؛ والفرع المتعاطي في **ملفي** لا في HEAD (`?? shared/runtime_paths.py`) | **مُغلَق** في ح/٢٠، ومعها صُحِّح عيبٌ كشفه الحارس: رجوعٌ صامتٌ إلى `PROJECT_ROOT/var` ← الآن `runtime_var` فقط، والفشل **يرفع** |
| ٢٤-٧ | درس الإصدار: رفع `ATOM_VERSION` في ٨٣١ أفشل الإقلاع بـ`INVALID_ALERT_AGGREGATOR_STATE` | ✓ اللقطة تحمل `1.0.3` والكود رُجِّع لـ`1.0.2` → `restore()` ترفض | رُجِعت الإصدارات إلى HEAD (كودًا ومانيفستًا) وحُذف أثر القياس؛ **لا رفع إصدار لذرّةٍ لها حالة مُلتقَطة بلا قرار ترحيل** |

**الحصيلة النهائية بعد آخر تعديل:** `check_config_rebase` لا مخالفات (٤٦٦) · `check_path_authority` 🟢 · `check_boot` 226/0 · `validate_atoms` ٠ مخالفات · `check_project` ✅ · `baseline_regen --check` 0/0/0 (1318·1313·655) · ٤٤٧ + ٤٨٤ + ٨٠٤ نجاحات · `freeze_core` V1.31.0 `1b0d0f6b91505984…` · `var/` الجذر **0** بعد `pytest` من جذر المستودع · `C:/` **0** · `SHA256SUMS.txt` لم يُمَسّ · لا commit ولا push.

---

## جولة ٢٥ — قائمة تباعد كريبتو العشر للحُكم (قياسٌ فقط، لم يُنقل شيء)

| ذرّة | مصدر ↔ حاملة | تشابه | Δ أسطر | الأحدث فعلًا | الحكم المقترح |
|---|---|---|---|---|---|
| 1001 مدير_كون_الأصول | 1.2.0 ↔ 1.2.0 | 0.996 | +0/−4 | المصدر (08‑29) | نقل (علامة `manual_allow_bypassed`) |
| 2108 مستقبل_الأخبار | 2.0.1 ↔ 2.0.0 | 0.990 | ~1 (رقم فقط) | المصدر | تصفير رقمي |
| 2153 مستويات_الأمس | 1.2.0 ↔ 1.1.0 | 0.897 | +4/−13 | المصدر = حكم مالك | نقل (لا «أصفر») |
| 2154 الأرقام_المستديرة | 1.2.0 ↔ 1.1.0 | 0.776 | −31 | المصدر (09‑01) | نقل **مع** كتلة `auto_scale` |
| 2170 العقود_المفتوحة | 1.1.0 ↔ **2.0.0** | 0.829 | +17/~19 | **الحاملة** (09‑02) | **backport** إلى المصدر/الثالثة |
| 2615 جسر_مصدر_الأخبار | 2.0.0 ↔ 1.1.0 | **0.157** | ~201 | المصدر (مانيفست+ورقة) | نقل + إصلاح عقد `news.enriched` |
| 2621 مصدر_MEXC_REST | 1.3.0 ↔ **1.4.0** | 0.987 | +1 | **الحاملة** | **backport** مع 2170 (زوج) |
| 2707 مخزن_القرارات | 4.6.0 ↔ 4.2.0 | 0.972 | −13 | المصدر (بند ١١) | نقل |
| 2719 سجل_الأخطاء | 1.0.3 ↔ 1.0.2 | 0.986 | ~3 | المصدر (09‑01) | نقل (`asmar.core`→`quant_nq.core`) |
| 2806 نبضة_الوقت | 3.0.3 ↔ 3.0.2 | 0.991 | ~1 (رقم فقط) | المصدر | تصفير رقمي |

**القياسات البنيوية المصاحبة:** `BuildRegistry`: مصدر=**80** · حاملة=**77** · ثالثة=80 (و79/80 مطابقة للمصدر بايت‑ببايت؛ الفارق الوحيد `2800` بلا `_rebased_config`). الحاملة **تنقصها 2270–2277** و**تنفرد بـ 2160/2261–2264**، و**76/77 من ممانيفستات اختبارها موجودة ومطابقة للمصدر لكنها لا تُجمَع بـ pytest («no tests ran»)** (تصحيح قياسٍ في مسوّدة الورقة: `find -maxdepth 2` أسقطها)+. مرجع الاختبارات: **20 passed** في المصدر.
`EXPECTED_CRYPTO_ROOT = 80` يقيس شجرة المصدر؛ فإقلاع كريبتو من `crypto_runtime` وحده يفشل عليه (77 ≠ 80).
**لم يُنسخ ولا ملف.** التفاصيل وحُكمُ كل ذرّة سطرًا بسطر:  `تقارير_المالك/حكم_تباعد_كريبتو_جولة25.md`.

### جولة ٢٥·ب — تنفيذ الدفعة الآمنة (بعد حكم المالك)

نُقل من `atoms_crypto` إلى `crypto_runtime/atoms_crypto` **و**`crypto_runtime/atoms`: **1001 · 2153 · 2154 · 2615 · 2707 · 2719** (atom.py + المانيفست حيث يلزم). **تُرك 2170+2621** في الحاملة (زوج متوافق، بصمتاه `fb08a75f9b`/`d132183af3` لم تُمَسّا). **تُرك 2108+2806**: فرقهما رقم `ATOM_VERSION` وحده — نقلٌ جزئيّ كان سيخلق عدم تطابق كود↔مانيفست (وهو ما أصلحه فحص 2026‑08‑29)، و2806 تلتقط حالة.
**تصحيح النشر في 2615:** أُضيف `market.news.enriched` إلى `publishes` في مانيفست المصدر/الثالثة (الذرّة تنشره؛ السجلّ `٣_الأحداث:185` يسمّي `crypto:2615`)، وحُلَّ فائض حاملة 2615 بالاستبدال الكامل — فمحا **`db_path: C:\Users\NQ\ASMAR_NEWS\data\news.db`** (مسار ويندوز مُصدَّر؛ قياس ما بقي: **٠**).
**`startup_mode` أُبقي `manual`** حيث هو (حكم التدقيق: 51 manual = تصميم شجرة أحمد).
**عيبٌ صُنع ثم ضُبط:** إدخال يدوي في حاملة 2154 كسر الـYAML → النواة رفضت المانيفست وسقطت الذرّة من `بدأت` بينما `check_boot` 🟢 — فالضبط صار: نسخ حرفي + **فحص parse لكل ممانيفست الأشجار الخمس: ٠ مرفوض**.
**القياس بعد:** كريبتو بدأت=**22**/فشلت=[] · فورекس 226/0 · `check_boot` 🟢 233/80 · **35 passed** · بصمات ٦/٦ ثلاثية التطابق · `check_crypto_feed` ٠ · `check_crypto_isolation` ٠ 🟢 · `manual_execution` 🟢 (٨٠ ممانيفست) · `baseline --check` 0/0/0 · `validate_atoms` ٠ مخالفات · 2153 → `unhealthy «PRIOR_DAY_NOT_READ»` (الأحمر بدل الأصفر = حكم المالك يعمل) · `var/`=0 · `C:/`=0 · `SHA256SUMS.txt` لم يُمَسّ.
**مفتوح:** `check_crypto_chain` 🛑 ٧ = ٢٢٧٢–٢٢٧٧ مفقودة من شجرة الإقلاع (قرار البنية المؤجَّل) + انتظار 2153 لقرائن حقيقية. و`check_crypto_ahmad_parity` لا حُكم هنا (شجرة أحمد غائبة؛ تقاس عند المالك عبر `NQ_AHMAD_TREE`).

### جولة ٢٥·ج — قرار المالك: «حُزم واترك الكريبتو — شريكي يصلحه بطريقته»

نُفِّذت الدفعة الآمنة ثم **رُجِع عنها بالكامل** بأمر المالك؛ شجرة الكريبتو التي تُقلع عادت إلى حالتها المُسلَّمة (**١٦/١٦ ملف = بصمة HEAD**)، وأُعيدت رقعةُ المسار (قرار جولة ٢٢) إلى التسعة ملفات التي أصابها الرجوعُ بالجملة: **مُعلَّبة 110 · ناقصة 0 · أخطاء ترجمة 0**. خطّ الأساس وُلِّد من جديد: `--check` = 0/0/0.
**الحكم الذرّة بذرّة وقياساتُه** (مصادر git، `التاريخ.md`، تبديل 2170/2621، فارق 80↔77، 2270–2277، 2160/2261–2264) باقية في `تقارير_المالك/حكم_تباعد_كريبتو_جولة25.md` **هديةً للشريك بلا تنفيذ**.
**البطارية النهائية بعد الرجوع:** كريبتو 22/0 · فورекس 226/0 · `check_boot` 🟢 · العزل ٠ اختلافات · `check_config_rebase` 🟢 · `check_path_authority` 🟢 · `validate_atoms` ٠ · `freeze_core` V1.31.0 · 35+85 اختباراً ناجحة · `var/`=0 · `C:/`=0 · النواتان موقوفتان · `SHA256SUMS.txt` لم يُمَسّ.
