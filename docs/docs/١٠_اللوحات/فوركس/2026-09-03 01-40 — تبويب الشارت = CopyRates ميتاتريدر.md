# تبويب الشارت = تنفيذ MetaTrader — ليس تحليلًا

**التاريخ:** 2026-09-03 01:40
**القسم:** فوركس · اللوحة
**الملفات:** `governance/ui/src/sections/Charts.tsx` · `governance/ui/src/core/engine.ts` · `governance/server.py` (`exec_chart` · `GET /gov/exec-candles`) · `mt5/QUANT_NQ.mq5` v3.12

---

## الحكم الذي طُبِّق

شارت الفوركس في اللوحة **عين تنفيذ** على نفس شموع المنصّة:

- المصدر: جداول الجسر التي يكتبها الإكسبرت (`copyrates_v2` إن وُجدت، وإلا تجميع تِكّات `ticks_v2`، وإلا أطر أعلى من M1).
- العدد: إحماء EA **200** عمود M1 لكل رمز ولكل فريم (`InpWarmupBars`).
- القطع: لا يمسح الشارت. آخر CopyRates تبقى مع شارة انقطاع.
- الساعات: لا خلط مع cTrader / Yahoo / ساعة الجهاز. لا `±10800`. لا توسيع «قديم».

مسار الكريبتو في نفس الملف **لم يُمسّ** (MEXC كما كان).

---

## ما تراه العين بعد ويندوز

هذا الصندوق **لا يبني اللوحة**. `governance/ui/built/` ما زال `index-DwVvlIy4.js` من تغليف 2026-09-02. المالك لا يرى الشارت الجديد حتى:

1. `npm run build` داخل `governance/ui` على ويندوز، **أو** hot-reload إن شغّل المصدر.
2. ترجمة `mt5/QUANT_NQ.mq5` **3.12** في MetaEditor حتى يُكتب `copyrates_v2`.

---

## ما لم يُغيَّر

582 · 613 · 618 · 622 · 112 · 103 · 359 — صفر لمس في شغل الشارت.
