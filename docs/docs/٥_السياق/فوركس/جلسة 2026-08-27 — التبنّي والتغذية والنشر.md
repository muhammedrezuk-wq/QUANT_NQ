> ⚠️ **تنبيه 2026-08-28:** هذه الورقة **سجلّ تاريخيّ** ليوم ٢٧-٠٨، وتذكر نسخًا
> على سطح المكتب. **كلّها نسخ ميتة ومنسيّة بأمر المالك (٢٨-٠٨)** — لا تُقرأ،
> ولا يُقارَن بها، ولا يُنقل منها. **المشروع الحيّ الوحيد:** `C:\Users\NQ\QUANT_NQ`.

---
# سياق الجلسة — 2026-08-27 · تبنّي 1.27.0 · التغذية · النشر · اللوحة والأخبار

> وثيقة استمراريّة: أين المشروع الآن، وما نُفّذ، وما تبقّى. تُقرأ أوّل أي جلسة جاية.
>
> **آخر تحديث: 2026-08-27 21:53** (بتوقيت الجهاز).

---

## 📍 الحالة الحاليّة
- **المشروع الحيّ:** `C:\Users\NQ\QUANT_NQ` — نواة **1.27.0** (Build `QUANT_NQ_COMPLETE_FULL`، pytest 858/1، Core seal PASS، Release gate READY).
- **الحساب:** ديمو `52992818` (IC Markets Global). **زوج متعادل مفتوح** (net ≈ -1.15، gross ≈ 37165).
- **الأصل القديم للمرجعة:** `C:\Users\NQ\Desktop\5` (نسخة 1.21.1 الشغّالة) — منها نُقلت أوراقنا.

## 🔧 ما نُفّذ هالجلسة
1. **تبنّي 1.27.0:** نُقل `Desktop\6` يدويًّا محلّ QUANT_NQ (مع حفظ الخزنة + venv + git). سبب اليدويّ: robocopy كسر core/atoms.
2. **إصلاح خلّلي التغذية** (موروثان من الدمج، مفصّلان بذاكرة `unified-runner-config-path-bugs`):
   - الخزنة: `core_forex.yaml` → `vault_path: ../runtime/secrets.enc` + `dpapi_blob: ../runtime/device.key` (كان `forex.key` المفقود).
   - `run_forex.py`: حُذف سطر `NQ_BRIDGE_DB` (كان يطغى على `nq_brain.db`).
3. **التغذية حيّة:** 622 (سي‌تريدر FIX) متّصل، أسعار تتدفّق (US30/USTEC/XAUUSD)، جسر MT5 يقرأ `nq_brain.db` الطازة.
4. **النشر:**
   - **العام** `github.com/muhammedrezuk-wq/QUANT_NQ_1`: README إنجليزيّ احترافيّ + قناة `t.me/quantnq` + كود فقط (بلا أوراق حسّاسة، صفر أسرار). عُدّل `publish_public.ps1` ليستثني الدستور/الهندسة/التدقيق.
   - **الخاص** `github.com/muhammedrezuk-wq/QUANT_NQ`: كومِت `1e7f7a8` — 1.27.0 كامل + LICENSE.
5. **نُقلت أوراقنا من 5** (المالك NQ/الرؤية · الهندسة/١٩ · سياق · الدستور · التدقيق) للجديد.
6. **إصلاح تغذية الأخبار** (نفس صنف خلل مسار الإعداد — ذاكرة `unified-runner-config-path-bugs`):
   - `run_forex.py`: `NQ_NEWS_DB` → `nq_brain.db` (كان `var/forex/news.db` الفارغة تطغى على منيفست 615 وتخفي الأخبار).
   - النتيجة (مقيسة من `/gov/atoms`): **#108 News Receiver [أخضر] published=310** · **#109 Economic Calendar [أخضر] 91 حدث** · #615 amber (`BRIDGE_UNREADABLE` — تنافس قراءة، القاعدة يكتبها البوت+MT5 بكثافة، والأخبار تعدّي رغمه بدليل 108).
   - **بوت الأخبار المنفصل** (`C:\Users\NQ\QUANT_NQ_NEWS`) شغّال ويكتب `nq_brain.db` — نسخة وحدة بقفل (محاولات تشغيل ثانية تُرفض بأمان).
7. **تصحيح ورقة كانت تكذب:** README العام (مجلّد `Desktop\نشر QUANT_NQ`) كان يقول `core 1.24.0` والحقيقة المقيسة من القفل **1.27.0** — صُحّح. باقي أرقامه مطابقة للكود (23 مختوم · 233 فوركس · 76 كريبتو · 71 فحص). ⚠️ **يحتاج إعادة نشر بيد المالك** ليصل التصحيح للمستودع العام المنشور.
8. **جلب إصلاح اللوحات من حزمة `Desktop\مجلد جديد`** (بناها طرف آخر، معها تقرير تدقيق مش بخطّي): نُقل `governance/server.py` + `unified_hub.py` + كل `ui/src/` + البناء `ui/built/` للحيّ (23 ملف + 27 بناء) — **بلا لمس التغذية**. قياس بعيني: اللوحة تُعرض **كاملة التنسيق** (251 قاعدة CSS · خلفيّة `rgb(5,6,8)` · خطّ IBM Plex Sans Arabic) بالمتصفّح الداخليّ. «الصفحة الخام» عند المالك = **كاش كروم**، حلّه `Ctrl+Shift+R`. (النقل كان جزئيًّا أوّلًا — قارنت `governance/` وحده.)
9. **إصلاح صفحة الأخبار** (نفس صنف خلل المسار — الحالة الرابعة، ذاكرة `unified-runner-config-path-bugs`): `governance/server.py` `TRADE_DB` كان يقرأ `var/forex/bridge.db` (ملفّ غير موجود) → `/gov/news` = `{available:false, news:[]}` رغم 348 خبرًا بـnq_brain.db. صُحّح للفوركس → `nq_brain.db` → **`/gov/news` يرجّع الأخبار (available=True)**. يصلح `/gov/trades` أيضًا.
12. **تنظيم أوراق docs (2026-08-27 21:53):** ٥_السياق كان مخلوطًا (أنواع مختلطة). نُظّم بمجلّدات مخصّصة (نقل داخل docs لا حذف): **`٨_المالك` جديد — 20 ورقة** (الرؤية · الختم · الطبقات الثلاث · «ورقة المالك ×N» · شرح المالك بلسانه) · **`١_الهندسة` +7** (المعادلات · عقود المحرّك/القرار/التحوّط/الأقسام · غرفة القيادة) · **`١_الهندسة/اللوحة` جديد — 5** (أوراق اللوحة ٤٠–٤٤) · **`٥_السياق`** يبقى تشغيليًّا (الفهرس · الجلسة · الشغل الحيّ · ملاحظات). **الباقي بطلب المالك: مطابقة كل ورقة على الكود وتصحيح الكذب — تدقيق كبير يُنفَّذ قسمًا-قسمًا، لا يُدَّعى إنجازه دفعة.**
11. **تصحيح خطأ أرشفة أوراق المالك (2026-08-27 21:17):** كنت أرشفت أوراق 5 كـ«مكرّرة» بمجلّد `Desktop\أرشيف أوراق مكرّرة`. الفحص أثبت **39 ورقة وحيدة غير مكرّرة** (أوراق الختم `NQ ختم مالك`/`سجل ختم مالك` · الطبقات الثلاث للتنفيذ · `ورقتي — الشغل الحيّ` · `٠٠٠ الفهرس` · `رؤية_المالك` · معادلات الأقسام · أوراق اللوحة ٤٠–٤٤). **لم تُحذف — كانت سليمة بالأرشيف.** أُعيدت **108 أوراق مفصّلة** لأقسامها الفعّالة (نسخ لا نقل، الأصل باقٍ): الدستور 13 · الهندسة 23 · السياق 45 (فيها `سياق/` 39 + `المالك NQ/`) · التدقيق 35. *(درس: لا أقرّر «مكرّر/ماله لزوم» إلا بفحص اسم-ملف مقابل المشروع كامل — [[owner-deletes-by-hand]].)* وبعد فحص آخر حزمة `Desktop\مجلد جديد` (21:44): بنيتها نفس المشروع، جِبت منها ورقتين وحيدتين — `٦_التدقيق/تدقيق_إصلاح_اللوحات` + **قسم جديد `٧_نشر_ويندوز_السحابي/النشر.md`**. وأقسام ٢_المسارات/٣_الأحداث/٤_الأقسام مجمّعة (ورقة واحدة) في **كل** النسخ (5 · مجلد جديد · الحيّ) — لا نسخة مفصّلة لها أصلًا.
10. **إصلاح تجمّد البثّ الحيّ باللوحة** (`governance/unified_hub.py` — `_ws_relay`): مبدّل WebSocket بالهب كان يقرأ ردّ مصافحة الخادم بـ`recv(4096)` فيلتقط المصافحة + بداية أوّل إطار ملتصقة، ثمّ **يرمي البقايا بعد `\r\n\r\n`** — فتتزحزح محاذاة الإطارات ويتجمّد البثّ بعد اللقطة الأولى. **قياس معزول:** 8092 مباشرة = 182 رسالة، عبر الهب = **1 فقط**. النتيجة: «الشارت» و«اللوحة الجديدة» وكل تبويب حيّ يبقى «بانتظار…». **الإصلاح:** تمرير البقايا للعميل بدل رميها → **الهب صار 180 رسالة**، والبثّ يحمل الأسعار (`feed.ctrader.tick`) وبطاقات الأقسام والقرارات. أُعيد تشغيل الهب **وحده** (8090) بلا لمس التغذية (8010/8092). *(ملاحظة: «الشارت»/«الروم» قد يبقيان «بانتظار» لأنّ النظام موقوف بالحماية فلا دورات قرار تنتج بطاقات/أسعار مرسومة — منفصل عن البثّ. وسلامة الملفّات #7 مُعلَّمة «مُعدَّلة» لأنّ ملفّات عُدّلت هالجلسة — طبيعيّ.)*

## 🛑 حالة التداول الحيّة — ليش ما بياخد صفقة (مقيسة من `/gov/atoms`)
النظام **محترم، الحماية بتمنع** — مش عطل:
- **#516 `KILL_SWITCH_ACTIVE`** — مفتاح الطوارئ مُفعّل.
- **#508 `EXPOSURE_BREACHED`** — gross 37165 مقابل حدّ 5000 (الزوج المتعادل) → صفقات جديدة ممنوعة.
- **#466** — كل القرارات مرفوضة (8550 مرفوض / 0 موافَق).
- **#513 `SIZING_UNAVAILABLE`** · **#717 `ROWS_IN_THE_FUTURE`** (خلل ختم زمنيّ).
- **لفتح صفقة (قرار المالك بختم NQ):** طفّي #516، وحلّ التعرّض (سكّر الزوج أو ارفع الحدّ 5000).

## ⏳ ما تُرك (دون بناء — بانتظار قرار)
- **اللوحة — حالتها بعد إصلاحات هالجلسة (بنود ٨–١٠، مقيسة بعيني):** التنسيق يشتغل (251 قاعدة CSS · خلفيّة `rgb(5,6,8)` · خطّ عربي)، الأسماء عربيّة، والبثّ الحيّ يمرّ (١ → ١٨٠ رسالة تحمل الأسعار والأقسام والقرارات). **«الرئيسية» غنيّة كاملة (91 عنصر).** المتبقّي الحقيقيّ: «الشارت» و«الروم» يبقيان «بانتظار…» لأنّ **النظام موقوف بالحماية فلا دورات قرار تنتج بطاقات/رسمًا** — لا عطل لوحة؛ و«اللوحة الجديدة» عرض مختصر يتعبّى بالقرارات. *(«الصفحة الخام» عند المالك = كاش كروم → `Ctrl+Shift+R`.)*
- **سلامة الملفّات #7 مُعلَّمة «مُعدَّلة»:** كشفت تعديلات هالجلسة (`core/CORE.lock` + `dir:atoms/`) — تعمل صح، بس ترجع خضراء فقط بإعادة ختم/تأسيس خطّ الأساس (`integrity_baseline.json`) بيد المالك (النواة مختومة — إذن جديد).
- **نضج Live** (تقييم المالك): لا فحص حساب وسيط فعليّ · قرار وصول RDP/متصفّح · مطابقة رموز · Soak 24س · Restart · أمر Live صغير بموافقة صريحة.
- **خلل زمنيّ #717** (ROWS_IN_THE_FUTURE) — يُفحص.

## 🗺️ خريطة التشغيل
- **MT5:** `terminal64.exe` (IC Markets Global) شغّال، يكتب `%APPDATA%\MetaQuotes\Terminal\Common\Files\nq_brain.db` حيًّا.
- **المنافذ:** 8010 فوركس · 8020 كريبتو · 8090 الهب/اللوحة · 8092/8093 حوكمة · 8098 تلغرام.
- **الإطلاق النظيف:** `venv\Scripts\python.exe scripts\launch_unified.py` (نوافذ منفصلة، يفحص المنافذ فما يعمل ازدواج).
- **البيئة:** venv بايثون 3.12.

## قواعد المالك المرعيّة هالجلسة
بور شيل فقط · شغل لايف لا موك · لا تنفيذ ماليّ بلا ختم NQ · النشر بيد المالك (الحاجب يمنع git push منّي) · لا حذف — أرشفة.
