# الأعطال المعروفة — ابحث هنا **قبل** أي تشخيص

> **آخر تحديث: 2026-08-28 21:30 — NQ**
> كل سطر هنا كلّف ساعات ليُعرف. **لا تُعِد تشخيصه.**

---

## 🔑 جذر واحد يفسّر أربعة أعراض — ساعة خادم الوسيط

**الأعراض التي تراها:**
```
ACCOUNT_STATE_STALE        (#619)
REFERENCE_NOT_USABLE       (#521 · يقتل فتح الهيدج)
CLOCK_NOT_SYNCED           (بوّابة #552)
ROWS_IN_THE_FUTURE         (#717)
```
**الجذر الواحد:** ساعة خادم `ICMarketsSC-Demo` تسبق الحقيقيّ بـ**3 ساعات
بالضبط** (10,799.77 ثانية — قياس مباشر من محطّة MT5).
**السبب بالكود:** `mt5\QUANT_NQ.mq5:775` يستعمل `TimeGMT()` — ساعة خادم
الوسيط، **لا ساعة الويندوز**.

**❌ نُفي بالقياس — لا تُعِد فحصه:**
| الفرضيّة | الدليل على نفيها |
|---|---|
| ساعة الويندوز | `w32tm` = **103 ملّي ثانية** (قاسها المالك) |
| ذرّة #608 (NTP) | 0.12 ثانية عبر 3 خوادم عالميّة |
| ذرّة #3 (الساعة الرسميّة) | `SYNCED` |
| «خادم متلاعب» | فرق **ثابت** 3×3600 = منطقة زمنيّة، لا تلاعب |

**التفصيل:** `٣_مشترك/الوقت_والساعة/2026-08-28 20-15 …`

---

## ⚠️ فخّ v1/v2 — أخطر فخّ قراءة بالنظام

الـEA (نسخة 3.10) **هجر كل جداول v1 منذ ١٧-٠٨** وانتقل لـ`_v2`:

| ❌ لا تقرأ (مجمّدة منذ 11 يومًا) | ✅ اقرأ |
|---|---|
| `account` | `account_v2` |
| `prices` | (من التِكّات الحيّة) |
| `trade_events` | `trade_events_v2` |
| `ticks` | `ticks_v2` |
| `positions` | `positions_v2` |

**قراءة v1 تعطي صورة قديمة كاذبة.** وقعتُ فيها وظننت الـEA متوقّفًا وهو يعمل.

---

## ⚪ سلوك طبيعيّ — ليست أعطالًا

| ما تراه | لماذا هو طبيعيّ |
|---|---|
| `#609 open=0` · `#611 read=0` · `#601 written=0` | **صحيحة** — لا مراكز مفتوحة ولا صفقات جديدة |
| `#578 PAIR_RETRY_EXHAUSTED` | **بالتصميم** — لا يعيد المحاولة تلقائيًّا؛ يحتاج تفعيلًا يدويًّا جديدًا |
| `#7 File Integrity` كهرمانيّة | **قفلا تجميد مختلفان** (الجذر 1272 عنصرًا مقابل `forex_runtime` 1043) — لا خرق |
| `#615 BRIDGE_UNREADABLE` | تنافس قفل SQLite — **غير قاتل**، `#616` يقرأ نفس البيانات بنجاح |
| `#622 FIX_SYMBOLS_OUT_OF_SESSION` | ساعات سوق الرمز مغلقة — لا عطل |

---

## 🔧 أعطال تشغيليّة لها وصفة جاهزة

### النظام لا يقلع بعد فكّ ضغط جديد
```
Unified-link preparation failed: … Cannot create a file when that file already exists
```
**الوصفة:** `٣_مشترك/البنية_التحتية/2026-08-28 21-00 …` (إزالة روابط Junction معطوبة ثمّ `prepare_unified`).

### النشر يتوقّف
**حُلّ نهائيًّا** — السكربت صار يُصلح نفسه. التفصيل بنفس الورقة أعلاه.

---

## قالب الإضافة

```
## [نصّ العَرَض الحرفيّ كما يظهر]
**الجذر:** …    **مقاس بـ:** …    **التاريخ:** YYYY-MM-DD HH:MM
**نُفي:** [ما ثبت أنّه ليس السبب — كي لا يُعاد فحصه]
**التفصيل:** [مسار الورقة الكاملة]
```
