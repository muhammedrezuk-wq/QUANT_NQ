# NQ سجل ختم مالك

> ⚠️ **هذه الورقة سجلّ تاريخيّ.** كل إصدار ورقم ذرّة فيها صحيح **لحظة ختمه**، ولا يُقرأ كوصف للحاضر.
> للحالة الحيّة: `تسليم.md` و`بوابة السوق الحي.md`. للمشاكل: `A.md`.


سجلّ البنود المنفَّذة — البند السادس من ورقة «NQ ختم مالك».

---

| # | البند | ختم المالك | التاريخ |
|---|---|---|---|
| 47 | **مهارة تدقيق منصات التداول — ثلاثة أنماط صمت أُضيفت إليها بعد قياس أعطال المشروع** (**ختم مالك: «ضيفهم nq»**، ٢٠٢٦-٠٨-٢٥): المهارة الواردة تغطّي فصل البيئات والنموّ بلا سقف وسقف حجم الأمر وعتبةً ضُبطت كي لا تعترض — **ولا تغطّي أربعة أنماط قتلت هذا المشروع فعلًا**، قِيس كلٌّ منها بالكود قبل الكتابة: **(١) البوّابة المستحيلة** — عتبة سقفها الرياضيّ أدنى منها، والدليل `413/manifest.yaml:53 min_active_weight = 40.0` مقابل سقف مطلق `36` بمعامل سياق مسقوف بـ`0.36` (`413/atom.py:90-94`)، والمقيس أقصاه `24.1` على ١٨٦ عيّنة ⇒ `451 cycles=27,110 · complete=0` في ١٠٠٪. **(٢) الصفر المصنوع** — مقام صفر يُرجع `0.0` فيُقرأ «مقيس ومنخفض» وهو «غير معرَّف». **(٣) مفتاح بلا سلك** — معامل معتمَد من المالك وصفر قارئ له بالكود، والدليل `STALE_AFTER_S = 10.01` معتمَدًا بجدول `parameters` مقابل `shared/section_contract.py:45 = 5.0` محفورة، وعشرة معاملات بحالته. **(٤) أتقيس النظام الصحيح** — مالك المنفذ لا فتحُه، واختبار الإغلاق (المحمَّل + المستبعَد = ما على القرص). **المنفَّذ:** ملفّ جديد `references/silent-death.md` (11,447 بايت) يحمل الأربعة بأنماط كشفها ودليلها المطلوب بالتقرير · و**«الجسم على سطر التعريف»** أُضيف إلى `references/code-audit.md` (14,932 → 16,775) — النمط الذي كسر `365` ليلة ٢٠٢٦-٠٨-٢٤ فأسقط اشتراكاتها خارج الدالّة، ولا يمسكه فحص «الأسطر المضغوطة» لأنّه يعدّ الفواصل المنقوطة · و**«الخطوة ٧ · انضباط القياس الحيّ»** أُضيفت إلى `references/discovery.md` (8,916 → 10,875): النافذة تُعلن مع كل رقم، و«غائب عن النافذة» ≠ «ميّت» (الدليل: حدث بثّ ٣٧٢ مرّة في ٣٫٨ ساعة وغاب عن نافذة ٧٥ ثانية) · وسطر بالبطاقة يصل المرجع الجديد (`SKILL.md` 18,156 → 18,930). **والتحقّق:** اثنا عشر مرجعًا تذكرهم البطاقة موجودة كلّها على القرص · والعشرة التي لم تُقصد بقيت بأحجامها الأصليّة حرفًا بحرف · و`QUANT_NQ` لم يُلمس (`git` نظيف). | `nq` | ٢٠٢٦-٠٨-٢٥ |
| 46 | **`atom-code-audit.md` — مرجع الحزمة القديمة نُقل إلى المهارة المركّبة** (**ختم مالك: «nq»**، ٢٠٢٦-٠٨-٢٥): قِيست الحزم الثلاث بالبصمة: `(1)` و`(2)` نسخة واحدة (`f927a8da329c79b3`)، والقديمة تحمل **ملفًّا واحدًا لا يوجد بغيرها** — `atom-code-audit.md` (16,782 بايت)، وهو نفسه `code-audit.md` لكن بلغة هذا المشروع (الذرة ×13 · المانيفست ×2 · QUANT_NQ ×1، مقابل صفر في العامّ)، وأوسع منه بدورة الحياة (يفرد `initialize`/`start`/`stop`/`shutdown`/`health_check`/`snapshot` كلًّا بعنوانه). **المنفَّذ:** نُسخ الملفّ وحده إلى `references/` بتحقّق بصمة (`ffff889084eb0ba6` مصدرًا وقرصًا). **والمعلَن ولم يُنفَّذ:** البطاقة لا تذكره — الختم كان «انقل ملفًّا واحدًا» فلم تُمسّ البطاقة (بصمتها بقيت `f04a7d436981`)، ⇒ الملفّ موجود ولا يُفتح تلقائيًّا. | `nq` | ٢٠٢٦-٠٨-٢٥ |
| 45 | **مهارة `quant-platform-audit` رُكّبت عند المستخدم لا بالمشروع** (**ختم مالك: «nq»** بعد أمره «حطها عند مستخدم»، ٢٠٢٦-٠٨-٢٥): المهارة أداة تدقيق تخصّ النموذج لا النظام، فوضعها داخل `QUANT_NQ` يُدخلها في حساب الذرّات وحارس سلامة الملفّات بلا داعٍ. **المنفَّذ:** فُكّت النسخة `(2)` إلى `C:\Users\NQ\.claude\skills\quant-platform-audit\` — اثنا عشر ملفًّا (`SKILL.md` + أحد عشر مرجعًا). **بحارس مسار قبل الفكّ:** رُفض أي مدخل بمسار مطلق أو `..` أو خارج جذر المهارة، ورُفضت الكتابة فوق مجلّد قائم. **وبتحقّق بصمة بعده:** بصمة كل ملفّ داخل الحزمة قُوبلت ببصمته على القرص — **١٢ مطابقًا · صفر مختلف**. و`QUANT_NQ` لم يُلمس (`git` نظيف). **وحدّ صادق:** المهارات تُقرأ عند بدء الجلسة، فلا تعمل إلّا بجلسة جديدة. | `nq` | ٢٠٢٦-٠٨-٢٥ |
| 44 | **575 v2.1.0 — اليد التي تكتب أوامر الإدارة ما كانت تسمع إيقاف المالك** (**ختم مالك: «حل 575 بس ولا شي ثاني Nq»**، ٢٠٢٦-٠٨-٢٢): اشتراكاتها كانت ثلاثة (`execution.manage.command` · `execution.gate.command` · `SYS_SECOND`) **وليس فيها `emergency.halt` إطلاقًا** — مقروءة من المانيفست حرفيًّا لا موصوفة. فكانت تكتب على جسر الوسيط **والمالك موقِّف النظام**. **والدليل من جسر المال نفسه لا من ورقة:** جدول `commands` حمل **١٦٤٬٠٤٩ أمرًا فاشلًا، منها ١٦٣٬٩٦٦ نتيجتها `OWNER_HALT`** — ثلاث تذاكر (`1851516500`·`1851516489`·`1851516508`) أخذت ~٤٠٬٩٠٠ أمرًا لكلٍّ على مدى **١٣ ساعة ونصف متواصلة** (٢٠٢٦-٠٨-١١ ٠٠:٢٧ ← ١٣:٥٧، ~٢.٥ أمر/ثانية). **⇒ الإيقاف كان حاجزًا واحدًا وهو الأخير (الإكسبرت)، والدماغ لم يسمعه.** **المنفَّذ:** اشتراك على `emergency.halt` و`risk.kill_switch.reset_requested` **بنفس عقد 552 حرفًا بحرف** (لا عقد مخترَع): `account_id` يوقف حسابًا، `scope=SYSTEM` يوقف الكل، **وإيقاف لا يحمل أيّهما يُعَدّ ولا يُوسَّع افتراضًا** (`halt_identity_blocked`) — لأنّ التوسيع الصامت كذبة بالاتّجاه الآخر. أثناء الإيقاف: صفر كتابة، والسبب يُنشر `OWNER_HALT` — **نفس المفردة التي يردّها الوسيط**، لا جديدة. والصحّة `DEGRADED` تحمل `global_halted`/`halted_accounts`/`halt_blocked`. **أربعة حرّاس كُتبت أوّلًا وسقطت (`4 failed, 5 passed`) ثمّ نجحت (`9 passed`):** إيقاف النظام يمنع خمسة أوامر متتالية · إيقاف الحساب يمنع صاحبه ويمرّر غيره · الرفع يعيد الكتابة · إيقاف بلا هويّة يُعَدّ ولا يوقف. **تصحيح مقياس مسجَّل:** وصفتُ أوّلًا العاصفة بأنّها `NO_POSITION` — **خطأ**، `NO_POSITION` **٦٩ صفًّا فقط**؛ والسبب الحقيقي `OWNER_HALT`. وثبت أنّ حارس ملكيّة المركز (`POSITION_OWNERSHIP_MISMATCH`) **موجود أصلًا** ويسدّ طريق التذكرة الميّتة — فالثغرة كانت حالة واحدة: **مركز موجود والمالك موقِّف**. **صفر تغيير آخر** — حارس الملكيّة ومنع التكرار وبوّابة `enabled` كما هي. **مقيس عند التسليم:** الأسطول **1084 اختبارًا · صفر فشل** · المدقّق **3 مخالفات = خطّ الأساس** (451·552·580 حجمًا، لا من هذا الشغل) · 575 نظيفة بالمدقّق · **حيّة `2.1.0` healthy** بتحميل حيّ بلا إعادة تشغيل. | `Nq` | ٢٠٢٦-٠٨-٢٢ |
| 43 | **901 v2.5.1 — زرّ بوّابة التنفيذ كان ينفجر بعد النشر وقبل ختم الصفّ** (**ختم مالك: «صلححلي 901 nq»**، ٢٠٢٦-٠٨-٢٢): عدّادات `_executed` كانت **نسخة مكتوبة باليد** من جدول `ACTIONS` كُتبت بتسعة أفعال، و`ACTION_GATE = "execution_gate"` سقط منها. فكل أمر بوّابة: يُتحقّق ← **يُنشر فعلًا** على `execution.gate.command` ← ينفجر `KeyError: 'execution_gate'` عند `self._executed[action] += 1` ← **فلا يصل السطر الذي يختم الصفّ `DONE`** ⇒ يبقى `PENDING` ويُعاد التقاطه ونشره **كل نبضة ثانية بلا نهاية**. **مقيس من سجلّ الأخطاء الحيّ:** `فشل مستمع 'SYS_SECOND' (ناشر=806) من '901': 'execution_gate'` — **٥٥٨ مرّة ٢٠٢٦-٠٨-٢٠ · ٢٤٠ مرّة ٢٠٢٦-٠٨-٢١ · صفر ٢٠٢٦-٠٨-٢٢ بعد الإصلاح**. **الإصلاح من الجذر لا ترقيع:** لم يُضَف السطر الناقص — صار مصدر العدّادات **جدول الأفعال نفسه** (`{action: 0 for action in ACTIONS}`)، فأيّ فعل يُضاف لاحقًا يأخذ عدّاده ولا يمكن للجدولين أن يفترقا ثانيةً (**نفس درس البند ٣٧**: أربعة حرّاس كلٌّ يحمل نسخته المحفورة ← قائمة واحدة). **حارسان كُتبا أوّلًا وسقطا (`أفعال بلا عدّاد: ['execution_gate']`) ثمّ نجحا (`14 passed`):** سلوكيّ (الأمر يُنشر مرّة · الصفّ يُختم `DONE` · ولا يُعاد بنبضة ثانية) وبنيويّ (لا فعل في `ACTIONS` بلا عدّاد). **مخالفة من شغلي صُحّحت فورًا:** كتبتُ التعليق عربيًّا داخل `atom.py` فأمسكها المدقّق (**المادة ٣**) — نُقل إنكليزيًّا بالكود والشرح العربي إلى `الشرح.md`، والمخالفات رجعت **3 = خطّ الأساس**. **مقيس:** الأسطول **1080 اختبارًا · صفر فشل** · **حيّة `2.5.1` healthy** بتحميل حيّ. | `nq` | ٢٠٢٦-٠٨-٢٢ |
| 42 | **قصّ الطبقة الميتة من جسر المال إلى أرشيف سطح المكتب** (**ختم مالك: «قص من مجلد لا مجلد ارشيف مو نسخ nq»** — قصّ لا نسخ، ٢٠٢٦-٠٨-٢٢): **الجذر المقيس — الجسر كان بنسختين، حيّة وميتة:** النظام هاجر إلى عقد `_v2` وتُركت جداول `v1` بكامل تاريخها **بلا صاحب**. مقيس: `ticks` **٢٠٥٬١٠٣ صفًّا** بينما الإكسبرت يكتب على `ticks_v2` (`mt5/QUANT_NQ.mq5:668`) و618 يقرأ `ticks_v2` (`table_name` بالمانيفست) و`ticks_v2` **صفر صفّ** — أي **618 بريئة وشغلها سليم**. و`commands` **١٦٤٬٢٩١ صفًّا** بلا أيّ ذرّة مسؤولة عن تنظيفه. **نُفِّذ بيد المالك بسكربت مكتوب** (الطريق المعتمد: أكتب ويشغّل ويُقرأ ناتج طرفيّته — والمُصنِّف الأمني رفض تنفيذه منّي على قاعدة حيّة، فوافق الطريقان): **لا يُحذف صفّ قبل تأكيد تطابق العدد بالأرشيف** · يتوقّف إن وُجد أرشيف سابق فلا يدهسه · الحذف على دفعات ٥٠٠٠ صفّ بنَفَس ٥٠ ملي‑ثانية لأنّ 618 يفتح القاعدة ١٠٠ مرّة بالثانية · **بلا `VACUUM`** (يقفل القاعدة دقائق والنواة حيّة). **الناتج المقيس:** `ticks 205,103 → 0` · `commands 164,291 → 0` · الأرشيف **73.2 ميغا** بـ`nq_bridge_v1_2026-08-21.db` + `بيان_النقل.md`. **ولم يُلمس حيٌّ واحد** (تُحقِّق بعده): `trade_events 165` · `trade_events_v2 165` · `symbol_specs 8` · `symbol_specs_v2 1` · `account_v2 1` · `calendar 716` · `news 159` · `candles_history 200` · `prices 8` — و`trade_events` القديم **تُرك عمدًا رغم تكراره** لأنّ 517 يقرأ منه فعلًا (بند `٩٠-٣٣`). **والأثر الحيّ المقيس:** `618 read failed: database is locked` كان **٩ مرّات** قبل النقل و**صفر** بسجلّ ٢٠٢٦-٠٨-٢٢ بعده · و712 خرج من `database is locked` إلى `healthy stored=381,100`. **تصحيح مقياس مسجَّل:** نسبتُ القفل أوّلًا إلى «حذف غير محدود بمعاملة واحدة» بـ618 — **خطأ**، `ticks_v2` فارغ فالحذف لحظيّ؛ الضغط كان من **حجم القاعدة (101 ميغا) مع فتح 618 لها ١٠٠ مرّة/ثانية**، وأثبته زوال القفل بزوال الحجم. | `nq` | ٢٠٢٦-٠٨-٢٢ |
| 44 | **ضبط الورق بعد ليلة الحكم التِكّي — وأربع كذبات أُسقطت بالقياس** (أمر المالك ٢٠٢٦-٠٨-٢٢: «ما بدي ورق عم يكذب، ورق خالص على أرشيف»): **(١) `٣٠ الأقسام`** — كتلة 600 كانت تعدّ «الإحماء» جسرًا حيًّا و**602 محذوفة بيد المالك**؛ وُسمت 🗄️ بسبب الحذف مقيسًا (٢٠٠ شمعة إحماء كانت تُطفئ إنذار الجوع فتُخلّف ٧١ تمثالًا أخضر). وسطر «إحماء صادق: النضج بعد ~٢٠ شمعة/رمز» سقط سنده. وأُضيف حكم «الوحدة تِكّة لا شمعة» على رأس قسم المحلّلات مع أرقام الاختناق. **(٢) `٩٠ الباقي`** — القسم الأوّل «العطل الحيّ: التحليل متوقّف» **مُنتهٍ**: `أ` سقط بالقياس (622 يختم `broker` فيمرّ شرط 103)، و`ب` قائم بنصّه لا بأثره (**١٥ من ١٥ محلّلًا على حدث الشمعة · صفر على التِكّة** — فُحصت الليلة). والحالة النافذة: `103 closed=25 · 151…165 candles=25 · 451 cycles=25 complete=25 superseded=0` — **أوّل اكتمال ١٠٠٪ بالتاريخ**. والقواعد الأربع التي وُصفت «متجمّدة منذ ٠٣:٣١» تُكتَب الآن (`analysis 78,167` · `structure 26,120` · `liquidity 27,741` · `decisions 28,167`، آخر كتابة خلال دقيقة). **(٣) عدد الأسطول** — ثلاثة مواضع تقول «٢١٧ مانيفست · ٢١٣ تلقائيّة»، والحقيقة **٢١٦ · ٢١٢ · ٤ يدويّة** بعد حذف 602؛ صُحّح بالفهرس و`٩٠` موضعين. **(٤) «صفر تراكم · صفر قفزة · صفر انقطاع»** بالفهرس — نقضه قياس نفس الليلة بعد ساعتين: `j=5` · `s=522,946` بايت مرميّة · `q` بلغ 343,395 · و613 سجّل ٧ انقطاعات؛ وُسم أنّه «صحيح لساعته لا لطبع التغذية». **وأربعة بنود جديدة بـ`٩٠`:** `٩٠-٣٦` عقد النضارة (منفَّذ) · `٩٠-٣٧` الناقل ينتظر مستمعيه و٦٤ مشتركًا على حدث الشمعة · `٩٠-٣٨` مفتاح أعلى المانيفست يُسقط الذرّة (`extra="forbid"`، كلّف ٤٣ ذرّة أربع دقائق) والمكان الشرعيّ `metadata:` · `٩٠-٣٩` 622 لا توقظ نفسها من صمت الوسيط. **وستّة بنود بالفهرس (١٨–٢٣)** ومنها ما لم يُفحص ويُعلَن أنّه لم يُفحص (جلسة FIX من نوع TRADE · معنويات Open API). **وبندان أُغلقا بالقياس:** `٨` منافذ الحوكمة الثلاثة تردّ 200، و`١٥` لا عمليّة cTrader تعمل (لكنّه يعود مع فتح المنصّة — إزالته بيده). ⛔ **ولم يُلمس كود ولا ذرّة ولا نواة — ورق فقط.** | `NQ` | ٢٠٢٦-٠٨-٢٢ |
| 43 | **عقد النضارة — «سليمة» لم تعد تعني «صائمة»** (ختم `NQ` ٢٠٢٦-٠٨-٢١): **الجذر الذي كشفه المالك:** «كانو يقولو انهم جوعنين وينقو، هلق بعد هي طبقة صارو عميان». صحيح حرفيًّا — فحص الجوع داخل الذرّات يسأل «هل رأيتُ شيئًا **يومًا**؟» لا «هل وصلني شيء **مؤخّرًا**؟» ([151/atom.py:274](../atoms/151_%D8%A7%D9%84%D8%A7%D8%AA%D8%AC%D8%A7%D9%87/atom.py) — `if self._candles_seen == 0`)، فحين أطعمها إحماءُ 602 مئتَي شمعة عند الإقلاع عبرت العتبة **مرّة واحدة إلى الأبد**. **مقيس:** بعد حذف المالك لـ103 و602 بيده، بقيت **٧١ ذرّة متجمّدة عند العدد 200 بالضبط وكلّها خضراء**؛ ومسح الأنماط يقول **٩٥ ذرّة بلا أيّ مقياس نضارة مقابل ٤٠ عندها مقياس**. **ولمَ خرج القياس من الذرّة:** `CLOCK_OWNERS={3,806,608}` — ثلاث ذرّات فقط تقرأ الساعة، و**٩٠ من العميان لا يسمعون `SYS_SECOND`**، فإضافة النضارة داخلها كانت ستضيف ٩٠ مشتركًا على حدث كلّ ثانية — أي إعادة بناء نفس الانهيار الذي قتله المالك للتوّ. **نُفِّذ (أ) في الحوكمة** ([server.py](../governance/server.py) — `_progress_print` · `idle_limits` · `hunger_of` وتعديل `label`): لا تسأل الذرّة عن حالها، بل تبصم **كل رقم تنشره عن نفسها** (الرسالة + التفاصيل) وتقيس كم بقي ساكنًا؛ والساكن فوق حدّه المعلَن يصير «**جائعة**» برتقالية. **بلا تعديل ذرّة واحدة · بلا فتح نواة · بلا تعديل واجهة** (اللوحة تقرأ `color`/`label_ar` أصلًا في كلّ أقسامها). **(ب) حارس المدقّق** ([validate_atoms.py](../governance/scripts/validate_atoms.py)): ذرّة تشترك بحدث ولا تعلن `max_idle_s` تُرصد، والعدد يُطبع **برأس التقرير** لا بين التحذيرات: «🔇 بلا حدّ جوع معلَن: **199** ذرّة — تقول سليمة وهي صائمة». **⛔ ولم يُخترع رقم واحد** — قانون «لا اختراع مدد»: الحوكمة تقيس المدّة الصادقة وتعرضها للجميع، والحكم **لا يصدر إلّا بعدد يعلنه المالك**؛ ولهذا لا تزال الحدود المعلَنة **صفرًا** ولم تُصبغ ذرّة بالبرتقاليّ ظلمًا (١٨٦ ذرّة تسكن ٣٠ ثانية، وكثير منها يسكن بحقّ كمستقبل الأخبار). **مقيس بعد التنفيذ:** ٢١١/٢١١ ذرّة تحمل حقل الجوع حيًّا · التماثيل تُظهر **٣٨٨.٧ ثانية** سكون والأحياء **٠.٠** · **١٠ اختبارات جديدة** (`tests/test_hunger_contract.py`) تحرس العقد ومنها أنّ الواقفة لا تُتّهم، والمتعثّرة لا يبتلع الجوعُ صوتَها، وذرّة نصّها ثابت بالتصميم لا تُتّهم ما دام رقم بتفاصيلها يتحرّك · والمدقّق **٣ مخالفات = نفس خطّ الأساس**. **وعطل جانبيّ من حذف 103 صُلح:** `tests/test_market_decision_path.py` كان يُسقط **جمع الاختبارات كلّه** عند الاستيراد (StopIteration على ذرّة غائبة) فيخفي ٣٥٥ اختبارًا سليمًا خلف ذرّة راحت — صار الغائب يُتخطّى. **الحصيلة: ٨ سقوط = خطّ الأساس المعروف · ٣٥٥ نجاح · ١ متخطًّى.** **وأُعيد تشغيل الحوكمة وحدها** (PID 9140) بطرفية مرئية — **النواة (PID 2868) لم تُمَسّ**. | `NQ` | ٢٠٢٦-٠٨-٢١ |
| 42 | **بوّابة التغذية: مفاتيح باللوحة · استثناء حجم · وتنظيف أثر 617 بعد أرشفتها** (أمر المالك المباشر ٢٠٢٦-٠٨-٢١ ضمن ختم 40): **(١) العطل الذي أوقف التغذية داخل النواة كان بدالّة التأطير التي كتبتُها** — `view = view[tail_end+1:]` ينسخ كل الباقي مع كل رسالة، **تعقيد تربيعيّ ودوّامة**: كلّما تأخّرنا كبر المخزن فزاد النسخ. **مقيس بعدّادات الذرّة نفسها: `q` يكبر 13,200 بايت/ث بينما `t` و`d` يتقدّمان 0.10/ث — أي جمود عمليّ**، ومنه جاء انقطاع السيرفر كل خمس دقائق (`c=4`). صار المشي بالفهرس: **8000 رسالة · 711 ك.ب في 10.5 ملي‑ثانية**. **(٢) وثلاثة أسداد معها**: النبضة تُخدَم **قبل** التراكم لا بعده (التغذية تحفظ خطّها قبل أن تُطعم أحدًا) · `Logout` يهدم الوصلة فورًا بدل تركها `CloseWait` خمس دقائق **مقيسة** · وقانون المالك المختوم «يمرَّر مباشر — لا لِحاق ولا غرق» نُفِّذ بعتبة `live_max_backlog_bytes` (قفزة للذيل + إعلان الفجوة + **مسح الدفاتر**، لأنّ دفترًا مبنيًّا فوق تحديثات مقفوزة دفتر يكذب). **(٣) والتِكّة صارت تُنشر على تغيّر أفضل سعر فقط** — وهي دلالة التِكّة الصحيحة، ونشر اقتباس لم يتغيّر يكلّف فان‑آوت كاملًا يخسره الخطّ الحيّ فجوةً معلَنة. **النتيجة المقيسة بعدها: صفر تراكم · صفر قفزة · صفر بايت مقفوز · صفر انقطاع**، و`ticks=24348 depth=33308 symbols=7`. **والقياس الصادق لكل رمز مقابل الجسر القديم** (تغيّرات سعر حقيقيّة بلا تكرار): `USTEC ×1.97 · US30 ×1.43 · XAUUSD ×1.33 · BTCUSD ×1.24 · US500 ×0.93 · GBPUSD ×0.83 · EURUSD ×0.67` — **المجموع 15.21 مقابل 11.60 = ×1.31**. **وأصحّح رقمًا أعلنتُه قبلها (16.23)**: كان يعدّ اقتباسات مكرّرة، والصادق 15.21؛ **والقياسان بساعتين مختلفتين فلا يُقارَنان تمامًا** — تراجع اليورو والإسترليني غالبًا هدوء سوق لا ضعف تغذية. **(٤) مفاتيح باللوحة بأمره** («بوّابة تغذية من أهمّ الأماكن وأخطرها»): صفحة «السوق» صار فيها بطاقة صحّة ذرّات التغذية **و`SectionConfigTable` لنطاق 600–650** — التسعة عشر إعدادًا كلّها تُضبط من اللوحة بالآليّة القائمة المُختبَرة (`/gov/atoms/{id}/config` + حفظ دفعة + «تحديث» يتحقّق بقراءة مستقلّة)، **بلا بناء مسار جديد**. **(٥) استثناء حجم موثَّق** لـ622 بـ`SIZE_EXEMPT` بسبب مكتوب — الذرّة بلغت الحدّ وستكبر بمصادر سي‑تريدر الباقية (العمق المتقدّم · معنويّات السوق · الشموع التاريخيّة · الهامش والمراكز · قيم المؤشّرات · التقويم). **(٦) والمالك أرشف 617 بيده** (قارئ ملفّ الجسر، لم يبقَ لها عمل بعد 622) — **فطُوردت آثارها كلّها بلا استثناء**: `check_events` و`check_boot` و`check_ctrader` و`proof_bridges` وُجّهت إلى 622 (والبرهان عاد **`PROOF_BRIDGES=17 PASS 0 FAIL`**)، و617 نُزعت من `NETWORK_ALLOWED` (622 لا تحتاجها: تستورد `transport` لا المقبس)، وخمسة اختبارات كانت تحرس قارئ الملفّ **أُزيلت بسبب مكتوب** — حارس بلا محروس ليس حارسًا، ومعانيها محروسة داخل اختبارات 622؛ وبقي اختبار مصدر الـcBot «لا يتداول» لأنّ ملفّه ما زال بالشجرة. وصفحة السوق صارت ثلاث ذرّات تغذية (622 · 618 · 620). **مقيس عند التسليم:** الأسطول **1084/1084** · `tests/` **340 ناجحًا** والثمانية الساقطة **كلّها سابقة لي ومُثبَتة كذلك بالفحص** (نسخة مكرّرة بـ`.claude/worktrees` · ختم يتوقّع 1.13.0 والحيّ 1.17.0 · اختبارات تفحص `Analysis.tsx`/`App.tsx` لم أمسّها · نسخة محلّل 2.0 مقابل 2.1 الحيّة) · المدقّق **3 مخالفات · 45 تحذيرًا** (نزلت من 47) · `tsc` بالأخطاء الخمسة القديمة وحدها · البناء نجح والجسر يقدّمه. **وما يبقى بيدك:** إيقاف الـcBot بسي‑تريدر (لم يعد له مستهلك)، وحذف أرشيف 617 نهائيًّا. **وحقيقة لم تتغيّر:** العمق يصل حقيقيًّا 7×7 متحرّكًا، **ولا محلِّل عمق في المشروع** — 106 يحسب `imbalance` ولا يقرؤه أحد. | ضمن ختم 40 | ٢٠٢٦-٠٨-٢١ |
| 41 | **622 v1.0.1 — أوّل جلسة FIX حيّة على الحساب الحقيقي، وثلاثة أعطال كشفها المصدر لا الاختبار** (ضمن ختم البند 40، ٢٠٢٦-٠٨-٢١): المالك وضع كلمة السرّ بالخزنة بمفتاح `ctrader_fix_password` (الخزنة تُفتح بـDPAPI عبر `runtime/device.key` بلا عبارة — مقيس `key_source: dpapi`). **الدخول نجح من أوّل محاولة**: 347 رمزًا بقائمة المصدر، وBTCUSD رقمه عند المصدر **114** (رقم لا اسم — كما تفرض المواصفة، ولم يُخترع). **وثلاثة أعطال ما كان الاختبار الصناعيّ ليكشف واحدًا منها، لأنّ عيّناتي كتبتُها أنا بالشكل الذي أتوقّعه والمصدر يرسل شكلًا آخر:** **(١) فاصل المجموعة مفترَض لا مقروء** — العيّنة الخام `268=14 | 279=0 | 269=1 | 278=… | 55=114 | 270=… | 271=…` تفتح بـ**279** لا بـ269، وكنت أقسّم عند 269 فيلتقط كل صفّ فعلَ التحديث الخاصّ **بالصفّ التالي** وأوّل صفّ يفقده — أي أنّ حذفًا كان سيقع على السعر الخطأ. الصواب بمواصفة FIX: **الفاصل أوّل حقل بعد عدّاد المجموعة، يُقرأ من البيانات**؛ وصار `split_groups` يكتشفه بنفسه فخدم قائمة الرموز (تفتح بـ55) بلا تخصيص. **(٢) الحذف يأتي بلا جهة فكان يُرمى كاملًا** — المقيس على 25 ثانية: `('X','2','-') ⇒ 1400` حذفًا **بلا 269 إطلاقًا** مقابل 708 شراء و706 بيع جديدة؛ المصدر يستبدل الدفتر كل رسالة (معرّفات جديدة + حذف القديمة)، وكودي `if not entry_type: continue` أسقط **كل** حذف. **الأثر المقيس: الدفتر تضخّم إلى 1595 مستوى وتجمّد أفضل سعر عند قيمة واحدة، فنزل الناتج إلى 0.03 تِكّة/ثانية.** صار الحذف يُطبَّق على المعرّف أينما كان (فريد عبر الدفتر)، ومن لا يجد هدفًا يُعَدّ يتيمًا ولا يُبتلع. **(٣) رسالة بلا حقل رمز تُرمى** — `55` يعيش داخل مداخل الإضافة فقط، فرسالة حذف خالصة تُسقَط كلّها ويظلّ الدفتر منحرفًا بصمت؛ صار الرمز يُحلّ من **معرّف الطلب `262`** (من صنعنا، يركب كل رسالة) و`55` احتياطًا. **البرهان بعد الإصلاح رسالةً برسالة على الحيّ: الدفتر ثابت 7 شراء × 7 بيع · أفضل سعر يتغيّر مع كل رسالة · صفر يتيم.** **والقياس الحيّ على السبعة (60 ثانية، بمنطق الذرّة نفسه):** `BTCUSD 4.80 تِكّة/ث (276 سعرًا مميّزًا · 952.42$ حركة مرصودة) · XAUUSD 5.35 · USTEC 3.24 · GBPUSD 2.41 · EURUSD 1.52 · US30 1.29 · US500 0.72` — **المجموع 19.33 تِكّة/ثانية**. **مقابل خطّ الأساس المقيس للطريق القديم: BTCUSD 2.50 ⇒ صار 4.80، أي الضِعف تقريبًا**، والعمق حقيقيّ متحرّك بدل الدفتر الثابت 6×6 من سطح المكتب. **وعطل بنيوي ثالث اكتُشف بالطريق وسُجّل:** `startup_mode: manual` بهذا النظام **لا تعني «لا تقلع تلقائيًّا» بل «غير موجودة»** — المُقلِع يحذف الذرّة قبل إنشائها (`core/bootloader.py:113`) فلا تُسجَّل، و`POST /api/atoms/{id}/start` يردّ 404 (`core/api/app.py:221`)؛ **مقيس: اليدويّات الأربع 107·256·257·258 غائبة تمامًا عن قائمة النواة**، وتعليق المُقلِع نفسه يقول «يبقى تشغيلها اليدوي متاحًا عبر API» وهو **مستحيل بكوده** — تعليق يخالف كوده، سُجّل ولم يُلمَس. فصارت 622 `auto`، والأمان محفوظ بما هو أقوى: جلسة `QUOTE` لا تملك التنفيذ، وحارس `auth_failed` يوقف تكرار محاولة دخول مرفوضة (رشقها على حساب حقيقيّ يقفله). **درس مسجَّل:** الاختبارات الأربعة عشر كانت خضراء طوال الوقت والعيوب الثلاثة قائمة — **الشكل الحقيقي لا يأتي إلّا من المصدر**؛ أُضيفت ثلاثة اختبارات دائمة مبنيّة على العيّنة الخام حرفيًّا. **مقيس عند التسليم:** الأسطول **1092/1092 · صفر فشل** · المدقّق **3 مخالفات · 47 تحذيرًا = خطّ الأساس** · `effective_lines` **327 من 350** · النسخة مرفوعة بالكود والمانيفست معًا. **ويحتاج كبسة إعادة تشغيل**: `shared/fix_session.py` لا يُحمَّل حيًّا. | ضمن ختم 40 | ٢٠٢٦-٠٨-٢١ |
| 40 | **الذرّة 622 — تغذية سي‑تريدر مباشرة عبر FIX، بلا منصّة ولا روبوت ولا ملفّ جسر** (**ختم مالك `NQ`** على التصميم رباعيّ البنود، ٢٠٢٦-٠٨-٢١): بأمره «جبلي أسرع تغذية». **الجذر المقيس أوّلًا — وثلاث فرضيّات لي سقطت بالقياس قبل أن أصل إليه:** فُحصت خمسة مواضع محتملة للخلل في كودنا وخرجت كلّها نظيفة (خيط الـcBot: نبضة وسيطها **1002 ملي‑ثانية** وصفر تأخّر · **صفر ثقب بالتسلسل** من 10,020 سجلًّا · القرص 8.4 ك.ب/ث و58٪ تُكتب بنفس الملي‑ثانية · طريقة الاشتراك: قناتا التِكّة والعمق أعطتا **1410 اقتباسًا لكلٍّ وفرق صفر** · وقارئنا بايثون عُدّ ملفّ الجسر بالبايت مباشرة). **والعطل الحقيقي بسطرين بالـcBot**: `QuantNQ_Feed.cs:86` يرمي حمولة التِكّة (`args`) ويقرأ `symbol.Bid` لحظة تشغيل المعالج — أي «سعر الرمز الآن» لا سعر التِكّة، فما سبقها يضيع بلا عدّاد؛ و`:153` يختم بـ`NowUtc()` وقت الكتابة لا وقت السوق، خرقًا لقاعدة «وقت السوق من التِكّة». **والأثر مقيس على BTCUSD في ستّين ثانية: 164 تِكّة رصدت 1036 دولارًا من حركة سعر حقيقيّة قدرها 2137 — أقلّ من نصف السوق.** **المنفَّذ:** (١) `transport/client.py` كسب بدائيّة ثالثة `StreamSession` — تيّار بايتات دائم فوق TLS، والمقبس يبقى في «المخرج الوحيد» بينما التأطير على المستدعي فلا يتسرّب بروتوكول لطبقة النقل؛ **و`transport/` خارج ختم النواة (تحقّقت: 23 ملفًّا مختومًا ولا واحد منها فيه) ⇒ لا فتح نواة ولا كسر ختم.** (٢) `shared/fix_session.py` جديد — FIX 4.4 خالصًا بلا مقبس (ترميز · تأطير بطول الجسم لا ببحث عن فاصل · تحليل · جلسة · دفتر مفهرس بمعرّف المصدر)، **مُثبَت 23/23 بلا شبكة**. (٣) **الذرّة 622** تنشر **نفس أحداث 617 حرفًا بحرف** (`feed.ctrader.tick` · `market.depth` · `market.ctrader.symbol_specs` · heartbeat · state) ⇒ **صفر تعديل على 613 و521 و106 و103 و150 وكل ما بعدها**. اشتراك واحد بعمق كامل يخرج منه الاثنان، فلا يخالف السعر دفتره. **ولا شيء مخترَع:** رقم الرمز من `SecurityListRequest` من المصدر نفسه (والغائب يُعلَن بقائمة `missing`)، والوقت من `SendingTime` الخاصّة بالرسالة، والدفتر أحاديّ الجهة يُنشَر بصدق **بلا تِكّة**، والتحديث اليتيم يُعَدّ ولا يُخترع له سعر. **وكلمة السرّ لا تمرّ بالذرّة**: تحمل اسم مفتاح (`ctrader_fix_password`) وتطلب القيمة من الخزنة لحظة الاتصال — لا تُخزَّن ولا تدخل لقطة، ومُثبَت باختبار أنّها لا تظهر بأي حمولة منشورة. (٤) 622 أُدرجت في `BACKGROUND_ALLOWED` بالمدقّق **بسبب مكتوب** (جلسة FIX تحتاج قارئًا دائمًا، وتصريفها على نبضة الثانية يعيد التكثيف الذي وُجدت الذرّة لإزالته) — نفس آليّة استثناء الحجم المختومة بالبند 27، استثناء معلَن لا تحذير صامت. **وخطأ اختبارٍ لي أمسكه الأسطول:** الأربعة عشر اختبارًا نجحت منفردة وسقطت **14/14** مع الأسطول لأنّي استعملت `asyncio.get_event_loop()` المشترك وقد أغلقه اختبار سابق — صُحّح بحلقة خاصّة لكل نداء. **مقيس عند التسليم:** الأسطول **1087/1087 خضراء · صفر فشل** · المدقّق **3 مخالفات · 47 تحذيرًا = خطّ الأساس بلا زيادة حرف** · `atom.py` **330 سطرًا فعليًّا** تحت حدّ 350 · ونقطتا FIX الحيّتان مفحوصتان (`live-uk-eqx-01` → 76.223.4.250، منفذ 5211 مفتوح بـ23 ملي‑ثانية). **الحساب الحيّ `6175701` وحده، وجلسة `QUOTE` لا تملك فتح صفقة أصلًا.** **و`startup_mode: manual` عمدًا — لا تقلع وحدها ولا تتصل بحساب حقيقيّ إلّا بيده.** **وما لم يُقَس ويُعلَن:** لم تُسجَّل جلسة دخول حيّة بعد — أرقام الرموز وحدود الجلسة وسرعة التغذية الحقيقيّة كلّها بانتظار كلمة السرّ بيده. **وحقيقة لا يغيّرها هذا العمل:** العمق سيصل حقيقيًّا **ولا محلِّل عمق في المشروع** — `imbalance` يحسبه 106 ولا يقرؤه أحد؛ تصحيح التغذية لا يخلق المستهلك. | **NQ** | ٢٠٢٦-٠٨-٢١ |
| 39 | **سلسلة التنفيذ — ثلاثة حواجز مقتولة بالتصميم، انفكّت** (**ختم مالك: «اول NQ»** على الخيار أ، ٢٠٢٦-٠٨-٢١): بأمر المالك «افتح كل البوّابات لنشوف السلسلة من سي تريدر لآخرها على BTCUSD»، فُحصت السلسلة كاملة فظهرت ثلاثة أعطال **بنيوية** لا وقتية — كلٌّ منها كان يكفي وحده لمنع أيّ أمر من الخروج إلى الأبد. **(١) مواصفات الرمز متقادمة للأبد**: 513 كان `STALE_ACCOUNT_SYMBOL_SPECS`. الجذر المقيس: cTrader يكتب سجلّ `spec` **مرّة واحدة عند بدء الجلسة** — **صفر سجلّ بآخر ٢٠٬٠٠٠ سطر بملفّ الجسر** — و617 كان يعيد بثّها **عند الإقلاع فقط**، بينما 513 يطلبها أحدث من نافذته. فتغذية حيّة بـ**٤٤١ ألف تِكّة** تقول «المواصفات متقادمة»، وبلا مواصفات لا تحجيم، وبلا تحجيم لا أمر. 617 v3.3.0 يجدّدها كل ٢٤٠ ثانية **بشرط أن تكون التِكّات واصلة** — فإن صمت المصدر تتقادم بصدق ولا تُنعَش بيانات ميّتة. النتيجة: 513 و583 صارا `healthy` و583 يقول `SNAPSHOT_READY`. **(٢) محرّك الدخول يقرأ السعر من الشمعة وحدها**: النظام كلّه تِكّيّ (٧٧ تِكّة مصدَّقة للرمز المفعَّل في ٢٨ ثانية) و576 كان يأخذ سعره من **الشمعة المغلقة فقط** — فينتظر دقيقة كاملة رقمًا يصل كل أجزاء من الثانية. صار يقرأ التِكّة المصدَّقة أيضًا. **(٣) والجذر الأعمق — حسابان لا يلتقيان**: التِكّات المصدَّقة تخصّ **حساب البيانات** (10096831 · cTrader) وحده، لأنّ موجّه التغذية 613 **يكبت المزوّد الثانوي** ما دام المفضّل حيًّا (قاعدة سليمة تمنع ازدواج السعر — مقيس: ٩٥٥ تِكّة MT5 مكبوتة مقابل ٣٢٢ مصدَّقة من سي تريدر). و**حساب التنفيذ** (52992818 · MT5) هو الذي يُفتح عليه المركز — فكان بلا سعر مصدَّق **بحكم التصميم لا بحكم التأخير**، و`MISSING_INPUTS · price=null` أبدية. بختم المالك على الخيار (أ): **السعر يخصّ الرمز لا الحساب الذي يحمل التغذية** — فيُسجَّل لكل حساب يعرفه المحرّك على الوسيط والرمز نفسيهما، بلا اختراع: هي الأداة نفسها في السوق نفسه، مسعَّرة مرّة. **برهان معزول على الكود الحيّ**: تِكّة حساب البيانات بسعر 77,324.51 ⇒ `price map` يحمل مفتاح **حساب التنفيذ** بالسعر نفسه ⇒ `status: OPENED · lot 0.26` ⇒ **حدثا `execution.order.requested` خرجا فعلًا**. **وعطلان أحدثتهما وأصلحتهما**: 576 تجاوز حدّ 350 سطرًا (352) فاختُصر تعليقه · واختبارات 453 و454 و463 كانت تقرأ **قاعدة المعايرة الحيّة** فسقطت حين نزلت أوزان التوليفة بأصفارها المقصودة — عُزلت قواعدها بمتغيّر البيئة `QUANT_ANALYSIS_SETTINGS_DB` فصار الاختبار يقيس الكود لا حالة الجهاز (عطل اختبارٍ سابق كشفته معايرة مشروعة). **مقيس:** الأسطول **217/217 · 1119 اختبارًا** · المدقّق **3 مخالفات · 47 تحذيرًا** (خط الأساس) · فاحص العيارات **exit 0**. **وما بقي بيد المالك حصرًا ولم يُلمَس**: مفتاحا الأمان `552.enabled` و`575.enabled` — مطفآن = معاينة بلا صفقة، وتشغيلهما يعني أوامر حقيقية على حساب MT5. | **NQ** | ٢٠٢٦-٠٨-٢١ |
| 38 | **قسم التحليل — المساران توأمان حقيقيّان، والتوليفة المعتمدة نازلة** (**ختم مالك مباشر** + وثيقة التوليفة النهائية، ٢٠٢٦-٠٨-٢١): **(١) التوأم الشمعيّ**: كان **١٣ من ١٥** محلّلًا ينطق على الشمعة **مفردات لا أرقامًا** (`engulfing` · `london` · `week_close` · `weak` · `normal`) — مقيس بالبثّ الحيّ — فلا يجد المجمّع صوتًا اتجاهيًّا واحدًا، ويُخرج صفرًا، **ويسمّيه «جاهزًا»**. الحلّ بلا إعادة كتابة ذرّة واحدة: **النواة صارت مسارية** (`LiveAnalyzerKernel(path=...)`) ونسخة ثانية منها تأكل `market_data.candle_closed` بنفس `_profile_movement` و`_analyze` — فالمحلّل نسختان من كائن واحد لا منطقان. ومفردة الذرّة **تُحفَظ** في `metadata.pattern` ولا تُمحى. **مقيس بعد الإصلاح على 151**: `score 100.0 · strength 41.954 · confidence 70.0 · depth 97.0` بدل `signal "up" · strength "moderate" · confidence 1.0 · depth None`. **(٢) ثلاثة أعطال ميكانيكيّة كشفها القياس بالطريق**: الحمولة تخرج من منطق الذرّة **بلا حساب ولا وسيط** (الهوية يضيفها غلاف القسم بعدها) فمطابقة التوأم بالمفتاح الثلاثيّ كانت تفشل **دائمًا** والدمج لا يقع أبدًا (التوأم ينشر 30 مرّة والذرّة تخرج بمفردتها) — صارت المطابقة بالرمز وحده عند غياب الهوية وبشرط ألّا يكون ملتبسًا · وذاكرة المعايرة كانت تتحدّث **للمسار السريع فقط** (`if path == PATH_FAST`) فمعايرة البطيء تُحفَظ ولا تصل الذرّة أبدًا · و`_fuse` كانت **تمحو دليلها**: عند حكم «عرضي» تختار المحلّلات العرضية وحدها ودرجتها صفر بالعقد، فتُخرج اتجاهًا 0 وثقةً 0 ولو مال خمسة محلّلين صعودًا بثقة 50–64 — صار الاتجاه = صافي التصويت الموزون والثقة تُقرأ على اللوحة كلّها. **(٣) عطل الدفعة — وهو سؤال المالك نفسه: «كيف بدّي أحطّهم دفعة وحدة؟»**: «حفظ إعدادات الكل» كان يرسل الأوزان **صفًّا صفًّا**، وكل أمر يشحن فرقه على النظائر الأربعة عشر (قاعدة Q2 — صحيحة لعيار مفرد)، فالأمر الثاني يزيح ما ثبّته الأوّل. **قياس**: طُلب `velocity=20` فاستقرّ **17.34**، وسبعة أصفار مقصودة صارت **صفرًا واحدًا**. فأُضيف **أمر جدول كامل** (`weights`) يكتب المعسكر دفعةً واحدة بلا إعادة توزيع، ويرفض ما لم يكن الخمسة عشر مكتملين ومجموعهم 100 — بأربع طبقات حماية: اللوحة ثمّ حارس الخادم ثمّ البوّابة ٩٠١ (v2.5.0) ثمّ المخزن (`INCOMPLETE_WEIGHT_TABLE` · `WEIGHT_TABLE_NOT_100`). وقاعدة المالك للعيار المفرد **لم تُمَسّ**. **(٤) التوليفة المعتمدة نزلت حرفيًّا** (210 أمرًا عبر البوّابة): مساران **35/65** و`agree_threshold 0.5` بمانيفست 166 · سريع `velocity 20 · acceleration 15 · volume 15 · momentum 15 · volume_quality 10 · spread 10 · volatility 10 · noise 5` وسبعة أصفار · بطيء `trend 20 · candle 15 · gap 15 · volume_quality 15 · session 10 · relative_strength 10 · momentum 5 · correlation 5 · time 5` وستّة أصفار · المجموع **100.0000 بالضبط** بالمسارين، وكل صفّ مسجَّل بجدول التدقيق. **(٥) درسٌ مقيس سُجّل**: الكتابة المباشرة بالمخزن **لا تصل الذرّات الحيّة** — لكلٍّ نسخة مخبّأة لا تتحدّث إلا بأمر البوّابة؛ فكل قياس بعد كتابة مباشرة يقيس القديم ويظنّه الجديد. **مقيس:** الأسطول **217/217 · 1119 اختبارًا** · المدقّق **3 مخالفات · 47 تحذيرًا** (خط الأساس؛ زدتُ مخالفتين أثناء العمل — عربيًّا داخل 166 ورقمًا سحريًّا بـ901 — وأُزيلتا) · فاحص العيارات **exit 0** · البناء نجح · `tsc` صفر خطأ جديد. **وتصحيحان معلَنان لنفسي**: ادّعيتُ «نسختان من الذرّة تعملان معًا» وكان الصواب أنّ الإعداد **لكل رمز على حدة** وأنا عايرتُ رمزًا وقستُ سبعة مخلوطين؛ وحوّلتُ `REQUIRED_UNITS` إلى أرقام ذرّات فكسرتُ ذرّة 210 — أمسكتها الاختبارات فأُعيدت الأسماء بعد قياس مفاتيح دورة القسم على السلك. **يحتاج كبسة إعادة تشغيل**: `shared/*` و`server.py` لا يُحمَّلان حيًّا. | **NQ** | ٢٠٢٦-٠٨-٢١ |
| 37 | **قسم التحليل مُقفَل — المساران يعملان ويُضبطان من اللوحة** (**ختم مالك مباشر `NQ`** ٢٠٢٦-٠٨-٢١، وأمره: «حطّ أوزانًا وعمقًا وعتبات بطريقة نظيفة واقفل القسم»): **(١) صفٌّ لكل مسار**: جدول المحلّلات صار صفّين لكل محلّل — «سريع · تِكّات» و«بطيء · شموع» — وكل صفّ يرسل أمره **بمساره**؛ قبلها كان كل تعديل يهبط على السريع وحده والبطيء **لا طريق له من اللوحة أصلًا**، والخادم يقرأ الصفّين مدموجين فيغلب آخرهما (`settings_by_path` الجديد يفصلهما). **(٢) العيارات ٣ ← ٦**: نزلت ثلاث عتبات كانت **محفورة بالكود لا يصلها المالك** — **عتبة القوّة** (لم يكن لها وجود: القوّة تُنشر ولا تحجب؛ صارت حاجزًا ثالثًا بسبب `STRENGTH_BELOW_THRESHOLD`، وافتراضها **صفر = لا حجب** فلا يتغيّر سلوك قائم بحرف) · **مهلة الطزاجة** (كانت 5.0 بالصنف) · **المنطقة الحيادية** (كانت ±5.0 بـ`_analyze`). **(٣) أربعة حرّاس كانوا يرفضون بصمت**: أمر العيار يمرّ بأربع بوّابات (اللوحة ← حارس الخادم ← بوّابة ٩٠١ ← المخزن)، وكلٌّ يحمل **نسخة محفورة** من قائمة الثلاثة — فقبِل المخزن الجديد ورفضه الخادم برسالة عامّة ولا يُعرف أيّ حارسٍ رفض. صاروا يقرأون **قائمة واحدة** من المحرّك (901 v2.5.0). **(٤) فاحص دائم جديد** `check_analysis_dials_contract.py` — **16 فحصًا** يثبت وحدة القائمة عبر الحرّاس الأربعة، واستقلال المسارين (§٢٦ عمليًّا: حفظٌ على البطيء لم يمسّ السريع)، وأن **العيار يحجب فعلًا** لا زينةً. **(٥) ذاكرة الشموع تنجو الإقلاع**: الغلاف الحيّ كان **يستبدل** `snapshot/restore` فتضيع حالة المسار البطيء كاملة كل إقلاع — و151 تطلب 55 شمعة والشمعة دقيقة ⇒ **55 دقيقة صمت بعد كل إعادة تشغيل**. صار الغلاف **يسلسل** (تِكّات تحت `live_analysis` وشموع تحت `atom`)، و**الخمسة عشر محلّلًا كلّهم v2.1.0** بحفظٍ خاصّ لكل واحد و**حارس نجاة** باختباره؛ عدا **158 و159** أُعلنتا «بلا حالة تستحقّ الحفظ» بعد فحص كودهما (نتيجتهما من طابع الشمعة الحالية وحده) — إعلانٌ لا اختراع. **(٦) البطيء صار يبثّ تفصيله**: 166 v2.4.0 يحمل `contributors` ببطاقة المسار البطيء — كانت **محسوبة أصلًا** ولا تصل البطاقة، فصفّ البطيء باللوحة لا يمتلئ أبدًا مهما فُعل (قياس حيّ بعد الوصل: **6 محلّلات** تبثّ، والباقي بالإحماء، و`strength` معلَنة `unknown` صراحةً لا صفرًا). **(٧) خطّ الأساس بيدي بأمر المالك**: 30 صفًّا (15 محلّلًا × مسارين) — عمق 60 · ثقة 60 · قوّة 0 · حيادية 5 · طزاجة **5ث للسريع و90ث للبطيء** (الشمعة دقيقة، فمهلة 5ث تجعل كل شمعة متقادمة سلفًا) · والأوزان سُوّيت على الحصّة المتساوية. **وعطل بيانات قديم انكشف بالقياس: أوزان المسار السريع كانت تجمع 108.3333 لا 100** (إرث قاعدة الظلّ، بند ٥ بالفهرس) — ومحرّك إعادة التوزيع لا يشفيه لأنه ينقل الفارق ولا يسوّي المجموع؛ فكُتبت الأوزان دفعةً واحدة والمجموع الآن **100.0000 بالضبط بالمسارين**، وكل صفّ مسجَّل بجدول التدقيق. **مقيس:** الأسطول **217/217 · 1119 اختبارًا** · المدقّق **3 مخالفات · 47 تحذيرًا** بلا زيادة · فاحص العيارات **exit 0** · `tsc` صفر خطأ جديد · البناء نجح. **يحتاج كبسة إعادة تشغيل**: `server.py` و`shared/live_analysis.py` لا يُحمَّلان حيًّا. | **NQ** | ٢٠٢٦-٠٨-٢١ |
| 36 | **ساعة النظام + 617 v3.2.0 — التِكّة الحيّة ما عادت تُحسب ميّتة** (**ختم مالك مباشر: «1NQ»**): بعد إعادة التشغيل ظهر **617 `CTRADER_TICK_DEAD`** بينما 618 ينشر بلا توقّف. التشخيص كشف مرضين متتاليين. **الأوّل — ساعة الجهاز:** خدمة `w32time` **متوقّفة** (`Manual`) ومصوَّبة على `time.windows.com` غير الرادّ، فساعة الجهاز متأخرة **٦٫٧٥ ثانية** عن الحقيقة (مقيسة من خادمَي NTP مستقلَّين). **المالك بيده** شغّل الخدمة وثبّتها `Automatic` وحوّلها إلى `pool.ntp.org`/`time.google.com` ⇒ الانحراف صار **٠٫٠٠٧ ثانية**، و**608 و3 و806 و111 رجعوا خضراء** (608: `median_offset=0.006s`). **الثاني — منطق 617 نفسه:** بقي أحمر وحده، والسبب أنه يقيس عمر التِكّة بـ`_official_time` — **ساعة مخزَّنة تتحدّث مع النبضة** — بينما الطابع يكتبه المصدر بساعته هو؛ فالرموز **الأسرع** (US30/USTEC كل ~٢٠ ملي‑ثانية) يطلع عمرها **سالبًا** وقاعدة `age<0 ⇒ DEAD` تقتلها أبدًا. **برهان مقيس على براءة التغذية:** قرأت ملف الجسر مباشرة فإذا ستّة رموز حيّة بأعمار دون الثانية (US30 ٠٫٠٢ · USTEC ٠٫٠٢ · EURUSD ٠٫٢٣ · XAUUSD ٠٫٢٤ · GBPUSD ٠٫٣٢ · BTCUSD ٠٫٤٤). **الإصلاح بختم المالك (خيار ١):** مرجع الطزاجة صار **ساعة المصدر** — أحدث طابع وصل (`StreamTracker.newest_stamp()`) — **والساعة الرسمية أرضية** حتى لا يتجمّد عمر مصدر ميّت عند الصفر فيبدو حيًّا؛ وطابع الحدث المنشور يبقى بالوقت المحكوم. **محاولة أولى رُفضت بالقياس:** تثبيت `time.time()` مباشرة أسقط اختبارين قائمين يعملان على ساعة محاكاة — فصُحّح المسار بدل تمريره. **مقيس:** 617 **10/10** بحارس جديد يعيد بناء الحالة حرفيًّا · الأسطول **217/217 · 1104 اختبارًا** · المدقّق بلا زيادة (3 · 47) · و**617 حيّة v3.2.0 سليمة `ticks=439635 depth=504137`**. **نقطة مرفوعة للمالك:** الجسر يحمل **ستّة رموز** حيّة بينما **618 ينشر `symbols=1`** — خمسة رموز تصل ولا تُنشَر. لم تُفحَص بعد. | **NQ** (1NQ) | ٢٠٢٦-٠٨-٢١ |
| 35 | **حزمة ح منفَّذة — وموجة التفعيل تمّت بيد المالك** (ضمن ختم 22، بوكيل تحت إشرافي): **ح٣** تسع أوراق (`Desktop\1` الثماني + `اجوبة.md`) **نُسخت** إلى `سياق\` وحُدّث `٠٠٠ الفهرس` — **نسخ لا نقل، والأصل باقٍ بمكانه** (تحقّقت بنفسي: 8/8 + اجوبة، والأصلان موجودان). **ح٢** فحص جديد `governance\checks\check_no_side_path_contract.py` يحمّل ذرّات **467/576/581 الفعلية** على ناقل داخل العملية (لا محاكاة) ويثبت: 467 لا يمرّر قرارًا غير معتمد ولا مجهول الجانب ولا يعيد إرسال بلا سبب · 576 يرفض بـ`NO_PARENT_AUTHORITY` بلا قرار أب أو أمر مالك · 581 يبقى `wait` حتى يصله عبور بوّابة حقيقيّ. **شغّلته بنفسي: 10/10 · exit 0**، والوكيل أثبت أنه ليس فحصًا صوريًّا بقيمة خاطئة متعمَّدة أعطت `FAIL`. **ح١ جزئيّ بالضرورة وموثَّق بصدق** (`سياق\ح١ عينات القبول — ٢٠٢٦-٠٨-٢١.md`، 25 ك.ب): الجذر المقيس **اعتماد 466 = صفر من 317 دورة** — القيمة الاتجاهية المدموجة قرب الصفر بعيدًا عن عتبة ±50، فيسقط تلقائيًّا كل ما بعدها (467 لا يمرّر · 580 لم يبدأ · 576 لم يفتح · طبقة التنفيذ بالانتظار الأول). من ثماني عينات العقد: **واحدة كاملة · اثنتان جزئيتان · خمس غير متاحة بسبب مقيس** — ولا عيّنة ملفَّقة. **تصحيح قياس مسجَّل:** وصف «451 متعثّرة ولا دورات» **غير دقيق** — 451 تتقدّم فعلًا (268→317 دورة تحت المراقبة)، الثابت عند صفر هو **الاعتماد** لا الدورات. **وانحراف معلَن:** الوكيل استعمل أوامر قراءة بـBash بأول بحثه خلافًا لقانون PowerShell، وأفصح عنه. **وموجة التفعيل: المالك أعاد التشغيل** — والمنافذ الثلاثة صارت حيّة بقياسي (`/gov/tilt/rules` → `{available:true, rules:[]}` · `/gov/decisions` → صفوف حقيقية · `/gov/parameters/audit` → صفوف حقيقية)، و**213 ذرّة تعمل**. **عطلان أحمران بعد الإقلاع مرفوعان للمالك: 617 `CTRADER_TICK_DEAD`** (عمر التِكّة يكبر 5.2s→13.1s — تغذية السعر الحيّة ميّتة) **و7 `UNTRUSTED`**. | ضمن ختم 22 | ٢٠٢٦-٠٨-٢١ |
| 34 | **حزمة ج منفَّذة — اللوحات بلا كذب** (ضمن ختم 22، بوكيل تحت إشرافي): **ج١** بطاقة الرمز بالثمانية تدمج 451 مع 453، و**`ratio` لا ينشره 451 أصلًا** فكُتب «مجهول — لا يُنشر» بدل اختراع حساب بالواجهة (اختلاف ورقة↔كود مسجَّل). **ج٢** صفحة القرار أُعيد بناؤها: هوية ستّية + الثماني · جدول الحواجز الرباعي من 454 · بطاقات الأهلية من 455/456/457 بفحوصها · رحلة القرار الثماني المراحل من سجل 450 · سجل 707 عبر منفذ `/gov/decisions`؛ و**نص «التنفيذ (550) غير موصول» أُزيل نهائيًّا — تحقّقت بنفسي: صفر بقايا**. **ج٣** بطاقات الأهداف لكل رمز من 581 فوق لوحة البوابات القائمة (بلا تكرار). **ج٤** نمط «الفعّالة/المطلوبة/الحالة» بالعيارات والمُعامِلات + سجل تدقيق المُعامِلات من جدول موجود فعلًا (3 صفوف حيّة)؛ **وزرّ الرجوع لم يُضَف عمدًا** لأن لا آلية استرجاع بالكود — «لا زرّ يكذب». **ج٥** لوحة تفعيل الأصل من 576، و**غياب `owner_command_id` من حدث الحالة أُعلن غيابًا** لا اختراعًا. **مقيس وتحقّقت منه بنفسي:** البناء نجح والحزمة الحيّة `index-DWJDuGxR.js` **1212 ك.ب** مقدَّمة من الجسر الآن، و`tsc` بلا أي خطأ جديد (الخمسة القائمة سابقة). **ويحتاج كبستك ليصير حيًّا:** منفذا `/gov/decisions` و`/gov/parameters/audit` — موجودان بالمصدر، والخادم أقدم منهما. **انحراف معلَن:** الوكيل استعمل أوامر قراءة بـBash بأول بحثه قبل أن ينتبه لقانون PowerShell — أفصح عنها، وكل كتابة وبناء وقياس نُفّذ بـPowerShell. | ضمن ختم 22 | ٢٠٢٦-٠٨-٢١ |
| 33 | **بقية حزمة ت (ت٢ · ت٤ · ت٥) منفَّذة ومحمَّلة حيًّا** (ضمن ختم 22، بوكيل تحت إشرافي): **ت٢** — 551 v4.2.0 يشترك بحدث 513 `risk.position_size.rejected` ويتذكّر السبب بنطاق (حساب/وسيط/رمز) يُمحى عند أوّل تحجيم صالح، فصار حدث التخطّي يحمل **سبب 513 الحرفيّ** (مقروءًا من كوده لا مخترَعًا) مع زوج الهوية والغائب `None` صريحة — الرفض ما عاد يضيع صامتًا. **ت٤** — فُحصت السلسلة كلّها (450–458 · 466 · 513 · 516 · 551–552 · 563 · 576–578 · 581 · 583 · 585 · 601 · 707)، والفجوة **الوحيدة المثبتة**: كاشا استرجاع الهوية اللذان بناهما ت١ (لقطة 583 وحكم هامش 585 داخل 552) كانا يُصفَّران عند كلّ إقلاع — فصارا يعبران `snapshot()`/`restore()` بنفس حدّ الذاكرة القائم 4096 (552 v5.2.0). وما عداهما **تصميم متعمَّد لا فجوة** ولم يُعَد بناؤه. **ت٥** — 707 v4.1.0 كسب عمودَي `decision_id`/`gate_request_id` بهجرة آمنة (`ALTER TABLE` عند غياب العمود فقط، تتحمّل الإقلاع المتكرّر)؛ **قياس على القاعدة الحيّة: 15658 صفًّا نجت، والعمودان موجودان، و10 صفوف تحمل معرّف قرار فعلًا**. **تحقّقت من الأرقام بنفسي بعد الوكيل**: الأسطول **217/217 ذرّة · 1103 اختبارًا · صفر فشل** (كان 1095)، والمدقّق **3 مخالفات · 47 تحذيرًا** — مطابق لخط الأساس بلا زيادة، والثلاثة حيّة سليمة (551 v4.2.0 · 552 v5.2.0 · 707 v4.1.0). **نقطة قرار مرفوعة:** 551 لا يحمل زوج الهوية على **الأمر المبنيّ الناجح** نفسه (خارج نصّ ت٢، ويُعوَّض اليوم باسترجاع 552 من اللقطة) — سدّها قرارك. **ومخالفة الحجم تكبر:** 552 صارت **385 سطرًا** (من 361) — الاستثناء الموثَّق أو التقسيم بانتظار حكمك. | ضمن ختم 22 | ٢٠٢٦-٠٨-٢١ |
| 32 | **محرك الترجيح 580 v1.1.0 منفَّذ — والمحرك بلا أثر حتى يحكم المالك** (ضمن ختم 22، ورقة ق١٠ + تعديل الاستمراريّة §٥٢): الذرّة كانت هيكلًا فصارت المحرك الفعليّ — **مدخلها الوحيد `decision.gate.passed` من البوّابة 467** (القرار المعتمد وثمانيّته؛ لا درجات ما قبل الفلترة أبدًا)، و**منحنيات المالك متّصلة**: نقاط (عتبة←مقدار) باستيفاء خطّيّ، تحت أوّل نقطة 0.0 وفوق آخر نقطة آخر مقدار — لا شرائح ولا قفزات 0/1. **يبدأ بلا أيّ منحنى**: كلّ منحنى يخلقه المالك من اللوحة (901 → `tilt.rule.command`) ويُخزَّن معامَلاتيًّا بمخزن الذرّة `var/store/tilt_rules.db` بمعرّف أمر فريد ويُعاد نشره على `tilt.rules.state`. **`state` و`weight` مرفوضان سلَّمًا عمدًا** (§١٣/§١٥): الحالة حاجز لا رقم — NOT_READY يمنع الزيادة وSTALE/INVALID/ERROR تمنع كلّ شيء، ولا تخفيف عدديّ يُخترع. **المجهول صفر معلَن لا خصم** (§٤٢). **السقف يبدأ 0.0** (§٢٣/§٤٣ — الورقة لا تعطي رقمًا) فلا ترجيح يخرج من المحرك اليوم أصلًا. **لا يملك قرارًا ولا يقلب جانبًا ولا يلمس هدفًا**؛ ربط الترجيح بمعادلة الهدف **نقطة قرار مالك مفتوحة (§٣٣/§٣٤)** و581 ممنوعة من تطبيقه حتى تُحسم. **مقيس: 17/17 اختبارًا خضراء** تحت المشغّل الرسميّ، والذرّة **حيّة 1.1.0** بحالة صادقة `NO_GATE_DECISION_YET`. **إصلاحان بملفّ الاختبار لا بالمنطق** (الوكيل انقطع قبل تشغيله ولا مرّة): لاقطة `tmp` غير موجودة كانت تُسقط الملفّ بـ**17 خطأ جمع** تحت المشغّل الرسميّ (صارت `tmp_path`)، وتوكيد كان ينسى نشرة الإقلاع الصادقة «بلا منحنيات» فيطلب صفر نشرات بعد `start()`. **فجوتان مرفوعتان للمالك**: (١) الذرّة **546 سطرًا فعليًّا > 350** — مخالفة مدقّق م9/ملف4، والاستثناء الموثَّق أو التقسيم **حكمك**، لم أستثنِ من عندي؛ (٢) **منفذ `/gov/tilt/rules` غير حيّ**: `governance/server.py` كُتب 12:10 والخوادم قائمة منذ 9:53 مساءً — الذرّات تُحمَّل حيًّا والخادم لا، فقسم «محرك الترجيح» باللوحة معروض بلا مصدر بيانات **حتى كبسة إعادة التشغيل بيدك**. | ضمن ختم 22 | ٢٠٢٦-٠٨-٢١ |
| 31 | **حزمة ت (ت١ + ت٣) منفَّذة ومحمَّلة حيًّا — خيط هوية القرار يصل آخر السلسلة** (ضمن ختم 22): خمس ذرّات — **552** v5.1.0 · **563** v3.1.0 · **517** v4.1.0 · **601** v4.1.0 · **578** v5.1.0، كلّها **حيّة بنسخها الجديدة وسليمة** (مقروءة من النواة لا موصوفة). **ت١**: `decision_id` و`gate_request_id` يعبران سلسلة التنفيذ كاملة — 578 يحملهما بطلبات الفتح والتخفيض كما وصلاه، و552 يجعلهما حقلين رسميّين بالأمر المقفول **ويسترجعهما من سجلّ اللقطة الموثوق (583) بمفتاح `snapshot_id` حين يسقطهما 551** (الخيط لا ينقطع)، و601 يعبر بهما الجسر **داخل `params_json`** — عمود وصفيّ داخليّ — **بلا مسّ شكل الجدول الخارجيّ ولا أيّ مفتاح يقرأه الإكسبرت**، ويقرؤهما من صفوفنا عند تحصيل النتيجة فينجوان من أيّ إقلاع؛ و563 **يلحمهما من سجلّ الطلبات الدائم** لأنّ أحداث الوسيط لا تحملهما أصلًا؛ و517 يوصلهما لتقرير الخسارة مدموجين مع حالة الصفقة. **القانون الواحد بالخمسة: الغائب يمرّ `None` معلَنًا** (`identity_missing` + `identity_warnings=['identity_incomplete']`) **ولا يُخترع أبدًا**، والغياب **لا يحجب** إلّا عند 552 (البوّابة الوحيدة صاحبة الحجب). **ت٣**: رفض 552 صار يسمّي مرحلته (`FINAL_VALIDATION`/`ASSET_ACTIVATION`/`PARENT_DECISION`/`MARGIN_VERDICT`/`SNAPSHOT_VALIDITY`) ويحمل رباعية الحاجز `{value, threshold, reason, measured_at}` — والقيمة والعتبة `None` حيث لا قياس حقيقيّ؛ ومدخلان جديدان: حكم الهامش من 585 ولقطة 583 بذاكرة محدودة 4096 (حدّ ذاكرة لا قيمة تداول). **قياس الأسطول كلّه بعد الشغل: 217/217 ذرّة و1095 اختبارًا خضراء · صفر فشل.** **ويُعلَن صراحةً ما لم يُنفَّذ — متحقَّقًا من الكود لا من الورق**: **ت٢** (رفض التحجيم لا يضيع — 551 بلا أثر لسبب رفض 513) و**ت٤** (فجوات التخزين الدائم) و**أعمدة الربط بمخزن 707** (صفر ذكر لـ`decision_id`/`gate_request_id` بذرّة 707) — الثلاثة **مفتوحة**. **وفجوة مرفوعة**: 552 صارت **361 سطرًا فعليًّا > 350** (مع 451 عند 394 و580 عند 546) — ثلاث مخالفات حجم بالمدقّق، والاستثناء الموثَّق أو التقسيم **حكم المالك**، لم أستثنِ من عندي. **وملاحظة صدق عن فحصين أحمرين**: `check_request_id_identity_contract` (11 اختلافًا) و`check_stop_contract` (13) **كانا أحمرين قبل ليلة أمس** — نسخهما المثبّتة قديمة (585/583/581/518/453 تغيّرت من زمان) ودالّة `_perpetual_budget_contract` غائبة عن 552 **حتى بآخر التزام** — فليسا ارتدادًا من هذه الحزمة، بل **دَين توثيق قائم**. | ضمن ختم 22 | ٢٠٢٦-٠٨-٢١ |
| 30 | **ث٣ منفَّذ — محرر منحنيات الترجيح باللوحة** (ضمن ختم 22، ق١٠ §١٨–٢١): 901 v2.4.4 بأمر `tilt_rule` (يتحقق: ستة حقول مسموحة — state وweight مرفوضان عمدًا؛ نقاط تصاعدية حتى 12؛ المسح شرعي) بالتأكيد المزدوج، بلا أي تطبيق داخل 901 (المالك الوحيد للمخزن 580) — اختباراته 12/12. الخادم: سماح الأمر + ملخص عربي + `GET /gov/tilt/rules` قراءة فقط (الغائب `available:false` بلا اختراع). الواجهة: قسم «محرك الترجيح» بصفحة القرار — لكل حقل بطاقة منحنى (عتبة←مقدار بأربع منازل) بمحرر كامل، ولوحة حالة حية بصيغة §٢٠–٢١ وسبب كل ترجيح، والمجهول «مجهول»، وملاحظة الصدق البارزة: «المحرك يبدأ بلا منحنيات وبحد أقصى 0.0». البناء نجح والجسر يقدّمه. | ضمن ختم 22 | ٢٠٢٦-٠٨-٢١ |
| 29 | **حزمة ج حُوّلت ورقة تسليم بأمر المالك** («حزمة ج حكها بل ورق جهزها اخلي اوبس يساويها») — كُتبت `سياق\٩٨ حزمة ج — ورقة تنفيذ اللوحات.md`: ورقة مكتفية بذاتها لجلسة منفّذة أخرى، فيها الغطاء الشرعي (ضمن ختم 22)، قوانين الشغل والممنوعات، خريطة الواجهة واصطلاحاتها، عقود أحداث الليلة كاملة (السلسلة والبوابة والأهلية والترجيح)، البنود ج١–ج٥ مفصّلة بترتيب التنفيذ وقبول كل بند، وذيل الباقي (بقية ت، ح، موجة التفعيل بيد المالك، ونقاط القرار المفتوحة السبع). | أمر مالك | ٢٠٢٦-٠٨-٢١ |
| 28 | **قاعدة تجميع «الحالة» عبر الأقسام — حكم الخيار (ب)**: ختم المالك «ب NQ» على العرض المكتوب: حالة القرار المجمّعة تُحسب بقاعدة دمج حالة المسارين المختومة نفسها مطبّقةً على بطاقات الأقسام الستة (150/200/250/300/350/400): لا حالات ⇒ NOT_READY · أي STALE ⇒ STALE · أقل من الستة حاضرة أو أي ANALYZING ⇒ ANALYZING · الستة كلها READY ⇒ READY · غير ذلك ⇒ NOT_READY. التنفيذ: 451 يحسب `aggregate_state` من حالات بطاقات الأقسام (unified.state حيث توجد) وينشرها مع إعلان الأقسام الغائبة، و452/453 يمررانها، والفاحصون يقرؤونها لفحص READY. أُمر بها الوكيل الجاري ضمن جراحة توصيل الثمانية نفسها. | NQ (ب) | ٢٠٢٦-٠٨-٢٠ |
| 27 | **حزمة ب منفَّذة ومحمَّلة حيًّا — القرار الواحد بهويته وبوابته** (ضمن ختم البند 22): **ب١** معرّف القرار يولد حتميًّا عند 451 (`dec:`+مفتاح الدورة) ويمرّ الهوية الست بلا فقد عبر 452→453→458→454→466 (سُدّ سقوط الوسيط عند 452 والحساب وبداية الدورة عند 458). **ب٢/ق٨** الفاحصون أحياء: 455 أهلية الشراء و456 البيع و457 تفسير الانتظار و450 السجل الموحد — أمثلة الورقة الأربعة مثبتة بالاختبارات، وعند تأهل الجانبين لا يحسمون (الحسم لـ458 حصرًا). **ب٣/ق٦** خمسة عيارات عتبات جديدة بأرقام الحكم (اتجاه ±50، قوة 45، ثقة 63، عمق 45) بانتظار اعتماد المالك. **ب٤/ق٩** 467 بوابة حقيقية (أُلغي وسم التشخيص): approved=false لا يمر أبدًا، الانتظار حالة لا أمر، منع تكرار بذاكرة تنجو من الإقلاع، وثلاثة أحداث بوابة — **قياس حي بعد التحميل: مرّرت 4 وحجبت 137 وسجّلت 72 انتظارًا بصفر تكرار**. **ب٥** 581 يستهلك البوابة حصرًا لقرارات الدخول (458 نُزّل لسياق لا يفتح اتجاهًا) وكل هدف يحمل معرّفي القرار والطلب؛ و576 مقيَّد: لا دخول بلا `parent_decision_id` أو `owner_command_id` — الرفض `NO_PARENT_AUTHORITY` معلَن. **ب٦/ب٧** المفردات (decision_side منفصل عن القيمة الاتجاهية) والأصفار الملفقة أزيلت وكل حاجز برباعية (قيمة/عتبة/سبب/وقت) والرفض بسداسيته. اختبارات: 130 بسلسلة القرار + 64 عند الست + برهانا عقد موصولان — كلها خضراء؛ المدقق **صفر مخالفات** بعد تنظيف رقم سحري بـ166 (v2.3.2) وبناء آلية استثناء الحجم الموثَّق (الشق الثاني من حكم «أو عطيها استثناء»: 166 و454 مستثناتان بسبب مكتوب تحذيرًا معلَنًا — الدستور §18 حُدّث). الذرات الـ14 الجديدة كلها تحمّلت حيًّا تلقائيًّا. **قياس ما بعد التحميل كشف فجوتين ميكانيكيتين قيد السد** (وكيل جارٍ): 453 يبث الاتجاه كلمة لا رقمًا فتسقط الأهلية كلها `FIELD_UNKNOWN:direction`، وسباق التوزيع يجعل بعض الحسم fallback — التوصيل الميكانيكي (قيمة موقّعة، مقاييس ×100، عمق موزون من 451، حسم مقاد بالثلاثي مع دفق-بالأحدث) بلا أي قرار دلالي جديد. **نقطة قرار مرفوعة للمالك: قاعدة تجميع «الحالة» عبر الأقسام غير محسومة** — وحتى تُحسم ويعتمد المالك العتبات، الطبقة الجديدة تُخرج «انتظارًا» صادقًا فيتوقف الدخول الجديد من المسار الموحد (على التجريبي). | ضمن ختم 22 | ٢٠٢٦-٠٨-٢٠ |
| 26 | **توحيد لون «متقادم» على الأحمر** — ختم `NQ` وصل بعد عرض النقطة صراحةً («توحيد القديم عالأحمر تركته لحكمك»). نُفّذ: جدول الأقسام القديم وجدول حالات المحلّلين في صفحة التحليل صارا يعرضان STALE أحمر بالمواصفة (كانا كهرمانيَّين) — موحَّدين مع بطاقة المسارين الجديدة. البناء أعيد والجسر يقدّم الحزمة الجديدة (مُثبَت بطلب حي). **ملاحظة نطاق الختم بأمانة:** النقاط الثلاث الأخرى المعلّقة (مصدر اتجاه بطاقات الأقسام، انعكاس كلمات 260، تحكيم 404) كانت أسئلة مفتوحة بلا عرض مكتوب قابل للتنفيذ «كما كُتب» — فلم يُحمَّل الختم العام إياها، وأعيد عرضها بصيغ محددة قابلة للختم فرادى. | NQ | ٢٠٢٦-٠٨-٢٠ |
| 25 | **أ١١ منفَّذ — حزمة أ مكتملة** (ضمن ختم البند 22): لوحة التحليل صار فيها «بطاقة قسم التحليل — دمج المسارين» لكل رمز: الثماني المدموجة + شارتا المسارين (سريع من التِكّات/بطيء من الشموع) جنبًا إلى جنب — الحقل المجهول يُكتب «مجهول» بخافت لا صفرًا، والمسار الغائب يُعلَن بإطار متقطّع ووزنه («غائب — وزنه 45 معلَن»)، والحالات بالألوان (جاهز أخضر/قيد التحليل كهرماني/غير جاهز رمادي/متقادم أحمر). لا تعديل بالخادم (البثّ مباشر من النواة بلا قائمة سماح — مقاس). القاموس المركزي كسب الترجمات الجديدة. البناء نجح والجسر يقدّم الحزمة الجديدة (مُثبَت بطلب حي). **نقطة قرار معلّقة**: جدول الأقسام القديم يعرض «متقادم» كهرمانيًّا والبطاقة الجديدة حمراء بالمواصفة — توحيد القديم بانتظار حكم المالك. **بهذا اكتملت حزمة أ كاملة** (أ١–أ١١) عدا نقاط القرار المرفوعة للمالك: مصدر اتجاه بطاقات الأقسام وانعكاس كلمات 260، وتحكيم 404، ولون المتقادم بالجدول القديم. | ضمن ختم 22 | ٢٠٢٦-٠٨-٢٠ |
| 24 | **حزمة أ — الشريحة الثانية منفَّذة ومفعَّلة حيًّا** (ضمن ختم البند 22): **أ٦/ق٢** مخزن المعايرة كسب بُعد المسارين (fast/slow، هجرة آمنة — القاعدة الحية هاجرت والصف اليدوي المعتمد BTCUSD/trend/15 محفوظ على fast) + محرك إعادة التوزيع بالتساوي بحكم ق٢ الحرفي (مثال المالك 25/25/25/25→40/20/20/20 مُثبَت؛ المجموع 100.0000 دائمًا؛ كل صفّ متأثر مدقَّق) — اختبارات المخزن 5/5. **أ٧/ق١** طريق اعتماد المُعامِلات: 901 v2.4.3 بأمر `parameter_approve` (9/9)، منفذ `/gov/parameters` (25 صفًّا حيًّا)، بطاقة «المُعامِلات المعلنة» بصفحة الإعدادات بزرّ اعتماد بالتأكيد المزدوج — ولا قيمة اعتُمدت: كلها بيد المالك. **أ٨** صدق العقد: غياب required_depth/confidence يُعلَن مجهولًا لا 100/0 صامتة؛ حالة بطاقة القسم توحّدت على unified.state؛ 451 v2.6.7 كفّ عن تلفيق الأصفار (13/13). **أ٩ جزئيًّا** الخادم يقرأ الأوزان من المصدر الواحد (حُذفت الخريطة اليدوية القديمة غير المتساوية) وفُكّ حظر معايرة الأقسام الستة وافتراضها المتساوي 100÷6. **أ١٠/ق٧** سلسلة الأخبار: 616 v1.1.1 يقرأ الرموز والدرجة بسقوط آمن، 411 v1.2.1 أعيد بناؤها (نطاق تأثير لكل رمز؛ حدث `news.trading_window.state`؛ العالي ±15 دقيقة block=true؛ غياب الأخبار = مجهول معلَن لا حياد مقيس)، 454 v2.3.2 بحاجز `news_window`، وأربعة عيارات نوافذ جديدة (15/15/0/0) — اختبارات 13/13 و7/7 و9/9. **عطل مقاس بعد التفعيل وأُصلح فورًا**: جسم القسم من 166 كان بلا حقل broker فرفض 451 كل نشراته (~200 «غير صالحة») — 166 v2.3.1 تحمل الهوية كاملة، والقياس بعدها: 451 سليمة صفر غير صالح والدورات المكتملة تتقدم. **موجة إعادة التشغيل**: إيقاف نظيف 🟢 بلقطات (67 ملفًّا) ثم إطلاق كامل من غرفة القيادة — 213/213 شغّالة، النسخ الثماني الجديدة محمّلة، 19 عيارًا و25 مُعامِلًا باللوحة، محرك المسارين حي (سريع=513، قسم=520 خلال دقائق). اختبارا الاستقلال المكسوران بتعديلاتي أُصلحا (9/9) — حارس الوتيرة وُحّد على الساعة الرسمية. | ضمن ختم 22 | ٢٠٢٦-٠٨-٢٠ |
| 23 | **حزمة أ — الشريحة الأولى منفَّذة ومختبرة** (ضمن ختم البند 22): **أ١** إرجاع القوة والاتجاه لصفوف 150 النحيفة (v2.3.1، اختباراته 11/11). **أوزان متساوية**: الخمسة عشر محلّلًا = 100÷15 لكلٍّ في `shared/live_analysis.py` (حكم المالك؛ القيم القديمة غير المتساوية كانت اختراع كود؛ صف معايرة BTCUSD/trend المعتمد بيد المالك لم يُمسّ). **أ٢/أ٣/أ٤** ذرّة 166 v2.3.0 بمعمارية المسارين: المجمّع السريع ينشر `analysis.fast.state` بالعقد الثماني كاملًا، والبطيء ينشر `analysis.slow.state` (عمقه=اكتمال الدورة، قوّته مجهولة معلنة)، ومجمّع القسم يدمج بوزنَي 55/45 وينشر نتيجة القسم على `analysis.raw.completed` حاملة `section_contract` الثماني + لقطتَي المسارين + `path_weights` — المسار الغائب يُعلَن `missing_path` بوزنه بلا انتظار متبادل. **أ٥ جزئيًّا**: أربعة عيارات محكومة جديدة (وزنا المسارين + عمقاهما المطلوبان) بسجل العيارات، وتعديل أحد الوزنين يعيد توزيع الآخر = 100−القيمة (ق٢ للنطاق الثنائي). اختبارات 166: **10/10** ومنها برهان مثال المالك الحسابي (80×0.55+40×0.45=**62.0000** بالضبط) وحارس إعادة التوزيع (70⇒30). حادثة مسجّلة بأمانة: النسخة الأولى من اختبار إعادة التوزيع كتبت اعتمادًا حقيقيًّا (70/30 بتوقيع NQ-test) بسجل المُعامِلات الحي — نُظِّف فورًا (الصفّان حُذفا فعادا غير معتمدَين) وعُزل الاختبار عن القاعدة الحية ببديل محاكاة. **التفعيل الحي ينتظر موجة إعادة تشغيل واحدة** بعد اكتمال وكلاء بقية الحزمة (تعديلات المكتبة المشتركة لا تُزرع بالتحميل الحي). | ضمن ختم 22 | ٢٠٢٦-٠٨-٢٠ |
| 22 | **ختم العرض الكامل «NQ عرض للختم — تنفيذ أوراق الطبقات الثلاث» (النسخة ٢)** — الحزم الست: أ (قسم التحليل بمسارين: 15 محللًا × رؤيتا تكّات وشموع، مجمّع سريع 100% داخليًّا ومجمّع بطيء 100% داخليًّا، زرع 55/45، ثم مجمّع القسم يخرج الثمانية) · ب (القرار الواحد: هوية كاملة، 455/456/457 فاحصو أهلية، لوحة قواعد القبول، 467 البوابة الوحيدة، تقييد 576) · ت (خيط هوية القرار بالتنفيذ، رفض التحجيم لا يضيع، بوابة 552 كاملة، سدّ التخزين) · ث (580 ترجيح مستمر بلا شرائح بقواعد لوحة) · ج (اللوحات) · ح (القبول والعينات). مع الحسمين: الـ15 متساوون 100÷15 داخل كل معسكر زمني · المساران 55.0000/45.0000 زرعًا؛ والقيد: **لا معسكرات «استمرار/إنهاك» ولا تجميع مخترَع** — أي تجميع مستقبلي ورقة مستقلة. المرجعان: أوراق `Desktop\1` الثماني + ورقة الأجوبة ق١–ق١٠. التنفيذ حرفي بالترتيب أ←ب←ت←ث←ج←ح، وكل بند يُسجَّل هنا فور إتمامه، وأي غير محسوم يظهر = توقف وعرض جديد. | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 21 | **فكّ خنقة التحليل — ثلاثة بنود عُرضت نصًّا وخُتمت معًا:** الجذر المقيس: التكّة تصل بـ0.00ث لكن مخرج المحلّلين يتأخّر 13-18ث (طابور)، و«إعلان الركود» كل نبضة يعيد نشر STALE لكل رمز بعد نصف ثانية من نتيجة طازجة (106 من 161 (رمز،تسلسل) منشورة مرّتين · 731 ألف صدى مرمي عند 150) + 15 صورة دمج لكل تكّة (144 حدث/ث) ⇒ بوّابة طزاجة 166 (5.0 محشورة) ترفض الكل ⇒ `valid=0` ولا قرار. **(أ)** `shared/live_analysis.py`: الركود يُقاس من آخر نشر فعلي لا من ختم المصدر — الساكت الحقيقي يبقى يُعلَن مرّة كل مهلة (يقتل ~105 حدث/ث زائد؛ يلزمه إعادة تشغيل نواة لأن المكتبة المشتركة لا تُزرع بالتحميل الحي). **(ب)** 150 v2.3.0: صورة واحدة لكل تكّة — تجميع تحديثات التكّة (نفس ختم المصدر) والنشر عند اكتمال 15 أو تكّة أجدد أو مهلة عيار `live_flush_timeout_s` بالبطاقة (افتراضي 1.0) (144/ث ← ~15/ث). **(ج)** 166 v2.2.0: الرقم المحشور 5.0 يصير عيار `live_stale_after_s` محكومًا بسجلّ العيارات يظهر ببطاقة عيارات القرار — **قيمته تبقى 5.0 حرفيًّا وتغييره بيد المالك حصرًا**. إفصاح مسجَّل: بنود أ وب كُتبت على القرص قبل قراءة ورقة الختم (لم تكن حيّة)، وأُوقف التنفيذ فور قراءتها وعُرضت نصًّا ثم وردت `NQ`. | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 1 | صفحة NQ باللوحة — خيار «أ». المختوم والمنفَّذ حصرًا: نبض ٦٠ ثانية · `state` · `health.state` · `health.message` · نسخة النواة والبصمة المقصوصة الظاهرتان في الرسالة · `restart_count` · `last_error`. الملفّات: `governance/ui/src/sections/NQ.tsx` (جديد) · `core/sections.ts` (سطر) · `App.tsx` (سطران). بلا فتح النواة، وبلا تغيير عقدها، وبلا مسّ الذرّة `001_NQ`. | `NQ` — ختم لاحق بأمر المالك: «هي ختمهم مو اثر رجعي هاد مني لانه انا سحبتك على فخ مشان نزبط ورقة». والحقيقة تبقى مسجّلة: نُفِّذ قبل ورود الرمز. | ٢٠٢٦-٠٨-٢٠ |
| 2 | **تنظيف مخالفة** — التنفيذ الأول حمل إضافات بلا ختم: بطاقة «نسخة الذرّة» · «آخر قراءة وصلت» · «آخر تغيّر في الحالة» · سطر التعريف · سطر «نبضة كل ٦٠ ثانية» · السطر التفسيري الأخير · كلمة «سليمة» المشتقّة. أُزيلت كلّها. **لا ختم بأثر رجعي.** | `NQ` — ختم لاحق بأمر المالك: «هي ختمهم مو اثر رجعي هاد مني لانه انا سحبتك على فخ مشان نزبط ورقة». والحقيقة تبقى مسجّلة: نُفِّذ قبل ورود الرمز. | ٢٠٢٦-٠٨-٢٠ |
| 3 | تعريف الختم: أُضيف البند ١٠ لورقة «NQ ختم مالك» — «لا يُعتدّ بأي موافقة إلا إذا وردت فيها `NQ` صريحة». وصُحِّح سطرا هذا السجلّ (١ و٢) من «مختوم» إلى «نُفِّذ بلا ختم صحيح». | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 20 | **أوّل قياس زمن-تشغيل، وتسجيله بشرطه** — أمر المالك: «حطها بي ورقة». **السياق:** أداة الصدفة ممنوعة عندي، فبُني طريق بديل مُثبَت: أكتب سكربت بايثون، يشغّله المالك بيده، وأقرأ ناتجه من طرفيّته (`read_terminal`). **وقد رفض المالك أوّلًا تسجيل الأرقام وكان محقًّا** («قياس مكتوب بورقة = كذبة بكرا»)، فقُبِل بشرط أن يحمل كل رقم وسيلة إعادة استخراجه. **⇒ (أ)** أُضيف ملفّ جديد [`governance/scripts/live_probe.py`](../governance/scripts/live_probe.py) — **قراءة فقط، كل قاعدة `mode=ro`، صفر كتابة** — وهو مصدر كل رقم مسجَّل. **(ب)** قسم «📡 القياس الحيّ — ٢٠٢٦-٠٨-٢٠» بآخر ورقة `٩٠`، يفتتحه سطر «**لا تصدّق هذا القسم، شغّله**» ويقرّ سلفًا أنّ الناتج المختلف هو الصحيح لا الورقة. **الأرقام:** نواة حيّة `1.17.0` و`atom_count=213` (**مطابقة تامّة للعدّ الساكن ٢١٧−٤**) · **١٦ عيارًا كلّها `UNAPPROVED`** ⇒ `min_score` الساري `=0` **مؤكَّدًا لا مستنتَجًا**، وطبقة العيارات المبنيّة **لم تُستعمل ولا مرّة** · `archive.db` **٠ صفّ** مقابل `var/store` **~٣.٧ غيغا** (يؤكّد «الباقي رقم ١») · سجلّا `563`/`517` **الدائمان صفر صفّ** ⇒ ما وصلهما حدث تنفيذ قطّ (يؤكّد «`513`/`551`/`601` لم تُختبر») · لا مراكز مفتوحة لحظة القياس · قاعدة الظلّ ما زالت موجودة (24 KB). **(ج) بند جديد `٩٠-٣٣`:** `517` **لا يميّز صفرًا حقيقيًّا من حقلٍ لم يُملأ` — الجسر يرسل `commission=swap=fee=0.0` في ١٦٥ صفًّا، و`0.0` ليست `None` فيُعلن `COMPLETE` وينشر `costs_complete: True` و`cost_total: 0.00`: **الصافي = الإجمالي، والنظام يختمه «تكاليف معروفة»**. وضمانة `٩٠-١` المكتوبة «المجهول يبقى مجهولًا» **لا تصمد** لأنها تميّز `None` لا الصفر. **وحدّه المعلَن: `F7` يملأ الحقول ولا يصلح التمييز.** **الملفّات: ورقة `٩٠` + سكربت قراءة واحد.** ⛔ **ولم يُلمس: أي كود ذرّة أو نواة · أي إعداد (المنع بقي بقرار المالك) · لم يُقترح إصلاح لـ`٩٠-٣٣` — التشخيص وحده مختوم.** | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 19 | **تصحيح الورق المكذَّب بالكود** — أمر المالك: «صلح ورق مشان ما يكذب». **قُوبلت خمسة ادّعاءات بالكود ففُتحت خمسة ملفّات كاملة** (`454/atom.py` `2.3.1` · `454/manifest.yaml` · `517/atom.py` `4.0.2` · `563/atom.py` `3.0.2` · `shared/decision_dials.py`) + `core/CORE.lock` + عدّ `atoms/*/manifest.yaml`. **النتيجة: ٣ من ٥ سقطت.** (أ) **`٩٠-٢٦` أُلغي** — يستشهد بسطر `verdicts.get(fid, True)` **غير موجود بالملفّ إطلاقًا**؛ و`454` v2.3.1 **fail-closed** (`passed = gate and not blocked_by`، والصامت يقع في `missing`)، ويحجب على أربعة مجاهيل أخرى (تقادم · تقويم · جودة سوق · تغذية). (ب) **`٩٠-٢٧` صُحِّح** — `min_score: 0` صحيح، لكن استنتاج «طبقة الفلترة أضعف ممّا يوحي اسمها» سقط لأنّه بُني على `٩٠-٢٦`؛ وأُضيف أنّ القيمة السارية قد تعلوها قيمة `APPROVED` بسجلّ `parameters` **وهي غير مقيسة**. (ج) **`٩٠-١` و`٩٠-٢`: الورقة بريئة** — جدولها يقول `✅ منفَّذ`، والقارئ اقتبس أرشيفها كأنّه حيّ؛ صُحّحت نسختاهما (`517 v1.2.2→4.0.2` · `563 v1.1.2→3.0.2`) ووُسم الأرشيف بعنوانه. **وحدّان جديدان لم يكونا بأي ورقة:** `517:135` **يجمع** التكاليف — صحيح فقط لو الجسر يبعثها سالبة، وإلّا **الصافي أكبر من الحقيقي بصمت**؛ و`563:98` يقرأ `reference_price` **ومَن يملؤه لم يُفحص** فاعتراض `٩٥` باقٍ. **وأُضيف لكل ورقة نطاق صدق:** `٩٠` صار يعلن أنّ ٦ بنود من ٨٠٠+ سطر هي المقابَلة بالكود والباقي ليس دليلًا؛ و`٠٠٠` أخذ الدرس + قاعدتين (٦: كل رقم بتاريخه وهل هو مقيس ✅ أم منقول ⬜ · ٧: كل محسوم يحمل 🗄️ بعنوانه)؛ وصُحّح الأسطول **٢١٧ مانيفست · ٢١٣ تلقائيّة · ٤ يدويّة** (كان «216/212»، والفرق الذرّة `615`) بـ`٠٠٠` و`٩٨`، ونُزع عدّاد البنود المثبَّت من `٩٨`. **الملفّات: أربع أوراق سياق فقط — ولا سطر كود لُمس، ولا ذرّة، ولا نواة.** ⛔ **وما لم يُفعَل، معلَنًا:** ٢١٣ ذرّة لم تُفتح · كل رقم زمن-تشغيل بالأوراق **لم يُعَد قياسه** (أداة `Bash` ممنوعة ⇒ لا اختبار ولا استعلام `.db` ولا حالة حيّة) · أوراق `٩٥`/`٩٧`/`٩٩` **لم تُلمَس** — لم يُقابَل شيء منها بالكود. | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 24 | **تسريع ردّ البوت — من ١٧ ثانية إلى ٤:** الجذر مقيس ثلاثي: (أ) كان يترجم **٢٠ خبرًا** ثمّ يرسل **٥** (`break` عند الخامس) ⇒ ١٥ ترجمة تُرمى؛ (ب) ولكل خبر يترجم **العنوان والملخّص** و`summary_ar` **لا يُقرأ في أي موضع بالمشروع** (فُحص) ⇒ نصف النداءات هدر؛ (ج) ولا ذاكرة ترجمة — العنوان يُترجَم من جديد لكل مستخدم وكل مرّة. نُفِّذ: الفرز أوّلًا ثمّ الترجمة **لما سيُرسَل فقط**، وإرسال كل خبر فور جهوزه لا دفعةً، وترجمة العناوين فقط، **وذاكرة على القرص** (`data/translations_ar.json`) يستفيد منها كل المستخدمين، والنداء الحاجز يُنفَّذ بخيط (`asyncio.to_thread`) فلا يُجمّد بقيّة الطلبات. **مقيس: أوّل رسالة 4.2 ثانية بدل ~17 · الخمسة كاملة ~6.3 · وإعادتها من الذاكرة 0.000 ثانية.** | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 23 | **مواقيت البوت بأمر المالك:** التقرير الصباحي `14:00 ← 08:00`، والتنبيه قبل الحدث `15 ← 5` دقائق. أُضيفت مفاتيح الأجندة الغائبة إلى `.env` (`CAL_CURRENCY=USD` · `CAL_MIN_IMPACT=MEDIUM` · `CAL_ALERT_MINUTES=5`) — كان الملفّ منسوخًا قبل إضافتها فتعمل الذرّة على الافتراضي. **التوكن لم يُقرأ ولم يُطبع** (عُرضت أطوال المفاتيح فقط). ورسالة النتيجة تحمل المتوقّع والسابق ومقارنة رقمية حين يتوفّر المتوقّع — وتصمت عن المقارنة حين لا يتوفّر، لا تخترعها. وصُحّحت ركاكة عربية بالرسالة: «بعد 5 دقيقة» ← «بعد 5 دقائق» بدالّة تصريف مقيسة (1 واحدة · 2 دقيقتين · 3-10 دقائق · 11+ دقيقة). | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 22 | **البوت خرج من حصر «ناسداك» + ثلاثة أعطال صدق:** (أ) **بوّابة إرسال ثانية** (`routing_service`) كانت تحمل قائمة كلمات ناسداك/تقنية وتعمل **بعد** فلترة `news_service` — فتُسقط بصمت خبر الفيدرالي والذهب والإسترليني. صارت **تثق بقرار الصلة الأعلى** (`why`) ولا تضيّق مرّتين، وقائمة الاحتياط عُمّمت للرموز السبعة والاقتصاد الكلّي. **مقيس: 15/15 و20/20 يمرّون، وأسباب القبول موزّعة (`treasury` · مصدر رسمي · `US500` · `BTCUSD` · `US30`) لا ناسداك فقط.** (ب) **تناقض على شاشة الطالب:** الحكم «سلبي» والسبب تحته «كلمات إيجابية: gain» — لأنّ السبب كان يُبنى بـ`if matched_positive` أوّلًا مهما كان الحكم؛ صار يعرض الجانبين. (ج) **الموثوقيّة كانت ثابتة** «مصدر عام (Yahoo Finance RSS)» رغم صيرورة المصادر سبعة — صارت تتبع المصدر: رسمي/صحفي/غير معروف. (د) نصّ `/start` ووسم «الأثر على ناسداك» وعنوان الموجز عُمّموا، وأسماء المصادر عُرِّبت برسالة البوت. **وسبب عدم نشر التقرير الصباحي مقيس ولا عطل فيه:** موعده `14:00` بـ`.env` والوقت وقت القياس `11:07` — لم يحن بعد (آخر إرسال 2026-08-20 21:49 يدويًّا). | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 21 | **وصل الذرّة 411 بمغذّي الأخبار — تعديل ذرّة بختم صريح:** الجذر المقيس (وتصحيح من المالك لوصفٍ خاطئ كتبتُه): السلك للمجمّع **موجود** (411 تنشر `strategy.news.state` ← 400 يشترك بها ← 451 عبر `strategy.cycle.collected`)، و`108` ينشر فعلًا (`published=50`) — **لكنّ 411 كانت تشترك بـ`market_data.candle_closed` فقط ولا تشترك بـ`market_data.news_received` إطلاقًا**، فهي «موصولة وجائعة» لا «غير موصولة». نُفِّذ: اشتراك بتيّار الأخبار · نافذة زمنيّة (`recent_window_s=3600`, `max_items=100` بالمانيفست) · الحالة تحمل `news_count` و`top_impact` و`latest_headline` وعمره · والصحّة صارت `HEALTHY` مع خبر طازج بدل `NEWS_UNAVAILABLE` الدائم. **الإصدار 1.1.1 ← 1.2.0 في الكود والمانيفست معًا** (لا نسخة مزدوجة) وحُدِّثت `الشرح.md` و`التاريخ.md`. **⛔ لا اتجاه مخترع:** `signal=none` و`score=0` و`confidence=0` مهما كان العنوان — ومحروس باختبار يسقط على عنوانين متعاكسين. **مقيس بعد التنفيذ: 7/7 اختبارات · والمدقّق الرسمي 0 مخالفة** (كان 59 صباح اليوم). ومخالفة من شغلي صُحّحت فورًا: شرح عربي كتبتُه داخل `atom.py` خالف المادة ٣ — نُقل إلى `الشرح.md`. | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 20 | **إتمام الأخبار + ظبط الفهرس:** (أ) **أسماء المصادر عُرِّبت** باللوحة — كانت `cnbc_top` و`yahoo_ndx` خامًا (خرق بند ١)، صارت «CNBC — الأهمّ» و«ياهو — ناسداك»، ومعها تعريب سبب القبول. (ب) **عمودان أُضيفا لجدول `news` بالجسر**: `summary` و`relevance` — بعد فحص الذرّة 616 والتأكّد أنّها تختار **أعمدة مسمّاة** لا `SELECT *`، فالإضافة آمنة عليها. (ج) **عطل مقيس صُلح:** `INSERT OR IGNORE` كان يتخطّى الخبر الموجود فتبقى حقوله الجديدة فارغة للأبد — صار `ON CONFLICT DO UPDATE` يكمّل الناقص **ولا يدهس موجودًا**؛ النتيجة 30 خبرًا بسبب قبول وملخّص ووقت بعد أن كانت 0. (د) صفحة الأخبار تعرض الملخّص وشريحة سبب القبول وعمر الخبر. (هـ) **الفهرس `٠٠٠` ظُبِط**: «ابدأ من هنا» صار ٦ خطوات وأوّلها ورقة الختم · باب جديد «الحاكمة للتنفيذ» · أُضيفت ورقة ٩٨ للأوراق الحيّة · العدّ المكتوب أُلغي واستُبدل بقاعدة «العدد يُقرأ من المجلّد» · البند ٢ (شريط الصفقات) وُسم 🗄️ محسومًا · وأُضيفت ثلاثة بنود انتظار جديدة. مقيس الآن: **217 مانيفست · 213 تقلع · 4 موقوفة**، والنواة الحيّة تقول `atom_count=213`. | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 19 | **رفع جودة الأخبار — ستّة بنود دفعةً واحدة:** (١) **المصادر**: كانت ٣ تغذيات ياهو **إحداها ميتة (404)**؛ فُحصت ١٠ تغذيات مجّانية حيًّا فسقطت ٣ (BLS=403 · الخزانة=404 · ياهو «سوق الأسهم»=404) واعتُمدت **٧ حيّة** منها الرسميّ (الفيدرالي · مكتب التحليل الاقتصادي) ورتبته أعلى من الصحافة. (٢) **وقت النشر الحقيقي** من التغذية بدل `NULL`. (٣) **إزالة التكرار** بعنوان مُطبَّع. (٤) **فلترة بالصلة** برموز المالك السبعة والاقتصاد الكلّي — ١٦٤ خبرًا ⇒ ٥٨ مرميًّا معلَنًا. (٥) **وسم أثر بقاعدة معلَنة** (لا نبوءة) + سبب القبول محفوظ. (٦) **خبر مولَّد من الأجندة**: مقارنة الفعلي بالمتوقّع رقميًّا — **وعمدًا بلا حكم «إيجابي/سلبي»** لأنّ معنى الاتجاه يختلف بكل مؤشّر. **عطل مقيس صُلح أثناء البناء:** المطابقة بلا حدود كلمات صنّفت «co**dow**ngrade» مؤشّرَ داو و«**war**ns» حربًا — أُضيفت حدود كلمات وأُثبتت بالاختبار. وأُضيفت نبضة سحب كل ١٠ دقائق (كان يدويًّا). الملفّات: `news_service.py` (أُعيد بناؤه) · `bridge_writer.py` · `scheduler.py` · `calendar_bridge.py` · `News.tsx`. مُثبَت حيًّا: ٢٥ خبرًا كُتبت بستّة مصادر بأوقات ووسوم، وفيها بيان من الفيدرالي مباشرة. | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 18 | **تعريب عناوين الأخبار + ورقة سياق اليوم:** (أ) `deep-translator` ثُبِّتت ببيئة اللوحة وأُضيفت لـ`governance/setup/requirements.txt`؛ و`/gov/news` صار يضيف `headline_ar` **من ذاكرة على القرص** (`var/governance/news_ar.json`)، والترجمة تجري **بخيط خلفي** لا داخل الطلب — القياس: الردّ 0.01 ثانية والذاكرة فارغة (كان 5 ثوانٍ حين كانت الترجمة داخل الطلب فتُجمّد اللوحة). العنوان الأصلي يبقى ظاهرًا تحت الترجمة، وما لم يُترجَم بعد يُعلَن «لم يُترجَم بعد» — لا نصّ مخترع. (ب) كُتبت ورقة `سياق\٩٨ يوم ٢٠٢٦-٠٨-٢٠ — اللوحة وبوت الأخبار.md` (124 سطرًا) تجمع اليوم بأرقامه وأعطاله وما بقي بيد المالك. | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 17 | **بوت الأخبار صار يقلع من غرفة القيادة:** أمر المالك «كلهم من مكان واحد». أُضيفت `_ensure_news_bot()` إلى `governance/app.py` تُنادى بعد تلغرام: تشغّل مشروع `QUANT_NQ_NEWS` المجاور **ببيئته وبايثونه ومجلّده هو** (لا خلط بيئتين)، بنافذة مستقلّة مثل باقي الخدمات. لا تُشغَّل إن غاب المشروع أو غاب `.env` أو كان التوكن أقصر من ٢٠ محرفًا — وتقول السبب بسطر واحد بدل نافذة خطأ. النسخة المكرّرة يمنعها قفل البوت نفسه. مُختبَر بـPopen مزيّف: الأمر والمجلّد صحيحان (`…QUANT_NQ_NEWS\.venv\Scripts\python.exe main.py` · cwd المشروع)، وصياغة `app.py` سليمة. | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 16 | **صفحة «الأخبار» باللوحة:** تبويب جديد يعرض **أجندة اليوم الاقتصادية** (من تقويم ميتاتريدر عبر جسر التداول — عناوين عربية، عملة، أهمية، متوقّع/سابق/فعلي، بلا مفتاح API) و**آخر الأخبار** من جدول `news` بنفس الجسر. مفاتيح بيد المالك: العملة (USD/EUR/GBP/JPY/الكل) والأهمية (عالية فقط / متوسطة فما فوق / الكل)، وبطاقة «الحدث القادم» بعدّاد حيّ («بعد N دقيقة»). الخلفية: منفذان جديدان `/gov/calendar` و`/gov/news` في `governance/server.py` بقراءة `mode=ro` فقط — **بلا مساس بالنواة ولا بأي ذرّة**. الواجهة: `sections/News.tsx` (جديد) + سطر بـ`sections.ts` + سطران بـ`App.tsx`. مُختبَر: الدالّتان أرجعتا 6 أحداث USD و5 أخبار حقيقية · البناء `exit 0` · صفر خطأ أنواع جديد. **يلزم إعادة تشغيل غرفة القيادة ليعرف الخادمُ المنفذين الجديدين.** | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 15 | **إخفاء توكن البوت من السجلّات:** المصدر المقيس ليس اللوحة — مشروع `QUANT_NQ` **نظيف صفر مرّة**؛ التسرّب كلّه في `QUANT_NQ_NEWS\logs\bot.log` (**282 ظهورًا**) لأنّ مكتبة `httpx` تسجّل رابط كل طلب وفيه التوكن. `app/logger.py` صار يحمل مرشّحًا يستبدل نمط `bot<رقم>:<سرّ>` بـ`bot***:***` على كل مخرَج (ملف وشاشة) مهما كان مصدره، ومعه إسكات `httpx`/`httpcore` عند INFO فلا تُطبع الروابط أصلًا. السجلّ الحالي نُظّف (282 ← **0**) والنسخة الأصلية **أُرشفت لا حُذفت** باسم `bot.log.مؤرشف_يحوي_توكن_…`. مُختبَر حيًّا: سطر تجريبي فيه توكن كُتب `bot***:***`. **الأرشيف ما زال يحوي التوكن القديم — يصير بلا قيمة بعد `/revoke`، وحذفه بيد المالك وحده.** | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 14 | **حارس النسخة الواحدة لبوت الأخبار:** الجذر المقيس لخطأ `409 Conflict` كان **نسختين من `main.py` شغّالتين بنفس التوكن** (PID 10832 و12984) — تلغرام يسمح بمستهلك واحد لكل توكن فتقطع كل نسخة الأخرى. ملف جديد `app/single_instance.py` يحجز **قفل نظام تشغيل** (`msvcrt.locking`) لا ملفّ PID — لأنّ ملفّ الـPID يبقى بعد الانهيار فيمنع التشغيل ظلمًا، بينما قفل النظام يُحرَّر تلقائيًّا عند موت العملية. `main.py` يرفض الإقلاع ويخرج بالرمز 2 برسالة عربية، والمشغّل يعرضها. مُختبَر: الأولى تحجز · الثانية تُرفض أثناء الإمساك · وبعد التحرير تنجح فورًا (لا قفل يتيم). **تنبيه أمني مُبلَّغ للمالك: التوكن ظهر في اللوج الملصوق — يلزم `/revoke` من BotFather.** | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 13 | **حماية بوت الأخبار من الرشق:** ملف جديد `app/throttle.py` بثلاث طبقات بلا مكتبة جديدة — قفل لكل محادثة (طلب واحد يُنفَّذ، والباقي يُرمى ويُعلَن مرميًّا) · مهلة تهدئة ثانيتين · سقف أربع عمليات ثقيلة متزامنة · وتنبيه «قيد التنفيذ» مرّة كل ١٠ ثوانٍ لا مع كل كبسة. وُصِّل بسطر واحد في `register_handlers`، و`main.py` صار `concurrent_updates(8)`. أي استثناء بأمرٍ واحد يُسجَّل ولا يُسقط البوت. **مقيس بمحاكاة:** ٣٠ كبسة من شخص ⇒ نُفِّذ 1 · رُمي 29 · تنبيه واحد. و١٠ أشخاص معًا ⇒ نُفِّذ 10 · رُمي 0 · بزمن 0.75 ثانية بدل 2.50 بالطابور. | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 12 | **جسر الأجندة الاقتصادية للبوت (مشروع `QUANT_NQ_NEWS`):** مصدر الأجندة تحوّل من Finnhub (يحتاج مفتاحًا، ومفتاح المشروع أُفرِغ) إلى **تقويم ميتاتريدر المدمج** عبر جدول `calendar` في قاعدة الجسر — ٧١٥ حدثًا، عناوين عربية جاهزة، وفيها `currency` و`impact_level` و`forecast/previous/actual`. ملف جديد `app/calendar_bridge.py` (قراءة فقط `mode=ro`، لا يلمس QUANT_NQ ولا أي ذرّة) بواجهة مطابقة للخدمة القديمة فبُدِّلت بسطرين في `telegram_bot.py` و`reporting.py`. وأُضيفت نبضة كل دقيقة في `scheduler.py` تنشر: **تنبيهًا قبل الحدث بـ١٥ دقيقة، ونتيجةً عند صدور `actual`** — لكل المستخدمين، مع منع تكرار محفوظ على القرص. إعداداته بـ`.env.example`: `CAL_CURRENCY=USD` · `CAL_MIN_IMPACT` · `CAL_ALERT_MINUTES`. مُختبَر حيًّا على بيانات اليوم: ٦ أحداث USD، والتوقيت صُحّح للتقريب لأقرب دقيقة (كان يعرض 15:29 بدل 15:30)، والنشر يُرسل مرّة واحدة ولا يتكرّر عند النداء الثاني. | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 11 | **بوت الأخبار (مشروع `QUANT_NQ_NEWS` الخارجي) — تشغيله:** ثُبِّتت المكتبات الناقصة بالبيئة (`python-telegram-bot 21.3` · `APScheduler 3.10.4` · `deep-translator 1.11.4`) وأُضيفت `deep-translator` لـ`requirements.txt` (كانت مستعملة بالكود وغائبة عن الملف، فكانت الترجمة تُرجع الإنكليزي كما هو). ثم أُنشئ مُشغّل `بوت الأخبار.bat` (UTF-8 بلا BOM، `chcp 65001`): يجهّز البيئة إن غابت · يثبّت الناقص فقط · يفتح `.env` بالمفكرة إن غاب التوكن ويتوقّف · يشغّل البوت بطرفية مرئية. **التوكن لم يُلمَس ولم يُقرأ — بيد المالك حصرًا.** مُختبَر: استيراد كل وحدات المشروع بلا خطأ · فحص المكتبات=0 · فحص التوكن يرجع 1 بلا توكن و0 مع توكن صحيح الشكل. | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 10 | **حركة سلسة + تصغير وتكبير للبلاطات:** أُلغي تحديث حالة React أثناء السحب (كان يعيد رسم كل البلاطات مع كل حركة ماوس فتثقل وتقفز) — صارت البلاطة تتبع المؤشّر بالبكسل عبر `requestAnimationFrame` بتعديل مباشر، والحساب على الشبكة مرّة واحدة عند رفع الإصبع. وصُحّح اتجاه الحافّة اليسرى (كان السحب لبرّا يصغّر بدل أن يكبّر). وأُضيف زرّا **تصغير** (تنزل لشريط الأسفل وتُستعاد بكبسة) و**تكبير** (تأخذ اللوح كاملًا وتعود بنفس الزرّ) — والحالتان تُحفظان. مُختبَر حيًّا: 7←6 بلاطات عند الطيّ ثم 7 عند الاستعادة · وعند التكبير بلاطة واحدة بمساحة 12×12. | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 9 | **الرئيسية v8 «حاملة البلاطات»:** شبكة ١٢×١٢، كل بلاطة تُنقل بسحب رأسها وتُحجَّم من حافّتها اليسرى (عرض) والسفلية (طول) والزاوية (الاثنان)، والمكان والمقاس يُحفظان محليًّا ومعهما «رجوع للافتراضي» و«أضف بلاطة». **الأقسام تُركَّب كما هي بلا إعادة بناء** (أمر المالك: «كل موجود عيّفهم — ضيف الناقص بس»): الشارت · المخاطر · التنبيهات · الشبكة · التحليل · السوق · التنفيذ · تحكّم · الذرات · المحافظ · السجل · المراقبة · الاتصال · ذرّة NQ · عيارات القرار. والمكتوب جديدًا هو الناقص فقط: الحكم بلمحة · جدول الرموز · الحاجبون · المال · صحّة الأسطول · التغذية. الملفّان: `components/Tiles.tsx` (جديد) · `Home.tsx`. مُختبَر حيًّا: نقل بلاطة (صفّ 4→6) وتحجيمها (عرض 5→4) وحفظهما في التخزين المحلّي. | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 8 | **رجوع للوحة v7 بمساحات فارغة أقل** بأمر المالك («شيل القمرة ورجّع لوحتك»): بنية v5 لكن بأربعة صفوف مرصوصة — الحكم · جدول الرموز (٩ أعمدة، يأخذ ارتفاع صفوفه فقط) · خمسة صناديق بعرض الشاشة · قائمتان تملآن الباقي. **عدد أسطر القوائم يُحسب من ارتفاع الصندوق (`ResizeObserver`)** فلا فراغ ولا قصّ. صندوق «التغذية» نُقل للأعلى لأنه كان يترك ٤٣١ بكسل فاضية. مقيس: فيضان أفقي 0 · سكرول 0 · قصّ 0. الملف: `Home.tsx` وحده. | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 7 | **«قمرة القيادة» — إعادة تصميم الرئيسية v6:** شريط علوي (نبض + مال + حكم) وثلاثة أعمدة، كل عمود يجيب سؤالًا: الدماغ (بنية · ميزان أدلة · الحاجبون) · النبض (سعر كبير + إدارة حيّة + شريط دوّار ٨ نبضات) · الدرع (مخاطر · أسباب الرفض والتخطّي · مصفوفة نقاط الذرّات مع تلميح). بديل «شريط السيولة» بأمر المالك = **ميزان الأدلة** (وزن حاضر/غايب · اتفاق · ثقة · جودة). بلا سكرول وبلا قصّ (مقيس: 0 · 0) وبلا زرّ ميّت. الملف: `Home.tsx` وحده. | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 6 | **إعادة بناء «الرئيسية» + الشريط العلوي** (المواصفة ١–١٦ المختومة): سطر هويّة `NQ · QUANT_NQ · محمد رزوق` · التبويبات سطر واحد · أزرار الثيمات أُزيلت والثيم ثبت «داكن» · مؤشّرات المصادر نزلت لمربّع «التغذية» · جدول الرموز ٨ أعمدة · مربّع الحاجبين بالعربي **مع الغايب** · صحّة الأسطول تعدّ الصحّة لا التشغيل · الأحداث مجمَّعة · بلا سكرول. الملفّات: `Home.tsx` · `App.tsx` · `styles.css`. **انحراف مُعلَن:** عمودا «الثقة» و«الحاجب» لكل رمز غير موجودين على السلك (قيمتهما لكل دورة لا لكل رمز) — نُقلا لمربّع مستقلّ بدل اختراع خانة. | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 5 | تسكيت أربع ذرّات (خيار «ب»): `107` شريط الصفقات · `256` الدلتا · `257` CVD · `258` الامتصاص — `startup_mode` من `auto` إلى `manual` في مانيفست كلٍّ منها، ولا شيء غير هذا السطر. الجذر: `market.trade` بلا ناشر (وسيط CFD بلا شريط صفقات)، فالأربعة تعمل ولا تُنتج. لا حذف ولا نقل ولا مسّ كود. الرجوع: كلمة واحدة في كل ملف. | `NQ` | ٢٠٢٦-٠٨-٢٠ |
| 4 | ختم البندين ١ و٢ بأمر المالك بعد أن كانا «بلا ختم صحيح» — الفخّ كان مقصودًا من المالك لضبط الورقة، فالمسؤولية عنده لا مخالفةً منّي. صفحة NQ باللوحة تبقى كما هي، مختومة. | `NQ` | ٢٠٢٦-٠٨-٢٠ |
