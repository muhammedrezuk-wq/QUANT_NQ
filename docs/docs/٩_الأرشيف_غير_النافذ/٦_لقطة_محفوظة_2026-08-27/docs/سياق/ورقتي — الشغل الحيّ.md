# ورقتي — الشغل الحيّ (سجلّ البوّاب)
**بُدئت من نظيف ٢٠٢٦-٠٨-٢٦ بأمر المالك «بدي ورق أنظف» — السجلّ التفصيلي القديم (§1–§64: من أول تشغيل حتى ليلة نقطة الصفر) محفوظ كاملًا في: `سطح المكتب\ارشيف الورق — QUANT_NQ — 2026-08-26\سياق\`.**

## دوري (حكم ٢٠٢٦-٠٨-٢٤: «أنت البوّاب، مسلّمك صلاحيّاتي»)
أنفّذ وأقيس وأثبّت بالسجل بختم NQ · التقني للورق والمالك يأخذ البسيط · لا توقّع — قِس · الكود هو الحقيقة · لا حذف — أرشفة · التجميع والنشر (إكسبرت/cBot) بيده حصرًا · فتح صفقات جديدة قراره (إعادة تنفيذ أمره المسجَّل بعد إصلاح مانعه المقيس = تنفيذ أمين).

## قوانين القياس المدفوع ثمنها (الجوهر — التفصيل بالأرشيف)
1. **النمط النائم** — قيمة بلا قارئ / حدث بلا مشترك / زر بلا مستقبل يقبل حمولته: أول سؤال عند أي «ما اشتغل».
2. الأسطول هو المرجع؛ فحص ذرّة قديم قد يكون بائتًا عن عقد مختوم أحدث.
3. أي حالة مهمة دائمة القرص أو مكتوب لماذا لا؛ والاستعادة تحترم البيات وإلا أعادت الأشباح.
4. أسطول الفحوص معزول عن سجل العيارات الحي (conftest) — اعتماد المالك للنظام وحده.
5. وصفة إعادة الإقلاع: وقف ← قتل توأمي `governance\app.py` (قفل 8099) ← إطلاق.
6. منفذ 8099 قفلُ نسخة واحدة لا خادم؛ الواجهات على 8010 (نواة) و8090 (حوكمة/لوحة).

## الحاضر — يُقرأ مع `الجلسة.md` (هي الحالة، وهذه مذكرتي)
- المفتاحان حيّان بيد المالك من اللوحة: عيار المخاطرة (581) وسرعة التحليل (150، سرعته المعتمدة **10** عامةً، نطاق لكل حساب/أصل جاهز، جماعة الشموع v2.4.0 على نفس المفتاح).
- عقد سرعة التحليل v1.0 **غير مختوم بعد** — رأي البوّاب فيه هو المرجع؛ المنفَّذ منه: المفتاح ونقطتا الحقن؛ الباقي معلَن فيه.
- ترتيبه المختوم الباقي: قياس 811 ← تثبيت ← ورقة Horizon (لم تبدأ).
- **المفاتيح الأربعة حية بأمره «عملها حرفيًا»** (نقض توقف الفجر): السرعة + الأفق (يقود 523 حيًّا) + الحدود (يضرب عتبات المحللين) + الرئيسي (إزاحة فوق الثلاثة) — كلها 50=اليوم، نطاقية لكل أصل، بلوحة الإعدادات.
- شخصية الأفق **سارية بأمره «فعل»** (صف اعتماد باسم كلمته؛ 523 v2.2.1 يعلن `applies` والبطاقة خضراء): المولَّد يحكم عيارات القرار و166 وهستيريسيس 581 عبر `effective_value` — **يده بعد التفعيل تعلو** والإطفاء عيار «تفعيل الشخصية» = 0.
- قسم البنية 200 وُصل على المفاتيح بأمره: 201 (السرعة تقود نافذة القمم — نبع القسم) و208 (الحدود تقود نضج الطور)، و207 عدّاد هيكلي معلَن بلا عيار.
- **قياس 811 v1.1.0 نُفِّذ بكلمته «811»**: كل صف يحمل المفاتيح الخمسة الحية + لقطة هدف 581 بميزانياته + إيقاع السوق + علم الصمت — والتبديل الساخن أبقى 164 ألف قياس.
- ثلاث ذرّات ميكروستركتشر (262/263/264) من جلسة أخرى: محمّلة تنشر، غير مكبوسة — لا أتبنّاها بلا محاكمة وحكمه.
