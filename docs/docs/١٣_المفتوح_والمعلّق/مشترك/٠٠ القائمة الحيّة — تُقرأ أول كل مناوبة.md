# القائمة الحيّة — ما هو مفتوح ومعلّق

> **تُقرأ أوّل كل مناوبة، وتُحدَّث فور إغلاق أي بند.**
> **آخر تحديث تدقيقي: 2026-09-01 — الالتزام `270f96bf0eae`.**
>
> **إضافة 2026-09-03 01:40 (هذا الاستخراج — بلا ويندوز):**
> * شارت التنفيذ **مصدرًا** في الشجرة. العين لا تراه حتى يُبنى `governance/ui/built` وتُترجم EA 3.12.
> * ذرّة ٧: خُطّ الجذر جُدِّد بعد 37 متقادم + 10 مضاف. `CORE.lock` كان «معدّلًا» في الخط القديم والختم `1.31.0` سليم — **لا reseal**.
> * `forex_runtime` / `crypto_runtime` بلا مانيفست 007 في هذا الزيب (لا Junction).
> * لا `git push`. لا اعتماد حي من هذا التغليف.
>
> **الشجرة النافذة الآن هي `docs/docs/` فقط.** ما يرد لاحقًا عن حادثة
> `docs/اكمل من هنا` سجل تاريخي، وليس توجيهًا لإنشاء تلك الشجرة من جديد.

---

> ⚠️ **تصحيح مؤرَّخ — 2026-09-01 13:15 · مقاس على ويندوز بعد دمج الخطّ السحابي (الكبسة `fa89e8ac127b`)**
>
> موانع الاعتماد أدناه قيست كلّها **على لينكس**، والسجلّ الوارد قال ذلك صراحةً.
> أُعيد قياسها على جهاز المالك:
>
> | البند | الورقة تقول | **المقيس على ويندوز** |
> |---|---|---|
> | A1 سلّة المستودع | `875 passed, 2 skipped` | **896 نجحت · 1 متخطّاة · صفر فشل** |
> | A3 بناء لوحة الجذر غائب | `check_project.py` يفشل عليه | **✅ يُغلق** — الفحص ناجح و`governance/ui/built/index.html` موجود |
> | A6 `SHA256SUMS.txt` | مفتوح | **مفتوح كما هو** — لم يُجدَّد، بوّابة لم يرفعها المالك |
> | A8 الجاهزية الحيّة | `8020` غير قابل للوصول · الخزنة `NOT_CONFIGURED` | **الخزنة `READY`** (`vault_present=True` · `key_count=3`) و**`8020` يستمع**. يبقى مفتوحًا: إثبات تنفيذ حيّ عبر وسيط |
>
> **⚠️ حالة تشغيل مختلطة (مقاسة 13:17):** نواة الفوركس `8010` (PID 16408)
> تعمل منذ **2026-08-31 21:11** — أي بكود **ما قبل** الدمج. أمّا `8020`
> و`8090` و`8098` و`8099` فبدأت **13:13 اليوم** بالكود الجديد.
> **إعادة إقلاع `8010` بيد المالك.**
>
> **وبند ٦ القديم** (مانيفست حارس الكريبتو «مفقود») أُغلق بالقياس: لم يكن
> ملفًّا ناقصًا بل مسارًا ينقصه مقطع `قسم 2001-2050/` في `baseline_regen.py`.
>
> التفصيل الكامل: `١١_المناوبات/NQ_فوركس/2026-09-01 13-12 — دمج الخطّ السحابي وإثباته على ويندوز (ختم NQ).md`

---

> ⚠️ **تحديث ثانٍ — 2026-09-01 14:20 · الكبسة `70a4852`**
>
> **أُغلق بالقياس:**
> * **A8 (الجاهزيّة الحيّة)** — الخزنة `READY` والمنافذ السبعة تستمع.
>   يبقى منه إثبات تنفيذ حيّ عبر وسيط وحده.
> * **البند ١٢ (بوت الأخبار «لم يسحب منذ ستّة أيّام»)** — **بائت**:
>   المقيس اليوم `last_news_fetch = 2026-09-01 13:36` · 177 خبرًا مسحوبًا
>   · 30 محفوظًا · **صفر خطأ 409**. البوت يعمل ببيئته المعزولة.
>
> **فُتح جديدًا — قرار المالك:**
> * **زرّ `٨_كلشي_بزر_واحد.bat`** موثَّق بـ`POINT_ZERO.md` وغير موجود
>   بالشجرة الحيّة. يعيش على وسم `archive/2026-09-01/work` وحده.
>   **يُجلَب أم يُشطب من الورقة؟**
>
> **وتنظيف المستودع:** ستّة فروع أُغلقت بعد أن وُسم كلٌّ منها
> (`archive/2026-09-01/*`) — لا فقد. `master` وحده الآن، والشجرة الحيّة
> مطابقة له حرفًا بحرف.
>
> **وثلاثة فخاخ دخلت `١٤_الأعطال_المعروفة`:** نهايات `LF` بملفّات `.bat` ·
> عربيّ فوق `chcp` · وخدمة تعمل ببيئة ناقصة والفشل مبتلَع.
>
> التفصيل: `١١_المناوبات/NQ_فوركس/2026-09-01 14-13 — تنظيف الجذر وعزل البيئة وزرّا الجذر (ختم NQ).md`

---

## 🔴 موانع الاعتماد الباقية بعد إصلاحات ختم `NQ`

| # | البند | الدليل المقاس | الحالة |
|---|---|---|---|
| A3 | بناء لوحة الجذر غائب | `check_project.py` يفشل على `governance/ui/built/index.html` | مفتوح |
| A6 | `SHA256SUMS.txt` غير جارٍ | القياس بعد الإصلاح: `4482` مدخلة؛ `3325` مطابق، `649` مفقودة، `508` مختلفة | مفتوح |
| A7 | عقد بيانات كريبتو مزدوج | `ROOT/var/crypto` مقابل `crypto_runtime/var` | مفتوح |
| A8 | الجاهزية الحية غير مثبتة | كريبتو `8020` غير قابل للوصول، والخزنة `NOT_CONFIGURED` | مفتوح |
| A9 | معمارية الإطلاق غير موحدة | سوق مستقل/لوحتان/hub بحسب نقطة الدخول | يحتاج قرارًا وتوثيقًا |
| A10 | عقد كريبتو القديم يخالف المانيفستات | `unified_release.json` يقول full auto=false و`77`؛ الواقع `80` وفي full الموجود `22 auto` | مفتوح |

`verify_unified.py = READY` يبقى صحيحًا في نطاقه، ولا يغلق A3/A6–A10.

### ما أُغلق بختم `NQ — ابدأ بالكود فقط`

- A1: سلة المستودع صارت `875 passed, 2 skipped`.
- A2: سلة كريبتو صارت `237 passed`؛ أُصلحت 1001 و2103 و2719.
- A4/A5: أُصلحت الأداة وجُددت الخطوط: root `1300`، Forex `1300`، Crypto
  `642`؛ كلها `stale=0 removed=0 added=0 scope=مطابق`.
- صُحح مسار الجذر في `50` مشغل اختبار كريبتو؛ القياس `50/50` ناجح.

### سياسة التشغيل اليدوي التي لم تُغيّر

الذرّات `2702`, `2707`, `2720` ما زالت `startup_mode: manual`. لم تُفعّل،
ولم يُفصل إطلاق السوقين بقرار تنفيذي. يبقى الاختيار بينهما بيد المالك.

---

## 🔴 بانتظار قرار المالك (لا يتقدّم بلا كلمته)

| # | البند | القسم | الأثر |
|---|---|---|---|
| ٢ | **مجلّد `QUANT_NQ_RC7`** — إبقاء أم نقل؟ **⚠️ لم يعد موجودًا بالجذر** (مقاس 17:05) — يُغلق البند إن أكّد المالك | مشترك | لم يُلمس |
| ٣ | **قفل نسخ المكتبات** (انجراف الإصدارات) — أين يُكتب؟ | مشترك | مناقشة توقّفت لتعارضها مع التجميد |
| ٤ | **ورقة Scalping v3.0 — المراحل B–H** — B (Baseline في 811) حيًّا · **C (تحليل) تمّت ✅ PASS مقاس** · D–F (قرار/Dial/موقف) تالية بالترتيب وبقياس قبل/بعد | مشترك | مرحلة A + C تمّتا ✅ — D (413/451–458) هي التالية |
| ٩ | **حالة التجميد `FRZ-20260827-F39E291C`** — الورقة تقول مجمّد، والكود يقول أمر المالك «حلها وفك تجميد» (نواة 1.27.1، ٢٨-٠٨). **ولا أثر للختم في أي ملفّ كود أو إعداد** (بحث كامل 17:05) | مشترك | سؤال مفتوح على المالك |

## 🟡 فحص مطلوب (لم يُنفَّذ بعد — لا تفترض النتيجة)

| # | البند | القسم | الأولويّة |
|---|---|---|---|
| ٥ | الذرّات التسع المعرَّضة لطوابع MT5 (#116 #552 #581 #582 #583 #618 #575 #601 #609) | مشترك | متوسّطة |
| ٧ | `#615 BRIDGE_UNREADABLE` — تنافس قفل SQLite (غير قاتل، #616 يقرأ بنجاح: `news=364 calendar=500`) | مشترك | منخفضة |
| ٨ | `#717 ROWS_IN_THE_FUTURE` — يُرجَّح نفس جذر الساعة، **لم يُؤكَّد فرديًّا** | مشترك | متوسّطة |
| ١٠ | **`#622 FIX_LOGON_REFUSED_NO_RETRY: LOGOUT: RET_INVALID_DATA`** — الهويّة بالمانيفست **مطابقة لإعدادات الوسيط حرفيًّا** (أكّدها المالك 18:28: `live-uk-eqx-01` · 5211 SSL · `live4.icmarkets.6175701` · `cServer` · `QUOTE`). المتغيّر الوحيد: قيمة `ctrader_fix_password` بالخزنة يجب أن تكون كلمة سرّ حساب **6175701** | فوركس | عالية |
| ١١ | **`#619 ACCOUNT_STATE_STALE`** بينما `account_v2` بالجسر محدَّثة من ثانية واحدة (مقاس 17:55) | فوركس | عالية |
| ١٢ | **بوت الأخبار**: `last_news_fetch = 2026-08-25 23:40` — لم يسحب خبرًا منذ ستّة أيّام رغم أنّ المنصّة تعمل، والتقويم يُكتب اليوم. `macro_bridge_available=false` | فوركس | عالية |

## ⚪ معروف ومقبول (ليست أعطالًا — لا تُشخَّص من جديد)

- `#609 open=0` · `#611 read=0` · `#601 written=0` — **صحيحة**: لا مراكز
  مفتوحة ولا صفقات جديدة.
- `#578 PAIR_RETRY_EXHAUSTED` — **لا يعيد المحاولة تلقائيًّا** بالتصميم؛
  يحتاج تفعيلًا يدويًا جديدًا من اللوحة.
- `#468 allowed=1` — قائمة السماح فيها `BTCUSD` وحده بالمانيفست؛ أي رمز آخر
  يُحجب عند بوابة الأصول. **تفعيل الرمز بيد المالك من اللوحة.**
- `#7 File Integrity` أُغلق بختم `NQ`: root وForex `1300`، وكريبتو `642`،
  والفحص الصارم يرجع صفرًا بلا stale/removed/added.

---

## ✅ أُغلق (لا تُعِد العمل عليه)

| البند | متى | النتيجة |
|---|---|---|
| **اللوحة لا تُنذر عند سقوط النواة** | 2026-08-31 17:49 | ✅ **الجذر: `transport.ts` كان يُصفّر مؤقّت الإعلان (١٠ث) عند كل إغلاق وإعادة المحاولة كل ٢ث ⇒ لا يصل أبدًا.** أُصلح بختم `NQ` وأُثبت حيًّا: الشريط الأحمر يظهر وتتجمّد الصفحة |
| **المساران يفتحان كريبتو بدل فوركس** | 2026-08-31 18:00 | ✅ **الجذر: كوكي `QUANT_MARKET` بلا رقم منفذ، والمتصفّح لا يفصل الكوكي بالمنفذ.** صار `QUANT_MARKET_<المنفذ>` + `alternate_port` صحيح. مُثبت بأربع حالات |
| **تلغرام ٦١٠ لا تُشغَّل من الزرّ المحلّي** | 2026-08-31 18:00 | ✅ أُضيفت إلى `launch_market.py` (قفل 8098، علَم `--no-telegram`). البوت حيّ `@muhammed_nasdaq_bot` — **بقي الاقتران بيد المالك** |
| **`check_telegram.py` يقول «متوقّفة» والمنصّة تعمل** | 2026-08-31 18:05 | ✅ كان يقيس بـ`connect_ex` على منفذ قفل صامت (`listen(1)` بلا `accept`) فيمتلئ الطابور. صار `lock_held()` |
| **ساعة خادم الوسيط (3 ساعات)** | 2026-08-31 16:43 | ✅ **حُلّت** — قياس حيّ من `nq_brain.db` أظهر فرق 1.6 ثانية |
| **اختبار الحياة (مرحلة A) — Scalping v3.0** | 2026-08-29 01:50 | ✅ PASS مقاس |
| **النماذج الأربعة غير الجاهزة بـ#359** | 2026-08-29 01:35 | ✅ أُغلقت بالقياس — قرار المالك مطلوب لفتح المسار العاديّ |
| النظام لا يقلع بعد فكّ ضغط | 2026-08-28 14:30 | ✅ وصفة إزالة روابط Junction |
| النشر يتوقّف كل مرّة | 2026-08-28 ~16:40 | ✅ السكربت يُصلح نفسه |
| زرّ غرفة القيادة لا يعمل | 2026-08-28 20:37 | ✅ ختم `NQ` |
| اعتماد المُعامِلات الستّة | 2026-08-28 ~18:30 | ✅ `provisional=false` |
| فوضى مجلّد `docs` | 2026-08-28 21:09 | ✅ 301 ورقة مرتّبة |

---

## 🚨 حادثة التوثيق — 2026-08-31

كان مجلّد `docs` يحمل **نسختين متطابقتين**: `docs\docs` و`docs\اكمل من هنا`.
بين 17:05 و18:15 فُرّغت `docs\docs` بالكامل — وهي التي كانت تحمل تحديثات
16:43. أُعيد البناء من مصدرين:

* **7 ملفات** من `D:\داتا\مشروع-للقراءة` (نسخة 16:17).
* **ورقتان** (مناوبة 16:43 + هذه القائمة) أُعيدت كتابتهما من نصّهما كما قُرئ
  الساعة 17:05 في نفس الجلسة — ومعلَّمتان بذلك صراحةً.

**تصحيح لاحق نافذ:** الشجرة الحية الوحيدة في الالتزام الحالي هي
`docs\docs`. حُذفت `docs\اكمل من هنا` بوصفها نسخة مكررة. لا تُنشئ نسخة
ثانية من التوثيق؛ ما سبق يصف لحظة الحادثة فقط.

**وأُضيفت ورقة جديدة تسدّ ثغرة قديمة:**
`١٦_الخرائط_والمراجع/مشتركة/٠٠ المشاريع الشقيقة — ما هو خارج هذا المجلّد.md`
— لأنّ **`C:\Users\NQ\QUANT_NQ_NEWS` (بوت الأخبار) لم يكن مذكورًا في التوثيق
ولا مرّة واحدة** (مقاس: صفر تطابق)، فكانت كل مناوبة تجهله. حكم المالك:
«كلكم ما بتعرفوا لأنّو ما بتحطّوا ورقة بتقول في ملفات وأوراق تدلّ على مشروع
الأخبار».
