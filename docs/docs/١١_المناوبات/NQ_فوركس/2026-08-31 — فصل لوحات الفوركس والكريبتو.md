# 2026-08-31 — فصل لوحات الفوركس والكريبتو

## أمر المالك

تشغيل الفوركس والكريبتو كلٌّ على لوحة وعنوان مستقل، مع ثلاثة أزرار:

- تشغيل الفوركس فقط.
- تشغيل الكريبتو فقط.
- تشغيل الاثنين معًا، مع بقاء كل قسم معزولًا.

## التنفيذ

- أُضيف `scripts/launch_market.py` لإقلاع سوق واحد أو السوقين.
- لوحة الفوركس: `127.0.0.1:8090`، نواة `8010`، حوكمة `8092`.
- لوحة الكريبتو: `127.0.0.1:8091`، نواة `8020`، حوكمة `8093`.
- أُزيل زر التبديل `فوركس ⇄ كريبتو` من الواجهة.
- صار اسم اللوحة يوضح السوق المستقل بدل التبديل.
- أُصلحت أزرار غرفة القيادة لتستعمل المسارات المنفصلة.
- زر الاثنين يشغّل الخدمتين، ولا يدمج اللوحتين.

## العزل

لا يُسمح للوحة الفوركس بقراءة أحداث أو تبويبات الكريبتو، ولا للوحة الكريبتو بقراءة أحداث أو تبويبات الفوركس. لا تغيير في محتوى أو منطق الكريبتو؛ التغيير هو فصل عنوان الإقلاع والواجهة.

## التحقق

- `launch_market.py` اجتاز فحص Python.
- بناء الواجهة: ناجح — 498 وحدة.
- لا أمر تداول أُرسل.
- لا مفاتيح أو أسرار سُجلت.
- لا ZIP أُنشئ.
