# مناوبة NQ — 2026-09-03

**البداية:** 01:34  ·  **النهاية:** 01:45
**القسم:** فوركس
**الأمر الحرفيّ:** «سجل شغلك بي ورق مشروع وحزم ولا تنسى7»

---

## قبل أن أبدأ

- الشارت **مصدرًا** مطبّق من المناوبة السابقة: `exec_chart` · `/gov/exec-candles` · `brokerMarket` · EA `3.12` `CopyRates`×200.
- الذرّات 582 / 613 / 618 / 622 **لم تُمسّ** في شغل الشارت.
- النواة: `freeze_core.py verify` = **سليمة · Core V1.31.0** · 23 ملف · البصمة `1b0d0f6b91505984…`
- `tools/baseline_regen.py --check` قبل التجديد: **1310** عنصرًا · متقادم **37** · مزال **0** · مضاف **10**.
- المفتاح الذي أشار إليه المالك `file:core/CORE.lock` (عُرض `CORE.locNQ`) **كان ضمن الـ37** لأن خطّ ٧ قديم، **لا لأن الختم انكسر**.

---

## سجلّ الحركة

| الساعة | ماذا | النتيجة |
|---|---|---|
| 01:34 | `freeze_core.py verify` | النواة مطابقة للختم — لم يُعدْ الختم |
| 01:34 | `--check` لجذر ٧ | 37 متقادم + 10 مضاف · منها `file:core/CORE.lock` و`mt5/QUANT_NQ.mq5` و`governance/server.py` و`Charts.tsx` و`engine.ts` |
| 01:34 | نطاق forex_runtime / crypto_runtime | لا مانيفست حارس في هذا الاستخراج (لا Junction) — **لم يُختلق** |
| 01:40 | أوراق ١٠ / ١١ / ١٤ / ٦ / ١٢ / ١٣ | كُتبت |
| 01:42 | `baseline_regen.py` للجذر فقط | خطّ `integrity_baseline.json` يطابق الشجرة بعد شغل الشارت والحسابين التوأم |
| 01:44 | `QUANT_NQ.zip` | زيب نظيف بجانب المجلد · لا أسرار · لا `var` · لا تشغيل ويندوز · لا `git push` |

---

## الاكتشافات

- **٧ يرى `CORE.lock` معدّلًا ≠ النواة مكسورة.** الختم الحالي يطابق الملفات الـ23. الخط القاعدي كان من شجرة أقدم (582/618/لوحة التوأم + الشارت). العلاج: تجديد الخط، **لا** `reseal`.
- `dir:core/::__version__.py` أيضًا في قائمة المتقادم — والختم يحمله أصلًا. نفس الحكم.
- `forex_runtime/` هنا `.gitkeep` + `integrity_baseline.json` + `var/` فقط. سكربت التجديد يفشل على النطاقين التشغيليين **لأن المانيفست غير موجود في الاستخراج**، لا لأن ذرّة ٧ عاطلة.

---

## ما أُنجز

> توثيق شغل الشارت + تجديد خطّ ٧ للجذر + تغليف `QUANT_NQ.zip`.

## ما بقي مفتوحًا

> رؤية الشارت على ويندوز: إعادة بناء `governance/ui/built` + ترجمة EA 3.12 في MetaEditor. الحزمة تحمل المصدر والـ`built` **القديم** (`index-DwVvlIy4.js`).

## ⚠️ تحذير للشريك

> لا تقرأ هذا الزيب كاعتماد تداول حي. لا `git push` من هذا الصندوق. لا تعدّل 582/613/618/622 «لتصحيح الشارت». لا تُعد ختم النواة لأن ٧ ذكر `CORE.lock`.
