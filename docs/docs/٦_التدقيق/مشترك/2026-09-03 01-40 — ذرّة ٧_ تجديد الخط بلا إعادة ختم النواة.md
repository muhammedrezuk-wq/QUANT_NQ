# ذرّة ٧ — تجديد الخط القاعدي بلا إعادة ختم النواة

**التاريخ:** 2026-09-03 01:40
**الأمر:** «ولا تنسى7» مع إشارة المالك `modified: file:core/CORE.locNQ` = المفتاح `file:core/CORE.lock`.

---

## القياس قبل التجديد

```
عناصر الحرس: 1310 (الحد الأدنى 700)
متقادم: 37 · مزال: 0 · مضاف: 10 · النطاق: مطابق
--check: يرفض التجديد
```

`python3 governance/scripts/freeze_core.py verify`:

```
النواة سليمة ومطابقة للختم — Core V1.31.0
23 ملف، البصمة 1b0d0f6b91505984…
```

بصمة `core/CORE.lock` على القرص ≠ البصمة داخل `integrity_baseline.json` القديم. الختم **نفسه** يطابق ملفات `core/`.

**الحكم:** انحراف خطّ ٧، لا كسر تجميد. **لم يُستدعَ freeze/reseal.**

---

## المتقادم (37) — باختصار

شغل سابق في هذا الاستخراج لم يُجدَّد خطّه: 516 · 582 · 613 اختبار · 616 · 618 · 619 · `launch_market.py` · `stop_all.py` · `governance/server.py` · `run_core.py` · لوحة التوأم (App/styles/engine/sections/store/transport + تبويبات) · `Charts.tsx` · `mt5/QUANT_NQ.mq5` · `core/__version__.py` · **`file:core/CORE.lock`**.

## المضاف (10)

`scripts/e2e_runner.py` · `financial_validation.py` · `product_gate.py` · `product_gate_20point.py` · `product_gate_verify.py` · `tools/build_architecture_atlas.py` · `FeedLeds.tsx` · `AccountsBar.tsx` · `Backtest.tsx` · `Lab.tsx`

## نطاقات التشغيل

`forex_runtime` و`crypto_runtime` في هذا الزيب **بلا** مانيفست 007 (لا ذرات منعكسة). `baseline_regen.py` يطبع فشل نطاق — **متوقع لهذا الاستخراج**. الجذر وحده يُجدَّد.

---

## ما نُفِّذ

`python3 tools/baseline_regen.py` على نطاق الجذر (كتابة `integrity_baseline.json`). ذرّة ٧ نفسها (`atom.py` / المانيفست / الإصدار 4.2.0) **لم تُعدَّل**.
