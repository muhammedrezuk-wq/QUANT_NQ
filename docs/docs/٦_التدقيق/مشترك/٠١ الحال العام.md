# ١ · الحال العام — لقطة تاريخية

> **مستبدل للحالة الحالية:** هذه قياسات جهاز حي بتاريخ 2026-08-25 ولا تصف
> checkout الحالي. راجع القائمة الحية ومناوبة تدقيق 2026-09-01. لا تنقل
> `SECURITY_STATE=READY` أو أعداد الذرّات أدناه إلى تقرير حالي دون قياس جديد.

**تاريخ القياس التاريخي:** 2026-08-25 05:00 — من النظام الحي آنذاك.

```
النواة            ok · CORE_VERSION 1.17.0   (قياس الفجر — ليلًا صارت 1.21.1)
على القرص         225 مجلَّد ذرّة        ← أُضيفت 811 عدّاد سلوك القرار
محمَّلة            221 — وكلّها running
مستبعَدة          4   ·  107 · 256 · 257 · 258
الصحّة             210 سليمة · 10 متعثّرة · 1 خلل
فشل الإقلاع       صفر  ·  فشل المسح: صفر
المدقّق الرسميّ    225 ذرّة · **صفر مخالفات** · 18 تحذيرًا
اللوحة            تعمل — والروم يظهر بالأسعار والاتجاهات
```

**الحساب مغلق: 221 + 4 = 225.**

**(تحديث ٢٥-٠٨ ليلًا — ما تبدّل بعد قياس الفجر، مقيسًا من الكود والجسر الحيّ):**
- **النواة 1.21.1** (`core/__version__.py:210`) — أُعيد بناء الناقل صناديق بريد لكل معالج بختم المالك؛ سطر «لم تُمسّ» انتهى نهاره.
- **413 v2.2.0 بختم nq** — «البوّابة المستحيلة» أُغلقت: العتبة صارت قابلة للبلوغ.
- **451 v3.0.1** — روم آخر قيمة لكل مصدر بآفاق طزاجة: الاكتمال 95% وأوّل حكم اتجاهيّ بالتاريخ.
- **MT5 حيّ**: `connected=1` · إكسبرت `3.10` · **أوّل صفقة بتاريخ النظام** — زوج 0.23+0.23 BTCUSD مفتوح بتذاكر حقيقيّة (`commands=5` · `positions_v2=2`) والمطابقة MATCH.

---

## المتعثّرون

```
104  مستقبل الحجم        NO_VOLUME_YET        ← التِكّة الحيّة بلا حقل حجم إطلاقًا
451  تجميع القرار        complete=0 · top_missing=400 · وكل الخمسة غائبون ١٠٠٪
513  تحجيم المركز        STALE_ACCOUNT_SYMBOL_SPECS
580  منفذ الترجيح        NO_GATE_DECISION_YET
618  مصدر جسر MT5        MT5_TICK_FEED_STALE      (ميتاتريدر مطفأ على الجهاز)
619  حالة الحساب         ACCOUNT_STATE_STALE      (نفس السبب)
707  مخزن القرار         database is locked       ← عاد بعد أن كان قد زال ٠٨-٢٢ · السبب لم يُقَس
850  حاكم التغيير        NO_CANDIDATES_YET
```

**(تحديث ٢٥-٠٨ ليلًا: أربعة من القائمة خرجوا من التعثّر — `451` بالروم v3.0.1 كامل 95% · `580` وصله أوّل قرار معتمد ومرّ · `618`/`619` ميتاتريدر متّصل والإكسبرت 3.10 يبثّ. الباقون على قياس الفجر.)**

**و`622` تتذبذب بين سليمة ومتعثّرة بحسب جلسة كل رمز** — والحكم صار يسمّي الرمز الساكت بعد `v1.2.0`،
فما كان يبدو «سقوطًا كل ليلة» هو استراحة يوميّة معلَنة.

## الخلل الواحد

```
7  سلامة الملفّات   modified: … (وليس added)
```

**السبب أُصلح ٢٠٢٦-٠٨-٢٥ (`4.2.0`):** كان `_walk_source` يبني المفتاح بفاصل ويندوز الخلفيّ
بينما الخطّ القاعديّ بفاصل أماميّ (**١٬٢١١ مفتاحًا · ١٬١٢٢ بشرطة أماميّة · صفر خلفيّة**)،
فلا مفتاح يتطابق ويُقرأ كل ملفّ «مضافًا» (`+2233`). صار المفتاح يُطبَّع على POSIX.

**⇒ الحارس صار يقول الصدق: يعرض الآن الملفّات المعدَّلة فعلًا.**
**وما بقي: توليد خطّ أساس جديد بعد كبسات اليوم — وهو قرار المالك، والحارس يبقى أحمر حتى ذلك.**

---

## التغذية — سليمة، والحكم صار صادقًا

```
622  v1.2.0   t=63,983 · d=76,054 · c=21 · j=0 · s=0
     BTCUSD · EURUSD · GBPUSD   تبثّ بعمر أقلّ من ثانيتين
     US30 · US500 · USTEC · XAUUSD   خارج جلستها اليوميّة منذ ٢٣:٤٩
613 · 620 · 621 · 601   سليمة
618 · 619               تنتظران ميتاتريدر (مطفأ على الجهاز وقت قياس الفجر)
```

**(تحديث ٢٥-٠٨ ليلًا: ميتاتريدر حيّ — `connected=1` والإكسبرت `3.10` يبثّ، و`618`/`619` خرجتا من الانتظار.)**

**عطل أُصلح هذه الليلة:** كان حكم الصحّة يعمّم — `if any(row["state"] == DEAD ...)` —
فرمزٌ واحد خارج جلسته يجعل التغذية كلّها `FIX_TICK_DEAD`.
**⇒ كان النظام يظهر «ساقطًا» كل ليلة بين ٢٣:٤٩ و٠١:٠٠ وهو يعمل.**
الآن: كلّهم ميّتون ⇒ `UNHEALTHY`؛ بعضهم ⇒ `DEGRADED` **مع تسمية الرموز الساكتة**.

---

## الحاجز — قياس الفجر، والسبب مقيس بالسطر (سقط ليلًا — التحديث آخر القسم)

```
451   cycles=1,039 · complete=0 · top_missing=400
```

**الجذر: `atoms\413_دمج_الإشارات\atom.py:134`**

```python
active >= self._min_active_weight       #  12.72  ≥  40.0   ✗
```

**والعتبة غير قابلة للبلوغ رياضيًّا:**
`active = (عدد الاتجاهيّات الجاهزة × 14.2857) × context_factor`، و`context_factor` مسقوف
بـ`invalidation_quality.strength ≈ 35.98` ⇒ **0.36**.
حتى لو صارت الاتجاهيّات السبع كلّها `READY`: `100 × 0.36 = 36 < 40`.
وبالمتوسّط المقيس (`cf = 0.2885`): **السقف المطلق 28.85 < 40**.

**المقيس على 186 عيّنة:** `ready = 0` · أقصى `active_weight` = 24.1 · المتوسّط 7.5 – 9.6.

**وجذر ثانويّ:** `150` غائبة في 43/44 دورة لأنّ `166` يُبلغ `direction = null`.
**وحاجز ثالث مستقلّ:** `513` — `NO_ACCOUNT_EQUITY` و`STALE_ACCOUNT_SYMBOL_SPECS`.

**⇒ `min_active_weight` عيار — والعيارات بيد المالك بنصّ رؤيته.**

**(تحديث ٢٥-٠٨ ليلًا: حُسم — `413 v2.2.0` بختم nq أغلقت «البوّابة المستحيلة» والعتبة صارت قابلة للبلوغ، و`451 v3.0.1` روم بآفاق طزاجة اكتماله 95%. النتيجة: أوّل حكم اتجاهيّ وأوّل صفقة بتاريخ النظام مرّا اليوم.)**

---

## المدقّق الرسميّ

```
224 ذرّة · صفر مخالفة · 18 تحذيرًا
   3 مستثناة بحكم المالك:  166 (502) · 454 (459) · 622 (386)
  15 حلقة خلفيّة
```

**⚠️ والتدقيق المستقلّ كشف أنّ المدقّق أعمى في أربعة فحوص** — التفصيل في
`سطح المكتب\00\التقرير الشامل — خمسة وكلاء — 2026-08-25.md`:

```
الأسلوب المضغوط   يرى 0   والمقيس 45 ذرّة · 176 سطرًا
الساعة على حمولة   يرى 0   والمقيس 7 ذرّات
الإسقاط الصامت     يرى 0   والفحص لا ينطلق أبدًا (شرطه يتطلّب خلوّ الملفّ من أيّ عدّاد)
حدّ 350 سطرًا      يرى 3   والمقيس 7 بحساب الملفّات الأخوة (27 ملفًّا · 1,846 سطرًا لا يقرؤها)
```

**ورق الذرّات: 224/224 كامل · 227 ملفّ اختبار.**
**طبقة الأمان:** `SECURITY_STATE=READY` · الخزنة مفتوحة · مفتاحان.

---

**`الدستور/` للقوانين والقيود · `الهندسة/` للمعمار والرؤية**