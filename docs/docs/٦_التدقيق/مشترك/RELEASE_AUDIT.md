# QUANT_NQ — تقرير تاريخي للفحص والتغليف

> **تنبيه نافذ — تدقيق 2026-09-01:** هذه الورقة تسجل فحص حزمة بتاريخ
> 2026-08-31، ولا تصف checkout الحالي عند `270f96bf0eae`. ملف التسليم المذكور
> أدناه غير موجود في مساحة العمل، فلا يمكن إعادة تدقيق محتواه. بعد إصلاحات
> ختم `NQ` صارت سلة المستودع `875 passed, 2 skipped` وكريبتو `237 passed`
> وخطوط السلامة مطابقة، لكن بناء `governance/ui/built/` ما زال غائبًا وفحص
> المشروع يفشل والشيكسَم قديم. لا تُستعمل هذه الورقة وحدها لإعلان الجاهزية.

التاريخ التاريخي: 2026-08-31

## الفحوصات المنفذة

- فحص تجميع Python للمجلدات `core` و`governance` و`atoms` و`transport`: ناجح.
- فحص خاص لذرة ناقل التلمترية 810: ناجح.
- بناء واجهة React/Vite: ناجح، 496 وحدة.
- مطابقة خريطة الأقسام مع مسارات `App.tsx`: 41 من 41.
- لم تُشغّل أوامر تداول أو فتح صفقات.
- لم تُضمّن مفاتيح أو ملفات أسرار تشغيلية في الحزمة.

## ملاحظات غير مانعة

- تحذير البناء الوحيد: حجم حزمة JavaScript أكبر من 500 kB.
- عند تشغيل النواة ظهرت جداول بيانات ناقصة في بيئة الفحص: `ticks_v2` و`symbol_specs_v2` و`account_v2`.
- اختبارات async تحتاج إضافة `pytest-asyncio` في بيئة الاختبار.

## نطاق التغييرات

- إصلاح نشر حالة `telemetry.carrier.state` في ناقل 810.
- إظهار حالة الناقل في التشخيص.
- حفظ التبويب النشط بعد التحديث.
- جعل NQ أول تبويب فوركس.
- جعل شريط التكبير بخطوة 1%.
- إضافة وضع الرجوع إلى اللون الأساسي.
- تحسينات بصرية للشبكة والإعدادات.
- لم تُلمس الرئيسية أو محتوى الكريبتو.

## ملف التسليم

`/home/user/QUANT_NQ_release.zip`

الحزمة تستثني الاعتماديات المولّدة، وملفات التخزين المؤقت، وملفات الأسرار التشغيلية.
