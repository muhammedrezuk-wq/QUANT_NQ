# تقرير شامل — قواعد ميتاتريدر وكل الجسور

**تاريخ القياس:** 2026-08-28
**المنهج:** قراءة مباشرة من `nq_brain.db` + حالة الذرّات الحيّة من `/gov/atoms` + قراءة الكود المصدريّ. صفر تعديل — المشروع مجمّد.

---

## ١) قاعدة البيانات الوحيدة

**`%APPDATA%\MetaQuotes\Terminal\Common\Files\nq_brain.db` — 97 ميغابايت، 18 جدولًا.**
لا توجد أي قاعدة أخرى بمجلّد MT5 المشترك.

### جرد الجداول الكامل (صفوف · عمر آخر كتابة)

| الجدول | صفوف | آخر كتابة | الحكم |
|---|---:|---:|---|
| `account_v2` | 1 | **الآن (0 ث)** | 🟢 حيّ — المصدر النافذ للحساب |
| `display` | 1 | **الآن (0 ث)** | 🟢 حيّ |
| `calendar` | 957 | **13 ثانية** | 🟢 حيّ — الأجندة تتدفّق |
| `news` | 364 | 12.4 ساعة | 🟡 حيّ لكن بطيء (الأخبار حدثيّة لا مستمرّة) |
| `trade_events_v2` | 173 | 20.5 ساعة | 🟡 حيّ — آخر صفقة أمس |
| `ticks_v2` | 42 | — | 🟢 يُقرأ حيًّا (#618 يعمل) |
| `symbol_specs_v2` | 1 | — | 🟢 نافذ |
| `candles_history` | 200 | — | 🟢 |
| `positions_v2` | 0 | — | ⚪ فارغ **صحيحًا** — لا مراكز مفتوحة فعلًا |
| `depth` · `ticks` · `positions` | 0 | — | ⚪ فارغة |
| `commands` | 5 | 72.6 ساعة | 🔴 **مهجور** — لا أمر كُتب منذ 3 أيّام |
| **`account`** (v1) | 1 | **267.6 ساعة** | 🔴 **مهجور تمامًا (11 يومًا)** |
| **`prices`** (v1) | 8 | **267.6 ساعة** | 🔴 **مهجور تمامًا (11 يومًا)** |
| **`trade_events`** (v1) | 165 | **291 ساعة** | 🔴 **مهجور تمامًا (12 يومًا)** |
| `symbol_specs` (v1) | 8 | — | 🔴 مهجور |
| `sqlite_sequence` | 6 | — | ⚪ داخليّ لـSQLite |

### 🔑 النمط الحاسم المكتشَف

**الـEA (نسخة 3.10) هجر كل جداول الإصدار الأول (v1) وانتقل كليًّا لجداول v2
منذ ~11 يومًا.** كل جدول v1 متجمّد على آخر قيمة قديمة، بينما نظيره v2 حيّ.
هذا يفسّر لبسًا وقعتُ فيه سابقًا حين قرأت `account` (v1) وظننت الـEA متوقّفًا —
**كان الـEA يكتب في `account_v2` طوال الوقت.**

---

## ٢) حالة كل الجسور (11 ذرّة) — مقاسة حيًّا

| # | الجسر | الحالة | القراءة |
|---|---|---|---|
| 601 | Brain Bridge Writer | 🟢 | `bridge writable, written=0` — جاهز، لا أوامر تُكتب (متّسق مع `commands` المهجور) |
| 609 | Position Sync | 🟢 | `open=0` — **صحيح**، `positions_v2` فارغ فعلًا |
| 611 | Trade Events Reader | 🟢 | `READY_AWAITING_FIRST_MT5_TRADE_EVENT` — **انتظار مشروع**، آخر صفقة عمرها 20.5 ساعة |
| 613 | Market Feed Hub | 🟢 | يوجّه 7 رموز عبر مسارين |
| **615** | **News Source Bridge** | **🔴 amber** | **`BRIDGE_UNREADABLE`** — التفصيل أدناه |
| 616 | News Bridge Reader | 🟢 | `news=364 calendar=500` — يقرأ بنجاح |
| 618 | MT5 Bridge Source | 🟢 | `published=642` من `ticks_v2` |
| **619** | **Account State Reader** | **🔴 amber** | **`ACCOUNT_STATE_STALE`** — التفصيل أدناه |
| 620 | Yahoo Finance Source | 🟢 | 3 رموز، `published=34` |
| 621 | cTrader Reference Bridge | 🟢 | `forwarded=208` |
| 622 | cTrader FIX Price Feed | 🟢 | `ticks=9488 depth=10479` — 7 رموز |

**9 من 11 جسرًا تعمل بلا خلل. اثنان فقط معطوبان.**

---

## ٣) العطل الأول — #615 `BRIDGE_UNREADABLE`

**تعارض مصدرين مقاس بالكود:**
```
منيفست 615:  db_path = C:\Users\NQ\QUANT_NQ_NEWS\data\news.db   (موجود فعلًا)
run_forex.py: NQ_NEWS_DB = ...\Common\Files\nq_brain.db          (يتجاوز المنيفست)
```
كود 615 (`_resolve_db`) يعطي الأولويّة لمتغيّر البيئة، فيقرأ `nq_brain.db`.
**السبب المرجَّح لـ`BRIDGE_UNREADABLE`: تنافس قفل SQLite** — القاعدة 97 ميغا
يكتب فيها الـEA وبوت الأخبار بكثافة متزامنة.

**⚠️ لكن أثره محدود:** `#616 News Bridge Reader` يقرأ **نفس البيانات بنجاح**
(`news=364 calendar=500`)، والأخبار تصل للنظام فعلًا. **العطل ازدواجيّ لا قاتل.**

---

## ٤) العطل الثاني — #619 `ACCOUNT_STATE_STALE`

**موثّق بالكامل بورقة ٤٢.** خلاصته: الـEA يكتب `updated_at` بـ`TimeGMT()`
(ساعة خادم الوسيط) بينما المشروع يقارنها بوقت UTC رسميّ — والفارق المقاس
مباشرة من محطّة MT5: **10,799.77 ثانية = 3 ساعات بالضبط** — أي **فرق منطقة
زمنيّة ثابت، لا انجراف ولا تلاعب.**

**البيانات نفسها سليمة تمامًا:** `account_v2` عمره **0 ثانية**، ورصيده 588.27
يطابق شاشة MT5 حرفيًّا. **الرفض شكليّ (طابع زمنيّ)، لا موضوعيّ.**

---

## ٥) الخلاصة

```
✅ قاعدة واحدة (97MB) · 18 جدولًا · جداول v2 كلّها حيّة
✅ 9 من 11 جسرًا تعمل بلا خلل
🔴 عطلان فقط — كلاهما موثّق وغير قاتل:
     #615 تنافس قفل (والبيانات تصل عبر #616)
     #619 فرق منطقة زمنيّة 3 ساعات (والبيانات طازة فعلًا)
🔑 اكتشاف: كل جداول v1 مهجورة منذ 11 يومًا — أي قراءة منها تعطي صورة كاذبة
```

**لا تنفيذ — توثيق وقياس فقط. أي إصلاح يحتاج فكّ تجميد وختم `NQ`.**
