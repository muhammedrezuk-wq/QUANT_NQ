# مطابقة شجرة الكريبتو مع ملفّ أحمد — بايت ببايت

**الوقت:** 2026-08-29 14:10 · **المناوب:** NQ · **الختم:** `NQ`

---

## السبب (بلسان المالك)

> «هو سلّمني هذا الملف وقال: هذا شغلي، الباقي عليك. وهو **ما شاف مشروعنا أبدًا**،
> وملفّه عم يفتح صفقات. خايف ما يستلم ملفّي ولا يعرف شو صار — **لهيك لازم ما يختلف**.»

## القياس قبل المطابقة

المرجع: `Desktop\احمد\crypto_runtime\atoms` (شجرته الحيّة — 84 ذرّة).
*(كنتُ قارنت أوّلًا `crypto_runtime\atoms_crypto` وهي شجرة أقدم عنده — 77 ذرّة
بلا طبقة الاستراتيجيّة. **خطأ منّي صُحّح بالقياس.**)*

| الاختلاف | العدد | مصدره |
|---|---:|---|
| **كود ذرّة (`atom.py`)** | **صفر** | — |
| `startup_mode` (`manual → auto`) | 51 | **أنا** |
| `#2621` `warmup_bars` (200 → 600) | 1 | **أنا** |
| ذرّتان زائدتان: `#2160` · `#2608` | 2 | **أنا** |

**✅ ولا سطر من كود أحمد تغيّر — الاختلاف كلّه إعدادات أضفتُها.**

## لماذا كان إعداده صحيحًا وإعدادي التفافًا

`#2153` (مستويات الأمس) تحتاج **عبور منتصف الليل** في تدفّق الشموع.
عند أحمد **كلاود يعمل متواصلًا** ⇒ يمرّ اليوم فعليًّا ⇒ تُحسب المستويات
بـ`warmup_bars: 200` بلا مشكلة. **نظامنا يُعاد تشغيله باستمرار** ⇒ لا يرى
تدوير يوم ⇒ رفعتُ الرقم إلى 600 (50 ساعة) **كالتفاف على بيئتنا، لا كتصحيح لعمله.**

## ما نُفِّذ

1. **نُسخت ملفّاته فوق ملفّاتنا** لكل ذرّة مشتركة يختلف فيها أي ملفّ —
   **52 ملفًّا**، مع نسخة احتياطيّة لكل ملفّ استُبدل.
2. **`#2160` و`#2608`** (ليستا في شجرته) **نُقلتا خارج المشروع — لم تُحذفا.**

```
نسختنا قبل المطابقة  →  Desktop\نسختنا قبل المطابقة مع أحمد 2026-08-29
الذرّتان المنقولتان   →  Desktop\ذرّات ليست في شجرة أحمد 2026-08-29
```

## التحقّق — بعد المطابقة

```
ذرّات أحمد: 84   |   ذرّاتنا: 84
عنده وليس عندنا : 0
عندنا وليس عنده : 0
اختلاف كود      : 0
اختلاف إعداد     : 0
مطابقة حرفيًّا  : 84 / 84   (بصمة SHA-256 لكل ملف .py و .yaml)
```

**التشغيل بعدها:** 33 ذرّة كريبتو · 29 أخضر · 4 كهرمانيّ · **صفر أحمر**
(نفس ترتيب عدده المسجَّل: 34 نشِطة). محكمة الزناد 🟢 54 تأكيد · صفر نقض.

## الأثر المقبول عمدًا

- **51 ذرّة بنية تحتيّة عادت نائمة** (`manual`) — كما قرّرها أحمد بعد تحقّق
  موثّق («نائم عمدًا لغياب مصدر بيانات حقيقيّ»).
- **`#2274` يبقى `emitted=0`** — سلسلة التوصيات لن تُغلق في بيئتنا حتى يعمل
  النظام متّصلًا عبر منتصف ليل. **هذا ثمن المطابقة، وقرار المالك.**

## قاعدة مستمرّة

**شجرة `atoms_crypto` مرآة لملفّ أحمد.** أي تغيير فيها يجب أن يُتّفق معه أوّلًا،
وإلا رفض استلام الملفّ. **التجارب على الكريبتو تُجرى على نسخة خارج المشروع.**
