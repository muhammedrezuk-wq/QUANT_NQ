# 2026-08-31 18:55 — الأخبار تصل وتُعرَّب · استرجاع التوثيق · سكربت النشر

**المناوبة:** NQ / فوركس · **الختم:** `NQ`. تكملة لورقة 18:15.

---

## ١) تبويب الأخبار كان فارغًا — والجسر ممتلئ

**العَرَض:** «آخر الأخبار — 0 · ما وصل خبر بعد — الجسر ما كتب شيئًا» + سطر
أحمر «جسر التداول غير موجود» — حتى بعد رفع كلّ الفلاتر إلى «الكل».

**سلسلة القياس (لا تخمين):**
1. `nq_brain.db` فيه **391 خبرًا** آخر كتابة 18:20:04، و**1130 حدث تقويم**
   آخر كتابة 18:26:38 — الجسر ممتلئ وطازج.
2. الذرّات سليمة: 108 `published=49` · 616 `news=364 calendar=500` ·
   والكريبتو 2108 `published=55` · 2615 `polls=1`.
3. لكن `/gov/news` و`/gov/calendar` يردّان `{"available": false, "news": []}`
   — من الخادم مباشرة (8092) لا من الهبّ.

**الجذر:** `scripts/run_governance.py` كان يفرض
`os.environ["NQ_NEWS_DB"] = ROOT/"var"/<السوق>/"news.db"` و`NQ_BRIDGE_DB` على
`bridge.db` — **والثلاثة لم توجد قطّ** (مقاس). و`server.py` يقرأ `NQ_NEWS_DB`
أوّلًا، فالفرض يدوس الافتراض الموثَّق (جسر ميتاتريدر) ⇒ `TRADE_DB.is_file()`
تساوي False ⇒ `available:false`.

**وهذا نفس العطل الذي يصفه تعليق `server.py:63-67` كمُصلَح** («كانت تجعل
`/gov/news` يكذب: available:false رغم وصول مئات الأخبار») — أُصلح هناك
بالافتراض، ونُقض هنا بالفرض.

**الإصلاح:** حُذف الفرضان. **الإثبات:** `available: true` مع أخبار وأحداث
حقيقيّة، والشريط الأحمر اختفى، واللوحة امتلأت.

> **درس:** لا يُفرَض مسار قاعدة بيانات في مُشغِّل إن كان الخادم يحمل افتراضًا
> موثَّقًا. الفرض يغلب الافتراض دائمًا، ويُبطل إصلاحًا سابقًا بلا أن يمسّه.

---

## ٢) الأخبار كانت إنكليزيّة — والمالك لا يقرأ الإنكليزيّة

**قاعدة ثبّتها المالك:** «ثبّت قاعدة أنا ما بعرف إنكليزي».

**الجذر الأوّل:** حقل `headline_ar` يعود فارغًا دائمًا. مكتبة
`deep_translator` **غير منصَّبة وغير مذكورة في `requirements.txt`**،
و`_translate_worker` يبتلع `ImportError` بصمت (`except Exception: return`) —
حارس بلا مفتاح: لا ترجمة ولا أثر في أيّ سجلّ.
**الإصلاح:** المكتبة في المتطلّبات + إنذار يُطبع مرّة واحدة عند غيابها.

**الجذر الثاني (أخطر):** شرط قبول الترجمة كان «غير فارغة وتختلف عن الأصل»،
فمرّت صفحة خطأ من الخدمة نصّها `Error 500 (Server Error)!!1…` وخُزّنت **ترجمةً
دائمة** (الذاكرة تخزّن مرّة للعمر). **٨ من ٣٠ مدخلة كانت مسمومة.**
**الإصلاح:** الترجمة تُرفض ما لم تحمل حرفًا عربيًّا واحدًا على الأقلّ
(`_has_arabic`)، والمسمومة حُذفت فأُعيدت ترجمتها صحيحة.

**الإثبات:** ١٢ من ١٢ عنوانًا معرَّبة ونظيفة على اللوحة.

---

## ٣) حادثة التوثيق — استُرجع

شجرة `docs\docs` (وكانت الأحدث) فُرّغت بين 17:05 و18:15. استُرجع:
**٧ ملفات** من `D:\داتا\مشروع-للقراءة` (نسخة 16:17) · **ورقتان** أُعيدت
كتابتهما من نصّهما كما قُرئ 17:05 (مناوبة 16:43 + القائمة الحيّة) ومعلَّمتان.
**الشجرة الحيّة الوحيدة الآن: `docs\اكمل من هنا`.** لا تُنشأ نسخة ثانية —
التكرار هو ما جعل الحذف غير ملحوظ.

**وأُنشئت ورقة تسدّ ثغرة قديمة:**
`١٦_الخرائط_والمراجع/مشتركة/٠٠ المشاريع الشقيقة — ما هو خارج هذا المجلّد.md`
— لأنّ `C:\Users\NQ\QUANT_NQ_NEWS` (بوت الأخبار) **لم يُذكر في التوثيق ولا
مرّة** (مقاس: صفر تطابق)، فكانت كل مناوبة تجهله.

---

## ٤) سكربت النشر `Desktop\نشر QUANT_NQ\publish_private.ps1`

**العطل:** يتوقّف عند `error: No such remote 'origin'` على نسخة نظيفة.
**الجذر:** تحت `$ErrorActionPreference='Stop'`، يلفّ ويندوز باورشيل 5.1 كلّ
سطر stderr لأمر خارجيّ في `ErrorRecord` فيصير استثناءً قاتلًا — مع أنّ فشل
`git remote get-url origin 2>$null` **مقصود ومعالَج بالسطر التالي**.
**الإصلاح:** سؤال `git remote` عن القائمة بدل انتزاع خطأ، و
`git rev-parse --quiet --verify HEAD` بلا توجيه stderr.
**الإثبات:** في مستودع تجريبيّ، النمط القديم رمى الاستثناء نفسه حرفيًّا،
والجديد مرّ بسلام.

---

## ما أُنجز
عُدّل: `scripts/run_governance.py` · `governance/server.py` ·
`requirements.txt` · و`Desktop\نشر QUANT_NQ\publish_private.ps1` (خارج
المشروع). نُصّبت `deep-translator` في `venv`. أُعيد توليد خطّي الأساس.
**الاختبارات: 1957 نجحت · 1 تخطّت · 0 فشلت.**

## ما بقي مفتوحًا
* **اقتران تلغرام ٦١٠** — الرمز يُعرض في مخرَج المنصّة، بيد المالك.
* **622:** `RET_INVALID_DATA` — الهويّة مطابقة لإعدادات الوسيط حرفيًّا
  (أكّدها المالك)، فالمتغيّر الوحيد قيمة `ctrader_fix_password` لحساب 6175701.
* **619** `ACCOUNT_STATE_STALE` · **468** `allowed=1` · `crypto_runtime` بلا
  حارس سلامة.
* بوت الأخبار: `last_news_fetch` عالق على 2026-08-25 رغم أنّ `last_news_pull`
  يتحدّث — حقل حالة لا يُحدَّث في أحد المسارين.

## تحذير للشريك
سطر الإنكليزيّة الصغير تحت العنوان العربيّ **مقصود** (الأصل تحت الترجمة —
قاموس صادق لا قناع). إن أراد المالك إخفاءه فهو قرار عرض لا إصلاح عطل.
