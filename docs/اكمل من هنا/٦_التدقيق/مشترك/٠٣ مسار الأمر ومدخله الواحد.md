# مسار الأمر — من القرار إلى الوسيط

```
451 → 452 → 453 → 454 → 466 → 467
                              ↓ decision.gate.passed
581 → 583 → 578 → 586 → 585 → 516 → 551 → 584 → 552  ← البوّابة الوحيدة (تسع بوّابات)
                              ↓ trading.final_decision
601 → صفّ SQLite بحالة PENDING (مؤشّر نتائج دائم على القرص)
                              ↓
الإكسبرت QUANT_NQ.mq5 v3.10 → أمر حقيقيّ عند الوسيط (يعيد التذكرة في نتيجة الأمر)
```

**(تحديث ٢٥-٠٨ ليلًا: أُعيد رسم السلسلة على المانيفستات الحيّة — `467` بوّابة القرار تنشر
`decision.gate.passed` إلى `581` (سلّم الدرجات) ثم `583` (اللقطة) و`578` (الأزواج بذاكرة دائمة)
و`586` (حلّ الرمز) و`585` (الهامش) و`516` (القاطع) قبل البناء `551` والشرعيّة `584` والمدقّق `552`.
والإكسبرت اليوم v3.10.)**

**مثبَت بالقياس:** `trading.final_decision` **تنشرها `552` وحدها**. والسبعة الأخرى
(`360` · `522` · `550` · `560` · `563` · `601` · `707`) تسمعها ولا تنشرها
*(تحديث ٢٥-٠٨: كانت ثمانية بعدّ `626` — ولا وجود لمجلّد `626` في `atoms/`)*.
⇒ **مدخل واحد لمسار الأمر — لا باب خلفيّ.**

---

**`الدستور/` للقوانين والقيود · `الهندسة/` للمعمار والرؤية**
