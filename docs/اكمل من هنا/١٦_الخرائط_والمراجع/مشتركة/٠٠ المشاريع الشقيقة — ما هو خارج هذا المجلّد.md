# ٠٠ — المشاريع الشقيقة: ما هو خارج هذا المجلّد

> **أُنشئت 2026-08-31 18:25 بختم `NQ`.** سببها حكم المالك حرفيًّا:
> «كلكم ما بتعرفوا لأنّو ما بتحطّوا ورقة بتقول في ملفات وأوراق تدلّ على مشروع
> الأخبار». **مقاس وقتها: صفر ذكر لـ`QUANT_NQ_NEWS` في كامل التوثيق.**

**تُقرأ مع `٠٠ اقرأني — البروتوكول الإلزامي`.** كل من يبدأ مناوبة يظنّ أنّ
`C:\Users\NQ\QUANT_NQ` هو كلّ النظام — وهذا خطأ. تحته جدول ما هو خارجه.

---

## ١) `C:\Users\NQ\QUANT_NQ_NEWS` — بوت الأخبار (مشروع مستقلّ)

مشروع بايثون منفصل تمامًا: بيئته `.venv` الخاصّة، وإعداداته `.env` الخاصّة،
ولا يستورد سطرًا من النواة. **يغذّي لوحة الأخبار في غرفة القيادة.**

| | |
|---|---|
| التشغيل | `بوت الأخبار.bat` بجذره (يجهّز `.venv` ويثبّت ثمّ يشغّل `main.py`) |
| قفل النسخة الواحدة | `data/bot.lock` — نسختان بنفس التوكن ⇒ تلغرام يرفض بـ409 |
| السجلّ | `logs/bot.log` |
| الحالة الحيّة | `data/system_state.json` (آخر سحب أخبار · آخر تقويم · آخر نبضة) |
| الجسر إلى النظام | `bridge_writer.py` يكتب في `nq_brain.db` جدولَي `news` و`calendar` |
| مفاتيح `.env` | `BOT_TOKEN` · `OWNER_CHAT_ID` · `NQ_BRAIN_PATH` · `FINNHUB_API_KEY` · `CAL_CURRENCY` · `CAL_MIN_IMPACT` · `CAL_ALERT_MINUTES` |

### كيف يصل الخبر إلى اللوحة
```
QUANT_NQ_NEWS (main.py · scheduler: pull_news / announce_calendar)
      └─ bridge_writer.py ──► nq_brain.db : news + calendar
                                   └─ 616 قارئ جسر الأخبار ──► 108 مستقبل الأخبار
                                                                    └─ تبويب «الأخبار»
```

**النتيجة العمليّة:** إن كان تبويب «الأخبار» فارغًا فالسبب غالبًا **خارج هذا
المشروع** — بوت الأخبار متوقّف أو لم يسحب. لا تشخّص 108/615/616 قبل فحص
`QUANT_NQ_NEWS/data/system_state.json`.

> **تنبيه للتوكن:** توكن بوت الأخبار في `.env` بنصّ صريح — وهو **غير** توكن
> منصّة ٦١٠ (ذاك في الخزنة المشفّرة بالمفتاح `telegram_bot_token`). بوتان
> مختلفان لا يُخلطان.

---

## ٢) `C:\Users\NQ\AppData\Roaming\MetaQuotes\Terminal\Common\Files\nq_brain.db`

جسر ميتاتريدر — **يكتبه الإكسبرت `QUANT_NQ` داخل المنصّة**، لا النظام. جداوله:
`ticks_v2` · `account_v2` · `symbol_specs_v2` · `positions_v2` · `trade_events_v2`
· `news` · `calendar` · `commands`. إن مات الإكسبرت جمدت هذه الجداول ولو كان
النظام كلّه سليمًا.

---

## ٣) `D:\داتا` — أدوات المالك خارج المشروع

`تشغيل\حارس.py` (يراقب التوثيق ويملأ `D:\داتا\صندوق_الوارد.md`) ·
`جسر-السحابة\mixer.py` (وكلاء خارجيّون) · `صوت\server.py` ·
`مشروع-للقراءة` (نسخة قراءة فقط — **ممنوع الكتابة فيها**) ·
`سياق\السياق.txt` (سياق موحّد يُطبَّق على مساحات عمل أخرى).

---

## ٤) `governance/ui/built` — لوحة مبنيّة

اللوحة تُخدَم **مبنيّة** من `built/`، لا من `src/`. أي تعديل في `src` لا يظهر
حتى `npm run build` داخل `governance/ui`. و`node_modules` غير مشمولة بالحزمة،
فأوّل بناء بعد فكّ ضغط يحتاج `npm ci`.

---

## قاعدة
**أي مكوّن يعيش خارج جذر المشروع يُسجَّل في هذه الورقة يوم يُضاف.** المناوبة
التالية لا تقرأ ذهنك — تقرأ هذا الملفّ.
