# ورقة سجل العمل — من بداية المحادثة حتى الآن

**المشروع:** QUANT_NQ / لوحة حوكمة الفوركس
**التاريخ:** 2026-08-31
**النطاق:** الفوركس والواجهة الحوكمية فقط

---

## 1. القيود التي تم الالتزام بها

- عدم لمس محتوى الكريبتو أو مكوناته.
- إبقاء الرئيسية كما كانت.
- عدم اختلاق بيانات أو نتائج.
- عرض `—` عند غياب البيانات.
- عدم تشغيل تداول حقيقي أو إرسال أوامر.
- عدم فتح حسابات أو بوابات تنفيذ حقيقية.
- عدم وضع مفاتيح أو توكنات سرية داخل المحادثة أو الحزمة.
- إبقاء كل وظيفة في قسمها الصحيح.

---

## 2. مطابقة واجهات الفوركس مع الأحداث والحقول الفعلية

### السيولة
تمت إضافة عرض الحقول الفعلية:

- `confidence`
- `current_depth`
- `required_depth`
- `liquidity_pressure`
- `liquidity_quality`
- `metadata.timeframe`

### الاحتمالات
تمت إضافة عرض:

- الاتجاه.
- القوة.
- الجاهزية.
- حالة `ready`.
- الفريم الزمني.
- الاتجاه والاحتمال المدمجان.
- ثقة الدمج.

### الإحصاء التحليلي
تمت إضافة الفريم الزمني بجانب كل رمز.

### الاستراتيجيات
تمت إضافة:

- `signal`
- `complete`
- `warnings`
- `missing`
- `active_weight`
- `available_weight`
- `missing_weight`
- `context_factor`
- الفريم الزمني.

تم بناء الواجهة بعد هذه التعديلات بنجاح.

---

## 3. إصلاح ناقل التلمترية 810

### المشكلة
كان الحدث معلنًا في الكود والـmanifest:

```text
telemetry.carrier.state
```

لكن لم يكن هناك نشر فعلي للحدث، لذلك كانت اللوحة تعرض أن الناقل لم يصل.

### الإصلاح
تمت إضافة `_publish_state()` إلى ذرة 810، واستدعاؤها:

- عند بدء الذرة.
- بعد كل `flush()`.

### الحالة المنشورة

- `id: telemetry_carrier`
- `state: DEGRADED` أو `HEALTHY`
- `rows_seen`
- `batches`
- `rows_written`
- `buffered`
- `dropped`

تم فحص Python للذرة ونجح.

---

## 4. لوحة التشخيص

تمت مراجعة لوحة التشخيص، وهي تعرض:

- حالة النواة.
- عدد الذرات.
- نتيجة الإقلاع.
- الذرات التي أقلعت أو فشلت.
- الذرات التي نشرت أحداثًا.
- مخازن العائلة 700.
- الوصلات المكسورة.
- الذرات الصامتة وأسباب الصمت.
- الاشتراكات الموروثة من سوق آخر.
- المخرجات التي لا يوجد لها مستمع.

### إضافة جديدة
تمت إضافة بطاقة مباشرة لحالة ناقل التلمترية 810:

- يعمل.
- ينتظر بيانات.
- متوقف.
- غير محمّل.

---

## 5. اللمسات التجميلية للشبكة

تبويب الشبكة أصبح أوضح بصريًا من دون تغيير مصدر البيانات:

- دليل ألوان لعائلات الذرات.
- شارة توضّح أن البيانات حقيقية.
- توضيح أن الوميض يعني مرور حدث فعلي.
- تعليمات السحب للتدوير.
- تعليمات العجلة للتكبير.
- تعليمات الضغط على الذرة لفتح تفاصيلها.
- لا توجد نبضات أو حركات وهمية.

---

## 6. تبويب الإعدادات

تمت إضافة وتحسين:

- رأس بصري لإعدادات الفوركس.
- دليل ألوان: حيّ، يحتاج انتباه، خطر.
- وضع اللون الأساسي.
- وضع مخصّص.
- زر واضح: `رجوع للون الأساسي`.
- الزر يمسح الألوان المخصصة ويرجع ألوان الفوركس الأصلية فورًا.

### التكبير
كان شريط التكبير يتحرك كل 5%، وأصبح يتحرك كل 1%:

- الحد الأدنى: 80%.
- الحد الأعلى: 160%.
- القيمة تُطبّق فورًا وتُحفظ محليًا.
- الشبكة مستثناة من تكبير CSS كما كان مقررًا.

---

## 7. حفظ التبويب بعد التحديث

### المشكلة
عند تعديل أي لوحة ثم الضغط على تحديث، كانت الواجهة ترجع إلى أول لوحة.

### الإصلاح
تم حفظ آخر تبويب فوركس في `localStorage`:

- يبقى التبويب المفتوح بعد تحديث الصفحة.
- الانتقال من الرئيسية يحفظ التبويب أيضًا.
- الحفظ مستقل للفوركس.
- لا يغيّر ترتيب الكريبتو.

---

## 8. ترتيب التبويبات

تم جعل لوحة NQ، التي كانت آخر لوحة فوركس، أول تبويب دائمًا.

- لا يتأثر بذلك ترتيب تبويبات الكريبتو.
- حتى الترتيب القديم المحفوظ لا يعيد NQ إلى مكان آخر.

---

## 9. لوحة قسم 850 — حاكم التغيير

### المشكلة
كانت ذرة 850 موجودة في النظام، لكن لم تكن لها لوحة واجهة.

### اللوحة الجديدة
تم إنشاء لوحة كاملة لقسم 850 تعرض:

- حالة التجربة.
- حدود التكيّف.
- مفتاح الإيقاف.
- اقتراح إعادة المعايرة.
- عدد الأحداث الواصلة من أصل 4.
- البيانات غير الموجودة كـ `—`.

### أزرار داخلية للوحة 850
تمت إضافة أزرار عرض:

- نظرة عامة.
- التجارب.
- الحدود.
- مفتاح الإيقاف.
- الأحداث.

هذه الأزرار تغيّر العرض داخل اللوحة فقط، ولا تعتمد تغييرًا ولا ترسل أمرًا.

### إصلاح حدث إضافي في 850
كانت حدود التكيّف لا تُنشر عند الإقلاع، لذلك أضيف نشر `adaptation.limits.state` عند بداية الذرة، كي لا تبقى اللوحة فارغة حتى أول اقتراح.

---

## 10. فحص الأحداث

تم فحص الأحداث العامة وقسم 850.

### نتيجة المشروع

- الأحداث المنشورة: 360.
- الأحداث المسموعة: 288.
- الأحداث الأساسية موصولة.
- 5 مصادر خارجية اختيارية بلا ناشر:
  - `SYS_10MS`
  - `asset.extraction.retry_requested`
  - `market.trade`
  - `replay.session.start`
  - `replay.session.stop`
- 72 مخرجًا بلا مستمع، ومصنفة كمخرجات عرض أو سجل.

### نتيجة قسم 850

يستقبل:

- `recalibration.proposed`
- `adaptation.kill_switch.state`

وينشر:

- `experiment.state`
- `adaptation.limits.state`

الناشرون الفعليون مربوطون بالذرات 840 و860.

---

## 11. فحص الذرة 007 / 7

تم التأكد أن الرقمين يشيران إلى نفس الذرة:

`007 سلامة الملفات`

### ما تم فحصه

- مطابقة الـmanifest مع الكود.
- نشر `tools.integrity.violation`.
- الاشتراك في `SYS_HOUR`.
- حماية baseline.
- حساب SHA-256.
- تجاهل الملفات المؤقتة وملفات الكاش.
- فحص Python.

### مشكلة baseline
ظهر اختبار أحمر لأن `integrity_baseline.json` كان قديمًا مقارنة بالتعديلات المقصودة على المشروع.

تم تحديث خط الأساس بعد التغييرات، ثم عاد الاختبار للنجاح.

---

## 12. إصلاح توثيق واجهة الحوكمة

### المشكلة
كانت رسالة:

`تعذّر توثيق واجهة الحوكمة.`

تظهر عندما تكون النواة غير مشغلة أو الـproxy غير متصل، رغم أن المشكلة ليست مفتاحًا خاطئًا.

### الإصلاح
تم تعديل التوثيق بحيث:

- لا تحجب الواجهة عند غياب النواة.
- تظهر البيانات كـ `—` عند عدم توفرها.
- يُطلب مفتاح فقط عند ردّ صريح `401`.
- لا يتم تخزين مفتاح التوثيق في localStorage أو الرابط أو الملفات.

---

## 13. تشغيل الخدمات

تم تشغيل واختبار:

- واجهة Vite على `5173`.
- نواة الفوركس على `8010`.
- واجهة الحوكمة على `8090`.

تم التأكد من:

- `ui=200`
- `version=200`
- `health=200`

ثم تم إيقاف الخدمات بإيقاف نظيف.

ظهرت أثناء التشغيل تحذيرات بيانات ناقصة في جداول:

- `ticks_v2`
- `symbol_specs_v2`
- `account_v2`

وهذه تمنع بيانات السوق والحساب الكاملة، لكنها لا تعني فشل الواجهة.

---

## 14. اعتماد مكتبات Python

تم تثبيت وفحص المكتبات الأساسية:

- `fastapi`
- `uvicorn[standard]`
- `pytest`
- `pytest-asyncio`

وجميع المكتبات المطلوبة موجودة في `requirements.txt`.

---

## 15. إصلاح أزرار Windows

### المشكلة
زر التشغيل المحلي كان يسبب أخطاء مثل:

```text
'تشغيل' is not recognized as an internal or external command
'�لعربي' is not recognized as an internal or external command
'tp:' is not recognized as an internal or external command
```

### السبب
مشاكل ترميز في ملف `.bat` العربي، مع عدم تجهيز بيئة Python والمكتبات قبل التشغيل.

### الإصلاح
تم إنشاء زر ASCII آمن:

```text
START_LOCAL.bat
```

وهو:

1. ينشئ `venv`.
2. يثبت `requirements.txt`.
3. يشغّل الوضع المحلي.
4. يعرض الأخطاء بوضوح.
5. لا يحتاج مفتاحًا.
6. لا يرسل تداولًا حقيقيًا.

تم أيضًا تحديث زر التشغيل المحلي العربي ليجهز المكتبات قبل تشغيل المشروع.

---

## 16. الاختبارات النهائية

بعد تثبيت `pytest-asyncio` وتشغيل كامل الاختبارات:

```text
866 passed
1 skipped
0 failed
```

لا يوجد اختبار فاشل.

---

## 17. تدقيق الأكواد وليس الاختبارات فقط

تم تنفيذ:

- `compileall` للكود.
- `py_compile` للذرات المهمة.
- المدقق الرسمي للذرات.
- مدقق الأحداث.
- فحص سلامة التنفيذ.
- بناء الواجهة.
- فحص ربط الأقسام مع `App.tsx`.

### نتيجة مدقق الذرات

- 233 ذرة مفحوصة.
- 0 مخالفات.
- 21 تحذيرًا.

### التحذيرات المسجلة

- إسقاط صامت محتمل في الذرة 870.
- حجم أكبر من الحد في الذرات 166 و454 و622، وهي استثناءات مسجلة.
- 17 حلقة خلفية `asyncio.create_task` مصنفة تحذيرًا.
- فحص العزل الكامل يحتاج تشغيل النواتين معًا؛ لم يُحسم أثناء غيابهما.

---

## 18. التغليف النهائي

تم إنشاء الحزمة:

```text
QUANT_NQ_release.zip
```

وتستثني:

- `node_modules`
- `__pycache__`
- ملفات الكاش.
- `.venv`
- ملفات البيئة.
- `runtime/device.key`
- ملفات التخزين التشغيلي.

داخل الحزمة يوجد أيضًا:

```text
RELEASE_AUDIT.md
تشغيل_Windows_آمن.md
```

---

## الخلاصة

المشروع الآن:

- مبني بنجاح.
- جميع الاختبارات تمرّ.
- واجهة قسم 850 موجودة ومربوطة.
- ذرة 007 مفحوصة.
- ناقل التلمترية 810 ينشر حالته فعليًا.
- التبويب المفتوح يبقى بعد التحديث.
- زر اللون الأساسي موجود.
- زر التكبير صار سلسًا بنسبة 1%.
- أزرار Windows لديها زر ASCII آمن.
- لا توجد صفقة أو أوامر تداول حقيقية تم تنفيذها.

**الحزمة النهائية:** `QUANT_NQ_release.zip`
