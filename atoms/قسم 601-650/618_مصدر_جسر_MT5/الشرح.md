# الذرة 618 — MT5 Bridge Source

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٣. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٦١٨` |
| الاسم | MT5 Bridge Source |
| المجلّد | `618_مصدر_جسر_MT5` |
| الإصدار | `4.0.1` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `SYS_SECOND`

**تُرسِل (من `manifest.yaml`):**
- `feed.mt5.tick`
- `market.symbol_specs`

**إعداداتها:** `db_path` · `table_name` · `spec_table` · `spec_refresh_s` · `poll_interval_s` · `batch_limit` · `delete_consumed` · `max_age_s`

<!-- BITAQA_END -->












## الدور

هذه الذرة جزء من حالة الأصل والحساب. هذا الملف يصف عقدها المعلن فقط؛ لا يضيف منطقًا ولا يعدّل النواة.

## عقد الكود

- المعرّف: `618`
- إصدار الذرة: `2.1.0`
- نقطة الدخول: `atom:Atom`
- نمط الإقلاع: `auto`

## الأحداث التي تنشرها

- `feed.mt5.tick`
- `market.symbol_specs`

## الأحداث التي تستقبلها

- لا تستقبل أحداثًا معلنة.

## الاعتماديات

- لا توجد اعتماديات معلنة.

## الصحة ودورة الحياة

- الفحص كل: `10000 ms`
- مهلة الفحص: `5000 ms`
- عتبة الفشل: `3`
- إعادة التشغيل عند الفشل: `False`

## الإعداد المعلن

```json
{
  "db_path": "C:\\Users\\NQ\\AppData\\Roaming\\MetaQuotes\\Terminal\\Common\\Files\\nq_brain.db",
  "table_name": "ticks",
  "spec_table": "symbol_specs",
  "spec_refresh_s": 300,
  "poll_interval_s": 0.1,
  "batch_limit": 500,
  "delete_consumed": true
}
```

## كيف أتأكد أنها تعمل

1. افتح غرفة القيادة من طبقة الحوكمة.
2. شغّل فحص المشروع وتأكد أن الذرة `618` بدأت بلا استبعاد.
3. راقب حالة الذرة في اللوحة وانتظر الحدث المعلن عند وصول مدخل حقيقي.
4. إذا لم يصل المدخل، يظهر السبب كمصدر ناقص أو حالة انتظار؛ لا تُخترع بيانات مكانه.

## حدود هذه الورقة

هذه الورقة توثيق عقد فقط. الحقيقة التشغيلية تؤخذ من حالة النواة الحية وسجل المنصات، وليس من الورقة وحدها.

## إثبات Patch قبل الختم — 2026-08-24
انشر SYS_10MS؛ يجب أن تُصرف ticks_v2 وفق poll_interval_s وتبقى مواصفات الوسيط معلنة.
