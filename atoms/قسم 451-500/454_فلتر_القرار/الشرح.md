# الذرة 454 — Decision Filter

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٣. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٤٥٤` |
| الاسم | Decision Filter |
| المجلّد | `454_فلتر_القرار` |
| الإصدار | `2.4.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٦٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `decision.resolved.state`
- `decision.settings.command`
- `decision.filter.confidence.state`
- `decision.filter.conditions.state`
- `decision.filter.timing.state`
- `decision.filter.position.state`
- `decision.filter.freshness.state`
- `decision.filter.asset.state`
- `market_data.calendar.state`
- `market_data.quality.state`
- `market_data.feed.state`
- `news.trading_window.state`

**تُرسِل (من `manifest.yaml`):**
- `decision.filtered.state`
- `decision.settings.state`

**إعداداتها:** `min_score` · `filter_ttl_s`

<!-- BITAQA_END -->












## الدور

هذه الذرة جزء من حالة الأصل والحساب. هذا الملف يصف عقدها المعلن فقط؛ لا يضيف منطقًا ولا يعدّل النواة.

## عقد الكود

- المعرّف: `454`
- إصدار الذرة: `2.0.0`
- نقطة الدخول: `atom:Atom`
- نمط الإقلاع: `auto`

## الأحداث التي تنشرها

- `decision.filtered.state`

## الأحداث التي تستقبلها

- `decision.resolved.state`
- `decision.filter.confidence.state`
- `decision.filter.conditions.state`
- `decision.filter.timing.state`
- `decision.filter.position.state`
- `decision.filter.freshness.state`
- `decision.filter.asset.state`

## الاعتماديات

- لا توجد اعتماديات معلنة.

## الصحة ودورة الحياة

- الفحص كل: `60000 ms`
- مهلة الفحص: `5000 ms`
- عتبة الفشل: `3`
- إعادة التشغيل عند الفشل: `False`

## الإعداد المعلن

```json
{
  "min_score": 0
}
```

## كيف أتأكد أنها تعمل

1. افتح غرفة القيادة من طبقة الحوكمة.
2. شغّل فحص المشروع وتأكد أن الذرة `454` بدأت بلا استبعاد.
3. راقب حالة الذرة في اللوحة وانتظر الحدث المعلن عند وصول مدخل حقيقي.
4. إذا لم يصل المدخل، يظهر السبب كمصدر ناقص أو حالة انتظار؛ لا تُخترع بيانات مكانه.

## حدود هذه الورقة

هذه الورقة توثيق عقد فقط. الحقيقة التشغيلية تؤخذ من حالة النواة الحية وسجل المنصات، وليس من الورقة وحدها.


## تحديث مسار بيانات السوق — 2.0.0

يطبق هذا الإصدار عقد البيانات المعتمدة ونطاق الملكية: الخام يمر عبر 112 ثم 113
ثم 114 قبل التحليل، وحالات الخبر والجودة وحياة التغذية تؤثر في بوابة القرار
الجديد فقط. البيانات المرتبطة بحساب تُفهرس بالحساب والرمز/التذكرة حسب دور الذرة.