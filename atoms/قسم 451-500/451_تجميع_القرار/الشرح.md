# الذرة 451 — مجمّع القرار

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٣. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٤٥١` |
| الاسم | Decision Aggregator |
| المجلّد | `451_تجميع_القرار` |
| الإصدار | `2.14.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٦٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `market.tick.validated`
- `analysis.raw.completed`
- `probability.cycle.collected`
- `probability.confidence.state`
- `market.structure.updated`
- `market.liquidity.updated`
- `stats.cycle.collected`
- `strategy.section.live`

**تُرسِل (من `manifest.yaml`):**
- `decision.aggregated.state`
- `decision.room.state`

**إعداداتها:** `require_same_cycle` · `expected_families`

<!-- BITAQA_END -->












## تعديل نقطة استهلاك التحليل

تخزّن الذرة أحدث `analysis.raw.completed` حي فقط عندما تكون حالته `DECISION_READY` وجاهزًا وصالحًا. النتيجة غير الجاهزة أو القديمة تحذف الدليل المخزن ولا تتحول إلى حياد. عند دورة القرار القائمة على الشمعة تُدرج أحدث حالة جاهزة لنفس الحساب والأصل فقط؛ لا يختلط حساب أو أصل بآخر.

هذا التعديل لا يحول طبقة القرار إلى قرار بالتكة ولا يغيّر الاستراتيجيات؛ إنه يفرض عقد الجاهزية عند نقطة استهلاك التحليل.

## إثبات Patch قبل الختم — 2026-08-24
انشر section.live بحقل unified.ratio واتجاه مجهول؛ يجب أن يحمل صفّ الروم الحقول الثمانية وأن يكون direction=null لا 0.
