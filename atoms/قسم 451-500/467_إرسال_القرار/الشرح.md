# الذرة 467 — Decision Dispatch

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٣. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٤٦٧` |
| الاسم | Decision Dispatch |
| المجلّد | `467_إرسال_القرار` |
| الإصدار | `2.1.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٦٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `decision.approved.state`
- `SYS_SECOND`

**تُرسِل (من `manifest.yaml`):**
- `decision.dispatch.state`
- `decision.gate.passed`
- `decision.gate.blocked`
- `decision.gate.recorded`

<!-- BITAQA_END -->












## الدور

هذه الذرة جزء من حالة الأصل والحساب. هذا الملف يصف عقدها المعلن فقط؛ لا يضيف منطقًا ولا يعدّل النواة.

## عقد الكود

- المعرّف: `467`
- إصدار الذرة: `2.1.0`
- نقطة الدخول: `atom:Atom`
- نمط الإقلاع: `auto`

## الأحداث التي تنشرها

- `decision.gate.passed` *(بختم NQ بند 22 حزمة ب — ق٩: بوابة الانتقال الوحيدة من القرار المعتمد إلى التنفيذ. معتمد + جانب `buy`/`sell` ⇒ حمولة تمرير كامل بالهوية الست والثمانية والحواجز + `gate_request_id` حتمي + `gated_at`. نفس معرّف القرار لا يمر مرتين إلا بسبب تنفيذي موثق `redispatch_reason`.)*
- `decision.gate.blocked` *(`approved=false` لا يمر أبدًا — يُسجَّل بسببه وهويته كما وصلت.)*
- `decision.gate.recorded` *(انتظار ⇒ حالة قرار تُحفظ، لا أمر دخول.)*
- `decision.dispatch.state` *(سجل الإرسال لمخزن `707` — باقٍ بحكم البند `٦١`، وصار `executable: true`: وسم التشخيص أُلغي بحكم ق٩؛ الذرّة ما زالت لا تنشر `execution.order.requested` — طلب التنفيذ لمسار `581` وحده.)*

## الأحداث التي تستقبلها

- `decision.approved.state`
- `SYS_SECOND` *(نبضة الوقت الرسمي لختم `gated_at` — لا ساعة محلية.)*

## الاعتماديات

- لا توجد اعتماديات معلنة.

## الصحة ودورة الحياة

- الفحص كل: `60000 ms`
- مهلة الفحص: `5000 ms`
- عتبة الفشل: `3`
- إعادة التشغيل عند الفشل: `False`

## الإعداد المعلن

- لا يوجد إعداد خاص معلن.

## كيف أتأكد أنها تعمل

1. افتح غرفة القيادة من طبقة الحوكمة.
2. شغّل فحص المشروع وتأكد أن الذرة `467` بدأت بلا استبعاد.
3. راقب حالة الذرة في اللوحة وانتظر الحدث المعلن عند وصول مدخل حقيقي.
4. إذا لم يصل المدخل، يظهر السبب كمصدر ناقص أو حالة انتظار؛ لا تُخترع بيانات مكانه.

## حدود هذه الورقة

هذه الورقة توثيق عقد فقط. الحقيقة التشغيلية تؤخذ من حالة النواة الحية وسجل المنصات، وليس من الورقة وحدها.