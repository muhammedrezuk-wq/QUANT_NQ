# الذرّة 261 — محلّل العمق

> أُنشئت بحملة ١–٤٤٩ الدفعة أ (أمر المالك ٢٠٢٦-٠٨-٢٣): «مع باني شموع ومحلل عمق» — التدقيق كان سجّل: «العمق يصل ولا محلِّل عمق».

**مهمتها:** كل لقطة عمق من 106 تُترجم بطاقة قسم سيولة صادقة — الاتجاه = اختلال كتاب العرض/الطلب (±100)، القوة = |الاختلال|، الدليل = عدد المستويات. كل رقم من اللقطة نفسها.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٣. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٢٦١` |
| الاسم | Depth Analyzer |
| المجلّد | `261_محلل_العمق` |
| الإصدار | `1.0.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `market_data.depth_updated`

**تُرسِل (من `manifest.yaml`):**
- `liquidity.depth.state`

**إعداداتها:** `weight` · `required_levels`

<!-- BITAQA_END -->

