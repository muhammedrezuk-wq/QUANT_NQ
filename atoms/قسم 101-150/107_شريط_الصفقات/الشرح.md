# الذرة 107 — Trade Tape Receiver

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٣. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `١٠٧` |
| الاسم | Trade Tape Receiver |
| المجلّد | `107_شريط_الصفقات` |
| الإصدار | `2.0.1` |
| حرجة؟ | `false` |
| الإقلاع | `manual` |
| فحص الصحّة | كل `٣٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `market.trade`

**تُرسِل (من `manifest.yaml`):**
- `market_data.trade_tape_updated`

**إعداداتها:** `source_event` · `window_size` · `publish_every_n_trades`

<!-- BITAQA_END -->












## الدور

هذه الذرة جزء من حالة الأصل والحساب. هذا الملف يصف عقدها المعلن فقط؛ لا يضيف منطقًا ولا يعدّل النواة.

## عقد الكود

- المعرّف: `107`
- إصدار الذرة: `2.0.0`
- نقطة الدخول: `atom:Atom`
- نمط الإقلاع: `auto`

## الأحداث التي تنشرها

- `market_data.trade_tape_updated`

## الأحداث التي تستقبلها

- `market.trade`

## الاعتماديات

- لا توجد اعتماديات معلنة.

## الصحة ودورة الحياة

- الفحص كل: `30000 ms`
- مهلة الفحص: `5000 ms`
- عتبة الفشل: `3`
- إعادة التشغيل عند الفشل: `True`

## الإعداد المعلن

```json
{
  "source_event": "market.trade",
  "window_size": 500,
  "publish_every_n_trades": 10
}
```

## كيف أتأكد أنها تعمل

1. افتح غرفة القيادة من طبقة الحوكمة.
2. شغّل فحص المشروع وتأكد أن الذرة `107` بدأت بلا استبعاد.
3. راقب حالة الذرة في اللوحة وانتظر الحدث المعلن عند وصول مدخل حقيقي.
4. إذا لم يصل المدخل، يظهر السبب كمصدر ناقص أو حالة انتظار؛ لا تُخترع بيانات مكانه.

## حدود هذه الورقة

هذه الورقة توثيق عقد فقط. الحقيقة التشغيلية تؤخذ من حالة النواة الحية وسجل المنصات، وليس من الورقة وحدها.

## الحالة التشغيلية الملزمة

لا يوجد ناشر حقيقي لـ`market.trade` في الحزمة الحالية. لذلك الذرة **غير مكتملة الوظيفة**، وتبقى صحتها `DEGRADED / UNAVAILABLE_NO_TRADE_SOURCE` ولا يجوز بناء قرار تداول على خرجها. لا يُخترع مصدر داخلي بديل.