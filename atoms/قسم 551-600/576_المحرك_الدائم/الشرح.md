# الذرة 576 — Perpetual Engine

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٣. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٥٧٦` |
| الاسم | Perpetual Engine |
| المجلّد | `576_المحرك_الدائم` |
| الإصدار | `3.3.1` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٦٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `perpetual.asset.activate`
- `perpetual.asset.deactivate`
- `decision.gate.passed`
- `SYS_SECOND`
- `dial.profile.state`
- `market.symbol_specs`
- `market_data.candle_closed`
- `risk.asset_ledger.state`
- `platform.positions.state`
- `platform.account.state`

**تُرسِل (من `manifest.yaml`):**
- `execution.order.requested`
- `perpetual.entry.state`
- `perpetual.entry.rejected`

**إعداداتها:** `lot_step` · `min_lot` · `max_lot` · `fallback_stop_distance_frac`

<!-- BITAQA_END -->












## الدور

هذه الذرة جزء من حالة الأصل والحساب. هذا الملف يصف عقدها المعلن فقط؛ لا يضيف منطقًا ولا يعدّل النواة.

## عقد الكود

- المعرّف: `576`
- إصدار الذرة: `3.1.0`
- نقطة الدخول: `atom:Atom`
- نمط الإقلاع: `auto`

## الأحداث التي تنشرها

- `execution.order.requested` *(بختم NQ بند 22 حزمة ب — ق٩ ب٥: لا يصدر إلا بأصل أب صريح بالحمولة — `parent_decision_id` أو `owner_command_id` (من `command_id` القادم من `901`) — ويُختم الأصل على كل أمر.)*
- `perpetual.entry.state`
- `perpetual.entry.rejected` *(تفعيل بلا أصل ⇒ `reason: NO_PARENT_AUTHORITY` وعدّاد صحّة، ولا يُخزَّن معلّقًا — لا زناد شمعة للدخول.)*

## الأحداث التي تستقبلها

- `perpetual.asset.activate`
- `perpetual.asset.deactivate`
- `decision.gate.passed`
- `dial.profile.state`
- `market.symbol_specs`
- `market_data.candle_closed`
- `risk.asset_ledger.state`
- `platform.positions.state`

## الاعتماديات

- لا توجد اعتماديات معلنة.

## الصحة ودورة الحياة

- الفحص كل: `60000 ms`
- مهلة الفحص: `5000 ms`
- عتبة الفشل: `3`
- إعادة التشغيل عند الفشل: `False`

## الإعداد المعلن

```json
{
  "lot_step": 0.01,
  "min_lot": 0.01,
  "max_lot": 20.0,
  "fallback_stop_distance_frac": 0.005
}
```

## كيف أتأكد أنها تعمل

1. افتح غرفة القيادة من طبقة الحوكمة.
2. شغّل فحص المشروع وتأكد أن الذرة `576` بدأت بلا استبعاد.
3. راقب حالة الذرة في اللوحة وانتظر الحدث المعلن عند وصول مدخل حقيقي.
4. إذا لم يصل المدخل، يظهر السبب كمصدر ناقص أو حالة انتظار؛ لا تُخترع بيانات مكانه.

## حدود هذه الورقة

هذه الورقة توثيق عقد فقط. الحقيقة التشغيلية تؤخذ من حالة النواة الحية وسجل المنصات، وليس من الورقة وحدها.