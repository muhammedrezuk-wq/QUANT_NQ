# الذرة 552 — Order Validator

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٣. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٥٥٢` |
| الاسم | Order Validator |
| المجلّد | `552_مدقق_الأمر` |
| الإصدار | `5.2.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١` ثانية |

**تسمع (من `manifest.yaml`):**
- `execution.gate.state`
- `execution.order.legal`
- `emergency.halt`
- `risk.kill_switch.reset_requested`
- `allowed.symbols.state`
- `feed.mt5.tick`
- `execution.gate.command`
- `market.symbol_specs`
- `platform.account.state`
- `execution.reconcile.state`
- `reference.health.state`
- `risk.exposure.state`
- `risk.margin.validation.completed`
- `execution.snapshot.state`

**تُرسِل (من `manifest.yaml`):**
- `trading.final_decision`
- `execution.order.rejected`
- `execution.gate.state`

**إعداداتها:** `enabled` · `max_spread_points` · `spread_ttl_s`

<!-- BITAQA_END -->












## الدور

هذه الذرة جزء من حالة الأصل والحساب. هذا الملف يصف عقدها المعلن فقط؛ لا يضيف منطقًا ولا يعدّل النواة.

## عقد الكود

- المعرّف: `552`
- إصدار الذرة: `5.0.0`
- نقطة الدخول: `atom:Atom`
- نمط الإقلاع: `auto`

## الأحداث التي تنشرها

- `trading.final_decision`
- `execution.order.rejected` — كل رفض، بلا استثناء: بما فيها البوّابة المقفولة (`reason=disabled`) والإيقاف الطارئ (`reason=halted`) وكل أسباب `order_validation.py`. **لا يوجد حدث منفصل `execution.order.preview`.**
- `execution.gate.state` — الحالة الحقيقيّة (`enabled`/`status`) عند كل تغيير؛ هذا مصدر «مفتوحة/مقفولة» الصحيح، لا رسالة الصحّة

## الأحداث التي تستقبلها

- `execution.order.legal`
- `emergency.halt`
- `risk.kill_switch.reset_requested`

## الاعتماديات

- لا توجد اعتماديات معلنة.

## الصحة ودورة الحياة

- الفحص كل: `1000 ms`
- مهلة الفحص: `500 ms`
- عتبة الفشل: `3`
- إعادة التشغيل عند الفشل: `False`

## الإعداد المعلن

```json
{
  "enabled": false
}
```

## كيف أتأكد أنها تعمل

1. افتح غرفة القيادة من طبقة الحوكمة.
2. شغّل فحص المشروع وتأكد أن الذرة `552` بدأت بلا استبعاد.
3. راقب حالة الذرة في اللوحة وانتظر الحدث المعلن عند وصول مدخل حقيقي.
4. إذا لم يصل المدخل، يظهر السبب كمصدر ناقص أو حالة انتظار؛ لا تُخترع بيانات مكانه.

## حدود هذه الورقة

هذه الورقة توثيق عقد فقط. الحقيقة التشغيلية تؤخذ من حالة النواة الحية وسجل المنصات، وليس من الورقة وحدها.

## إثبات Patch قبل الختم — 2026-08-24
شغّل اختبارات الذرّة؛ منطق snapshot/restore/health يبقى نفسه وatom.py تحت 350.
