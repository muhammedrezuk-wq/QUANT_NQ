# الذرة 581 — Position Delta Engine

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٣. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٥٨١` |
| الاسم | Position Delta Engine |
| المجلّد | `581_محرك_فرق_المركز` |
| الإصدار | `3.2.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٥` ثانية |

**تسمع (من `manifest.yaml`):**
- `decision.gate.passed`
- `decision.gate.blocked`
- `decision.gate.recorded`
- `decision.resolved.state`
- `decision.approved.state`
- `risk.asset_ledger.state`
- `asset.portfolio.state`
- `dial.profile.state`
- `market.symbol_specs`
- `feed.mt5.tick`
- `market_data.candle_closed`
- `platform.positions.state`
- `risk.asset_stop.state`

**تُرسِل (من `manifest.yaml`):**
- `perpetual.target.state`

**إعداداتها:** `bands` · `hedge_bands` · `s_enter` · `s_exit` · `max_target_volume` · `max_step_volume` · `min_volume` · `hedge_cost_per_volume`

<!-- BITAQA_END -->












## الدور

هذه الذرة جزء من حالة الأصل والحساب. هذا الملف يصف عقدها المعلن فقط؛ لا يضيف منطقًا ولا يعدّل النواة.

## عقد الكود

- المعرّف: `581`
- إصدار الذرة: `3.2.0`
- نقطة الدخول: `atom:Atom`
- نمط الإقلاع: `auto`

## الأحداث التي تنشرها

- `perpetual.target.state` *(كل هدف يحمل `decision_id` و`gate_request_id` من مادة قراره — تمهيد ت١ لسلسلة التنفيذ بهوية الأب.)*

## الأحداث التي تستقبلها

- `decision.gate.passed` *(بختم NQ بند 22 حزمة ب — ق٩ ب٤: المصدر الحصري لقرارات الدخول — الجانب والهوية والثمانية من بوابة `467` وحدها.)*
- `decision.gate.blocked` *(حالة حجب: تصفير التعرّض مع حفظ الإجمالي.)*
- `decision.gate.recorded` *(انتظار: حالة قرار لا أمر.)*
- `decision.resolved.state` *(خُفِّض إلى سياق بلا جانب — قوة/دورة فقط؛ لا يفتح اتجاهًا أبدًا.)*
- `decision.approved.state` *(تأكيد إضافي — فيتو، لا مصدر.)*
- `risk.asset_ledger.state`
- `asset.portfolio.state`
- `dial.profile.state`
- `market.symbol_specs`
- `feed.mt5.tick`
- `market_data.candle_closed`
- `platform.positions.state`
- `risk.asset_stop.state`

## الاعتماديات

- لا توجد اعتماديات معلنة.

## الصحة ودورة الحياة

- الفحص كل: `15000 ms`
- مهلة الفحص: `5000 ms`
- عتبة الفشل: `3`
- إعادة التشغيل عند الفشل: `False`

## الإعداد المعلن

```json
{
  "bands": {
    "0.0": 0.0,
    "0.2": 0.1,
    "0.4": 0.25,
    "0.6": 0.5,
    "0.75": 0.7,
    "0.9": 1.0
  },
  "max_target_volume": 20,
  "max_step_volume": 1,
  "min_volume": 0.01,
  "hedge_cost_per_volume": 0.0
}
```

## كيف أتأكد أنها تعمل

1. افتح غرفة القيادة من طبقة الحوكمة.
2. شغّل فحص المشروع وتأكد أن الذرة `581` بدأت بلا استبعاد.
3. راقب حالة الذرة في اللوحة وانتظر الحدث المعلن عند وصول مدخل حقيقي.
4. إذا لم يصل المدخل، يظهر السبب كمصدر ناقص أو حالة انتظار؛ لا تُخترع بيانات مكانه.

## حدود هذه الورقة

هذه الورقة توثيق عقد فقط. الحقيقة التشغيلية تؤخذ من حالة النواة الحية وسجل المنصات، وليس من الورقة وحدها.

## إثبات Patch قبل الختم — 2026-08-24
شغّل اختبارات الأهداف؛ النتائج تبقى نفسها وatom.py تحت 350.
