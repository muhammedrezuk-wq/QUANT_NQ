# الذرة 550 — Execution Manager

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٣. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٥٥٠` |
| الاسم | Truthful Execution Lifecycle |
| المجلّد | `550_مدير_التنفيذ` |
| الإصدار | `2.1.2` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٦٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `execution.unified.state`
- `execution.order.built`
- `trading.final_decision`
- `execution.order.rejected`
- `execution.order.skipped`
- `platform.brain_signal.written`
- `platform.brain_signal.write_failed`
- `execution.command.ack`
- `execution.command.failed`
- `platform.trade_event`
- `execution.manage.command`
- `execution.manage.written`
- `market.outcome.realized`
- `emergency.halt`
- `risk.kill_switch.reset_requested`

**تُرسِل (من `manifest.yaml`):**
- `execution.unified.state`

<!-- BITAQA_END -->












## الدور

هذه الذرة جزء من حالة الأصل والحساب. هذا الملف يصف عقدها المعلن فقط؛ لا يضيف منطقًا ولا يعدّل النواة.

## عقد الكود

- المعرّف: `550`
- إصدار الذرة: `2.1.0`
- نقطة الدخول: `atom:Atom`
- نمط الإقلاع: `auto`

## الأحداث التي تنشرها

- `execution.unified.state`

## الأحداث التي تستقبلها

- `execution.order.built`
- `execution.order.skipped` — من ٥٥١ (سبب توقّف قبل البوّابة، جديد ٢٠٢٦-٠٨-١٩)
- `trading.final_decision`
- `execution.order.rejected` — من ٥٥٢، بما فيها البوّابة المقفولة (`reason=disabled`). **لا يوجد حدث منفصل `execution.order.preview`** — لم يعد موجودًا بكود ٥٥٢ الحالي إطلاقًا (راجع `سياق/٩٠`)
- `execution.manage.command`
- `execution.manage.written`
- `market.outcome.realized`
- `emergency.halt`
- `risk.kill_switch.reset_requested`

## الاعتماديات

- لا توجد اعتماديات معلنة.

## الصحة ودورة الحياة

- الفحص كل: `60000 ms`
- مهلة الفحص: `5000 ms`
- عتبة الفشل: `3`
- إعادة التشغيل عند الفشل: `False`

## الإعداد المعلن

- لا يوجد إعداد خاص معلن.

## كيف أتأكد أنها تعمل

1. افتح غرفة القيادة من طبقة الحوكمة.
2. شغّل فحص المشروع وتأكد أن الذرة `550` بدأت بلا استبعاد.
3. راقب حالة الذرة في اللوحة وانتظر الحدث المعلن عند وصول مدخل حقيقي.
4. إذا لم يصل المدخل، يظهر السبب كمصدر ناقص أو حالة انتظار؛ لا تُخترع بيانات مكانه.

## حدود هذه الورقة

هذه الورقة توثيق عقد فقط. الحقيقة التشغيلية تؤخذ من حالة النواة الحية وسجل المنصات، وليس من الورقة وحدها.