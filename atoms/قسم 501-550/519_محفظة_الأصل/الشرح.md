# الذرة 519 — Asset Portfolio

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٣. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٥١٩` |
| الاسم | Asset Portfolio |
| المجلّد | `519_محفظة_الأصل` |
| الإصدار | `3.1.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٥` ثانية |

**تسمع (من `manifest.yaml`):**
- `asset.portfolio.owner_intent`
- `risk.asset_ledger.state`
- `platform.trade_event`
- `platform.account.state`
- `platform.terminal_state`
- `emergency.halt`
- `risk.kill_switch.reset_requested`
- `risk.asset.command`

**تُرسِل (من `manifest.yaml`):**
- `asset.portfolio.state`
- `asset.portfolio.owner_intent`

**إعداداتها:** `exit_ratio`

<!-- BITAQA_END -->












## الدور

هذه الذرة جزء من حالة الأصل والحساب. هذا الملف يصف عقدها المعلن فقط؛ لا يضيف منطقًا ولا يعدّل النواة.

## عقد الكود

- المعرّف: `519`
- إصدار الذرة: `2.0.0`
- نقطة الدخول: `atom:Atom`
- نمط الإقلاع: `auto`

## الأحداث التي تنشرها

- `asset.portfolio.state`

## الأحداث التي تستقبلها

- `risk.asset_ledger.state`
- `platform.trade_event`
- `platform.account.state`
- `platform.terminal_state`
- `emergency.halt`
- `risk.kill_switch.reset_requested`
- `risk.asset.command`

## الاعتماديات

- لا توجد اعتماديات معلنة.

## الصحة ودورة الحياة

- الفحص كل: `15000 ms`
- مهلة الفحص: `5000 ms`
- عتبة الفشل: `3`
- إعادة التشغيل عند الفشل: `False`

## الإعداد المعلن

```json
{
  "exit_ratio": 0.9
}
```

## كيف أتأكد أنها تعمل

1. افتح غرفة القيادة من طبقة الحوكمة.
2. شغّل فحص المشروع وتأكد أن الذرة `519` بدأت بلا استبعاد.
3. راقب حالة الذرة في اللوحة وانتظر الحدث المعلن عند وصول مدخل حقيقي.
4. إذا لم يصل المدخل، يظهر السبب كمصدر ناقص أو حالة انتظار؛ لا تُخترع بيانات مكانه.

## حدود هذه الورقة

هذه الورقة توثيق عقد فقط. الحقيقة التشغيلية تؤخذ من حالة النواة الحية وسجل المنصات، وليس من الورقة وحدها.

## إثبات Patch قبل الختم — 2026-08-24
أرسل trade_event ليس سببه SL؛ يجب أن يزيد dropped_trade في الصحّة.
