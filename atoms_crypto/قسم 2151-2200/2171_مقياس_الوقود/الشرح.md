# Cascade Fuel Gauge (Bi-directional)

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٣. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٢١٧١` |
| الاسم | Cascade Fuel Gauge (Bi-directional) |
| المجلّد | `2171_مقياس_الوقود` |
| الإصدار | `1.1.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٥` ثانية |

**تسمع (من `manifest.yaml`):**
- `market.oi`
- `market.funding`
- `market.premium`
- `market.candle`

**تُرسِل (من `manifest.yaml`):**
- `sense.fuel.state`

**إعداداتها:** `window_s` · `oi_build_pct` · `px_move_pct` · `spent_pct` · `long_funding` · `short_funding` · `premium_hot_bps` · `premium_short_bps` · `max_age_s` · `max_log_rows`

<!-- BITAQA_END -->

















