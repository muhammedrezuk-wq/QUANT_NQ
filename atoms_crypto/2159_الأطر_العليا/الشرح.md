# Higher-Timeframe Context (4H map + 15m structure)

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٢١٥٩` |
| الاسم | Higher-Timeframe Context (4H map + 15m structure) |
| المجلّد | `2159_الأطر_العليا` |
| الإصدار | `1.1.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٣٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `market.candle`

**تُرسِل (من `manifest.yaml`):**
- `sense.htf.state`

**إعداداتها:** `timeframe_map` · `timeframe_struct` · `range_bars` · `slope_bars` · `struct_bars` · `extreme_high_pct` · `extreme_low_pct` · `max_age_s`

<!-- BITAQA_END -->
