# التاريخ — 707_مخزن_القرارات

**الذرّة:** `٧٠٧` · **الإصدار:** `4.6.0`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-٢٨ | `4.6.0` | **اكتمال السجل — دفتر الفجوات بند ١١.** اشتراكٌ جديد `crypto.decision.sized_entry.state`(٢٢٧٥) بمرحلةٍ جديدة `CANDIDATE_EVALUATED` — حتى الآن كانت هذه الذرّة تكتب فقط `decision.approved.state` (المعتمَد النهائيّ)، فأي مرشّحٍ رفضه ٢٢٧٥ (بوّابة اقتصادية/حلقة خارجية/توقّف يوميّ) كان يختفي كليًّا بلا سجل. `03-protocol.md` §٥ "السجل — journal.csv (ضمير المشروع)" حرفيًّا: "تُسجَّل كل إشارة مفعّلة: الحقيقية بنتائجها، والافتراضية التي كانت ستتفعل... القياس الصادق يحسب ما كان سيحدث لا ما نجونا منه صدفة." حدثٌ كريبتويٌّ خاصّ — نسخة الفوركس من هذه الذرّة لا تستقبله أبدًا (بلا ناشرٍ هناك)، بلا أثرٍ سلبيّ. الأعمدة المسمّاة (`symbol`/`direction`/`approved`/`reason`/...) تُملأ حسبما توفّر بحمولة كل حالة (مرشّحٌ رُفض قبل التحجيم يحمل حقولًا أقلّ من مرشّحٍ اعتُمد — صادقٌ لا نقصًا)؛ الحمولة الكاملة (`entry_class`/`grade`/`ring`/`gate_margin`/...) محفوظةٌ دومًا بـ`payload_json` (`keep_full_payload=true`). راجع أيضًا `governance/server.py`'s `decisions_rows()` (بند ١٥، إزالة الصفوف المكرَّرة) — تعديلٌ في ملفٍّ آخر، موثَّقٌ بتاريخه هناك. |
| ٢٠٢٦-٠٨-٢٨ | `4.5.0` | إضافة عمود `take_profit_runner REAL` — الراكض الاختياريّ لرتبة ألف من بطاقة الإشارة v3.1 (2277 v1.2.0 عبر 2276 v2.2.0، دفتر الفجوات بند ٧). نفس نمط هجرة `take_profit_2`/`entry_price` بالضبط (`_MIGRATION_COLUMNS_REAL`). `NULL` صريحٌ لرتبة باء ولكل صفٍّ سابقٍ للهجرة — لا اختراع. `governance/server.py`'s `decisions_rows()` عُدِّل بنفس نمط `has_link_columns` — **يحتاج إعادة تشغيل خادم الحوكمة**. |
| ٢٠٢٦-٠٨-٢٨ | `4.4.0` | إضافة عمود `take_profit_2 REAL` — الهدف الثاني لبطاقة الإشارة v3.1 (2277 الجديدة، عبر 2276 v2.0.0). نفس نمط هجرة `entry_price` بالضبط (`_MIGRATION_COLUMNS_REAL`، REAL لا TEXT). `governance/server.py`'s `decisions_rows()` عُدِّل ليُدرجه بنفس نمط `has_link_columns` إن وُجد — **يحتاج إعادة تشغيل خادم الحوكمة** كسابقتها. |
| ٢٠٢٦-٠٨-٢٨ | `4.3.0` | إضافة عمود `entry_price REAL` (سؤال المستخدم "ليش مافي دخول بالصفقة؟" — 2276 v1.1.0 صار ينشره فعليًّا عبر 2274→2275). نفس نمط هجرة ت٥ (v4.1.0) بالضبط: `ALTER TABLE ADD COLUMN` يعمل فقط حين يغيب العمود، لكن بنوع `REAL` لا `TEXT` (عمودٌ رقميّ لا هويّة) — لذا هجرةٌ منفصلة (`_MIGRATION_COLUMNS_REAL`) لا امتدادٌ للقائمة الحالية المُقفَلة على TEXT. `_row()` يقرأ `payload.entry_price`؛ غيابه (كل المراحل الأخرى غير APPROVED) يبقى `NULL` صريحًا. `governance/server.py`'s `decisions_rows()` عُدِّل بنفس نمط `has_link_columns` ليُدرج العمود إن وُجد — **يحتاج إعادة تشغيل خادم الحوكمة** (الملفّ لا يُحمَّل حيًّا، بخلاف الذرّات). |
| ٢٠٢٦-٠٨-٢١ | `4.1.0` | **بقية حزمة ت — ت٥ (عمودا الربط) — ضمن ختم البند 22.** المخطط كان **بلا** `decision_id`/`gate_request_id` إطلاقًا (صفر ذكر، متحقَّق من الكود قبل التنفيذ). أُضيف العمودان لـ`_SCHEMA` (قواعد جديدة) **و**هجرة آمنة بـ`ALTER TABLE ADD COLUMN … TEXT` تعمل فقط حين يُظهر `PRAGMA table_info` غياب العمود (قاعدة قائمة بصفوف لا تُفقَد، وإعادة تشغيل على قاعدة مهاجَرة أصلًا لا تُخطئ لأن العمود صار موجودًا). `_row()` يقرأ الحقلين من حمولة كل مرحلة كما وصلت — غيابهما يبقى `NULL` صريحًا لا مُخترَعًا (أغلب المراحل ما قبل حزمة ت لا تحملهما أصلًا، وهذا صادق لا عطل). **مقيس:** 3 اختبارات جديدة — تخزين العمودين من حمولة تحمل الهوية، غيابهما NULL، وهجرة قاعدة قديمة بصفّ موجود مسبقًا مع محاكاة إعادة تشغيل ثانية على القاعدة المهاجَرة — 7/7 خضراء تحت المشغّل الرسميّ. |
| ٢٠٢٦-٠٨-١٨ | `4.0.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`

## 2026-08-26 — v4.2.0 — تسجيل هدف 581 (عقد المحورين v1.1 §3-6)
اشتراك perpetual.target.state ومرحلة TARGET_STATE: حقول risk_dial/base_target/gross_target/target_net والميزانيات الثلاث تُحفظ كاملة في payload_json. باعتماد المالك.
