# Liquidation Density Map (Structural)

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٣. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٢٢٦٧` |
| الاسم | Liquidation Density Map (Structural) |
| المجلّد | `2267_التصفيات` |
| الإصدار | `1.0.0` |
| حرجة؟ | `false` |
| الإقلاع | `manual` |
| فحص الصحّة | كل `١٥` ثانية |

**تسمع (من `manifest.yaml`):**
- `feed.binance.liquidation`

**تُرسِل (من `manifest.yaml`):**
- `sense.liquidations.state`

**إعداداتها:** `bin_bps` · `window_s` · `top_zones` · `max_age_s`

<!-- BITAQA_END -->

















