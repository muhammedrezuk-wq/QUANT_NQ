# Aggressor Flow (Executors + Absorption)

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٣. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٢٢٦٦` |
| الاسم | Aggressor Flow (Executors + Absorption) |
| المجلّد | `2266_المنفّذون` |
| الإصدار | `1.1.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٥` ثانية |

**تسمع (من `manifest.yaml`):**
- `market.trade`

**تُرسِل (من `manifest.yaml`):**
- `sense.aggressor.state`

**إعداداتها:** `window_trades` · `dominance_threshold` · `flat_bps` · `min_samples` · `max_age_s`

<!-- BITAQA_END -->

















