# التاريخ — 2272_كسور_الأمس

**الذرّة:** `٢٢٧٢` · **الإصدار:** `1.0.0`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-٢٨ | `1.0.0` | إنشاءٌ جديد — ثالث ذرّة من طبقة الاستراتيجية والقرار. تشترك `sense.prior_day.state`(2153)+`market.candle`. آلة حالةٍ (none→broken→retested→confirmed) لكل PDH/PDL لكل رمز، تطبيقًا لـ`scalping/02-rules.md` §٥②. لا تُصدر حكم الصنف ③ بنفسها (تحتاج وقودًا+مسافةً من 2274) — تنشر `distance_points`/`volume` خامَين لذلك. `touch_tolerance_pct=0.05` افتراضٌ كريبتويٌّ غير موثَّق (لا رقم "لمس" صريح في scalping/) — علَّمته صراحةً في الشرح. **مُثبَت حيًّا** (`/api/atoms/2272`، بعد أوّل إغلاقة ٥د حقيقية تالية للإقلاع): `healthy`، `symbols=18 updates=2 confirmations=0` — ١٨ رمزًا حقيقيًّا بمستويات أمسٍ مجمَّدة، صفر تأكيداتٍ حتى الآن (متوقَّعٌ — كسرٌ+ارتدادٌ ناجحان حدثٌ أندر من إغلاقتين). |
