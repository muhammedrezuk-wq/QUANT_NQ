# التاريخ — 2273_محكمة_الزناد

**الذرّة:** `٢٢٧٣` · **الإصدار:** `1.0.0`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-٢٨ | `1.0.0` | إنشاءٌ جديد — رابع ذرّة من طبقة الاستراتيجية والقرار. تشترك الحواسّ الخمس الموثَّقة حرفيًّا في `scalping/02-rules.md` §٥ "محكمة الزناد لحظة أي تفعيل" (المنفّذون/2266 · الجدران/2265 · OI/2170 · العلاوة/2173 · العدسة 1د/2161). تنشر حكمًا مستقلًّا لكل اتجاه (long/short) لكل رمز. الفيتو حصرًا عند اجتماع قصٍّ عنيفٍ من المنفّذين + OI ضدّنا معًا — الجدران والعلاوة والعدسة لا تُصوّت فيتو أبدًا (تطابقًا مع إعلان 2265 و2161 الصريح عن نفسيهما "شاهدٌ لا قاضٍ"/"أداة عيون لا قرار"). `dominance_threshold=0.65` منسوخةٌ حرفيًّا من عتبة 2266 الافتراضية نفسها (مصدرٌ واحد، لا تكرارٌ مستقل). `cut_bps`/`walls_ratio_threshold`/`lens_vol_x_threshold` افتراضاتٌ كريبتويّة غير موثَّقة رقميًّا في scalping/ (الوصف نوعيّ: "قصّ بحجم عنيف" بلا رقم) — معلَّمةٌ صراحةً بالشرح. **مُثبَت حيًّا** (`/api/atoms/2273`): `healthy`، `symbols=19 updates=7699 verdicts={CONFIRM:624, VETO:20, ABSTAIN:14754}` — فيتوهات حقيقية رُصدت فعلًا (٢٠)، لا منطقٌ نظريّ فقط. |
