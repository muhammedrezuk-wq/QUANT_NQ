# Crypto Asset Universe Manager

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `١٠٠١` |
| الاسم | Crypto Asset Universe Manager |
| المجلّد | `1001_مدير_كون_الأصول` |
| الإصدار | `1.2.0` |
| حرجة؟ | `true` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٣٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `crypto.universe.override.command`
- `crypto.universe.scan.requested`

**تُرسِل (من `manifest.yaml`):**
- `crypto.market.ticker.all`
- `crypto.universe.snapshot.state`
- `crypto.universe.membership.state`
- `crypto.universe.rejected.state`

**إعداداتها:** `poll_interval_s` · `core_min_liquidity_usd_24h` · `outer_min_liquidity_usd_24h` · `max_spread_ticks` · `max_daily_range_pct` · `core_target_count` · `outer_target_count` · `entry_resilience_days` · `exit_failure_days` · `overrides_path` · `membership_state_path` · `open_positions_path`

<!-- BITAQA_END -->
