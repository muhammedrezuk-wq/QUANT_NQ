# Session Volume Profile (POC/VAH/VAL)

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٣. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٢١٥٥` |
| الاسم | Session Volume Profile (POC/VAH/VAL) |
| المجلّد | `2155_بروفايل_الحجم` |
| الإصدار | `1.1.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٣٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `market.candle`

**تُرسِل (من `manifest.yaml`):**
- `sense.volume_profile.state`

**إعداداتها:** `timeframe` · `bin_frac` · `value_area_pct` · `max_age_s`

<!-- BITAQA_END -->

















