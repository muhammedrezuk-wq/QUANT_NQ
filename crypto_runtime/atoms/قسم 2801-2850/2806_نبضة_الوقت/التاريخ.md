# التاريخ — 806_نبضة_الوقت

**الذرّة:** `٨٠٦` · **الإصدار:** `3.0.3`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-٢٨ | `3.0.3` | `startup_mode: manual → auto`. **اكتشافٌ أثناء بناء بوّابة الخبر (٢٦١٥ v2.0.0)**: هذه الذرّة هي الناشر الوحيد لكل نبضات `SYS_SECOND`/`SYS_5MIN`/`SYS_15MIN`/`SYS_HOUR`/`SYS_DAY` بكامل الشجرة — كانت نائمةً منذ الإقلاع الأصليّ رغم `critical: true` بمعرِّفها الخاصّ، فلا شيء يشترك أي نبضةٍ نظاميّة عمل فعليًّا حتى اليوم (لا ٢٦١٥ الجديدة، ولا أي ذرّةٍ أخرى بالشجرة تشترك `SYS_*`). صفر تعديل بالكود — قرأته كاملًا: مستقلّةٌ تمامًا (`subscribes: []`)، بلا اعتماديّةٍ خارجية، دورة حياة مهامّها الخلفية سليمة (`asyncio.create_task` بـ`start()` مع إلغاءٍ وانتظارٍ صريحين بـ`stop()` — ليست نفس مخالفة "مهمّةٍ خلفية بلا انتظار" التي مُنعت بذرّاتٍ أخرى، فهذه مجدولٌ دائمٌ بتصميم، لا نبضة قراءةٍ منتهية). إقلاعٌ آمن: لا حدث ينشره يطابق بادئات التنفيذ. |
| ٢٠٢٦-٠٨-١٩ | `3.0.1` |
| ٢٠٢٦-٠٨-١٩ | `3.0.1` | **مبدأ الحداثة بالصحّة** (نفس إصلاح 003 v3.0.1 و617 v3.0.1): أقصى انحراف تاريخي واحد (لحظة الإقلاع قبل أول تصحيح) كان يُبقيها «متعثّرة» للأبد والجودة `SYNCED` وكل النبضات تعمل. صار الحكم على جودة الحاضر، والأقصى التاريخي معلنًا بالرسالة (`max_drift_ever`) لا حاكمًا. (٥/٥). |
| ٢٠٢٦-٠٨-١٨ | `3.0.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`
## 3.0.2 — 2026-08-20
- تنظيف مادة اللغة (دستور الذرّة): أزيل كل حرف عربي من atom.py — التعليقات العربية حُذفت والنصوص العربية استُبدلت برموز إنكليزية. صفر تغيير منطقي، الاختبارات خضراء.
- التعليق المحذوف (الحكم على جودة الحاضر لا الأقصى التاريخي) موثّق أصلًا في بند 3.0.1 أعلاه.
