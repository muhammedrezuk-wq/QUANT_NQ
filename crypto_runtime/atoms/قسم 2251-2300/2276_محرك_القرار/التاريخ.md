# التاريخ — 2276_محرك_القرار

**الذرّة:** `٢٢٧٦` · **الإصدار:** `2.2.0`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-٢٨ | `2.2.0` | تمريرٌ بحت: إضافة `take_profit_runner` (أعلى المستوى، كتوأمَي `take_profit`/`take_profit_2`) و`metadata.take_profit_runner_source`/`grade_target_profile` القادمة من ٢٢٧٧ v1.2.0 (تفريق أهداف رتبة ألف/باء، دفتر الفجوات بند ٧) — بلا أي حسابٍ جديد هنا، فقط عدم إسقاطها. |
| ٢٠٢٦-٠٨-٢٨ | `2.1.0` | بوّابة الخبر تصل لثمرتها هنا: `news_fresh=true` ⇒ الثقة تُخفَّض ٠.٠٥ (مقدارٌ مُختار، لا رقمًا موثَّقًا — `02-rules.md` §٨ يطلب التخفيض نوعيًّا لا رقميًّا). `reason` يضيف "📰 خبرٌ طازج" صراحةً عند الحالة، و`metadata.news_fresh`/`news_age_min` للعرض. **لا حظر** — القرار يُنشر دائمًا بغضّ النظر عن حالة الخبر، بأمر المستخدم الصريح. |
| ٢٠٢٦-٠٨-٢٨ | `2.0.0` | **تغييرٌ بنيويّ:** المصدر تبدّل من `crypto.decision.sized_entry.state`(2275) إلى `crypto.decision.signal_card.state`(2277 الجديدة) — بأمر المستخدم المباشر ببناء بطاقة الإشارة v3.1 كاملةً (مرساة+سلّم+وقف+هدفان). `stop_loss`/`take_profit`/`take_profit_2` تُنشَر الآن بأرقامٍ حقيقية (لا `null`) — أوّل مرّةٍ في تاريخ هذه الطبقة. صفّ يُتجاهَل إن وصل بلا `stop_loss` (بدل فحص `approved`، إذ 2277 لا تنشر إلا لمرشّحٍ معتمدٍ أصلًا). `metadata` تحمل الآن المرساة والرجلين ونسبة الوقف ومصدر كل هدفٍ ومستوى الإلغاء والوقف الزمني — للشفافية الكاملة على الداشبورد. |
| ٢٠٢٦-٠٨-٢٨ | `1.1.0` | إضافة `entry_price` (من `payload.price`، الرقم الحقيقيّ المُمرَّر الآن من 2274→2275→هنا) و`metadata.level_value` (المستوى محلّ الاختبار: PDH/PDL أو حافّة VAH/VAL حسب الصنف) لحمولة `decision.approved.state`. **`stop_loss`/`take_profit` يبقيان `null` كما كانا** — لا تغيير هنا؛ ما زالا يحتاجان طبقة إدارة خروجٍ (الطبقة ٢، ٢٢٧٧ المخطَّطة) لم تُبنَ بعد، بقرار صاحب المشروع الصريح. راجع تاريخ 2274 v1.4.0 لتفصيل السبب الكامل. |
| ٢٠٢٦-٠٨-٢٨ | `1.0.0` | إنشاءٌ جديد — سابع وآخر ذرّة من طبقة الاستراتيجية والقرار (٢٢٧٠-٢٢٧٦ مكتملة). تشترك 2275، تنشر **حصرًا** `decision.approved.state` (العقد المشترك مع نسخة 2707 الكريبتويّة الموجودة أصلًا) — لا `trading.final_decision` ولا `execution.*` أبدًا، تحديدًا لتفادي تصنيف `_verify_execution_safety_at_startup` لها كمشارك تنفيذٍ (البادئات execution./trading./broker. فقط) والرفض التلقائي لنطاق crypto. `volume`/`stop_loss`/`take_profit` تُنشر `null` صراحةً — لا حسابٌ مختلَق. `confidence` تقديرٌ نوعيّ من الرتبة/الصنف، موسومٌ صراحةً بأنه ليس احتمالًا إحصائيًّا. **مُثبَت حيًّا** (`/api/atoms/2276`): `running`، `degraded: AWAITING_FIRST_SIZED_ENTRY` — نفس السبب المتوقَّع أعلى السلسلة (2274/2275 بلا مرشّحٍ فعليّ بعد). `/gov/decisions` تحقَّق منها مباشرةً: `available: false` (قاعدة decisions.db لم تُنشأ بعد — لا صفٌّ لينشئها بعد). السلسلة كاملةً موصولةٌ ومُختبَرة؛ الحلقة الوحيدة الناقصة تشغيلٌ أطول حتى يصدر مرشّحٌ حقيقيّ واحد. |
