# التاريخ — 2275_محرك_المخاطر

**الذرّة:** `٢٢٧٥` · **الإصدار:** `1.6.1`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٩-٠١ | `1.6.1` | ختم NQ: تفعيل حدّي الخسارة اليومية والخسائر المتتالية من نتيجة صفقة يُدخلها أسمر يدويًا في لوحة MEXC بتأكيدين. `trade_id` إلزامي ويُحفظ في اللقطة لمنع مضاعفة النتيجة عند إعادة الإرسال؛ الربح يصفر سلسلة الخسائر، والخسارة تزيدها، والتعادل لا يُعدّ خسارة. تظهر عدادات النتائج والمكرر/المرفوض والـPnL والسلسلة في الصحة. هذا يوقف **التوصيات الجديدة** عند بلوغ الحد، ولا يدّعي إغلاق حساب MEXC أو منع أمر يدوي خارج النظام. |
| ٢٠٢٦-٠٨-٢٨ | `1.6.0` | **أقفال الحلقة الخارجية** — دفتر الفجوات، البند الأحمر الثالث. `strategy-full.txt` §٤ حرفيًّا: "رتبة ألف كاملة الأركان فقط. رتبة باء مرفوضة في الخارجية... نصف الحجم القياسي". قفلٌ جديد يُفحص بعد البوّابة الاقتصادية وقبل الترتيب: `ring=="outer"` و`grade!="A"` ⇒ `approved=False` (`OUTER_RING_GRADE_B_REJECTED`)؛ وإن اجتاز، `max_risk_usd` يُنصَّف صراحةً عند `ring=="outer"`. يعيد استخدام `_ring()` المحسوبة أصلًا لسلّم الترتيب — لا اشتراكٌ جديد. **قفلٌ ثالثٌ من نفس الفقرة (سبريد الجلسة الحقيقي للحلقة الخارجية بدل `cost_bps` الثابت للبوّابة الاقتصادية) غير مطبَّقٍ بعد** — موثَّقٌ كفجوةٍ متبقّية في الكود والشرح، لا حظر صامت. |
| ٢٠٢٦-٠٨-٢٨ | `1.5.0` | **البوّابة الاقتصادية الحادّة صارت حاجزًا فعليًّا** — دفتر الفجوات، البند الأحمر الثاني. `02-rules.md` §١ حرفيًّا بثلاث درجات: هامش<×٢ ⇒ `approved=False` (`ECONOMIC_GATE_BLOCKED`)، هامشٌ مجهول ⇒ `approved=False` أيضًا (`ECONOMIC_GATE_UNKNOWN` — فشلٌ آمن، `strategy-full.txt` §١٤: "ما لا يُقاس لا يدخل محرك الإطلاق")، وهامش ×2-3 يستبعد الصنف③ حصرًا (`ECONOMIC_GATE_BORDERLINE_CLASS3_EXCLUDED` — "مستويات الدرجة الأولى فقط" حرفيًّا، والصنفان ①② مبنيّان أصلًا على مستوى درجة أولى). عتبتان جديدتان قابلتان للمعايرة: `gate_block_below=2.0`، `gate_borderline_below=3.0`. الهامش يبقى معيار ترتيبٍ لمن اجتاز كما كان — لم يُحذف، أُضيف فوقه حاجزٌ فعليّ. عدّاد `gate_blocked` جديد بالصحّة للشفافية. |
| ٢٠٢٦-٠٨-٢٨ | `1.4.0` | تمريرٌ بحت: إضافة `news_fresh`/`news_age_min` القادمَين من ٢٢٧٤ — بلا أي حسابٍ جديد هنا، فقط عدم إسقاطهما. تُستهلَكان لاحقًا بـ٢٢٧٦ لتخفيض الثقة. |
| ٢٠٢٦-٠٨-٢٨ | `1.3.0` | تمريرٌ بحت: إضافة `price` و`evidence` (تحمل الآن `level_value` من 2274) لحمولة `crypto.decision.sized_entry.state` — بلا أي حسابٍ جديد هنا، فقط عدم إسقاط ما وصل من 2274. الغاية: سدّ فجوة "لا سعر دخول ظاهر بالبطاقة" (راجع تاريخ 2274 v1.4.0 لتفصيل السبب والمصدر). |
| ٢٠٢٦-٠٨-٢٨ | `1.2.0` | إضافة معيار "هامش البوابة الاقتصادية" لسلّم الترتيب (المرتبة الثانية بعد الرتبة، قبل جودة الصنف)، حرفيًّا من `02-rules.md` §١. تشترك الآن `sense.median_range.state`(2156)؛ الهامش = `median_bps ÷ cost_bps` (بأفقٍ=٥د فالجذر=١). `cost_bps=3.0` منقولٌ حرفيًّا. **تنويهٌ صريح موثَّقٌ بالكود والشرح:** هذا ترتيبٌ لا بوّابة — لا حظر فعليّ لمرشّحٍ بهامشٍ <×2 كما تنصّ §١ الأصلية؛ البوّابة الحادّة نفسها فجوةٌ مفتوحة، لا هذه الذرّة تطبّقها ولا 2274. |
| ٢٠٢٦-٠٨-٢٨ | `1.1.0` | إضافة معيار "النواة قبل الخارجية" لسلّم ترتيب المنافسة، حرفيًّا من مستند "الخريطة الهندسية" الجديد §٩.٣ ("الرتبة ← هامش البوابة ← جودة الصنف ← النواة قبل الخارجية"). تشترك الآن `crypto.universe.snapshot.state`(1001) لمعرفة عضوية core/outer لحظة الترتيب، وتنشر حقل `ring` صراحةً. معيار "هامش البوابة الاقتصادية" لم يُضَف — لا ذرّة تنشره كحقلٍ رقميّ اليوم؛ فجوةٌ معلَنة لا مُخترَعة. |
| ٢٠٢٦-٠٨-٢٨ | `1.0.0` | إنشاءٌ جديد — سادس ذرّة. تشترك مرشّح 2274. **تصحيح نطاقٍ عن الخطة الأصلية:** لا تحسب حجم عقدٍ (يحتاج سعر وقفٍ، لا حاسّة تنشره بعد — طبقة الخروج مؤجَّلة)؛ تحسب ميزانية مخاطرةٍ بالدولار فقط (`reference_equity_usd × 0.5%`) مطابقةً لتذكير بطاقة v3.1 الثابت، لا رقمًا دولاريًّا ثابتًا (`reference_equity_usd=300` افتراضٌ يُعدَّل حيًّا لرأس مالك الفعليّ). توقّف يوميّ (٣ خسائر متتالية أو −2%) دفاعيٌّ — يستمع `platform.trade_event` لكن لا مصدر تعبئاتٍ حقيقيّ في نطاق كريبتو اليوم، فيبقى صفرًا حتى يُوصَل. سلّم ترتيب المنافسة (`07-future.md`) يُرتّب المرشَّحين المتزامنين، لا يُقصي أحدًا — "صفقة واحدة" قرار المستخدم اليدويّ اليوم. **مُثبَت حيًّا** (`/api/atoms/2275`): `running`، `degraded: AWAITING_FIRST_CANDIDATE` — متوقَّعٌ وصحيح: 2274 لم تُصدر مرشّحًا بعد (راجع تاريخها)، فلا مدخل يصل هذه الذرّة؛ التوصيل بالكامل مؤكَّدٌ عبر السلسلة الكاملة 2270→2276، لا خللٌ في هذه الذرّة نفسها. |
