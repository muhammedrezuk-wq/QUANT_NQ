# التاريخ — 2277_بطاقة_الإشارة

**الذرّة:** `٢٢٧٧` · **الإصدار:** `1.2.1`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-٢٨ | `1.2.1` | **إصلاح علّةٍ حقيقية اكتُشفت بالتشغيل الحيّ:** التحقّق من صفقة PUMPFUN_USDT الحقيقية (رتبة ألف، ②break_retest) كشف `take_profit_runner`(٠.٠٠٤٦٤٢٢٦) **أقرب** من `take_profit_2`(٠.٠٠٤٦٣٥٨٤) — عكس الترتيب المفروض (الراكض يجب أن يكون الأبعد دومًا). السبب: احتياطية الراكض (v1.2.0) كانت `avg_entry ± 3R` — مضاعِفٌ ثابتٌ من الدخول، بينما الهدف٢ حين يأتي من مستوًى حقيقيّ (`next_level`) قد يقع أبعد من 3R (شوهد فعليًّا ~3.47R بهذه الصفقة). الإصلاح: الاحتياطية الآن `target2 ± 1R` — نسبيّةٌ للهدف٢ الفعليّ أينما وقع، تضمن `الراكض` أبعد من `هدف٢` دومًا ببنية الحساب نفسها، لا بفحصٍ إضافيّ. `take_profit_runner_source` الجديد لهذه الحالة: `fallback_target2_plus_1R` (بدل `fallback_3R`). |
| ٢٠٢٦-٠٨-٢٨ | `1.2.0` | **تفريق أهداف رتبة ألف/باء** — دفتر الفجوات بند ٧. `strategy-full.txt` §٤ "الخطوة 4" حرفيًّا: "رتبة ألف: أهداف كاملة + راكض مسموح" مقابل "رتبة باء: أهداف محافظة تسبق الجدران + جني أسرع + لا راكض". رتبة ألف: سلوكٌ كسابق عهده (1R/2R) + حقلٌ جديد `take_profit_runner` (أقرب مستوًى أبعد من الهدف٢، وإلا 3R). رتبة باء: هدف١=0.75R (بدل 1R)، هدف٢=1.5R احتياطًا (بدل 2R)، `take_profit_runner=null` دومًا. رتبةٌ مجهولة تُعامَل معاملة باء (فشلٌ آمنٌ تحفّظيّ). حقلٌ جديد `grade_target_profile` للشفافية. **مضاعِفا باء (0.75/1.5) ومضاعِف الراكض (3R) اصطلاحُ هذه الذرّة** — الوثيقة تنصّ "أسرع"/"مسموح" بلا رقمٍ حرفيّ، راجع الشرح. |
| ٢٠٢٦-٠٨-٢٨ | `1.1.0` | تمريرٌ بحت: إضافة `news_fresh`/`news_age_min` القادمَين من ٢٢٧٥. تُستهلَكان بـ٢٢٧٦ لتخفيض الثقة — لا تُغيِّران أي حسابٍ بهذه الذرّة. |
| ٢٠٢٦-٠٨-٢٨ | `1.0.0` | إنشاءٌ جديد — إغلاق فجوة "لا هدف ولا وقف" المُعلَنة صراحةً في تاريخ ٢٢٧٤/٢٢٧٥/٢٢٧٦. بأمر المستخدم المباشر: "شوفها بمجلد scalping وطبقها كاملة" — إشارةً لبطاقة الإشارة v3.1 (`03-protocol.md` §٣، الصيغة الملزمة النهائية، قرار صاحب المشروع 2026-08-25). تشترك `crypto.decision.sized_entry.state`(2275) + `crypto.universe.snapshot.state`(1001، لـ`price_tick_size`/`spread_ticks`) + `sense.walls.state`(2265، لأقرب جدار) + `sense.volume_profile.state`(2155، لـPOC/VAH/VAL) + `sense.prior_day.state`(2272 تشتركها أصلًا من نفس الحدث، لـPDH/PDL). تنشر `crypto.decision.signal_card.state` بالمرساة وسلّم الدخول والوقف والهدفين والإلغاء والوقف الزمني. **لم تُبنَ اعتباطًا** — كل رقمٍ مسنودٌ لسطرٍ محدَّد في `02-rules.md`/`03-protocol.md`، والتبسيطات المُضطَرَّة (تناظر السلّم، مصدر مسافة الوقف كنقطةٍ وسطى من نطاق، مستوى الإلغاء) معلَنةٌ صراحةً بالشرح، لا مُخفاة. `2276_محرك_القرار` عُدِّلت (v2.0.0) لتشترك هذه الذرّة بدل ٢٢٧٥ مباشرةً — راجع تاريخها. |
